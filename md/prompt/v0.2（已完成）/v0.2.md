先继续给我 v0.2 的执行方案，分几个 agent，分别干什么，先写个大概，一会再写提示词



星期三17:34






已处理 3m 4s



可以。v0.2 的核心不是“做新玩法”，而是把地图底层从 HexTile 迁移到 Province/Region 抽象，为 v0.3 前线系统铺路。
我建议分 6 个 agent，不要更多。v0.2 改的是地基，agent 太多会互相踩。
v0.2 目标
把当前：
GameState -> MapState -> tiles: [HexCoord: HexTile]
Division.coord: HexCoord
Command.to: HexCoord
逐步升级为：
GameState -> MapState -> regions: [RegionId: RegionNode]
Division.location: RegionId
Command.to: RegionId
HexCoord / HexTile 变成显示层或适配层
v0.2 暂时不做完整前线系统，只预留前线需要的数据能力。
Agent 1：架构迁移 Agent
职责：定规则层新模型。
负责：
设计 RegionId
设计 RegionNode / ProvinceNode
设计 RegionGraph
设计 RegionEdge
改造 MapState
定义 hex 与 region 的映射关系
核心产出：
struct RegionNode {
    let id: RegionId
    var name: String
    var owner: Faction
    var controller: Faction
    var terrain: BaseTerrain
    var neighbors: [RegionId]
    var displayHexes: [HexCoord]
    var city: CityInfo?
    var infrastructure: Int
    var supplyValue: Int
    var factories: Int
    var resources: [ResourceAmount]
    var coreOf: [Faction]
    var occupationState: OccupationState?
}
要求：不要直接删 HexCoord，先把它降级为显示/适配层。
Agent 2：数据迁移 Agent
职责：把阿登测试地图从 hex 数据改成 region 数据。
负责：
设计 ardennes_v02_regions.json
把原有六角格地图聚合成若干区块
每个区块包含若干显示 hex
设置区块相邻关系
设置城市、道路节点、补给源、控制权
写数据校验器或加载校验逻辑
建议 v0.2 地图不要太大，先做 12-18 个 region。
例如：
Bastogne
Houffalize
StVith
LuxembourgRoad
ArdennesForestNorth
ArdennesForestSouth
GermanEastDepot
AlliedWestDepot
Agent 3：规则迁移 Agent
职责：把规则从 hex 邻接迁移到 region 邻接。
负责：
移动规则改用 RegionGraph.neighbors
攻击范围改用 region distance
占领改为占领 region
补给线改为 region pathfinding
战争迷雾改为 region visibility
保持 RuleEngine 仍是唯一状态修改入口
注意：v0.2 不需要复杂省份形状，也不需要真实 HOI4 级战线。只要 region 邻接、控制权、补给路径正确。
Agent 4：Command/API 迁移 Agent
职责：统一玩家和 AI 命令接口。
负责：
把 MoveCommand.to: HexCoord 改为 RegionId
把 Division.coord 改为 location: RegionId
更新 AgentOrder
更新 AgentContext
保证 MockAI 仍能输出结构化 JSON
保证旧的点击 hex 可以被转换成 region 命令
关键原则：
UI 点击 HexCoord
-> MapDisplayAdapter 查到 RegionId
-> 生成 Command
-> RuleEngine 校验执行
不要让 UI 直接操作 region 内部状态。
Agent 5：显示适配 Agent
职责：保持六角格显示，但底层数据来自 region。
负责：
BoardScene 继续画 hex
hex 颜色由所属 region 的 terrain/controller 决定
点击 hex 时返回所属 region
选中 region 时高亮该 region 的所有 hex
单位显示在 region 的代表 hex 上
城市、补给源、工厂等挂在 region 上显示
这个 agent 不碰核心规则，只做显示和交互适配。
Agent 6：测试与集成 Agent
职责：防止迁移后 v0 基础玩法坏掉。
负责：
RegionGraphTests
RegionDataLoadingTests
MovementRulesRegionTests
SupplyRulesRegionTests
CommandValidationRegionTests
MockAIRegionCommandTests
回归测试：移动、攻击、占领、补给、AI 命令执行仍能跑通
集成顺序：
先接 Agent 1 的模型。
再接 Agent 2 的数据。
然后接 Agent 3 规则。
再接 Agent 4 命令。
最后接 Agent 5 UI。
Agent 6 贯穿测试和修集成问题。
v0.2 不做的事
这些留给 v0.3 或更后面：
不做自动前线排兵。
不做攻势箭头。
不做元帅 agent。
不做复杂经济。
不做完整国家大地图。
不做省份多边形美术。
不做多个省份内的战斗宽度。
不做每个 region 多单位堆叠复杂规则，先保持一个 region 一个主战单位或简单限制。
我建议的 v0.2 最小完成标准
v0.2 完成时应该能证明：
玩家看到的仍是六角格地图
但游戏规则已经按 region/province 运行
单位移动、攻击、占领、补给、AI 命令都使用 RegionId
HexCoord 只用于显示、点击和布局
这个版本完成后，v0.3 做前线就会自然很多，因为前线可以直接定义为：
己方 controller region 与敌方 controller region 的相邻边
下一步写提示词时，建议给每个 agent 单独一段，明确“只改哪些模块、不能改哪些模块、完成标准、测试标准”。

星期三17:37






先写agent1的提示词




星期三17:38






下面这段可以直接复制给 Agent 1：架构迁移 Agent。
你是 Agent 1：v0.2 架构迁移 Agent。

你的任务是为 iOS 二战回合制大战略/战棋游戏的 v0.2 版本建立“省份图 / 区块图”规则底层。当前 v0/v0.1 使用六角格作为地图和规则基础；v0.2 的目标是把规则层抽象为 Region/Province，让后续 v0.3 前线系统可以基于区块相邻边实现。

你需要先阅读以下文档：

- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

核心目标：

把当前或计划中的地图底层从：

GameState -> MapState -> tiles: [HexCoord: HexTile]
Division.coord: HexCoord
Command.to: HexCoord

逐步升级为：

GameState -> MapState -> regions: [RegionId: RegionNode]
Division.location: RegionId
Command.to: RegionId

同时保留 HexCoord / HexTile 作为显示层、点击层和早期兼容层。不要直接删除 HexCoord。

你的职责范围：

1. 设计并实现/定义 RegionId。
2. 设计并实现/定义 RegionNode / ProvinceNode。
3. 设计并实现/定义 RegionGraph。
4. 设计并实现/定义 RegionEdge。
5. 改造或规划 MapState，使其以 regions 作为规则底层。
6. 定义 hex 与 region 的映射关系。
7. 保证未来 UI 可以通过点击 HexCoord 找到对应 RegionId。
8. 保证未来规则系统可以通过 RegionGraph 做相邻、距离、路径、补给和前线判断。
9. 保留对 v0 六角格显示的兼容能力。

建议核心类型：

```swift
struct RegionId: Hashable, Codable, Equatable, RawRepresentable {
    let rawValue: String
}

struct RegionNode: Codable, Equatable, Identifiable {
    let id: RegionId
    var name: String
    var owner: Faction
    var controller: Faction
    var terrain: BaseTerrain
    var neighbors: [RegionId]
    var displayHexes: [HexCoord]
    var representativeHex: HexCoord
    var city: CityInfo?
    var infrastructure: Int
    var supplyValue: Int
    var factories: Int
    var resources: [ResourceAmount]
    var coreOf: [Faction]
    var occupationState: OccupationState?
    var isPassable: Bool
}

struct RegionEdge: Codable, Equatable, Hashable {
    let from: RegionId
    let to: RegionId
    var hasRoad: Bool
    var hasRiverCrossing: Bool
    var movementCostModifier: Int
}

struct RegionGraph: Codable, Equatable {
    var regions: [RegionId: RegionNode]
    var edges: Set<RegionEdge>
}
如果项目现有代码中还没有这些类型，请新增。
如果已有类似类型，请优先沿用现有命名和目录结构，不要重复造一套平行模型。
MapState 建议方向：
struct MapState: Codable, Equatable {
    var regions: [RegionId: RegionNode]
    var hexToRegion: [HexCoord: RegionId]
    var supplySources: [SupplySource]
    var objectives: [Objective]

    // v0/v0.1 兼容或显示层使用
    var displayTiles: [HexCoord: HexTile]
}
注意：
regions 是规则层权威地图。
displayTiles 只用于六角格显示和兼容。
hexToRegion 用于 UI 点击转换。
未来 Division.location、移动、攻击、占领、补给都应使用 RegionId，但本 agent 只负责架构模型，不负责完整规则迁移。
你可以新增的辅助类型：
struct CityInfo: Codable, Equatable {
    var name: String
    var victoryPoints: Int
    var isCapital: Bool
}

struct ResourceAmount: Codable, Equatable {
    var type: ResourceType
    var amount: Int
}

enum ResourceType: String, Codable {
    case steel
    case oil
    case aluminum
    case rubber
    case tungsten
    case chromium
}

struct OccupationState: Codable, Equatable {
    var resistance: Int
    var compliance: Int
}
必须实现或定义的基础能力：
region(for hex: HexCoord) -> RegionId?
region(id: RegionId) -> RegionNode?
neighbors(of regionId: RegionId) -> [RegionId]
areAdjacent(_ a: RegionId, _ b: RegionId) -> Bool
edgeBetween(_ a: RegionId, _ b: RegionId) -> RegionEdge?
representativeHex(for regionId: RegionId) -> HexCoord?
validateRegionGraph()，用于检查：region id 唯一；
neighbor 引用存在；
hexToRegion 不指向不存在的 region；
displayHexes 至少有一个 hex；
representativeHex 属于 displayHexes；
相邻关系最好双向一致。

明确不要做：
不实现 v0.3 前线系统。
不实现自动布防。
不实现攻势箭头。
不实现元帅 agent。
不实现真实经济系统。
不实现 UI 渲染。
不实现完整规则迁移。
不删除 HexCoord / HexTile。
不让 SpriteKit 或 SwiftUI 直接成为规则层权威数据源。
工程约束：
规则层和 UI 层必须分离。
所有模型尽量 Codable、Equatable，便于 JSON 加载、测试和回放。
不引入数据库。
不引入网络依赖。
不引入第三方库，除非项目已经使用。
不做无关重构。
不破坏 v0 当前能运行的最小闭环。
如果当前还没有 Swift 工程，只输出/新增清晰的架构文档或模型文件草案，等待项目骨架完成后再接入。
测试要求：
请添加或规划以下测试：
RegionId 可以稳定编码/解码。
RegionGraph.neighbors 返回正确相邻区块。
areAdjacent 对相邻和非相邻 region 返回正确结果。
hexToRegion 能把显示 hex 映射到 region。
representativeHex 必须属于对应 region 的 displayHexes。
validateRegionGraph 能发现缺失 neighbor、无效 region id、无 displayHexes、错误 representativeHex。
一个最小阿登 region graph 可以成功构建并通过校验。
完成标准：
项目中存在清晰的 Region/Province 规则层模型。
MapState 已经具备 region-first 的结构，或至少有明确兼容迁移路径。
HexCoord 保留为显示层/适配层能力。
后续 Agent 2 可以基于你的模型编写 ardennes_v02_regions.json。
后续 Agent 3 可以基于你的模型迁移移动、攻击、补给规则。
后续 Agent 5 可以通过 hexToRegion 实现六角格显示和点击适配。
所有新增测试通过，或在没有工程骨架时给出可直接落地的测试清单。
最终输出：
简短说明你改了哪些核心模型。
说明 HexCoord 与 RegionId 的关系。
说明哪些内容留给 Agent 2、Agent 3、Agent 5。
列出已运行的测试和结果。

这个 Agent 1 的边界要卡死：**只做地图规则层抽象，不迁规则、不做 UI、不写前线**。否则 v0.2 会失控。

星期三17:39






写agent2的提示词




星期三17:40






下面这段给 Agent 2：数据迁移 Agent。
你是 Agent 2：v0.2 数据迁移 Agent。

你的任务是为 iOS 二战回合制大战略/战棋游戏的 v0.2 版本准备“省份图 / 区块图”数据层。v0.2 的目标是把规则底层从纯六角格迁移到 Region/Province，但仍保留六角格显示。

你需要基于 Agent 1 提供的 Region/Province 模型，编写阿登测试战场的 v0.2 region 数据，并确保后续规则、UI、AI agent 都可以读取这些数据。

先阅读以下文档：

- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

同时阅读 Agent 1 已完成的模型文件或说明，重点确认：

- RegionId
- RegionNode / ProvinceNode
- RegionGraph
- RegionEdge
- MapState
- hexToRegion
- displayHexes
- representativeHex

如果 Agent 1 的实际命名和下面提示词略有不同，以 Agent 1 已落地的类型为准，不要重复创建一套平行模型。

核心目标：

创建 v0.2 阿登测试战场数据，让当前六角格测试板可以迁移为：

GameState -> MapState -> regions: [RegionId: RegionNode]

而不是继续把规则权威状态绑定在：

tiles: [HexCoord: HexTile]

你的职责范围：

1. 新增或规划 `ardennes_v02_regions.json`。
2. 把阿登测试战场拆成 12-18 个 Region。
3. 每个 Region 包含：
   - id
   - name
   - owner
   - controller
   - terrain
   - neighbors
   - displayHexes
   - representativeHex
   - city
   - infrastructure
   - supplyValue
   - factories
   - resources
   - coreOf
   - occupationState
   - isPassable
4. 新增或规划 `region_edges` 数据。
5. 新增或规划 `hexToRegion` 映射。
6. 设置德军和盟军补给源。
7. 设置 Bastogne、St. Vith、Houffalize 等关键目标。
8. 保证数据能通过 Agent 1 的 `validateRegionGraph()`。
9. 保证后续 Agent 3 可以用这些数据做移动、攻击、占领、补给。
10. 保证后续 Agent 5 可以用 `displayHexes` 和 `representativeHex` 继续画六角格地图。

建议文件：

```text
Data/ardennes_v02_regions.json
Data/ardennes_v02_units.json
Data/ardennes_v02_objectives.json
Data/ardennes_v02_agents.json
如果项目当前还没有 Swift 工程或 Data 目录，则先在规划文档中给出完整 JSON 草案，等待工程骨架完成后再落地。
建议 region 数量：15 个。
推荐 region 列表：
german_east_depot
eifel_approach
schnee_eifel
st_vith
houffalize
ardennes_forest_north
ardennes_forest_central
ardennes_forest_south
bastogne
bastogne_fortress
luxembourg_road
meuse_approach
allied_west_depot
northern_ridge
southern_ridge
推荐地形与战略定位：
german_east_depot
- 德军补给源
- controller: germany
- terrain: city 或 plain
- supplyValue: 5

eifel_approach
- 德军东部进攻入口
- terrain: plain
- 有道路
- 德军初始装甲集结方向

schnee_eifel
- 森林/山地混合防区
- terrain: forest 或 mountain
- 防御较强，移动慢

st_vith
- 次要城市目标
- terrain: city
- controller: allies
- victoryPoints: 2

houffalize
- 道路节点
- terrain: city 或 plain
- controller: allies
- 连接 Bastogne、St. Vith、德军东部入口

ardennes_forest_north
- 北部森林
- terrain: forest
- 移动慢，适合侧翼

ardennes_forest_central
- 中央森林
- terrain: forest
- 德军突破路径之一

ardennes_forest_south
- 南部森林
- terrain: forest
- 可用于绕后

bastogne
- 主要胜利目标城市
- terrain: city
- controller: allies
- victoryPoints: 5
- infrastructure: 3

bastogne_fortress
- Bastogne 防御核心
- terrain: fortress
- controller: allies
- 防御强，移动成本较高

luxembourg_road
- 南部道路走廊
- terrain: plain
- 有道路
- 适合装甲推进

meuse_approach
- 西部突破出口
- terrain: plain
- 德军后续战略目标方向

allied_west_depot
- 盟军补给源
- controller: allies
- terrain: city 或 plain
- supplyValue: 5

northern_ridge
- 北部山地/高地
- terrain: mountain
- 适合防守

southern_ridge
- 南部山地/高地
- terrain: mountain
- 可形成侧翼屏障
推荐相邻关系：
german_east_depot:
- eifel_approach
- schnee_eifel

eifel_approach:
- german_east_depot
- schnee_eifel
- st_vith
- ardennes_forest_central

schnee_eifel:
- german_east_depot
- eifel_approach
- st_vith
- northern_ridge

st_vith:
- schnee_eifel
- eifel_approach
- houffalize
- northern_ridge
- ardennes_forest_north

houffalize:
- st_vith
- ardennes_forest_north
- ardennes_forest_central
- bastogne
- bastogne_fortress

ardennes_forest_north:
- st_vith
- houffalize
- northern_ridge

ardennes_forest_central:
- eifel_approach
- houffalize
- ardennes_forest_south
- luxembourg_road
- bastogne

ardennes_forest_south:
- ardennes_forest_central
- luxembourg_road
- southern_ridge
- bastogne_fortress

bastogne:
- houffalize
- ardennes_forest_central
- bastogne_fortress
- meuse_approach
- allied_west_depot

bastogne_fortress:
- houffalize
- bastogne
- ardennes_forest_south
- luxembourg_road

luxembourg_road:
- ardennes_forest_central
- ardennes_forest_south
- bastogne_fortress
- southern_ridge

meuse_approach:
- bastogne
- allied_west_depot

allied_west_depot:
- bastogne
- meuse_approach

northern_ridge:
- schnee_eifel
- st_vith
- ardennes_forest_north

southern_ridge:
- luxembourg_road
- ardennes_forest_south
注意：相邻关系必须双向一致。
如果 JSON 里 region.neighbors 和 region_edges 同时存在，二者必须能互相校验。
displayHexes 规则：
每个 region 至少包含 1 个 HexCoord。
Bastogne、St. Vith、Houffalize 等关键地区建议包含 1-2 个 hex。
森林、山地、道路走廊可以包含 2-4 个 hex。
representativeHex 必须属于该 region 的 displayHexes。
不同 region 的 displayHexes 不应重叠。
hexToRegion 必须覆盖所有 displayHexes。
如果当前没有固定 v0 hex 地图，请创建一个临时 11x9 axial hex 布局草案。要求只保证内部一致，不要求真实地理精确。
单位初始位置建议：
德军：
1st_panzer_division -> eifel_approach
2nd_panzer_division -> german_east_depot
motorized_division -> schnee_eifel
artillery_group -> german_east_depot
盟军：
101st_airborne -> bastogne
st_vith_garrison -> st_vith
anti_tank_group -> bastogne_fortress
field_artillery -> allied_west_depot
补给源建议：
german_supply:
- faction: germany
- regionId: german_east_depot
- supplyValue: 5

allied_supply:
- faction: allies
- regionId: allied_west_depot
- supplyValue: 5
目标建议：
bastogne:
- regionId: bastogne
- owner: allies
- victoryPoints: 5
- mainObjective: true

st_vith:
- regionId: st_vith
- owner: allies
- victoryPoints: 2

houffalize:
- regionId: houffalize
- owner: allies
- victoryPoints: 1
- roadHub: true
JSON 输出要求：
字段名使用 lowerCamelCase。
id 使用 snake_case 字符串。
faction 值使用已有枚举，例如 germany、allies、neutral。
terrain 值必须匹配现有 BaseTerrain 或 TerrainType 枚举。
不要在 JSON 中写注释。
数值范围保持简单：infrastructure: 0-5
supplyValue: 0-5
factories: 0-3
resources amount: 0-5
resistance/compliance: 0-100

你可以新增的数据校验能力：
所有 region id 唯一。
所有 neighbor 都存在。
所有 neighbor 双向一致。
所有 region 至少有一个 displayHex。
representativeHex 属于 displayHexes。
hexToRegion 不指向不存在的 region。
displayHexes 不跨 region 重叠。
supplySource.regionId 必须存在。
objective.regionId 必须存在。
unit.location 必须存在。
明确不要做：
不迁移移动规则。
不迁移攻击规则。
不迁移补给规则。
不改 UI 渲染。
不实现前线系统。
不实现经济系统。
不实现工厂生产。
不实现科技。
不实现多 agent 政治系统。
不删除 v0 旧数据，除非有明确迁移兼容方案。
不让 JSON 数据依赖 SpriteKit 节点。
完成标准：
存在完整的 v0.2 阿登 region 数据。
15 个左右 region 全部有 id、名称、地形、控制权、邻接关系、displayHexes、representativeHex。
德军和盟军补给源已迁移到 region。
Bastogne、St. Vith、Houffalize 等目标已迁移到 region。
初始单位位置使用 RegionId。
数据能被 MapState / RegionGraph 加载。
数据能通过 region graph 校验。
后续 Agent 3 可以直接使用该数据迁移规则系统。
后续 Agent 5 可以直接使用 displayHexes 继续显示六角格。
测试要求：
请添加或规划以下测试：
ardennes_v02_regions.json 可以 decode。
region 数量在 12-18 之间。
所有 neighbor 引用有效。
所有相邻关系双向一致。
所有 displayHexes 非空。
所有 representativeHex 有效。
所有 supply source 指向有效 region。
所有 objective 指向有效 region。
所有 unit 初始 location 指向有效 region。
最小路径测试：german_east_depot 可以通过 region graph 到达 bastogne；
allied_west_depot 可以到达 bastogne；
st_vith 与 houffalize 相邻；
bastogne 与 bastogne_fortress 相邻。

最终输出：
说明新增或修改了哪些数据文件。
说明 region 数量、主要地区和初始控制权。
说明 hexToRegion / displayHexes 如何支持六角格显示。
说明哪些内容留给 Agent 3 和 Agent 5。
列出已运行的测试和结果。

Agent 2 的重点是**数据完整性**，不是规则。它交付的东西应该让后续 agent 不需要再争论“阿登地图有哪些区块、谁和谁相邻、单位放哪里”。

星期三17:41

写agent3的提示词




星期三17:42






下面这段给 Agent 3：规则迁移 Agent。
你是 Agent 3：v0.2 规则迁移 Agent。

你的任务是把 iOS 二战回合制大战略/战棋游戏的核心规则，从 v0/v0.1 的 HexCoord / HexTile 规则底层迁移到 v0.2 的 RegionId / RegionGraph 规则底层。

v0.2 的目标不是新增复杂玩法，而是完成规则地基迁移：单位移动、攻击、占领、补给、包围、视野、胜利判定都应基于 Region/Province，而不是直接依赖六角格坐标。

你需要先阅读以下文档：

- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

同时阅读 Agent 1 和 Agent 2 的交付内容，重点确认：

- RegionId
- RegionNode / ProvinceNode
- RegionGraph
- RegionEdge
- MapState
- hexToRegion
- ardennes_v02_regions.json
- v0.2 初始单位数据
- v0.2 目标数据
- v0.2 补给源数据

如果 Agent 1 / Agent 2 的实际命名和本提示词不同，以已经落地的类型为准，不要重复创建一套平行模型。

核心目标：

把规则层从：

```text
移动：HexCoord -> HexCoord
攻击：hex distance
占领：HexTile.controller
补给：hex pathfinding
视野：hex radius
胜利：hex / city tile
迁移为：
移动：RegionId -> RegionId
攻击：region graph distance
占领：RegionNode.controller
补给：region pathfinding
视野：region graph radius
胜利：objective.regionId
职责范围：
实现或迁移 region-based movement rules。
实现或迁移 region-based combat range / terrain defense。
实现或迁移 region-based occupation rules。
实现或迁移 region-based supply rules。
实现或迁移 region-based encirclement rules。
实现或迁移 region-based visibility rules。
实现或迁移 region-based victory rules。
保持 RuleEngine 是唯一修改 GameState 的入口。
保留必要的 HexCoord 兼容包装，但新规则权威输入应是 RegionId。
不做 UI、不做数据设计、不做前线系统。
建议新增或改造模块：
Rules/
  RegionMovementRules.swift
  RegionCombatRules.swift
  RegionSupplyRules.swift
  RegionVisibilityRules.swift
  RegionOccupationRules.swift
  RegionVictoryRules.swift
  RegionPathfinder.swift
  RegionRuleEngineAdapter.swift
如果项目已有类似模块，请在现有模块内扩展，不要重复创建重复职责的文件。
规则迁移要求：
1. 移动规则
移动输入应基于：
from: RegionId
to: RegionId
unit/division movement points
RegionGraph
RegionEdge
RegionNode.terrain
RegionNode.infrastructure
必须支持：
只能沿相邻 region 移动。
不能进入不可通行 region。
不能进入敌方单位占据的 region。
进入敌方控制 region 后停止移动。
道路 / 高基建降低移动成本。
森林、山地、要塞提高移动成本。
跨河 edge 增加移动成本。
无补给或组织度低的单位降低移动力。
建议移动成本：
plain: 1
city: 1
forest: 2
mountain: 3
fortress: 2
edge.hasRoad: cost 最低为 1
edge.hasRiverCrossing: +2
infrastructure >= 3: -1，最低为 1
需要实现：
func movementCost(from: RegionId, to: RegionId, state: GameState) -> Int?
func reachableRegions(for divisionId: String, in state: GameState) -> Set<RegionId>
func shortestPath(from: RegionId, to: RegionId, in graph: RegionGraph) -> [RegionId]?
2. 攻击规则
攻击范围基于 region graph distance，而不是 hex distance。
必须支持：
普通单位攻击相邻 region。
炮兵可以攻击距离 2 的 region。
攻击目标必须可见。
攻击方和目标必须敌对。
攻击方本回合未行动。
地形防御来自防守方所在 RegionNode。
河流防御来自攻击路径第一条 RegionEdge。
要塞和城市提供防御加成。
建议防御修正：
plain: 0
forest: +2
mountain: +3
city: +2
fortress: +4
defender behind river edge: +2
需要实现：
func regionDistance(_ a: RegionId, _ b: RegionId, graph: RegionGraph) -> Int?
func canAttack(attackerId: String, defenderId: String, state: GameState) -> Bool
func terrainDefenseBonus(for regionId: RegionId, state: GameState) -> Int
func resolveAttack(attackerId: String, defenderId: String, state: inout GameState) -> CombatResult
注意：
v0.2 可以暂时保留 v0/v0.1 的确定性伤害公式。
不要在本 agent 中新增复杂战斗宽度。
不要新增空军、海军、天气。
3. 占领规则
占领对象从 HexTile 迁移到 RegionNode。
必须支持：
单位移动进入敌方或中立 region 后，根据规则改变 RegionNode.controller。
城市、要塞、道路节点、补给源都基于 region controller。
占领事件写入 GameEvent。
Objective 的控制权通过 objective.regionId 判断。
建议：
func updateRegionControl(afterMove divisionId: String, state: inout GameState)
func controller(of regionId: RegionId, state: GameState) -> Faction
func setController(_ faction: Faction, for regionId: RegionId, state: inout GameState)
4. 补给规则
补给路径基于 RegionGraph。
必须支持：
每个阵营有补给源 region。
单位从当前位置寻找路径到己方补给源。
可通过己方控制 region、中立 region、己方单位所在 region。
不可通过敌方单位所在 region。
不可通过敌方控制城市/要塞。
敌方控制 region 增加补给成本或直接阻断，按当前设计选择一种简单规则。
补给状态至少包含：supplied、lowSupply、encircled。
建议补给成本：
edge.hasRoad: 1
plain/city: 1
forest: 2
mountain: 3
fortress: 2
river edge: +2
infrastructure 高则降低成本，最低 1
建议最大补给成本：
v0.2: 7 或 8
需要实现：
func hasSupplyLine(divisionId: String, state: GameState) -> Bool
func supplyPathCost(from: RegionId, to source: RegionId, state: GameState) -> Int?
func updateSupplyStates(state: inout GameState)
5. 包围规则
包围基于 region 邻接。
单位被包围的条件：
无有效补给线
且安全撤退相邻 region 少于 2 个
安全撤退 region 定义：
region 存在；
可通行；
没有敌军单位；
不是敌方控制城市/要塞；
不在明显敌方压制区；
后续有机会连回己方补给方向。
需要实现：
func safeRetreatRegions(for divisionId: String, state: GameState) -> [RegionId]
func isEncircled(divisionId: String, state: GameState) -> Bool
v0.2 不要求复杂 AI 撤退，只要规则能标记 encircled。
6. 视野规则
视野从 hex radius 迁移为 region graph radius。
必须支持：
每个阵营维护 region visibility。
单位根据自身视野值看见一定 graph distance 内的 region。
AI 的 AgentContext 不能读取不可见敌军。
explored / visible / unseen 的概念继续保留。
display hex 的迷雾表现留给 UI agent，本 agent 只负责 region visibility。
需要实现：
func visibleRegions(for faction: Faction, state: GameState) -> Set<RegionId>
func updateRegionVisibility(state: inout GameState)
func visibilityState(for regionId: RegionId, faction: Faction, state: GameState) -> VisibilityState
7. 胜利规则
胜利条件改用 objective.regionId。
必须支持：
德军占领 Bastogne 并保持 1 个完整回合。
德军占领 Bastogne + St. Vith。
任一方消灭足够敌军。
盟军在最大回合结束时仍控制 Bastogne。
盟军切断德军装甲补给并维持 1 个完整回合。
需要实现或迁移：
func checkVictoryConditions(state: GameState) -> VictoryState?
注意：
Objective 查询必须通过 region controller。
不要再依赖 city HexTile 的 controller。
8. RuleEngine 集成
RuleEngine 仍然是唯一状态修改入口。
要求：
玩家命令和 AI 命令都必须走同一套验证和执行逻辑。
本 agent 可以新增 region-based 内部方法。
如果当前 Command 仍然使用 HexCoord，先通过 adapter 转换为 RegionId。
如果 Agent 4 已经完成 Command/API 迁移，则直接使用 RegionId。
不要让 UI、AI 或数据加载器直接修改 region controller、division location、supplyState。
建议执行流程：
Command
-> CommandValidator
-> RegionRules
-> CommandExecutor
-> GameState mutation
-> GameEvent log
9. 兼容策略
v0.2 迁移期间不要硬删 HexCoord 规则。
允许保留：
func reachableHexes(...)
func movementCost(from: HexCoord, to: HexCoord, ...)
但它们应变成兼容包装：
HexCoord -> RegionId -> RegionRules -> displayHexes
长期规则权威必须是 RegionId。
明确不要做：
不设计 v0.2 region JSON 数据，这是 Agent 2 的职责。
不做 UI / SpriteKit 渲染，这是 Agent 5 的职责。
不做 Command/API 全面迁移，这是 Agent 4 的职责。
不实现 v0.3 前线系统。
不实现自动布防。
不实现攻势箭头。
不实现元帅 agent。
不实现经济、科技、生产。
不实现空军、海军、天气。
不做省份多边形美术。
不做无关重构。
不破坏现有 MockAI 和玩家命令闭环。
测试要求：
请添加或规划以下测试：
RegionMovementRulesTests
相邻 region 可移动；
非相邻 region 不可直接移动；
山地移动成本高于平原；
道路 edge 降低移动成本；
河流 edge 增加移动成本；
敌方单位占据 region 不可进入。

RegionCombatRulesTests
普通单位只能攻击相邻 region；
炮兵可攻击距离 2 region；
不可攻击不可见目标；
城市/要塞/山地提供防御加成；
攻击后 attacker hasActed。

RegionOccupationRulesTests
单位进入敌方城市 region 后 controller 改变；
占领 Bastogne 写入事件；
objective control 读取 region controller。

RegionSupplyRulesTests
german_east_depot 到前线单位存在补给线；
被敌方控制区切断后变成 lowSupply 或 encircled；
道路路径补给成本低于森林/山地路径；
supply source 不存在时返回无补给，不崩溃。

RegionVisibilityRulesTests
单位能看到 graph distance 内的 region；
不可见敌军不进入 AgentContext；
explored region 保留最后已知信息。

RegionVictoryRulesTests
Bastogne 被德国控制并保持完整回合触发德国胜利；
最大回合结束盟军仍控制 Bastogne 触发盟军胜利；
Bastogne + St. Vith 同时被德国控制触发德国胜利。

回归测试：
v0.2 阿登场景可以加载；
德军单位可以从 german_east_depot 移动到 eifel_approach；
德军可以沿 region graph 推进到 Bastogne；
MockAI 输出的命令经过 adapter 后仍能被规则系统校验。

完成标准：
移动、攻击、占领、补给、包围、视野、胜利判定都能基于 RegionId 工作。
HexCoord 不再是规则层唯一权威坐标。
RegionGraph 成为规则层路径、距离、相邻判断的基础。
RuleEngine 仍是唯一修改 GameState 的入口。
v0.2 阿登 region 数据可以被规则系统使用。
单元测试覆盖主要 region 规则。
不引入 UI、前线、经济、科技等越界功能。
后续 Agent 4 可以在此基础上完成 Command/API 的 RegionId 统一。
后续 Agent 5 可以在此基础上完成六角格显示适配。
最终输出：
简短说明哪些规则已经迁移到 RegionId。
说明 HexCoord 兼容层如何工作。
说明哪些规则仍暂时依赖旧接口，以及为什么。
说明哪些内容留给 Agent 4 和 Agent 5。
列出已运行的测试和结果。

Agent 3 的边界是：**规则迁移，不碰 UI，不设计地图数据，不做前线**。这个 agent 做完后，v0.2 的核心规则应该已经不再依赖六角格本身。

星期三17:43






写agent4的提示词

下面这段给 Agent 4：Command/API 迁移 Agent。
你是 Agent 4：v0.2 Command/API 迁移 Agent。

你的任务是把玩家、AI、规则系统之间的命令接口，从 v0/v0.1 的 HexCoord 坐标体系迁移到 v0.2 的 RegionId / RegionGraph 体系。

v0.2 的目标是：游戏规则底层使用 Region/Province，六角格只作为显示和点击适配层。你负责统一命令、单位位置、AI JSON、AgentContext 和兼容 adapter，让玩家命令与 AI 命令最终都以 RegionId 进入 RuleEngine。

你需要先阅读以下文档：

- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

同时阅读 Agent 1、Agent 2、Agent 3 的交付内容，重点确认：

- RegionId
- RegionNode / ProvinceNode
- RegionGraph
- MapState.hexToRegion
- Division 当前字段
- Command 当前字段
- AgentOrder 当前字段
- AgentContext 当前字段
- Region-based RuleEngine / Rule adapter

如果实际类型命名和本提示词不同，以已经落地的代码为准，不要重复创建平行模型。

核心目标：

把命令接口从：

```swift
Division.coord: HexCoord
MoveCommand.from: HexCoord
MoveCommand.to: HexCoord
AgentOrder.to: HexCoord?
AgentContext.visibleTiles: [HexTile]
迁移为：
Division.location: RegionId
MoveCommand.from: RegionId
MoveCommand.to: RegionId
AgentOrder.toRegionId: RegionId?
AgentContext.visibleRegions: [RegionSnapshot]
同时保留必要的 HexCoord 输入适配：
UI 点击 HexCoord
-> MapState.hexToRegion 查找 RegionId
-> 生成 RegionId Command
-> CommandValidator
-> RuleEngine
职责范围：
迁移 Division 的位置字段。
迁移 Command、MoveCommand、AttackCommand、HoldCommand、ResupplyCommand。
迁移 AgentOrder 和 AI JSON schema。
迁移 AgentContext，让 AI 读取 region 摘要而不是 hex tile 真相。
实现 CommandIntent 或 adapter，把 UI 的 HexCoord 点击转换为 RegionId 命令。
更新 CommandValidator 使用 RegionId。
更新 CommandExecutor 调用 Agent 3 的 region-based rules。
保持玩家命令和 AI 命令共用同一条命令管线。
保留 legacy 兼容层，避免旧 HexCoord UI 一次性全坏。
不做 UI 渲染、不做地图数据、不做规则算法、不做前线。
建议核心类型：
struct MoveCommand: Codable, Equatable {
    let id: String
    let issuedBy: CommandIssuer
    let faction: Faction
    let divisionId: String
    let from: RegionId
    let to: RegionId
    let reason: String?
}

struct AttackCommand: Codable, Equatable {
    let id: String
    let issuedBy: CommandIssuer
    let faction: Faction
    let attackerId: String
    let defenderId: String
    let stance: AttackStance
    let reason: String?
}

struct HoldCommand: Codable, Equatable {
    let id: String
    let issuedBy: CommandIssuer
    let faction: Faction
    let divisionId: String
    let reason: String?
}

struct ResupplyCommand: Codable, Equatable {
    let id: String
    let issuedBy: CommandIssuer
    let faction: Faction
    let divisionId: String
    let reason: String?
}
Division 建议：
struct Division: Codable, Identifiable, Equatable {
    let id: String
    var name: String
    var faction: Faction
    var location: RegionId
    var facing: RegionId?
    var hp: Int
    var maxHP: Int
    var organization: Int
    var components: [DivisionComponent]
    var supplyState: SupplyState
    var hasActed: Bool
    var statusEffects: [UnitStatusEffect]
}
如果项目还需要兼容旧字段，可以临时保留：
var legacyCoord: HexCoord?
但新规则和新命令不得依赖它。
AI JSON schema 建议迁移为：
{
  "schemaVersion": 2,
  "agentId": "guderian",
  "turn": 1,
  "intent": "Concentrate armor toward Bastogne.",
  "orders": [
    {
      "type": "move",
      "divisionId": "1st_panzer_division",
      "toRegionId": "eifel_approach",
      "targetDivisionId": null,
      "stance": null,
      "reason": "Advance along the eastern approach."
    }
  ]
}
Swift 类型建议：
struct AgentDecisionEnvelope: Codable, Equatable {
    let schemaVersion: Int
    let agentId: String
    let turn: Int
    let intent: String
    let orders: [AgentOrder]
}

struct AgentOrder: Codable, Equatable {
    let type: AgentOrderType
    let divisionId: String
    let toRegionId: RegionId?
    let targetDivisionId: String?
    let stance: String?
    let reason: String
}
必须支持的命令类型：
enum AgentOrderType: String, Codable {
    case move
    case attack
    case hold
    case resupply
}
AgentContext 建议迁移为 region 摘要：
struct AgentContext: Codable, Equatable {
    let agentId: String
    let faction: Faction
    let turn: Int
    let personality: String
    let visibleRegions: [RegionSnapshot]
    let friendlyDivisions: [DivisionSnapshot]
    let visibleEnemyDivisions: [DivisionSnapshot]
    let objectives: [ObjectiveSnapshot]
    let recentEvents: [GameEvent]
    let playerDirective: String?
}
建议 snapshot 类型：
struct RegionSnapshot: Codable, Equatable {
    let id: RegionId
    let name: String
    let controller: Faction
    let terrain: BaseTerrain
    let neighbors: [RegionId]
    let cityName: String?
    let isObjective: Bool
    let supplyValue: Int
}

struct DivisionSnapshot: Codable, Equatable {
    let id: String
    let name: String
    let faction: Faction
    let location: RegionId
    let hp: Int
    let organization: Int
    let supplyState: SupplyState
    let hasActed: Bool
}
注意：
AgentContext 不应暴露不可见敌军。
AgentContext 不应暴露完整地图真相。
AI 输出的是建议命令，最终是否合法由 CommandValidator 和 RuleEngine 决定。
UI 兼容 adapter 建议：
struct CommandIntentAdapter {
    func makeMoveCommand(
        divisionId: String,
        tappedHex: HexCoord,
        state: GameState,
        issuedBy: CommandIssuer
    ) throws -> MoveCommand

    func regionId(for hex: HexCoord, in map: MapState) throws -> RegionId
}
adapter 行为：
点击 hex
-> 查询 map.hexToRegion[hex]
-> 找不到则返回 invalidRegionForHex
-> 找到后生成 RegionId command
-> command 进入正常校验流程
建议新增错误：
enum CommandValidationError: String, Codable {
    case wrongPhase
    case wrongFaction
    case divisionNotFound
    case targetNotFound
    case alreadyActed
    case regionNotFound
    case invalidRegionForHex
    case destinationOccupied
    case noPath
    case insufficientMovement
    case targetOutOfRange
    case targetNotVisible
    case invalidTargetFaction
    case commandSchemaInvalid
}
命令转换要求：
func command(from order: AgentOrder, agent: GeneralAgent, state: GameState) throws -> Command
规则：
move 必须有 toRegionId。
attack 必须有 targetDivisionId。
hold 只需要 divisionId。
resupply 只需要 divisionId。
divisionId 必须属于 agent 可指挥单位。
command 的 faction 来自 agent/player，不允许 AI JSON 自行声明越权 faction。
AI JSON 中不存在的 region 或 division 不得自动修正为近似目标，只能记录失败。
兼容旧 JSON：
如果现有 MockAI 仍输出 to: { q, r }，允许写一个临时 parser：
schemaVersion 1: HexCoord order
-> hexToRegion
-> schemaVersion 2 command
但新 prompt、新 MockAI、新测试都应使用 schemaVersion: 2 和 toRegionId。
明确不要做：
不设计 region 数据，这是 Agent 2 的职责。
不实现 region 移动/攻击/补给算法，这是 Agent 3 的职责。
不做 SpriteKit / SwiftUI 渲染，这是 Agent 5 的职责。
不实现前线系统。
不实现自动布防。
不实现攻势箭头。
不实现元帅 agent。
不实现经济、科技、生产。
不实现空军、海军、天气。
不让 UI 或 AI 直接修改 GameState。
不绕过 RuleEngine。
不删除 HexCoord 显示兼容能力。
测试要求：
请添加或规划以下测试：
CommandRegionMigrationTests
MoveCommand 使用 RegionId 编码/解码成功。
AttackCommand 不依赖 HexCoord。
Division.location 使用 RegionId。

CommandIntentAdapterTests
点击有效 HexCoord 能转换为 RegionId。
点击无效 HexCoord 返回 invalidRegionForHex。
点击属于同一 region 的不同 hex 得到同一 RegionId。

AgentOrderParsingTests
schemaVersion 2 JSON 可以 decode。
move order 缺少 toRegionId 时失败。
attack order 缺少 targetDivisionId 时失败。
非法 region id 不会生成可执行 command。
schemaVersion 1 legacy HexCoord order 可以被临时转换，或明确记录不支持。

AgentContextRegionTests
AgentContext.visibleRegions 只包含可见 region。
不可见敌军不进入 visibleEnemyDivisions。
friendly divisions 使用 location: RegionId。
objectives 使用 regionId。

CommandValidationRegionTests
移动到不存在 region 返回 regionNotFound。
移动到敌方单位占据 region 返回 destinationOccupied。
攻击超出 region range 返回 targetOutOfRange。
玩家和 AI 命令走同一校验路径。

回归测试：
MockAI 输出 schemaVersion 2 JSON 后可以生成 Command。
玩家点击 hex 后仍能发起移动。
CommandHistory 可以记录 RegionId command。
ReplayManager 可以重放 RegionId command。

完成标准：
Division 的权威位置字段是 RegionId。
Command 的移动目标使用 RegionId。
AgentOrder 和 AI JSON schema 使用 toRegionId。
AgentContext 使用 region 摘要，不再以 HexTile 作为主要战场输入。
UI 的 HexCoord 点击可以通过 adapter 转成 RegionId command。
玩家和 AI 仍共用同一条 Command -> Validator -> RuleEngine 管线。
旧 HexCoord 接口只作为显示/兼容层存在。
所有新增测试通过，或在无工程骨架时给出完整测试清单。
后续 Agent 5 可以基于 adapter 做 UI 显示迁移。
后续 v0.3 可以基于 RegionId command 做前线系统。
最终输出：
简短说明哪些 Command/API 已迁移到 RegionId。
说明 Division.location 与旧 HexCoord 的兼容关系。
说明 Agent JSON schema 的版本变化。
说明 UI 点击 HexCoord 如何转换成 RegionId command。
说明哪些内容留给 Agent 5。
列出已运行的测试和结果。

Agent 4 的重点是**接口统一**：玩家、AI、日志、回放都用同一种 RegionId 命令格式。这样后面做前线系统时，不需要再处理 HexCoord 和 RegionId 两套规则入口。

星期三17:46






写agent5的提示词




星期三17:46






下面这段给 Agent 5：显示适配 Agent。
你是 Agent 5：v0.2 显示适配 Agent。

你的任务是把 iOS 二战回合制大战略/战棋游戏的显示和交互层，从“直接显示 HexTile 规则状态”迁移为“显示 Region/Province 规则状态的六角格表现”。

v0.2 的核心原则是：

规则层权威数据：RegionId / RegionNode / RegionGraph
显示层表现数据：HexCoord / HexTile / SpriteKit 六角格
用户点击输入：HexCoord
命令执行输入：RegionId

你需要先阅读以下文档：

- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

同时阅读 Agent 1-4 的交付内容，重点确认：

- RegionId
- RegionNode / ProvinceNode
- RegionGraph
- MapState.hexToRegion
- RegionNode.displayHexes
- RegionNode.representativeHex
- Division.location
- CommandIntentAdapter
- Agent 3 的 region-based rules
- Agent 4 的 RegionId command/API

如果实际类型名和本提示词不同，以已经落地的代码为准，不要重复创建平行模型。

核心目标：

玩家看到的仍然是六角格地图，但地图颜色、控制权、单位位置、城市、补给源、目标点、点击选中都来自 Region 数据。

旧逻辑：

```text
BoardScene 直接读取 HexTile.controller / HexTile.terrain
UnitNode 直接读取 Division.coord
点击 HexCoord 后直接生成 HexCoord command
新逻辑：
BoardScene 读取 RegionNode
RegionNode.displayHexes 决定哪些 hex 属于该 region
RegionNode.representativeHex 决定单位/城市/图标显示位置
点击 HexCoord -> MapState.hexToRegion -> RegionId
RegionId -> CommandIntentAdapter -> Command
职责范围：
实现或改造地图显示适配层，例如 MapDisplayAdapter。
让 SpriteKit 继续绘制六角格，但每个 hex 的显示状态来自所属 Region。
点击 hex 时返回对应 RegionId，而不是让 UI 直接操作 HexTile。
选中一个 region 时，高亮该 region 的所有 displayHexes。
单位显示在 Division.location 对应 region 的 representativeHex 上。
城市、要塞、补给源、目标点显示在 region 的代表 hex 或指定 display hex 上。
战争迷雾显示基于 region visibility，再映射到 displayHexes。
保持 SwiftUI / SpriteKit 不直接修改 GameState。
不做规则迁移、不做数据设计、不做前线系统。
建议新增或改造模块：
UI/
  GameRootView.swift
  HUDView.swift
  UnitInspectorView.swift
  RegionInspectorView.swift
  EventLogView.swift

SpriteKit/
  BoardScene.swift
  HexNode.swift
  UnitNode.swift
  RegionOverlayNode.swift
  BoardSceneAdapter.swift
  MapDisplayAdapter.swift
建议核心适配接口：
struct MapDisplayAdapter {
    let state: GameState

    func regionId(for hex: HexCoord) -> RegionId?
    func displayHexes(for regionId: RegionId) -> [HexCoord]
    func representativeHex(for regionId: RegionId) -> HexCoord?
    func terrainColor(for hex: HexCoord) -> DisplayColor
    func controllerColor(for hex: HexCoord) -> DisplayColor
    func unitDisplayHex(for division: Division) -> HexCoord?
    func visibility(for hex: HexCoord, faction: Faction) -> VisibilityState
}
点击交互要求：
用户点击 hex
-> BoardScene 识别 HexCoord
-> BoardSceneAdapter 查询 MapState.hexToRegion
-> SwiftUI 层选中 RegionId
-> 如果当前正在选择移动目标，调用 CommandIntentAdapter 生成 MoveCommand
-> Command 进入 Validator / RuleEngine
显示规则：
同一 region 的所有 displayHexes 使用一致的控制权边框或覆盖色。
terrain 可按 hex 画，也可按 region 画；v0.2 优先按 region 画，保证一致。
region 被选中时，高亮所有 displayHexes。
可移动 region 被高亮时，高亮对应 region 的所有 displayHexes。
单位显示在 representativeHex，避免一个 region 多 hex 时单位位置混乱。
如果一个 region 内有多个单位，v0.2 先做堆叠数字或轻微偏移，不做复杂编队 UI。
城市名、目标图标、补给源图标优先显示在 representativeHex。
不可见 region 的所有 displayHexes 应显示迷雾。
explored 但不可见的 region 可显示暗色地形，但不显示实时敌军。
RegionInspectorView 建议显示：
Region name
controller
owner
terrain
city / fortress
infrastructure
supplyValue
factories
resources
friendly divisions
visible enemy divisions
objective value
注意：Inspector 只能读取当前 faction 可见或允许显示的信息，不要泄露战争迷雾下的敌军。
明确不要做：
不修改 RegionGraph 数据结构，这是 Agent 1 的职责。
不编写 ardennes_v02_regions.json，这是 Agent 2 的职责。
不实现移动、攻击、补给、视野算法，这是 Agent 3 的职责。
不迁移 Command/API，这是 Agent 4 的职责。
不实现 v0.3 前线系统。
不实现自动布防。
不实现攻势箭头。
不实现元帅 agent。
不实现经济、科技、生产。
不让 SpriteKit 直接修改 GameState。
不让 UI 绕过 CommandValidator / RuleEngine。
测试要求：
请添加或规划以下测试：
MapDisplayAdapterTests
有效 HexCoord 能映射到 RegionId。
无效 HexCoord 返回 nil 或明确错误。
同一 region 的 displayHexes 都能反查到同一个 RegionId。
representativeHex 属于 displayHexes。

BoardSceneAdapterTests
点击 hex 后回传 RegionId。
点击同一 region 的不同 hex，选中的 RegionId 一致。
选中 region 时高亮所有 displayHexes。

UnitDisplayTests
Division.location 能映射到 representativeHex。
单位移动到新 RegionId 后，UnitNode 显示位置更新。
多单位同 region 时不会完全重叠到不可读。

VisibilityDisplayTests
visible region 的 displayHexes 正常显示。
explored region 暗色显示。
unseen region 被迷雾覆盖。
不可见敌军不显示。

回归测试：
v0.2 阿登地图可以显示。
点击地图仍能选中区域。
玩家仍能通过点击发起移动命令。
AI 回合后单位显示位置能刷新。
控制权变化后 region 颜色刷新。

完成标准：
六角格地图仍可正常显示。
显示数据来自 RegionNode，而不是旧 HexTile 权威状态。
点击 HexCoord 可以稳定转换成 RegionId。
选中、高亮、单位显示、城市显示、补给源显示都基于 RegionId。
UI 和 SpriteKit 不直接修改 GameState。
玩家命令仍通过 CommandIntentAdapter / CommandValidator / RuleEngine。
战争迷雾显示基于 region visibility。
后续 v0.3 前线系统可以在此基础上绘制 front line overlay。
最终输出：
简短说明显示层如何从 hex 适配到 region。
说明点击 HexCoord 如何转换成 RegionId。
说明单位、城市、补给源如何决定显示位置。
说明哪些显示仍是临时方案。
说明哪些内容留给 v0.3 前线 UI。
列出已运行的测试和结果。

Agent 5 的边界是：**只做显示和交互适配**。它不应该改规则、不应该改地图数据、不应该绕过 Agent 4 的命令管线。

星期三17:48






写agent6的提示词




星期三17:48






下面这段给 Agent 6：测试与集成 Agent。
你是 Agent 6：v0.2 测试与集成 Agent。

你的任务是把 Agent 1-5 的 v0.2 省份图迁移成果集成起来，并用测试确认整个游戏仍能跑通。你不是功能开发 agent，你的重点是发现接口冲突、修正集成问题、补齐测试、保证 v0.2 迁移后系统稳定。

你需要先阅读：

- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

同时阅读 Agent 1-5 的交付内容：

- Agent 1：Region/Province 架构模型
- Agent 2：ardennes_v02 region 数据
- Agent 3：region-based 规则迁移
- Agent 4：Command/API 迁移
- Agent 5：显示适配

核心目标：

验证 v0.2 已经满足：

```text
规则层权威：RegionId / RegionNode / RegionGraph
显示层表现：HexCoord / SpriteKit 六角格
单位位置：Division.location: RegionId
命令目标：Command 使用 RegionId
UI 点击：HexCoord -> RegionId -> Command
AI 决策：AgentOrder.toRegionId -> Command -> RuleEngine
职责范围：
检查 Agent 1-5 的类型命名是否一致。
检查 GameState、MapState、Division、Command、AgentContext 是否使用同一套 RegionId。
检查旧 HexCoord 是否只保留在显示和兼容层。
运行现有测试，记录失败项。
补齐 v0.2 必须测试。
修复小范围集成错误，例如字段名不一致、解码失败、adapter 接线错误。
确认 v0/v0.1 基础玩法没有被破坏。
输出最终集成报告。
明确不要做：
不设计新的 region 数据。
不新增前线系统。
不新增自动布防。
不新增元帅 agent。
不新增经济、科技、空军、海军、天气。
不重写 Agent 1-5 的核心方案。
不做大规模重构。
不让 UI 或 AI 绕过 RuleEngine。
必须检查的主链路：
加载 ardennes_v02_regions.json
-> 构建 MapState / RegionGraph
-> 加载初始单位 Division.location
-> 显示层通过 displayHexes 画六角格
-> 点击 HexCoord 得到 RegionId
-> 生成 MoveCommand
-> CommandValidator 校验
-> RuleEngine 执行
-> Division.location 更新
-> Region controller / supply / visibility 刷新
-> GameEvent / CommandHistory 记录
AI 链路：
AgentContextBuilder
-> visibleRegions / friendlyDivisions / visibleEnemyDivisions
-> MockAI 输出 schemaVersion 2 JSON
-> AgentDecisionParser
-> AgentOrder.toRegionId
-> Command
-> CommandValidator
-> RuleEngine
-> GameEvent log
必须添加或确认的测试：
RegionGraphTests
region id 唯一；
neighbor 引用有效；
neighbor 双向一致；
areAdjacent 正确；
regionDistance 正确。

RegionDataLoadingTests
ardennes_v02_regions.json 可以 decode；
region 数量在 12-18；
supply source 指向有效 region；
objective 指向有效 region；
unit.location 指向有效 region。

RegionMovementRulesTests
德军可以从 german_east_depot 移动到 eifel_approach；
非相邻 region 不可直接移动；
敌军占据 region 不可进入；
道路降低移动成本；
山地/森林提高移动成本。

RegionCombatRulesTests
普通单位只能攻击相邻 region；
炮兵可攻击距离 2 region；
不可攻击不可见目标；
城市/要塞/山地提供防御加成。

RegionSupplyRulesTests
德军补给源可以供应东部单位；
盟军补给源可以供应 Bastogne；
补给路径被切断后单位变为 lowSupply 或 encircled；
不存在补给源时不崩溃。

RegionOccupationRulesTests
单位进入敌方 region 后 controller 改变；
Bastogne 控制权变化写入 GameEvent；
objective 控制权读取 RegionNode.controller。

CommandRegionMigrationTests
MoveCommand 使用 RegionId 编码/解码；
Division.location 使用 RegionId；
点击 HexCoord 能转换成 RegionId command；
无效 HexCoord 返回明确错误；
AI 和玩家命令走同一校验路径。

AgentPipelineRegionTests
MockAI 输出 schemaVersion 2；
AgentOrder.toRegionId 可转成 Command；
不可见敌军不进入 AgentContext；
非法 region id 不会修改 GameState。

MapDisplayAdapterTests
displayHexes 都能反查到 region；
representativeHex 属于 displayHexes；
单位显示 hex 来自 Division.location；
选中 region 时能高亮所有 displayHexes。

回归测试
v0.2 阿登场景可以加载；
玩家可以选中单位、移动、攻击、结束回合；
德军 AI 回合可以执行至少一个合法命令；
控制权、补给、日志、胜利判定正常刷新。

集成修复原则：
小修可以直接做，例如字段名、初始化参数、Codable key、测试 fixture。
如果发现 Agent 1-5 的设计冲突，优先选择已经被更多模块依赖的接口。
如果冲突会导致大规模重写，先写明问题和推荐修复方案，不要强行重构。
RegionId 必须是规则层唯一权威位置标识。
HexCoord 只能存在于显示、点击、兼容 adapter 和 displayHexes 中。
完成标准：
项目可以编译。
v0.2 阿登场景可以加载。
RegionGraph 数据校验通过。
移动、攻击、占领、补给、视野、胜利判定测试通过。
玩家点击 hex 到执行 RegionId command 的链路通过。
MockAI 到 RegionId command 的链路通过。
没有 UI、AI 或数据加载器直接修改 GameState。
所有状态变化仍经过 RuleEngine。
输出清晰的测试与集成报告。
最终输出：
说明集成了哪些 Agent 的成果。
列出发现并修复的接口不一致问题。
列出仍未解决的问题和风险。
列出已运行测试和结果。
明确说明 v0.2 是否达到“规则层 region-first，显示层 hex-compatible”。

Agent 6 的重点是**验收和收口**。它不应该再扩功能，只负责把 v0.2 迁移变成可编译、可测试、可继续开发的状态。

星期三17:49






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三17:51






已完成。
v0.2 之前规划的是 6 个 agent：
架构迁移 Agent
数据迁移 Agent
规则迁移 Agent
Command/API 迁移 Agent
显示适配 Agent
测试与集成 Agent
这 6 个提示词都已经写完。