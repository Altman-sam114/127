# v0.21 QA Report

Date: 2026-06-16

## Scope

- Task 3: Info panel collapse/toggle
- Task 4: fixed bottom-right unit tooltip
- Task 5: New Game reset
- Task 6: skipped zoom by design
- Task 7: integration verification

## Implementation Summary

- Added `UI/InfoPanelToggle.swift`
  - Default collapsed.
  - `[ INFO ]` toggles expanded panel.
  - Collapsed state keeps HUD/action summary visible.

- Added `UI/UnitTooltipView.swift`
  - Fixed SwiftUI overlay at `.bottomTrailing`.
  - Shows selected unit name, type, strength, supply, retreat mode, acted state.
  - Hidden when `selectedDivision == nil`.

- Added `UI/NewGameButton.swift`
  - HUD-level `[ NEW GAME ]` action.
  - Calls `AppContainer.resetGame()`.

- Updated `AppContainer.resetGame()`
  - Reloads initial state through existing `dataLoader`.
  - Clears selected unit/hex/region, highlights, logs, last command, last AI record.
  - Calls `runAIIfNeeded()` after reset.

- Updated `RootGameView`
  - Regular side panel wrapped by `InfoPanelToggle`.
  - Portrait compact panel wrapped by `InfoPanelToggle`.
  - Landscape compact right panel wrapped by `InfoPanelToggle`.
  - Global `UnitTooltipView` overlay added.

- Updated `HUDView`
  - Optional New Game callback.
  - New Game and End Turn buttons kept compact/single-line.

## Automated Verification

Command:

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test \
  -scheme WWIIHexV0 \
  -destination 'id=A1EA6908-3306-44E0-9F99-F0F77DAE2404' \
  -derivedDataPath /private/tmp/WWIIHexV0DerivedData \
  CODE_SIGNING_ALLOWED=NO
```

Result:

- 135 tests
- 0 failures
- `** TEST SUCCEEDED **`

Static checks:

- `swiftc -parse WWIIHexV0/**/*.swift`: passed
- `plutil -lint WWIIHexV0.xcodeproj/project.pbxproj`: passed
- `git diff --check`: passed

## Simulator Smoke

- App installed and launched on simulator `A1EA6908-3306-44E0-9F99-F0F77DAE2404`.
- Screenshot captured: `/tmp/wwiihex_v021_smoke2.png`.
- Visible checks:
  - Hex board renders.
  - `[ INFO ]` collapsed button visible.
  - HUD visible in collapsed state.
  - `NEW GAME` visible.
  - `End Turn` visible.
  - Buttons no longer wrap in portrait screenshot.

## Manual Checklist

- [x] hex 放大可见
- [x] 拖动平移代码路径保留，未被任务 3-5 改动
- [x] tap 选单位代码路径保留，测试覆盖 `BoardInteractionTests`
- [x] NATO 兵牌代码路径保留，未被任务 3-5 改动
- [x] 横屏布局代码路径保留，右侧面板已接 `InfoPanelToggle`
- [x] Info 面板 toggle 已实现
- [x] 右下角单位信息框已实现
- [x] New Game 重置已实现

## Notes

- Task 6 zoom skipped. Current v0.21 uses enlarged fixed hex + camera drag.
- No Region/Hex rule, AI schema, Command schema, Combat, Supply, Movement changes.
- Hex tactical layer and Region strategic layer remain unchanged.
