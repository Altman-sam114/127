# v6.10 发布候选验收证据矩阵

日期：2026-07-06

本文把 `codex-v6.0-现代战争aiagent迁移总提示词.md` 的发布候选要求逐项映射到当前代码、文档、轻量检查和云端 artifact。结论必须保守：当前可声明为“发布候选代码和文档已准备，已核对的 `origin/main` 代码行为提交云端静态检查、通用 iOS build 和 `WWIIHexV0Probes` simulator probe 通过”。未获人工授权前，不声明正式发布或运行时发布级已验证。下方 artifact 覆盖表中代码行为提交；若本证据文档后续以 doc-only commit 更新，最终交付仍必须重新核对最新 `origin/main` run，并在交付中写明最新 run / artifact。

## 1. 云端 artifact 证据

本文已记录的最近已核对代码行为提交；若后续继续提交，最终交付必须以最新 `origin/main` run 为准重新下载 artifact：

| 项目 | 值 |
| --- | --- |
| branch | `main` |
| commit | `0829ed4672c36c2a54f371dd0113f5a3a89485a8` |
| workflow | `WWIIHexV0 CI Results` |
| runId | `28790707794` |
| runAttempt | `1` |
| artifact | `WWIIHexV0-ci-cloud-flow-v1-main-0829ed4-run28790707794-attempt1` |
| 本机缓存 | `/private/tmp/wwiihexv0-c-review-28790707794/WWIIHexV0-ci-cloud-flow-v1-main-0829ed4-run28790707794-attempt1/` |
| manifest | `branch=main`、`commitSha=0829ed4672c36c2a54f371dd0113f5a3a89485a8`、`runId=28790707794`、`runAttempt=1` |
| JUnit | `tests=4`、`failures=0`、完整 XCTest / UI / Smoke / Stage / Full 按当前 cloud-flow policy 标记 skipped |
| failure summary | `No CI failures detected.` |
| xcodebuild.log | `BUILD SUCCEEDED` |
| cloud outcomes | `staticChecksOutcome=success`，`buildOutcome=success`，`probeOutcome=success`，`simulatorOutcome=success`，`testOutcome=skipped` |

本次云端证据说明：`0829ed4672c36c2a54f371dd0113f5a3a89485a8` 覆盖云端 Probe workflow 接入、历史 Probe 断言现代化、灰潮默认数据 / command sector / current tactic 语义对齐和最终 artifact 复核；artifact manifest 标记 `staticChecksOutcome=success`、`buildOutcome=success`、`probeOutcome=success`、`simulatorOutcome=success`、`testOutcome=skipped`，`grey-tide-data.log`、`modern-visible-text.log`、`xcodebuild.log` 和 `probe-xctest.log` 均通过，`WWIIHexV0Probes` 为 18 tests / 0 failures。

注意：本文后续每次文档或代码提交都会触发新的 `origin/main` 云端 run；最终交付时必须以最新 commit 对应 artifact 为准重新核对。

## 2. 发布候选要求矩阵

| 总提示词要求 | 当前状态 | 证据 | 未验证风险 |
| --- | --- | --- | --- |
| 默认场景是现代战争剧本，不是阿登 | 已扩到发布候选规模 | `DataLoader.loadInitialGameState()` 优先加载 `grey_tide_2030_scenario` + `grey_tide_2030_regions`；当前灰潮为 120 hex / 30 region / 16 初始 formation / 25 objectives，旧阿登仅 fallback | 地图玩法节奏仍需运行时长回合验证 |
| MapEditor 覆盖默认资源不破坏灰潮高级元数据 | 已补桥接保护 | `MapEditorGameResourceBridge.overwriteDefaultGameResources` 在写回 `grey_tide_2030_*` 前回填既有 `maxTurns`、victory conditions、tile `riverEdges`、objective points、region VP、occupation、river crossing 和 mainObjective；MapEditor UI 只暴露现代 Blue / Red / Green 阵营；无名 supply key location 默认显示为 `Logistics Node q,r`，底层 supply kind / id 保持兼容 | 未做 MapEditor app 运行时点击和真实覆盖写回验收 |
| 主 UI 第一屏是可操作战场，不是说明页 | 已具备首版 | `RootGameView` 仍承载 SpriteKit 战场、HUD、任务 / Playtest / Agent 面板；Info toggle 保持自身 44pt 命中区，不再把按钮 frame 扩成整屏，降低遮挡地图点击风险；Playtest 面板提供 C2 Overlay 独立显示开关和只读 Legend，把 sensor、jammed sensor、EW area、fire result、contact confidence、contact type code 和 logistics 标记与地图态势关联 | 未做本机启动、截图和 UI 点击烟测 |
| 玩家可选择作战方或至少明确扮演方 | 已接入红/蓝新局选择 | `ModernPlaytestPanelView` 提供 New Operation Side segmented control；`AppContainer.resetGame(playerFaction:)` 按 Blue / Red 设置 `playerFaction`、初始 `activeFaction` 和 `GamePhase`；`GamePhase` 仍保留 `.germanAI` / `.alliedPlayer` Swift case 兼容旧代码，但 Codable、`GamePhase.dataValue(_:)`、DataLoader、MapEditor exporter 和默认灰潮 `initialPhase` 已支持 / 输出 `redCommand`、`blueCommand` 现代 alias；Action Gate 只读解释当前玩家 / AI / observer / end-turn 推进状态并点明当前阵营；AI 只接管非玩家敌对阵营 | 未做本机 UI 点击烟测确认选择器交互、Action Gate 排版和红方开局体验 |
| 新局 / 继续 / 重置不污染默认资源 | 已具备 schema 化本地快照 | `AppContainer.saveLocalSnapshot()` 保存 `LocalPlaytestSnapshot` envelope；`loadLocalSnapshot()` 兼容 envelope 与旧裸 `GameState`；默认 JSON 资源只读；Playtest feedback 对保存 / 继续 / 清除结果使用 success / failure tone，Clear Snapshot 使用 destructive role | 未做运行时保存 / 继续点击烟测和损坏快照 UI 验证 |
| AI 能通过结构化 directive 行动，失败有 fallback | 已具备审计路径 | `MarshalAgent -> Operational Directive -> ModernCommandChain -> TheaterDirectiveCompiler -> ZoneDirective`；`TurnManager` 记录 raw JSON、compiled directive 和 diagnostics；directive 或 advisory JSON 校验失败时尽量保留原始 JSON | 未做 10-20 回合 observer 长跑验证 |
| 玩家和 AI 都经统一规则管线 | 已保持 | 玩家任务由 `AppContainer` 生成 `Command` 或 `ZoneDirective`；`ModernMissionPanelView` 只读展示 Mission Status 并调用 AppContainer 闭包；Recon / UAV / Fire Mission / SEAD / EW / Resupply 启用条件分别复用 `CommandValidator` 预检 formation、target、弹药、冷却、目标质量、ROE、防空和友邻风险；Fire Mission 不再无选择地回退到第一个 visible contact；AI directive 经 `WarCommandExecutor`、`RuleEngine` 执行 | 未做运行时点击覆盖所有按钮 |
| 多方 ROE 不把 non-hostile 当敌人 | 已继续收口主路径 | 攻击和 Region 攻击校验、点击棋盘直接攻击、玩家宏观攻击 zone、ZOC、补给路径、邻敌补员 / 部署、WarDeployment enemy zone / presence、FrontLine 动态敌邻区和 FireSupport 防空威胁均走 `Faction.isHostile(to:)`；`Faction.opponent` 仍仅作 legacy compatibility 属性 | `OccupationRules.canOccupy` 仍允许地面移动夺取显式 neutral capturable 目标，以保持灰潮主目标控制闭环；restricted / civilian 深水 ROE 仍需后续版本细化 |
| restricted / civilian 火力 ROE | 已具备首版门禁 | `FireSupportRules` 将 neutral / green / 同阵营协同方控制区识别为 restricted fire zone；tube / rocket area fires 被 `restrictedFireZone` 拒绝，precision / loitering 只有 linked hostile target 时可执行，并通过 `FireRiskFlag.restrictedFireZone` 降级结算和写入 fireSupport 风险日志；linked contact 会用当前目标坐标参与 ROE / range / AD / EW / risk 计算，避免用过期 contact 坐标绕过门禁 | 仍未建模完整 no-fire zones、collateral damage、授权升级链和民用损害复盘 |
| neutral / civilian 目标口径不破坏胜负闭环 | 已文档化并纳入静态检查 | 十个主目标中 Harbor Terminal、River Bridge、Comms Center、Fuel Depot、Rail Junction、Refinery District 初始为 neutral，可被地面移动占领并进入胜负计数；Industrial Hub、Urban Core、Civic Center、River Ford、Southern Causeway、Civilian Evac Zone 是 neutral / civilian key-only 关键点，不计入十个主目标阈值；`scripts/check_grey_tide_data.rb` 校验 VictoryRules、scenario victoryConditions 和 region mainObjective 三套主目标集合一致，并核对 objective/key location 一一映射、region edge/neighbor、一线补给源和初始单位落点静态语义 | 仍未做运行时完整局验证；未来若扩展 objective schema，仍需同时更新数据、规则和检查脚本 |
| Hex 战术权威不变 | 已保持 | `flow.md` 明确 `HexTile.controller` 与 `Division.coord` 是战术权威；现代 ISR / Fire / EW 作为派生或任务层 | 未用重回归测试覆盖所有旧战术边界 |
| `regionToTheater` 是基础映射 | 已保持 | `flow.md` 和 `flowchart.md` 明确 region 只做初始 / fallback，不是运行时推进权威 | 未做动态战区长回合回归 |
| `hexToTheater` / `hexToFrontZone` 是动态权威 | 已保持 | `flow.md` 写明动态战区和部署层权威边界，命令推进仍落到 hex | 未做 Stage / Dynamic Theater Regression |
| 侦察、contact、EW、火力、防空、补给有可解释首版闭环 | 已具备抽象首版并补派生态势显示 | v6.4 `OperationalAwarenessState` / `VisibilityRules`，v6.5 `FireSupportState` / `FireSupportRules`，v6.7 任务入口，v6.8 overlay；`Division` 只读派生 readiness、fuel posture 和 signature posture，任务面板、单位详情和 tooltip 展示这些状态；Playtest 面板新增 C2 Overlay 独立显示开关和 Legend，补齐 sensor / jammed / EW / fire / contact / logistics 首版图例与开关控制；最近云端 build 已通过 | 真实武器库、真实 fuel 消耗、fuel / readiness / signature 独立字段、复杂空战未建模；运行时 UI 仍未验证 |
| AI 面板能展示 raw JSON、编译后 directive、结果和拒绝原因 | 已具备首版并增强结构化回放 | `TurnManager` 写 raw JSON；`AgentDecisionRecord.commandChainReplayItems` 保存已验证 sub-directive 摘要；`AgentPanelView` 主视图展示 Advisory command chain、command results 和 errors，并把 raw agent id 转为 command planner 显示名；完整 Operational Directives、diagnostics 与 Technical Replay JSON 默认折叠保留，避免普通玩家首先看到 schema 明细；失败路径保留 raw JSON 便于复盘；玩家和 AI / directive 拒绝路径使用 `CommandValidationError.displayMessage`，directive decoder、Modern Command Chain decoder、command intent mapping 和 command result fallback 失败也继续转成 Sector / Objective / Formation / Contact Track / tactic display 文案 | 未做 UI 截图验证长文本排版 |
| 战报解释侦察、contact、火力、EW、后勤、占领、撤退、失败 | 已具备首版 | `EventLog`、`CommandResult`、`fireSupport` / `intelligence` / `electronicWarfare` 日志分类、Mission Status、命令反馈、Playtest Action Gate 和 Field Prompts；EventLog metadata 不再把 directive related record id 直接显示给玩家；Mission Status 会列出 Ready Tasks 或首个 validator 阻塞原因，Playtest feedback 使用 success / warning / failure tone 与可访问标签，玩家宏观 directive 部分失败会显示首条可读规则拒绝原因，AI order diagnostics / AgentPanel command results / RuleEngine message / WarCommandExecutor / StrategicStateSynchronizer 事件日志不再直接显示 validation enum raw value、debug command string 或主要 region / zone raw id，事件分类和经济日志主路径使用 Logistics / sustainment 口径 | 未做完整战役回放验收 |
| 胜负判断形成现代闭环 | 已具备首版且 Playtest 可读 | `VictoryRules` / `RegionVictoryRules` 对 `grey_tide_2030` 使用十个主目标控制数判定：蓝方 7 个主目标提前胜，最终回合蓝方至少 6 个主目标胜，否则红方胜；`ModernPlaytestPanelView` 显示 Blue / Red / Neutral 主目标控制摘要，Blue 玩家看到达标阈值，Red 玩家看到阻止 Blue 达标的阈值 | 未做 10-20 回合 observer 或手动完整局验证；UI 排版未截图验收 |
| UI 没有主要二战文案残留 | 已继续收口主路径命中并新增可复现防回归脚本 | App display name 为 `Modern Command Agent`；补给源标签为 `LOG B` / `LOG R`；默认 `generals.json` 为虚构 Blue / Red commander；`GeneralDispatcher` seed 匹配排除已分配 commander；AI 面板空态、National Command、Commander / Unit / Region / Economy / Diplomacy 面板可见标签已避开 `guderian`、`MockAI`、ruler、theater、front zone、division、IC / supplies 旧口径和主要 raw id；Unit / Region inspector 的 operational zone、command sector、region/contact line 值不再直接显示 `theater_*` / `front_zone_*` / `region_*` raw value，默认灰潮 `TheaterNode.name` 也从 theater id 派生为可读 command sector 名称并传入 `FrontZone.name`，但 `TheaterId` / `FrontZoneId` / mapping raw value 保持不变；Agent / Diplomacy 面板的 National Command、focus sector、country/bloc relation 不再直接显示 `ruler_*` / `authority_*` / coalition raw id，Agent 面板 directive 摘要、objective 和 contact 目标、agent/context 主显示、command / region command / fire target display、directive decoder / Modern Command Chain decoder / command intent mapping 诊断也转为可读文案；legacy local LLM prompt 不再自称 WWII 原型，Modern Command Chain logistics rationale 使用 sustainment / logistics-constrained 口径，Playtest fallback scenario 标题改为 compatibility 口径；Blue 玩家目标阈值文案不再硬编码 Blue 主语，Red 玩家仍看到 deny Blue 达标口径；HUD、任务、单位 tooltip / inspector、事件日志和经济日志主路径把 `Supply` 展示收口为 Logistics / sustainment；Mission Planning / Ready Tasks / command result / directive reject / region controller 事件和 AgentPanel command result 主路径继续改用 Sustainment / Logistics 口径与 `Command.contextualDisplayName(in:)`，优先显示 formation、region 和 contact 的上下文名称；默认数据和 UI 走灰潮与现代 display name；`scripts/check_modern_visible_text.rb` 扫描主应用 Swift 字符串、命令结果 / 日志 / diagnostics 字符串、Core 可见 displayName 映射和默认现代 JSON 显示字段，防止主要 WWII / 阿登 / Panzer / Guderian / Germany / Allies / SUP A / SUP G 等旧口径回归；该脚本已接入 GitHub Actions artifact 的 `modern-visible-text.log` | target/module/bundle id、源码兼容名、旧 fallback 数据和历史文档仍保留；脚本是静态检查，不替代运行时截图和 UI 点击验收 |
| 新 JSON 通过解析 | 已纳入云端静态检查 | `grey_tide_2030_scenario.json`、`grey_tide_2030_regions.json`、`modern_unit_templates.json` 用 JSON 解析检查；默认灰潮 `initialPhase` 使用 `blueCommand`，`scripts/check_grey_tide_data.rb` 会在云端核对该现代 phase alias；该脚本还核对 tile / region / objective / unit template 引用，解析 `VictoryRules.greyTideMainObjectiveIds` 复查十个主目标三源一致性，并检查 objective/key location、edge/neighbor、补给源和初始单位落点语义；该脚本已接入 GitHub Actions artifact 的 `grey-tide-data.log` | JSON 语义仍需运行时加载和玩法验证 |
| 灰潮模板引用不静默降级 | 已收口 | `DataLoader.makeDivisions` 对 `grey_tide_2030` 禁止未知 templateId fallback；旧阿登和历史 fixture 仍可走 legacy fallback | 已通过轻量 parse / JSON 检查和最近云端 build；运行时加载仍待人工授权验证 |
| 云端 Probe 运行时入口 | 已接入 workflow，最近 artifact 已核对通过 | `.github/workflows/ci-results.yml` 在通用 iOS build 成功后选择可用 iPhone simulator，启动 simulator 并运行 `WWIIHexV0Probes`；`f1dd9fe/28789559115` 首次确认 workflow 与 simulator 可运行但旧 Probe 断言失败，`9a35abf/28790257478` 剩余默认 front line 旧断言失败，`0829ed4/28790707794` 已确认 `staticChecksOutcome=success`、`buildOutcome=success`、`simulatorOutcome=success`、`probeOutcome=success`，`WWIIHexV0Probes` 18/0；artifact 上传 `probe-xctest.log`、`simulator.log` 和 `WWIIHexV0Probes.xcresult` | 该 probe 不等于真实 App UI 启动、SpriteKit 截图、完整 XCTest、长回合 observer 或性能验证；本机仍未运行 Probe / simulator |
| project 文件改动通过 `plutil` | 已通过最近云端检查 | artifact manifest 标记 static success；`plutil.log` 为 `WWIIHexV0.xcodeproj/project.pbxproj: OK` | 本轮文档变更不改 project |
| README / flow / update_log 描述当前真实状态 | 已同步并继续收口 | `README.md`、`md/flow/flow.md`、`md/flow/flowchart.md`、`update_log.md` 已记录 v6.10 和本证据矩阵；README 已修正 RootGameView AI 触发口径，flowchart 已补 `DIP` 节点，update_log 已标明红/蓝新局选择器是在后续 v6.10 追加完成 | 若后续运行时验证失败，必须追加修正文档 |
| 未跑重测试范围和风险写清楚 | 已写清楚 | `md/test/test.md`、`v6.10_release_candidate_progress.md` 和本文列出禁止本机执行项与风险 | 运行时风险未消除，只是被明确记录 |

## 3. 仍需人工授权的发布前验证

未获授权前不得主动执行：

- 完整 XCTest / Smoke / Stage / Full。
- iOS Simulator、真机或 macOS app 启动。
- UI 点击烟测。
- SpriteKit 截图、视觉验收或 AppIcon 实机显示检查。
- 10-20 回合 observer 模式。
- 性能体感检查。

这些检查通过前，只能把 v6.10 称为发布候选准备态，不能称为正式发布或运行时已验证。
