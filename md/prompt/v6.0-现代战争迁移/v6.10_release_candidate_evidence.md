# v6.10 发布候选验收证据矩阵

日期：2026-07-05

本文把 `codex-v6.0-现代战争aiagent迁移总提示词.md` 的发布候选要求逐项映射到当前代码、文档、轻量检查和云端 artifact。结论必须保守：当前可声明为“发布候选代码和文档已准备，已核对的 `origin/main` 基线云端 build 通过”；未获人工授权前，不声明正式发布或运行时发布级已验证。

## 1. 云端 artifact 证据

上一轮已核对的基线代码行为提交；若后续继续提交，最终交付必须以最新 `origin/main` run 为准重新下载 artifact：

| 项目 | 值 |
| --- | --- |
| branch | `main` |
| commit | `67156c495b558e9216fd0427625bc8c6bc1bb7bb` |
| workflow | `WWIIHexV0 CI Results` |
| runId | `28730967730` |
| runAttempt | `1` |
| artifact | `WWIIHexV0-ci-cloud-flow-v1-main-67156c4-run28730967730-attempt1` |
| 本机缓存 | `/private/tmp/wwiihexv0-c-review-28730967730/WWIIHexV0-ci-cloud-flow-v1-main-67156c4-run28730967730-attempt1/` |
| manifest | `branch=main`、`commitSha=67156c495b558e9216fd0427625bc8c6bc1bb7bb`、`runId=28730967730`、`runAttempt=1` |
| JUnit | `tests=3`、`failures=0`、XCTest 按当前 cloud-flow policy 标记 skipped |
| failure summary | `No CI failures detected.` |
| xcodebuild.log | `BUILD SUCCEEDED` |
| static checks | `staticChecksOutcome=success`，`buildOutcome=success`，`testOutcome=skipped` |

注意：本文后续每次文档或代码提交都会触发新的 `origin/main` 云端 run；最终交付时必须以最新 commit 对应 artifact 为准重新核对。

## 2. 发布候选要求矩阵

| 总提示词要求 | 当前状态 | 证据 | 未验证风险 |
| --- | --- | --- | --- |
| 默认场景是现代战争剧本，不是阿登 | 已扩到发布候选规模 | `DataLoader.loadInitialGameState()` 优先加载 `grey_tide_2030_scenario` + `grey_tide_2030_regions`；当前灰潮为 120 hex / 30 region / 16 初始 formation / 25 objectives，旧阿登仅 fallback | 地图玩法节奏仍需运行时长回合验证 |
| 主 UI 第一屏是可操作战场，不是说明页 | 已具备首版 | `RootGameView` 仍承载 SpriteKit 战场、HUD、任务 / Playtest / Agent 面板 | 未做本机启动、截图和 UI 点击烟测 |
| 玩家可选择作战方或至少明确扮演方 | 已接入红/蓝新局选择 | `ModernPlaytestPanelView` 提供 New Operation Side segmented control；`AppContainer.resetGame(playerFaction:)` 按 Blue / Red 设置 `playerFaction`、初始 `activeFaction` 和 `GamePhase`；AI 只接管非玩家敌对阵营 | 未做本机 UI 点击烟测确认选择器交互和红方开局体验 |
| AI 能通过结构化 directive 行动，失败有 fallback | 已具备审计路径 | `MarshalAgent -> TheaterDirective -> ModernCommandChain -> TheaterDirectiveCompiler -> ZoneDirective`；`TurnManager` 记录 raw JSON、compiled directive 和 diagnostics | 未做 10-20 回合 observer 长跑验证 |
| 玩家和 AI 都经统一规则管线 | 已保持 | 玩家任务由 `AppContainer` 生成 `Command` 或 `ZoneDirective`；AI directive 经 `WarCommandExecutor`、`RuleEngine` 执行 | 未做运行时点击覆盖所有按钮 |
| Hex 战术权威不变 | 已保持 | `flow.md` 明确 `HexTile.controller` 与 `Division.coord` 是战术权威；现代 ISR / Fire / EW 作为派生或任务层 | 未用重回归测试覆盖所有旧战术边界 |
| `regionToTheater` 是基础映射 | 已保持 | `flow.md` 和 `flowchart.md` 明确 region 只做初始 / fallback，不是运行时推进权威 | 未做动态战区长回合回归 |
| `hexToTheater` / `hexToFrontZone` 是动态权威 | 已保持 | `flow.md` 写明动态战区和部署层权威边界，命令推进仍落到 hex | 未做 Stage / Dynamic Theater Regression |
| 侦察、contact、EW、火力、防空、补给有可解释首版闭环 | 已具备抽象首版 | v6.4 `OperationalAwarenessState` / `VisibilityRules`，v6.5 `FireSupportState` / `FireSupportRules`，v6.7 任务入口，v6.8 overlay | 真实武器库、fuel / readiness / signature 独立字段、复杂空战未建模 |
| AI 面板能展示 raw JSON、编译后 directive、结果和拒绝原因 | 已具备首版 | `TurnManager` 写 raw JSON；`AgentPanelView` / `WarDirectiveRecord` / `lastCommandMessage` 展示结果和 diagnostics | 未做 UI 截图验证长文本排版 |
| 战报解释侦察、contact、火力、EW、补给、占领、撤退、失败 | 已具备首版 | `EventLog`、`CommandResult`、`fireSupport` / `intelligence` / `electronicWarfare` 日志分类和命令反馈 | 未做完整战役回放验收 |
| 胜负判断形成现代闭环 | 已具备首版 | `VictoryRules` / `RegionVictoryRules` 对 `grey_tide_2030` 使用十个主目标控制数判定：蓝方 7 个主目标提前胜，最终回合蓝方至少 6 个主目标胜，否则红方胜 | 未做 10-20 回合 observer 或手动完整局验证 |
| UI 没有主要二战文案残留 | 已处理主路径首轮命中 | App display name 为 `Modern Command Agent`；补给源标签为 `SUP B` / `SUP R`；默认数据和 UI 走灰潮与现代 display name | target/module/bundle id、源码兼容名、旧 fallback 数据和历史文档仍保留 |
| 新 JSON 通过解析 | 已纳入轻量检查 | `grey_tide_2030_scenario.json`、`grey_tide_2030_regions.json`、`modern_unit_templates.json` 用 `jq empty` 检查；灰潮数据一致性脚本核对 tile / region / objective / unit template 引用 | JSON 语义仍需运行时加载和玩法验证 |
| project 文件改动通过 `plutil` | 已通过最近云端检查 | artifact manifest 标记 static success；`plutil.log` 为 `WWIIHexV0.xcodeproj/project.pbxproj: OK` | 本轮文档变更不改 project |
| README / flow / update_log 描述当前真实状态 | 已同步并继续收口 | `README.md`、`md/flow/flow.md`、`md/flow/flowchart.md`、`update_log.md` 已记录 v6.10 和本证据矩阵 | 若后续运行时验证失败，必须追加修正文档 |
| 未跑重测试范围和风险写清楚 | 已写清楚 | `md/test/test.md`、`v6.10_release_candidate_progress.md` 和本文列出禁止本机执行项与风险 | 运行时风险未消除，只是被明确记录 |

## 3. 仍需人工授权的发布前验证

未获授权前不得主动执行：

- Xcode build / XCTest / Probe / Smoke / Stage / Full。
- iOS Simulator、真机或 macOS app 启动。
- UI 点击烟测。
- SpriteKit 截图、视觉验收或 AppIcon 实机显示检查。
- 10-20 回合 observer 模式。
- 性能体感检查。

这些检查通过前，只能把 v6.10 称为发布候选准备态，不能称为正式发布或运行时已验证。
