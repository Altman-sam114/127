# v6.9 Playtest Loop Progress

日期：2026-07-05

## 目标

按 `codex-v6.0-现代战争aiagent迁移总提示词.md` 的 v6.9 要求，推进新手引导、继续、设置和试玩闭环首轮。本轮限定为主界面 Playtest 面板和受控本地快照，不做完整发布候选、不做多存档浏览器、不改战斗规则或 AI 指挥链。

## 已实现

- 新增 `WWIIHexV0/UI/ModernPlaytestPanelView.swift`。
- `RootGameView` 新增 `CompactInfoPanel.playtest / "Playtest"`。
- `AppContainer` 新增：
  - `saveLocalSnapshot()`；
  - `loadLocalSnapshot()`；
  - `clearLocalSnapshot()`；
  - `localSnapshotSummary`；
  - `canLoadLocalSnapshot`；
  - `scenarioDisplayName`；
  - `playtestGuidanceItems`。
- Playtest 面板展示 Operation / Player / Turn、New Operation、Save / Continue / Clear Snapshot、Observer AI、Default Layer、Field Prompts 和 last command feedback。
- 本地快照使用 `JSONEncoder` 编码当前 `GameState`，存入 `UserDefaults` key `modernCommandAgent.localSnapshot.v1`；摘要存入 `modernCommandAgent.localSnapshot.summary.v1`。
- 加载快照后仍经过 `StrategicStateBootstrapper().bootstrapIfNeeded` 与 `refreshGeneralAssignments`，并清空选择、高亮、临时交互日志和最近 AI / 指令记录。

## 架构边界

- 本地快照不写回 `grey_tide_2030_scenario.json`、`grey_tide_2030_regions.json` 或旧阿登 fallback 数据。
- Playtest 面板只调用 `AppContainer`，不直接改 `GameState`。
- Observer AI 和地图图层设置复用现有 `setObserverModeEnabled(_:)` 与 `setMapDisplayLayer(_:)`。
- 引导只在面板内显示短文本，不弹窗、不遮挡地图、不引入 onboarding 状态机。

## 本机轻量检查

已执行：

- `plutil -lint WWIIHexV0.xcodeproj/project.pbxproj`：OK。
- `env CLANG_MODULE_CACHE_PATH=/private/tmp/wwiihexv0-swift-module-cache swiftc -parse WWIIHexV0/App/AppContainer.swift WWIIHexV0/UI/RootGameView.swift WWIIHexV0/UI/ModernPlaytestPanelView.swift WWIIHexV0/UI/ModernCommandDesignTokens.swift`：通过，无输出错误。
- `rg -n "[[:blank:]]+$" README.md update_log.md md/flow/flow.md md/flow/flowchart.md md/prompt/v6.0-现代战争迁移/v6.9_playtest_loop_progress.md`：无匹配。
- `rg -n "默认先跑|默认 Probe|Probe -> Smoke|Stage Regression -> Full|代码改动按 .*Probe" AGENTS.md md/flow/flow.md`：无匹配。
- `git diff --check`：通过，无输出错误。

## 云端验证

待本轮 commit 并 push 到 `origin/main` 后，由 `.github/workflows/ci-results.yml` 生成未加密 artifact。Agent C 需核对 `ci-artifact-manifest.json`、`junit.xml`、`xcodebuild.log` 和 `ci-failure-summary.md`。

## 未完成和风险

- v6.9 当轮未做完整新局向导；v6.10 后续已补 Playtest 红/蓝新局选择器，完整多步骤新局向导和 AI 控制矩阵仍未做。
- 未做多存档槽、文件导出、iCloud、版本迁移 UI 或存档损坏修复面板。
- 未做本机 app 启动、模拟器、UI 点击、截图、10-20 回合观察者模式或性能检查。
- v6.10 仍需发布候选残留扫描、资源检查和人工授权重验证清单。
