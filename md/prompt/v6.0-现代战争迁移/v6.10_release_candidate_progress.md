# v6.10 发布候选准备进度

日期：2026-07-06

本文件记录 `codex-v6.0-现代战争aiagent迁移总提示词.md` 的 v6.10 收口结果。当前结论是：发布候选代码和文档已准备，最近代码行为提交已通过云端静态检查、通用 iOS build 和 `WWIIHexV0Probes` simulator probe artifact 核对；本轮已新增并通过灰潮默认剧本 10 个 AI 半回合轻量运行时 probe。当前已核对的 cloud Probe 继续覆盖 Playtest 红/蓝新局、Action Gate、主目标摘要、本地快照状态链路、Modern Mission Recon 经 `AppContainer -> CommandValidator -> RuleEngine / VisibilityRules` 消耗行动的规则入口、Observer AI 经 `AppContainer.advanceOrRunAI()` 自动推进两个 AI 半回合的容器入口，以及 restricted / civilian fire ROE 经 `RuleEngine -> CommandValidator -> FireSupportRules -> CommandExecutor` 的规则入口。真实 App 运行时启动、UI 点击、截图、10-20 回合 observer 长跑和性能体感仍未获人工授权，不能声明正式发布或发布级运行时已验证。

## 1. 本轮目标

- 补齐 v6.0-v6.10 的发布候选说明、残留扫描和人工授权重验证清单。
- 消除本轮确认的玩家可见主应用二战命名残留。
- 保持 hex / region / theater / front / deploy / command 规则权威不变。
- 不做 target、module、bundle id、源码类型名和旧 fallback 数据的全局大重命名。

## 2. 已处理项

| 项目 | 状态 | 依据 |
| --- | --- | --- |
| App 名称 | 已收口 | iOS / macOS 主游戏 target 的 `CFBundleDisplayName` 改为 `Modern Command Agent`。 |
| App 图标 | 已静态接入 | 新增 `WWIIHexV0/Assets.xcassets/AppIcon.appiconset`，iOS / macOS 主游戏 target 指定 `ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon`。 |
| 默认剧本 | 已扩到发布候选规模 | `DataLoader.loadInitialGameState()` 优先加载 `grey_tide_2030_scenario` + `grey_tide_2030_regions`；当前灰潮为 120 hex / 30 region / 16 初始 formation / 25 objectives，默认现代模板资源保留 10 个现代 template id。 |
| MapEditor 默认资源桥 | 已保留高级元数据 | MapEditor 可见模式收口为区域 / 作战区 / 任务编组；覆盖默认灰潮资源时回填既有 maxTurns、victoryConditions、riverEdges、VP、occupationState、river crossing 和 mainObjective 等编辑器未表达字段。 |
| 主界面 | 已具备首版 | 第一屏仍是 SwiftUI + SpriteKit 可操作战场，v6.8 C2 HUD / overlay 与 v6.9 Playtest tab 已接入；Playtest 面板已有 C2 Overlay 独立显示开关和 Legend；Info toggle 保持自身 44pt 命中区，不再把按钮 frame 扩成整屏。 |
| 玩家扮演方 | 已接入红/蓝新局选择，并追加 cloud Probe 覆盖 | Playtest 面板提供 New Operation Side segmented control，可用 Blue Force 或 Red Force 新开灰潮局；状态区继续显示 Player Side、Opposition、Control Mode 和 Action Gate；Action Gate 文案会点明当前玩家方或 active faction。新增 `testProbePlaytestSideSelectionActionGateAndSnapshot` 覆盖 `AppContainer` 公开状态链路，已随最新 artifact 核对通过。 |
| 主目标摘要 | 已接入 Playtest | Playtest 状态区显示十个主目标的 Blue / Red / Neutral 控制数；Blue 玩家看到 secure 阈值，Red 玩家看到 deny Blue 达标的阈值口径；Harbor Terminal、River Bridge、Comms Center、Fuel Depot、Rail Junction、Refinery District 是初始 neutral 主目标，Civilian Evac Zone 等 key-only 节点不计入十个主目标；灰潮数据检查会校验 VictoryRules、scenario victoryConditions 和 region mainObjective 三套主目标集合一致。 |
| 新局 / 继续 / 重置 | 已具备首版，并追加 cloud Probe 覆盖 | `ModernPlaytestPanelView` 调用 `resetGame`、`saveLocalSnapshot`、`loadLocalSnapshot`、`clearLocalSnapshot`；Playtest 命令反馈区分 success / warning / failure tone，关键按钮带 accessibility hint，Clear Snapshot 使用 destructive role。新增 Playtest 状态 probe 覆盖红/蓝新局后玩家方、active faction、phase、Action Gate、目标摘要、本地快照保存和恢复。 |
| 本地快照 schema | 已升级兼容 | `LocalPlaytestSnapshot` envelope 保存 schemaVersion / savedAt / playerFaction / GameState，并兼容旧裸 `GameState` 快照。 |
| 现代作战闭环 | 已具备抽象首版，并追加 Recon cloud Probe | Recon / UAV / EW / FireMission / SEAD / assault / hold / resupply 进入 `Command` 或 `ZoneDirective`；任务面板显示 Mission Status，Recon / UAV / FireMission / SEAD / EW / Resupply 按钮分别复用 validator 预检 formation、target、munition、冷却、目标质量、ROE、防空和友邻风险；Fire Mission 只使用选中 hex / region 命中的 visible contact、选中 region 或选中 hex，不再无选择地回退到第一个 contact；restricted fire zone 内 area fires 会被拒绝，precision / loitering 仅在 linked hostile target 上降级放行。新增 `testProbeModernMissionReconUsesAppContainerAndRulesPipeline`，已随最新 artifact 核对通过。 |
| restricted / civilian 火力 ROE | 已具备首版门禁，并已通过 cloud Probe 核对 | `FireSupportRules` 将 neutral / green / co-belligerent 控制区识别为 restricted fire zone；tube / rocket area fires 被 `restrictedFireZone` 拒绝，precision / loitering 只有 linked hostile target 时可执行并带 `restrictedFireZone` risk flag / 效果惩罚。本轮新增 `testProbeRestrictedFireZoneRequiresPrecisionLinkedHostileTarget`，用 `RuleEngine.execute(.fireMission...)` 验证 rocket 在 Green / civilian 控制区拒绝、precision linked hostile target 降级放行；已随 `a7538a6/28808359571` artifact 核对通过。 |
| ROE 敌我判定 | 已继续收口 | 攻击 / Region 攻击、ZOC、补给通行、邻敌补员 / 部署、WarDeployment enemy zone / presence、FrontLine 动态敌邻区、FireSupport 防空威胁和玩家点击攻击 / 宏观攻击目标改走 `Faction.isHostile(to:)`；显式 neutral 目标控制仍可通过地面移动夺取，用于灰潮主目标闭环。 |
| 胜负判断 | 已具备现代首版 | `VictoryRules` / `RegionVictoryRules` 对 `grey_tide_2030` 使用十个主目标控制数判定，旧阿登仍走旧胜负逻辑；neutral / civilian key-only 节点可用于态势和任务设计，但不参与蓝方 7 / 6 个主目标阈值。 |
| AI 不静默失败 | 已具备审计路径并新增 cloud Probe 覆盖 | `TurnManager` 写入 Operational Directive、Modern Command Chain、Compiled ZoneDirective JSON 和 diagnostics；解析失败时尽量保留原始 JSON 供复盘。本轮新增灰潮默认剧本 10 个 AI 半回合 cloud Probe，覆盖 AI 半回合推进、`end_turn` 执行、无 controllable phase 越权错误和关键运行时派生层不为空；灰潮初始态没有可执行 contact-line directive 时允许记录 no directives 诊断。 |
| 默认 AI 启动 manager | 已收口现代主路径 | `AppContainer.bootstrap()` 不再预建 `GameAgent.guderian(...)` / `.germany` manager；启动时先推导 `defaultPlayerFaction` 与默认敌对 AI faction，再创建对应 `Local Planner` 和 `MarshalAgent`。`GameAgent.guderian(...)` / `guderianFallback` 仍只作旧 `.germany` fixture 兼容桥，fallback 显示名改为 `Legacy Local Planner`。 |
| AI 回放摘要 | 已接入结构化面板 | `AgentDecisionRecord` 保存 `ModernCommandChainReplayItem`，`AgentPanelView` 显示 role / mission / priority / target / rationale，Agent / Context 主视图把 raw agent id 转为现代 command planner 显示名；完整 directive 明细、diagnostics 和 raw JSON 默认折叠在 Technical Replay 中。 |
| 命令反馈 | 已具备首版 | `lastCommandMessage`、`lastCommandFeedbackTone`、EventLog、AgentPanel、WarDirectiveRecord、Mission Status、Playtest Action Gate 和 Field Prompts 展示命令结果 / 拒绝原因 / 当前推进状态；Mission Status 会列出可用 Ready Tasks 或首个 validator 阻塞原因，玩家任务拒绝会同步写入 command feedback，宏观 directive 部分失败会带出首条规则拒绝原因；规则拒绝统一使用 `CommandValidationError.displayMessage`，Command / RegionCommand / FireMissionTarget 玩家显示、directive decoder、Modern Command Chain decoder 和 command intent mapping 诊断也继续改为 objective / sector / formation / contact track 可读文案，不再把 enum raw value、debug command string 或主要 raw id 直接暴露给玩家或 AI 回放。 |
| Readiness / Fuel / Signature 展示 | 已接入派生显示 | `Division` 基于 strength、supplyState、hasActed、retreat 和组件权重派生 readiness、fuel posture 与 signature posture；任务面板、单位详情和 tooltip 展示这些态势。未新增 Codable 字段，未作为新的规则门禁。 |
| JSON 可解析 | 已纳入云端静态检查 | 默认现代 JSON、现代模板和关键 fallback JSON 由云端解析检查；灰潮引用一致性、objective/key location 映射、region edge/neighbor、补给源、初始单位落点与十个主目标三源一致性用 `scripts/check_grey_tide_data.rb` 在 GitHub Actions 中核对。 |
| 现代模板严格性 | 已收口灰潮路径 | `DataLoader` 对 `grey_tide_2030` 禁止未知 templateId 静默 fallback；`modern_unit_templates.json` 只保留现代 template id，旧 `*_division` template id 兼容留在 `unit_templates.json` 和 `DataLoader` legacy fallback。 |
| README / flow 准确性 | 已同步 | README、`md/flow/flow.md`、`md/flow/flowchart.md`、`update_log.md` 已推进到 v6.10 口径。 |
| 玩家可见二战残留 | 已继续收口主路径命中 | App display name、地图补给源标签、AI 面板占位、默认 commander 数据、commander seed 去重、National Command、Commander / Unit / Region / Economy / Diplomacy 面板旧 ruler / theater / front zone / division / IC / supplies / raw id 可见口径已现代化；灰潮默认 `TheaterNode.name` 从 `TheaterId.rawValue` 派生为可读 command sector 名称，后续 `FrontZone.name` 继承该显示名但不改变 id / 映射权威；AI 面板 agent/context、legacy local LLM prompt、Modern Command Chain logistics rationale、AI interaction provider 组合名和 Playtest fallback scenario 标题继续收口为现代 command / planner / sustainment / compatibility 口径；HUD、任务、单位 tooltip / inspector、事件日志和经济日志主路径把 `Supply` 展示收口为 Logistics / sustainment 口径；默认现代模板资源移除旧 `*_division` alias id，剩余旧名归类为源码 / fallback / 历史文档兼容残留。 |
| 云端静态 / Probe 防线 | 已接入 workflow，最新已核对基线为 23/0 | `.github/workflows/ci-results.yml` 会在云端运行 `scripts/check_grey_tide_data.rb`、`scripts/check_modern_visible_text.rb`、通用 iOS build 和 `WWIIHexV0Probes` simulator probe；artifact manifest 记录 `staticChecksOutcome`、`buildOutcome`、`probeOutcome`、`simulatorOutcome`，并上传 `grey-tide-data.log`、`modern-visible-text.log`、`probe-xctest.log`、`simulator.log` 与 `WWIIHexV0Probes.xcresult`；本机默认不运行这些检查命令或 probe。最新已核对的 `66d37e1/28811594525` artifact 中静态检查、build、simulator 和 Probe 23/0 均通过，包含灰潮 10 个 AI 半回合、Playtest 状态、Modern Mission Recon、Observer AI 容器入口和 restricted / civilian fire ROE cloud Probe。 |
| 发布候选证据矩阵 | 已新增 | `md/prompt/v6.0-现代战争迁移/v6.10_release_candidate_evidence.md` 逐项映射总提示词要求、代码证据、artifact 和未验证风险。 |

## 2.1 已核对云端结果包

本文记录本轮开始前最近已核对的代码行为提交；该结果包覆盖下方代码行为 commit。若本进度文档后续以 doc-only commit 或代码提交更新，最终交付仍必须以最新 `origin/main` run 为准重新下载 artifact，并在交付中写明最新 run / artifact：

- commit：`66d37e17a882d1a674fcdf5ae34f252c4b8501ce`
- workflow：`WWIIHexV0 CI Results`
- run id：`28811594525`
- run attempt：`1`
- artifact：`WWIIHexV0-ci-cloud-flow-v1-main-66d37e1-run28811594525-attempt1`
- manifest：`branch=main`、`commitSha=66d37e17a882d1a674fcdf5ae34f252c4b8501ce`、`runId=28811594525`、`runAttempt=1`、`staticChecksOutcome=success`、`buildOutcome=success`、`probeOutcome=success`、`simulatorOutcome=success`、`testOutcome=skipped`
- JUnit：4 tests、0 failures；完整 XCTest / UI / Smoke / Stage / Full 按当前 cloud-flow policy skipped
- `xcodebuild.log`：`BUILD SUCCEEDED`
- `probe-xctest.log`：`WWIIHexV0Probes` 23 tests、0 failures，`testProbeGreyTideObserverTenAIHalfTurns`、`testProbePlaytestSideSelectionActionGateAndSnapshot`、`testProbeModernMissionReconUsesAppContainerAndRulesPipeline`、`testProbeAppContainerObserverRunsTwoAIHalfTurns` 和 `testProbeRestrictedFireZoneRequiresPrecisionLinkedHostileTarget` passed，`TEST SUCCEEDED`
- `ci-failure-summary.md`：`No CI failures detected.`
- 说明：`5764351b577dc3f98914feae4fc374e997687aa0` 首次新增 10 个 AI 半回合 Probe 后，run `28794865182` 因测试过度要求默认灰潮初始态必须产生可执行 contact-line directive 和 visible contacts 而失败；`8ee4feb69be92b8b67aed91584859fb75ea6d50a` 将断言收窄为 AI 半回合推进、`end_turn`、无 controllable phase 越权和派生层稳定后，run `28795809821` 已复核通过。`41f62c1a1d28e7d70364a412f8b9bd37ec8e840d` 继续新增 Playtest 红/蓝新局、Action Gate、主目标摘要、本地快照状态链路和 Modern Mission Recon 容器链路 Probe，run `28800640685` 已复核通过。`dca2afdc2dd9b4a6c720a25b43cf18e3b88af08b` 继续新增 Observer AI 容器入口 Probe，run `28803830640` 已复核通过。`a7538a600219965c0e3465be2b20653e05d3ee38` 继续新增 restricted / civilian fire ROE Probe，run `28808359571` 已复核通过。`66d37e17a882d1a674fcdf5ae34f252c4b8501ce` 继续收口默认 AI 启动 manager 和 compatibility fallback 日志，run `28811594525` 已复核通过。artifact manifest 标记 `staticChecksOutcome=success`、`buildOutcome=success`、`probeOutcome=success`、`simulatorOutcome=success`、`testOutcome=skipped`，`grey-tide-data.log`、`modern-visible-text.log`、`xcodebuild.log` 和 `probe-xctest.log` 均通过。

## 3. 本轮残留扫描结论

本轮扫描范围：

```sh
rg -n "INFOPLIST_KEY_CFBundleDisplayName|PRODUCT_NAME|PRODUCT_BUNDLE_IDENTIFIER|MARKETING_VERSION|CURRENT_PROJECT_VERSION" WWIIHexV0.xcodeproj/project.pbxproj
rg -n "WWII|Ardennes|Bastogne|Panzer|Guderian|Germany|Allies|Division|German|Allied" WWIIHexV0/App WWIIHexV0/UI WWIIHexV0/SpriteKit WWIIHexV0/Data README.md md/flow/flow.md update_log.md
rg -n "displayName|tacticDisplayName|operationalDisplayName|localizedName|scenarioDisplayName|CFBundleDisplayName|SUP A|SUP G|infantryDivision|panzerDivision|motorizedDivision" WWIIHexV0/App WWIIHexV0/UI WWIIHexV0/SpriteKit WWIIHexV0/Core WWIIHexV0/Data
```

处理结果：

- `WWIIHexV0.xcodeproj/project.pbxproj` 中 iOS / macOS 主游戏 `INFOPLIST_KEY_CFBundleDisplayName` 原为 `WWII Hex V0` / `WWII Hex V1.1 Mac`，已改为 `Modern Command Agent`。
- `WWIIHexV0/Assets.xcassets/AppIcon.appiconset` 已加入 iOS / macOS 主游戏 resources，并提供 iPhone、iPad、macOS 和 iOS marketing 所需尺寸；图标为抽象现代 C2 网络标识，不使用现实国家旗帜或敏感战场符号。
- `ModernPlaytestPanelView` 已把 New Operation Side、Player Side、Opposition、Control Mode 和 Action Gate 放在 Playtest 区，避免玩家只看到旧兼容 faction 或不清楚自己当前扮演方与推进状态。
- `AppContainer.resetGame(playerFaction:)` 会把 `playerFaction`、初始 `activeFaction` 和 `GamePhase` 一起切到所选 Blue / Red；非 observer 模式下 AI 只接管当前非玩家敌对阵营，本地快照额外保存 `playerFaction` raw value。
- `GamePhase` 仍保留 `.germanAI` / `.alliedPlayer` Swift case 以兼容旧代码，但 Codable 与 `GamePhase.dataValue(_:)` 已支持现代 `redCommand` / `blueCommand` alias；新保存的 `GameState` phase 写现代 alias，默认灰潮 `initialPhase` 改为 `blueCommand`，旧阿登 fallback 的 `alliedPlayer` 仍可解码。
- `AppContainer.saveLocalSnapshot()` 改为保存 `LocalPlaytestSnapshot` envelope，`loadLocalSnapshot()` 先读 envelope，失败再兼容旧裸 `GameState` 快照，避免玩家方和存档 schema 继续分散。
- `VictoryRules.greyTideObjectiveControlCounts(in:)` 暴露只读主目标计数，`AppContainer` 派生 Playtest 目标摘要，避免玩家只能从日志或最终胜负结果反推战役进度。
- `scripts/check_grey_tide_data.rb` 现在解析 `VictoryRules.greyTideMainObjectiveIds`，并校验它与 scenario `vc_blue_key_nodes` / `vc_red_defense_network` 的 `objectiveIds`、region `mainObjective=true` 集合一致，防止 key-only / civilian 节点误入灰潮胜负阈值。
- `AgentDecisionRecord` 现在保存 `ModernCommandChainReplayItem`，`AgentPanelView` 可直接展示 ISR / Fires / Air / EW / Logistics / Brigade 子任务摘要，不需要玩家只读 raw JSON 才理解 AI 回合；Operational Directive 或 Modern Command Chain 校验失败时也尽量保留原始 JSON，并在折叠 Technical Replay 中供审计。
- `ModernMissionPanelView` 现在显示 Mission Status，按钮启用条件区分 Recon、UAV、Fire Mission、SEAD、EW、Resupply、宏观 Assault 和 Hold；不可执行时 `accessibilityHint` 与状态文案给出 observer、非玩家 phase、未选 formation、已行动、缺少目标、弹药 / 冷却 / ROE / 防空 / 友邻风险等原因。
- `ModernPlaytestPanelView` 的 Field Prompts 现在复用 Action Gate、Mission Status、Ready Tasks、Fire Mission 目标门禁、contact 和 fire result 状态，避免提示与实际可执行任务脱节；红方玩家的目标阈值文案改为阻止 Blue 达标。
- `ModernPlaytestPanelView` 新增 C2 Overlay 独立显示开关和只读 Legend，用现有 C2 色标展示 Sensor、Jammed、EW Area、Fire Result、Low / Medium / High / Confirmed contact、Logistics 和 contact type codes，补齐 v6.8 overlay 首版缺少图例和独立显示控制的可读性缺口；仍未做运行时截图、tooltip 或视觉验收。
- 本轮继续把 Mission Status 从 Fire Mission-only 预检扩展到 Recon / UAV / SEAD / EW / Resupply 多任务 validator 预检，并让 Action Gate 文案点明当前阵营；`c72156062290a7d1a74a0c176030dc816b81fd5f` 已按最新 artifact 复核通过。
- 本轮继续收口 Playtest feedback：`AppContainer` 增加 `lastCommandFeedbackTone`，成功、警告、失败反馈在 Playtest 面板显示不同图标、颜色和 VoiceOver label；Field Prompts 不再全部显示完成态图标，Clear Snapshot 改为 destructive role；Fire Mission 不再无选择地回退到第一个 visible contact。`3cfb210ac48c02f5b771e4492965a7e1753d101c` 已按最新 artifact 复核通过。
- `RootGameView` 和旧 `InfoPanelToggle` 的 Info 按钮都改为 `Info` / `Hide Info` + `info.circle` Label，保留 44pt 最小触控区和展开 / 收起 accessibility value，不再把按钮自身扩成全屏命中区。
- `AgentPanelView` 把普通主视图收口为结构化 command chain、command results 和 errors；Operational Directives、diagnostics 与 raw JSON 改放到默认折叠的 `Technical Replay` 区，底层 `ZoneDirective` 类型名仍保留作源码兼容。
- 本轮继续收口 raw id 可见性：`UnitInspectorView` / `RegionInspectorView` 的 operational zone、command sector、region/contact line 值不再直接显示 `theater_*` / `front_zone_*` / `region_*` raw value；`AgentPanelView` / `DiplomacyPanelView` 的 National Command、focus sector、country/bloc relation 不再直接显示 `ruler_*` / `authority_*` / zone / coalition raw id；`GeneralCommandPanelView` planned operation、`AppContainer` commander feedback 和 fallback commander 名称也转为可读文案；Blue 玩家目标阈值文案不再硬编码 Blue 主语，Red 玩家仍看到 deny Blue 达标口径。`a0583a9/28776473599` 已按最新 artifact 复核通过；运行时 UI 排版仍未验证。
- 本轮继续收口默认数据与保存态旧 phase raw value：灰潮默认 `initialPhase` 改为 `blueCommand`，`DataLoader` 和 MapEditor exporter 改走 `GamePhase.dataValue`，`scripts/check_grey_tide_data.rb` 在云端要求灰潮初始 phase 使用现代 alias；旧 `alliedPlayer` / `germanAI` 仍仅作 legacy decode 兼容。该改动已由后续云端 artifact 覆盖复核。
- README 和 flow 文档同步修正 RootGameView 不再通过 `.task { runAIIfNeeded() }` 自动触发 AI，AI 由 HUD/command panel 的 `advanceOrRunAI()`、命令提交、重置和继续后的 `AppContainer.runAIIfNeeded()` 受控触发；`flowchart.md` 补定义 `DIP` 节点，避免 Mermaid 隐式裸节点。
- 灰潮主目标一致性检查已扩展到 `VictoryRules.greyTideMainObjectiveIds`、scenario victory conditions 和 region `mainObjective=true` 三源集合，避免 key-only / civilian 节点误入胜负阈值；`b9279de3db8328238e509e18ae5190bad583c1af` 已按最新 artifact 复核通过。
- 本轮把 readiness / fuel / signature 从“未显示”推进为派生态势显示：`Division` 只读计算 readiness percentage、fuel posture 和 signature posture，`ModernMissionPanelView`、`UnitInspectorView`、`UnitTooltipView` 展示这些状态；它仍不新增独立字段、不改变 CommandValidator / RuleEngine 门禁，已随最近 artifact 通过云端 build；运行时 UI 排版仍未验证。
- 本轮继续把灰潮静态检查从引用检查扩展到轻量语义检查：objective 与 key location 必须一一链接且坐标 / 名称一致，region `edges` 与各 region `neighbors` 必须一致，tile / region 补给源 faction 与 controller 不冲突，初始单位不能落敌方控制 hex；`grey-tide-data.log` 已随最近 artifact 通过。
- 玩家任务拒绝通过统一 helper 同步更新 `lastCommandMessage` 和 interaction log；玩家宏观 directive、AI order diagnostics、AgentPanel command result、RuleEngine message 和 WarCommandExecutor / StrategicStateSynchronizer 事件日志都使用同一套可读 validation / command display 文案，避免只显示 executed 0/N、debug command string、`targetOutOfRange` / `restrictedFireZone` 等内部 raw value。
- `TheaterDirectiveDecoderError`、`ModernCommandChainDecoderError` 和 `CommandIntentAdapterError` 的玩家 / AI 回放诊断继续收口：missing sector、target operational zone、missing objective、missing contact、role/mission mismatch、tactic category mismatch、region mapping 和 target region mismatch 改用 Sector / Zone / Objective / Formation / Contact Track / tactic display 文案，底层 directive id、Codable raw value 和规则映射不变；该改动已由后续云端 artifact 覆盖复核。
- `Command.displayName`、`RegionCommand.displayName` 和 `FireMissionTarget.displayName` 改为玩家可读行动摘要，`AgentDecisionRecord`、`RuleEngine` 和 directive 拒绝事件复用该文案；地图补给源标签从 `SUP B/R` 收口为 `LOG B/R`，后勤恢复事件使用 `SupplyState.displayName`。该改动已由后续云端 artifact 覆盖复核。
- 本轮继续收口 AI fallback / legacy planner 可见文案：自动 command sector commander、Local Planner、MockAI contact line reason、Modern Command Chain contact rationale 和 SEAD 事件叙事不再直接显示 front-zone / region / contact raw id、confidence raw value 或 side raw value；底层 `FrontZoneId`、`RegionId`、`ContactTrack.id` 和 `OperationalSideAlignment.rawValue` 不变。`b8666a5/28779340468` 已按最新 artifact 复核通过。
- 本轮继续收口 legacy local planner / simulated marshal 回放文案：DEPTH / FRONT / GARRISON、`v0.33` 部署提示和 simulated marshal tactic raw value 改为 reserve、contact line、security element 和 `TacticName.displayName` 的现代可读口径；底层 `AgentOrder.stance`、`TacticName.rawValue`、directive id 和执行管线不变。`253c936/28780436488` 已按最新 artifact 复核通过。
- 本轮继续收口灰潮 command sector 名称：`DataLoader.makeTheaterState` 不再把 `TheaterId.rawValue` 直接写入 `TheaterNode.name`，而是把 `blue_littoral_sector` 等 id 派生为 `Blue Littoral Sector` 一类显示名；`WarDeploymentManager` 后续仍复用 theater id 建立 `FrontZoneId` / `hexToFrontZone` / `regionToFrontZone`，只让 `FrontZone.name` 继承可读名称。底层 `TheaterId`、`FrontZoneId`、JSON raw value、Codable schema、动态 theater / deployment 映射权威不变；`83bcdbc/28782198835` 已按最新 artifact 复核通过。
- 本轮继续收口 AI / fallback 可见口径：`AgentPanelView` 和 `TurnManager.contextSummary` 不再把 raw agent id 直接作为主显示，`AgentPromptBuilder` 的 legacy local LLM prompt 不再自称 WWII 原型且把 phase / side / sustainment 改为现代显示文案，`ModernCommandChain` logistics rationale 从 Supply / low-supply 改为 Sustainment / logistics-constrained，`AppContainer.scenarioDisplayName` 对旧 fallback 和未知 scenario id 改用兼容 / 标题化显示；底层 `AgentDecisionRecord.agentId`、legacy JSON schema、`SupplyState`、scenario id、Codable 和执行管线不变；`4f69736/28786816664` 已按最新 artifact 复核通过。
- 本轮继续收口命令结果与后勤任务可见口径：Mission Planning / Ready Tasks / blocked mission status 的 `Resupply / Repair` 改为 `Sustain / Repair`，`ModernMissionType.resupply`、Agent order fallback、Command / RegionCommand display 和经济 bootstrap 日志改为 Sustainment / Logistics 口径；`Command.contextualDisplayName(in:)` 与 `FireMissionTarget.contextualDisplayName(in:)` 让 RuleEngine message、玩家 interaction log、directive reject / region controller 事件和 AgentPanel command result 优先使用 `Division.operationalDisplayName`、region name 与 contact 摘要，避免复盘主路径继续从 raw unit / contact / region id 派生玩家可见命令名。底层 `Command.resupply` case、schema raw value、Codable、`SupplyState`、fire target id 和 `Command` / `ZoneDirective` / `WarCommandExecutor` / `RuleEngine` 执行链路不变；`a6735b8/28788048049` 首次云端 run 的静态检查通过但 xcodebuild 因 AppContainer map closure 缺少显式 `return` 失败，追加 `97117cb/28788197165` 已按最新 artifact 复核通过。
- 本轮继续补齐云端运行时入口：`.github/workflows/ci-results.yml` 在通用 iOS build 成功后选择可用 iPhone simulator，启动 simulator 并运行 `WWIIHexV0Probes`，将 `probeOutcome`、`simulatorOutcome`、`probe-xctest.log`、`simulator.log` 和 `WWIIHexV0Probes.xcresult` 写入 artifact 口径；该 probe 覆盖已有数据启动、region/front/deployment、MockAI、ZoneDirective/WarCommandExecutor 等轻量运行时探针，不替代真实 App UI 启动、截图、长回合 observer 或性能体感。本机仍未运行 Probe / Xcode / simulator；该入口已由后续云端 artifact 覆盖复核。
- 本轮继续加深云端运行时 Probe：`WWIIHexV0ProbeTests` 新增灰潮默认剧本 10 个 AI 半回合 probe，直接使用 `TurnManager.runAITurn(..., pipelineMode: .marshalDirective)` 推进 active Blue / Red faction，检查 phase 与 active faction 对齐、end_turn 执行、没有 outside controllable phase 错误，并在 10 个半回合后复核 turn 精确推进 5、region graph、command sector、theater state 和 sensor coverage。灰潮初始态若没有可执行 contact-line directive，可记录 no directives 诊断但不视为 Probe 失败；该测试仍只是云端 simulator probe target 内的轻量链路，不等于真实 App observer mode 10-20 回合长跑、UI 点击、截图或性能验收。
- 本轮继续补 Playtest / Mission cloud Probe：`WWIIHexV0ProbeTests` 新增 `testProbePlaytestSideSelectionActionGateAndSnapshot`，覆盖 Blue / Red 新局后玩家方、active faction、phase、Control Mode、Action Gate、主目标摘要、本地快照保存和恢复；新增 `testProbeModernMissionReconUsesAppContainerAndRulesPipeline`，覆盖默认 Blue formation 经 `AppContainer.handleBoardTap` 与 `orderModernReconArea()` 走 `CommandValidator -> RuleEngine -> VisibilityRules` 后标记 `hasActed`、写入 intelligence 事件并关闭再次 Recon。它们仍不等于真实 SwiftUI 点击、截图或长回合 observer 验收，已随最新 `origin/main` artifact 核对通过。
- 本轮继续补 Observer AI 容器入口 cloud Probe：`WWIIHexV0ProbeTests` 新增 `testProbeAppContainerObserverRunsTwoAIHalfTurns`，覆盖 Blue 新局开启 Observer AI 后通过 `AppContainer.advanceOrRunAI()` 走容器异步 AI sequence，最多连续解析 Blue / Red 两个 AI half-turn，并回到 Blue command phase；断言 `end_turn` 执行、无 controllable phase 越权、Blue / Red directive records 和战略派生层稳定。该 Probe 仍不等于真实 App 10-20 回合 observer 长跑、UI 点击、截图或性能验收，已随最新 `origin/main` artifact 核对通过。
- 本轮继续补 restricted / civilian fire ROE cloud Probe：`WWIIHexV0ProbeTests` 新增 `testProbeRestrictedFireZoneRequiresPrecisionLinkedHostileTarget`，构造 Blue fires asset、Green / civilian 控制 hex、Red hostile target 和 linked contact，通过 `RuleEngine.execute(.fireMission...)` 验证 rocket area fire 被 `restrictedFireZone` 拒绝且不消耗行动 / 弹药，precision munition 对 linked hostile target 可执行、消耗精确弹药并在 `FireMissionResult` / fireSupport event 中保留 `restrictedFireZone` risk。该 Probe 仍不等于完整 no-fire zone、collateral damage、授权升级链或运行时 UI 验收，已随 `a7538a6/28808359571` artifact 核对通过。
- `f1dd9fe/28789559115` 首次云端 Probe 接入已确认 workflow 和 simulator 可运行：manifest 中 `staticChecksOutcome=success`、`buildOutcome=success`、`simulatorOutcome=success`，但 `probeOutcome=failure`；失败原因是历史 `WWIIHexV0Probes` 断言仍绑定 `mapeditor_scenario`、旧 theater id、旧 MockAI attack/tactic 和旧 parsed intent 文案。`WWIIHexV0ProbeTests.swift` 已更新为灰潮默认剧本、现代 command sector 和当前 tactic / directive 语义，后续 `0829ed4/28790707794` 与 `9a980e8/28791360323` 已复核 Probe 18/0。
- `9a35abf/28790257478` 第二轮云端 Probe 只剩旧默认 front line 非空断言失败；`0829ed4/28790707794` 已按最新 artifact 复核通过，manifest 中 `staticChecksOutcome=success`、`buildOutcome=success`、`simulatorOutcome=success`、`probeOutcome=success`，`WWIIHexV0Probes` 18/0。
- HUD、任务面板、单位 tooltip / inspector、事件日志分类和经济日志主路径的玩家可见 `Supply` 文案已改为 Logistics / sustainment；规则层 `SupplyRules`、`Division.supplyState` 和 JSON 字段仍作为源码兼容名保留。
- `MapEditorExporter` 对没有 city / fortress 名称的 supply key location 默认导出 `Logistics Node q,r`，保留底层 supply kind / id 兼容，避免编辑器覆盖灰潮资源时重新生成玩家可见 `Supply q,r` 名称。
- `WWIIHexV0/SpriteKit/HexNode.swift` 中补给源标签原为 `SUP A` / `SUP G`，先改为现代 Blue / Red 口径，本轮继续收口为按阵营 alignment 派生的 `LOG B` / `LOG R`，旧 `.allies` / `.germany` fallback 也显示为现代 Blue / Red 后勤标记。
- `GameState.initial()` 仍是灰潮和旧阿登资源都加载失败后的最末级 compatibility fallback，保留旧单位 id / 坐标和 legacy faction 以保护历史测试与旧 fixture；但初始事件日志已从 `Ardennes V0 scenario initialized.` 收口为 `Compatibility fallback scenario initialized.`，避免 fallback 被玩家看见时直接暴露旧战役名。本轮未改变默认 `DataLoader.loadInitialGameState()` 的灰潮优先顺序。
- `AppContainer.bootstrap()` 不再把启动容器预设为 `GameAgent.guderian(...)` + `.germany` marshal；现在按 bootstrapped state 推导玩家方和默认敌对 AI 方，为该 faction 创建 `Local Planner` 和对应 `MarshalAgent`。旧 `GameAgent.guderian(...)` 仍只在 `.germany` legacy path 使用，`guderianFallback` 的玩家可见名称收口为 `Legacy Local Planner`，底层 id 保留以兼容历史测试和 fixture。
- 本轮继续收口 AI provider 可见名：`AppContainer` 的 interaction log 会把 `MockAI+MarshalDirective` / `MockAI+Directive` 显示为 `Local Planner + Operational Directive` / `Local Planner + Directive`，底层 provider id 和 `AgentDecisionRecord.provider` 仍保留兼容值。
- 本轮继续收口现代模板资源：`modern_unit_templates.json` 删除未被灰潮默认单位引用的 `panzer_division`、`motorized_division`、`infantry_division`、`artillery_division`、`anti_tank_division`、`garrison_division` alias，旧模板 id 兼容继续由 `unit_templates.json`、旧阿登数据和 `DataLoader.fallbackComponents` 承担。
- 本轮继续收口预留统治者层信息边界：`RulerAgent` 仍不是默认现代 AI 主链路，但其敌军强度估计改为基于 `operationalAwareness.visibleContacts(for:)` 与 `VisibilityRules.contactStrengthEstimate`，避免未来启用时读取不可见敌军真实单位强度。
- `AgentPanelView` 空态不再显示 `guderian` / `MockAI` 占位，Command Chain 卡片标为只读 `Advisory`，统治者层显示为 `National Command`；默认 `generals.json` 改为灰潮虚构 Blue / Red commander，`GeneralDispatcher` seed 匹配排除已分配 commander；Commander / Unit / Region / Economy 面板把玩家可见的 ruler / theater / front zone / division / IC / supplies 标签收口为 national command / operational zone / command sector / formation / contact line / personnel / materiel / logistics。
- `scripts/check_modern_visible_text.rb` 已新增为可复现防回归检查：它扫描主应用 App / UI / SpriteKit Swift 字符串、会进入命令结果 / 日志 / diagnostics 的 Swift 字符串、若干 Core 玩家可见 displayName 映射，以及 `grey_tide_2030_*`、`modern_unit_templates.json`、`generals.json` 的显示字段和地图地名字段；对 WWII、Ardennes、Bastogne、Panzer、Guderian、Germany、Allies、Division、SUP A / SUP G、IC、Supplies、front zone、ruler、theater 等玩家可见旧口径 hard fail，并显式 allowlist `MockAI` provider id、旧阿登 fallback id、旧阿登胜负 fallback 和历史 overlay palette key 等非默认现代显示 literal。
- `EconomyState.ProductionKind` 仍保留 `infantryDivision` / `panzerDivision` / `motorizedDivision` raw case 名，但 `displayName` 已是 `Light Infantry Task Force`、`Armored Task Force`、`Mechanized Task Force`，属于源码兼容残留。
- `unit_templates.json`、`ardennes_*`、`general_agents.json`、`DataLoader.loadArdennesDataSet()` 和 `Legacy Fallback Scenario` 属于旧数据集 / fallback / 历史兼容残留，不是默认现代剧本主路径；默认 `generals.json` 已改为现代虚构 commander。
- `README.md`、`md/flow/flow.md`、`update_log.md` 中仍记录 WWIIHexV0、Ardennes、Guderian、Division 等历史名，用于迁移背景和兼容边界说明，不应被当作主游戏 UI 文案。
- target / module / bundle id 仍保留 `WWIIHexV0` / `WWIIHexV0Mac` / `com.example.WWIIHexV0*`，本轮不改，原因是全局改名会牵涉 Xcode target、scheme、测试、CI、source membership 和历史 imports，超出 v6.10 发布候选收口范围。

## 4. 发布前需人工授权的重验证

未获授权前不得主动在本机执行：

- Xcode build。
- iOS Simulator 或真机启动。
- macOS target 启动。
- 真实 App 10-20 回合 observer 模式；本轮新增的云端 10 个 AI 半回合 Probe、Playtest / Recon 状态 Probe、Observer AI 容器入口 Probe 和 restricted / civilian fire ROE Probe 不能替代该发布前运行时验收。
- 基础 UI 点击烟测。
- SpriteKit 截图或人工视觉检查。
- 性能体感检查。

当前可声明的范围：

- 本轮只做轻量语法、格式、JSON、project 文件和云端 artifact 检查。
- 若最新 `origin/main` GitHub Actions 通过，只能声明“发布候选代码和文档已准备，云端静态检查、通用 iOS build 和默认 Probes 通过，且灰潮 10 个 AI 半回合轻量 Probe、Playtest 状态 Probe、Modern Mission Recon 容器链路 Probe、Observer AI 容器入口 Probe 和 restricted / civilian fire ROE Probe 通过”；App 图标资源已静态存在，但 Dock / SpringBoard 实机显示、真实 App observer 长跑、UI 点击和性能仍需人工授权启动验证，不能声明“已发布”或“运行时发布级已验证”。

## 5. 下一步建议

1. 人工授权一次发布前运行时验证：iOS / macOS 启动、UI 点击、SpriteKit 截图、10-20 回合 observer 和性能体感。
2. 若运行时验证失败，继续在 `main` 上追加 v6.10.x 修复 commit，并重新下载 artifact 核对。
3. 若运行时验证通过，再考虑 v6.11 做多步骤新局向导、AI 控制矩阵，以及 target / bundle id / scheme 的正式产品命名迁移，避免在发布候选阶段引入工程级重命名风险。
