以下v0.5的大致方案“# v0.5 国家与部长 Agent 执行方案

## Summary

v0.5 的目标不是做完整经济、外交、科技和政变系统，而是建立“国家级 agent + 部长 agent + 指挥链仲裁”的骨架。核心交付是：高层 agent 能读取摘要、提出战略建议或目标，系统能把这些建议记录、展示、下发给前线/将领层，但不允许它们直接移动单位或绕过规则。

建议拆成 6 个 agent 并行推进，其中 A/B 先行，C/D/E/F 在接口稳定后接入。

## Agent 分工

### Agent A：多 Agent 架构与数据模型

负责定义 v0.5 的核心抽象。

工作内容：
- 扩展 `GeneralAgent` 为更通用的 `GameAgent` 或新增 `PoliticalAgent` / `StaffAgent`。
- 定义 agent 角色：统治者、元帅、军工部长、陆军部长、外交部长、情报部长、将领。
- 定义 `AgentRole`、`AgentAuthorityLevel`、`AgentPersonality`、`AgentRelationship`。
- 定义 agent 权限边界：高层 agent 只能输出战略意图和建议，不能直接下达单位级命令。
- 设计 `AgentMemory` 的最小结构：短期战况记忆、长期人物倾向、最近指令。
- 扩展 JSON 配置，例如 `agents.json` 或 `national_cabinet.json`。

交付物：
- 多 agent 数据结构。
- 示例德国国家内阁配置。
- 示例盟军国家内阁配置。
- 基础单元测试，验证 agent 配置可加载、角色权限正确。

依赖：
- 需要沿用 v0 的 `DecisionProvider`、`AgentContext` 思路。
- 不依赖 UI。

### Agent B：国家级摘要与上下文构建

负责把游戏状态压缩成不同层级 agent 可读的摘要。

工作内容：
- 定义 `NationalContext`：国家总体战况、资源概况、前线摘要、威胁、目标。
- 定义 `MinisterContext`：按部门裁剪信息。
- 定义 `TheaterContext`：战区级摘要，供元帅/战区司令使用。
- 定义 `CommanderContext`：沿用或扩展将领局部战场摘要。
- 实现上下文构建器，确保高层 agent 不读取不该知道的全量细节。
- 为本地 LLM 控制 token 量，做摘要长度限制。

交付物：
- `AgentContextBuilder` 分层版本。
- 国家级、部长级、战区级摘要结构。
- 测试用固定 `GameState`，能生成稳定摘要。
- 单元测试：不同角色拿到不同粒度信息。

依赖：
- Agent A 的角色模型。
- v0/v0.3 的 `GameState`、前线、经济占位数据。

### Agent C：战略意图与仲裁系统

负责解决多个 agent 同时提出建议时，谁说了算、如何落地。

工作内容：
- 定义 `StrategicDirective`：例如进攻方向、防守重点、生产优先级、科技建议、外交建议。
- 定义 `DirectivePriority`：统治者 > 元帅 > 部长 > 将领，或按领域仲裁。
- 定义 `DirectiveStatus`：proposed、approved、rejected、active、expired、completed。
- 实现 `DirectiveBoard` 或 `AgentCouncil`，集中管理所有高层建议。
- 实现冲突检测：例如陆军部长要求补员，统治者要求立即进攻。
- 将高层 directive 转换为下级 agent 的 `PlayerDirective` 或 `TheaterDirective`。

交付物：
- 战略指令模型。
- 仲裁器。
- 冲突日志。
- 测试：多个 agent 输出冲突建议时，系统能稳定选出结果并记录原因。

依赖：
- Agent A 的角色权限。
- Agent B 的摘要。
- 不直接依赖 SpriteKit UI。

### Agent D：部长 Agent 原型

负责第一批部长 agent 的行为模板和 Mock 决策。

工作内容：
- 实现军工部长：根据装备、工厂、损耗提出生产建议。
- 实现陆军部长：根据兵力、组织度、损失提出补员、训练、任免建议。
- 实现外交部长：先只输出外交态势和风险建议，不做真实外交规则。
- 实现情报部长：根据可见敌军、前线压力、未知区域输出威胁评估。
- 每个部长先用 MockAI 规则生成结构化建议，后续再接本地 LLM。
- 定义每个部长的 prompt 模板，但 v0.5 默认不强制调用真实 LLM。

交付物：
- 4 个部长 Mock provider。
- 每个部长的结构化 JSON 输出 schema。
- 每回合部长会议建议列表。
- 测试：固定战况下部长输出稳定、可解析、可被仲裁器接收。

依赖：
- Agent B 的部长上下文。
- Agent C 的战略指令模型。

### Agent E：统治者与玩家角色入口

负责玩家作为统治者的交互入口，以及统治者 agent 的总体目标输出。

工作内容：
- 设计 `RulerAgent`：国家目标、政治风格、战争优先级、对将领信任。
- 支持玩家以统治者身份发布国家级命令。
- 将玩家自然语言命令转换为 `StrategicDirective`。
- 统治者 agent 可以综合部长建议，形成最终战略目标。
- 保留未来“玩家扮演将领”的接口，但 v0.5 不实现完整将领生涯模式。

交付物：
- 统治者 agent 配置。
- 玩家国家级命令入口的数据模型。
- 统治者决策 JSON schema。
- 测试：玩家命令能进入仲裁层并下发给战区/将领。

依赖：
- Agent C 的 `DirectiveBoard`。
- Agent D 的部长建议。
- v0.4 的聊天命令能力。

### Agent F：内阁/Agent UI 与日志

负责让玩家看懂多 agent 系统在干什么。

工作内容：
- 新增内阁面板：统治者、部长、元帅、将领列表。
- 显示每个 agent 的角色、忠诚/信任、当前建议、最近决策。
- 显示部长会议日志。
- 显示已批准战略指令和被拒绝建议。
- 显示某条战略指令如何下发到前线/将领。
- 提供调试视图：原始 JSON、解析结果、仲裁结果。

交付物：
- `CabinetView` / `AgentCouncilView`。
- `DirectiveBoardView`。
- `AgentDecisionLogView` 扩展。
- UI smoke test 或手动验收步骤。

依赖：
- Agent A/C/D/E 的模型和日志数据。
- 不改变核心规则。

## 推荐开发顺序

1. Agent A 先做角色模型和配置。
2. Agent B 做分层摘要。
3. Agent C 做战略指令和仲裁。
4. Agent D 做部长 Mock 决策。
5. Agent E 接入统治者和玩家国家级命令。
6. Agent F 最后做 UI 和日志展示。

可以并行的部分：
- A 和 B 可以先约定接口后并行。
- D 可以在 C 的 `StrategicDirective` 初稿稳定后并行。
- F 应最后做，避免 UI 反复追模型变更。

## v0.5 明确不做

- 不做完整经济模拟。
- 不做完整科技树。
- 不做真实外交系统。
- 不做真实政变系统。
- 不做完整内购。
- 不让部长直接改资源、单位、地图或科技。
- 不让统治者 agent 绕过 `Command` / `Directive` 管线。
- 不让多个 agent 同时直接控制同一个单位。

## Acceptance Tests

- 能从 JSON 加载一个国家内阁，包含统治者、4 个部长、1 个元帅、至少 1 个将领。
- 每个 agent 都能生成与自己角色匹配的上下文摘要。
- 部长 MockAI 能输出结构化建议，并被 `DirectiveBoard` 接收。
- 仲裁器能处理至少 2 条冲突建议，并记录批准/拒绝原因。
- 玩家国家级命令能转换成 `StrategicDirective`。
- 被批准的战略指令能下发给战区/将领层，但不直接移动单位。
- UI 能展示内阁成员、建议、批准指令、拒绝原因和原始 JSON。
- 关闭真实 LLM 时，v0.5 仍可完整跑通。

## Assumptions

- v0.5 建立“国家与部长 agent 骨架”，不实现完整大战略数值系统。
- v0.5 默认使用 MockAI，真实本地 LLM 只作为可替换 provider。
- 阶段 0-4 的 `GameState`、`Command`、`DecisionProvider`、日志系统已经可用。
- 如果 v0.3 前线系统尚未完成，v0.5 的战略指令先下发为日志和计划，不强制驱动真实前线自动执行。
”。你先写agent1的提示词

按你的方案，我把 agent1 = Agent A：多 Agent 架构与数据模型。下面是可直接复制给下一个 agent 的提示词。
你是 Agent 1，负责 v0.5「国家与部长 Agent」中的多 Agent 架构与数据模型。

当前项目目标：
这是一个 iOS 二战大战略/战棋游戏。v0 已规划为 Swift + SwiftUI + SpriteKit 的六角格测试板。长期方向是本地 LLM 驱动多级 agent，包括统治者、元帅、部长、将领。v0.5 的目标不是做完整经济、外交、科技或政变系统，而是建立“国家级 agent + 部长 agent + 指挥链仲裁”的数据骨架。

你的任务范围只包含：
1. 多 agent 核心数据模型；
2. agent 角色与权限边界；
3. agent 人格、关系、记忆的最小结构；
4. 国家内阁 JSON 配置格式；
5. 示例德国与盟军内阁配置；
6. 基础加载与单元测试。

请先阅读这些文档：
- `md/phase0_v0_minimum_scope.md`
- `md/phase3_v0_engineering_architecture.md`
- `md/phase5_long_term_roadmap.md`
- `md/阶段性4:第一版可玩测试板任务拆解/README.md`

如果项目代码已经存在，先检查现有 `GeneralAgent`、`DecisionProvider`、`AgentContext`、`GameState`、`DataLoader` 等结构，沿用现有风格。不要重写无关模块。  
如果项目代码还没有建立，只输出可直接落地的 Swift 模型文件设计和 JSON 示例，不要虚构已存在文件。

核心设计要求：

一、Agent 类型模型

请设计一个通用 `GameAgent`，用于统一表示：
- 统治者；
- 元帅；
- 军工部长；
- 陆军部长；
- 外交部长；
- 情报部长；
- 将领。

如果现有代码已有 `GeneralAgent`，不要粗暴删除。推荐做法是：
- 保留 `GeneralAgent` 兼容 v0/v0.3；
- 新增 `GameAgent`；
- 或让 `GeneralAgent` 逐步迁移为 `GameAgent` 的一种 role；
- 保证 v0 的古德里安 agent 不被破坏。

建议模型包含：

```swift
struct GameAgent: Codable, Identifiable, Equatable {
    let id: String
    var displayName: String
    var faction: Faction
    var role: AgentRole
    var authorityLevel: AgentAuthorityLevel
    var personality: AgentPersonality
    var relationships: [AgentRelationship]
    var memory: AgentMemory
    var assignedScope: AgentScope
    var permissions: AgentPermissions
    var isPlayerControlled: Bool
}
二、角色与权限
定义：
enum AgentRole: String, Codable, CaseIterable {
    case ruler
    case fieldMarshal
    case armamentsMinister
    case armyMinister
    case foreignMinister
    case intelligenceMinister
    case general
}
定义权限层级：
enum AgentAuthorityLevel: String, Codable, CaseIterable {
    case national
    case theater
    case armyGroup
    case advisory
}
定义权限边界：
struct AgentPermissions: Codable, Equatable {
    var canIssueStrategicDirectives: Bool
    var canApproveDirectives: Bool
    var canAdviseProduction: Bool
    var canAdviseRecruitment: Bool
    var canAdviseDiplomacy: Bool
    var canAdviseIntelligence: Bool
    var canIssueTheaterDirectives: Bool
    var canIssueUnitCommands: Bool
}
重要约束：
统治者可以批准国家级战略指令；
元帅可以提出战区级作战意图；
部长只能提出建议，不能直接改资源、单位、科技、地图；
将领可以在自己权限内提出集团军/局部命令；
v0.5 高层 agent 不允许直接移动单位；
所有实际单位操作未来仍必须走 Command / RuleEngine 管线。
三、人格、关系与记忆
定义最小人格结构：
struct AgentPersonality: Codable, Equatable {
    var backgroundPrompt: String
    var aggression: Int
    var caution: Int
    var loyalty: Int
    var ambition: Int
    var politicalReliability: Int
    var preferredDoctrine: String?
}
数值范围统一使用 0...100。
定义关系结构：
struct AgentRelationship: Codable, Equatable {
    var targetAgentId: String
    var trust: Int
    var rivalry: Int
    var resentment: Int
    var notes: String?
}
定义最小记忆结构：
struct AgentMemory: Codable, Equatable {
    var shortTermNotes: [String]
    var longTermTraits: [String]
    var recentDirectives: [String]
    var recentDecisions: [String]
}
v0.5 只保存简单文本和 id，不做向量数据库，不做复杂长期记忆。
四、作用范围
定义 agent 的作用范围：
enum AgentScope: Codable, Equatable {
    case national
    case theater(theaterId: String)
    case armyGroup(armyGroupId: String)
    case cabinetDepartment(departmentId: String)
    case unassigned
}
如果 Swift Codable enum with associated values 在项目里已有固定写法，沿用现有写法。否则可以改成更稳定的结构：
struct AgentScope: Codable, Equatable {
    var type: AgentScopeType
    var id: String?
}
五、JSON 配置
新增或规划配置文件：
Data/agents.json
Data/national_cabinet_germany.json
Data/national_cabinet_allies.json
如果项目已有 Data 目录，放入现有目录；否则按阶段 3 文档建议放置。
德国示例至少包含：
统治者；
军工部长；
陆军部长；
外交部长；
情报部长；
1 名元帅；
古德里安将领。
盟军示例至少包含：
统治者；
军工部长；
陆军部长；
外交部长；
情报部长；
1 名元帅；
1 名将领。
注意：
可以使用历史灵感，但不要为了 v0.5 追求完整历史准确性；
当前重点是结构稳定、可加载、可扩展。
六、加载与测试
实现或规划加载逻辑：
从 JSON 加载 [GameAgent]；
校验 id 唯一；
校验 role 与 permissions 匹配；
校验数值范围在 0...100；
校验部长没有 canIssueUnitCommands = true；
校验统治者至少有 canApproveDirectives = true。
基础测试至少覆盖：
德国内阁 JSON 能成功加载；
盟军内阁 JSON 能成功加载；
每个 agent id 唯一；
部长不能直接下达单位命令；
将领可以保留单位级命令权限；
统治者拥有批准战略指令权限；
人格数值越界时加载失败或校验失败。
七、明确不要做
本 agent 不负责：
NationalContext、MinisterContext、TheaterContext 构建；
StrategicDirective 仲裁器；
部长 MockAI 决策；
聊天框；
UI；
前线自动执行；
真实 LLM 调用；
经济、科技、外交、政变的真实规则。
八、交付标准
完成后请输出：
新增或修改的模型文件清单；
JSON 配置文件清单；
权限规则说明；
测试清单和测试结果；
明确说明是否兼容现有 GeneralAgent；
明确说明后续 Agent B/C/D/E/F 应该依赖哪些类型。
验收标准：
多 agent 数据模型能表达 v0.5 需要的统治者、部长、元帅、将领；
示例德国与盟国内阁能被加载；
权限边界清楚，高层 agent 不能直接移动单位；
不破坏 v0 的 DecisionProvider / GeneralAgent 思路；
后续 agent 可以基于这些模型继续做摘要、仲裁、部长 MockAI 和 UI。

这个 agent1 的边界要卡紧：**只做模型、配置、加载、权限测试**。不要让它顺手把摘要、仲裁、UI 一起做了，否则后面几个 agent 会抢同一批核心接口。

星期三18:06






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三18:08






下面是 Agent 2 = Agent B：国家级摘要与上下文构建 的提示词。
你是 Agent 2，负责 v0.5「国家与部长 Agent」中的国家级摘要与上下文构建。

当前项目目标：
这是一个 iOS 二战大战略/战棋游戏。v0/v0.3 已围绕 `GameState`、`Command`、`DecisionProvider`、将领 agent、前线系统做规划。v0.5 要建立国家级 agent、部长 agent、元帅 agent、将领 agent 的分层上下文系统，让不同角色只能读取自己权限范围内的摘要信息。

你的任务范围只包含：
1. 分层 agent context 数据结构；
2. 从 `GameState` 构建国家级、部长级、战区级、将领级摘要；
3. 控制本地 LLM 输入长度；
4. 确保不同角色不能读取不该知道的信息；
5. 为后续战略指令、部长 MockAI、UI 展示提供稳定上下文。

请先阅读这些文档：
- `md/phase0_v0_minimum_scope.md`
- `md/phase3_v0_engineering_architecture.md`
- `md/phase5_long_term_roadmap.md`
- `md/阶段性4:第一版可玩测试板任务拆解/README.md`

并依赖 Agent 1 的交付：
- `GameAgent`
- `AgentRole`
- `AgentAuthorityLevel`
- `AgentPermissions`
- `AgentScope`
- `AgentPersonality`
- `AgentMemory`
- 国家内阁 JSON 配置

如果项目代码已经存在，先检查现有：
- `AgentContext`
- `AgentContextBuilder`
- `GameState`
- `VisibilityMap`
- `GeneralAgent`
- `Division`
- `MapState`
- `GameEvent`
- `DecisionProvider`

沿用现有风格，不要重写无关模块。

核心设计要求：

一、上下文分层

请设计统一的 agent context envelope：

```swift
struct AgentContextEnvelope: Codable, Equatable {
    let schemaVersion: Int
    let agentId: String
    let role: AgentRole
    let faction: Faction
    let turn: Int
    let scope: AgentScope
    let contextKind: AgentContextKind
    let summary: String
    let payload: AgentContextPayload
    let tokenEstimate: Int
}
定义上下文类型：
enum AgentContextKind: String, Codable, CaseIterable {
    case national
    case minister
    case theater
    case commander
}
定义 payload：
enum AgentContextPayload: Codable, Equatable {
    case national(NationalContext)
    case minister(MinisterContext)
    case theater(TheaterContext)
    case commander(CommanderContext)
}
如果项目不适合用 associated value enum，可以改成：
struct AgentContextPayload: Codable, Equatable {
    var national: NationalContext?
    var minister: MinisterContext?
    var theater: TheaterContext?
    var commander: CommanderContext?
}
优先选择项目中最稳定、最容易 JSON 编码的实现方式。
二、NationalContext
国家级上下文供统治者 agent 使用。
建议结构：
struct NationalContext: Codable, Equatable {
    var faction: Faction
    var turn: Int
    var warSummary: WarSummary
    var frontSummaries: [FrontSummary]
    var economySnapshot: EconomySnapshot?
    var manpowerSnapshot: ManpowerSnapshot?
    var productionSnapshot: ProductionSnapshot?
    var cabinetNotes: [String]
    var activeDirectives: [String]
    var recentEvents: [String]
}
v0.5 不要求完整经济系统。如果 GameState 还没有经济、生产、人力数据，使用 placeholder snapshot：
struct EconomySnapshot: Codable, Equatable {
    var status: String
    var availableResources: [String: Int]
    var factories: Int
    var taxIncome: Int
    var isPlaceholder: Bool
}
必须明确标记 isPlaceholder = true，避免后续 agent 误以为经济系统已完成。
三、MinisterContext
部长级上下文按部门裁剪信息。
建议结构：
struct MinisterContext: Codable, Equatable {
    var department: CabinetDepartment
    var faction: Faction
    var turn: Int
    var relevantSummary: String
    var warSummary: WarSummary
    var relevantFronts: [FrontSummary]
    var economySnapshot: EconomySnapshot?
    var productionSnapshot: ProductionSnapshot?
    var manpowerSnapshot: ManpowerSnapshot?
    var intelligenceSnapshot: IntelligenceSnapshot?
    var recentEvents: [String]
}
定义部门：
enum CabinetDepartment: String, Codable, CaseIterable {
    case armaments
    case army
    case foreignAffairs
    case intelligence
}
裁剪规则：
军工部长：重点读取工厂、装备、损耗、生产压力；不需要完整单位坐标。
陆军部长：重点读取兵力、组织度、补员、训练、损失；可读取前线压力摘要。
外交部长：v0.5 只读取外交占位、战争态势、风险摘要；不做真实外交。
情报部长：读取可见敌军、未知区域、前线威胁、最近异常事件；不得读取全图隐藏敌军。
四、TheaterContext
战区级上下文供元帅/战区司令使用。
建议结构：
struct TheaterContext: Codable, Equatable {
    var theaterId: String
    var faction: Faction
    var turn: Int
    var frontSummary: FrontSummary
    var assignedArmyGroups: [ArmyGroupSummary]
    var visibleEnemySummary: EnemyForceSummary
    var supplySummary: SupplySummary
    var objectives: [ObjectiveSummary]
    var recentEvents: [String]
}
如果 v0.3 前线系统还没实现：
使用当前地图控制边界生成简化 FrontSummary；
或返回 isPlaceholder = true 的前线摘要；
不要阻塞 v0.5 的上下文构建。
五、CommanderContext
将领级上下文应兼容 v0 已有 AgentContext。
建议结构：
struct CommanderContext: Codable, Equatable {
    var commanderId: String
    var faction: Faction
    var turn: Int
    var assignedDivisionIds: [String]
    var localMapSummary: String
    var friendlyUnits: [UnitSummary]
    var visibleEnemyUnits: [UnitSummary]
    var supplySummary: SupplySummary
    var currentDirective: String?
    var recentEvents: [String]
}
重要约束：
将领只能读取自己视野和分配范围内的信息；
不允许读取全图真实敌军；
如果 v0 没有战争迷雾，则先在字段名中明确 visibilityMode = "global-v0-placeholder" 或类似标记。
六、摘要基础类型
请定义可复用摘要结构：
struct WarSummary: Codable, Equatable {
    var overallStatus: String
    var controlledObjectives: [String]
    var contestedObjectives: [String]
    var enemyPressure: Int
    var supplyRisk: Int
}
struct FrontSummary: Codable, Equatable {
    var frontId: String
    var name: String
    var status: String
    var friendlyStrengthEstimate: Int
    var enemyStrengthEstimate: Int
    var gapRisk: Int
    var supplyRisk: Int
    var keyProvincesOrTiles: [String]
    var isPlaceholder: Bool
}
struct UnitSummary: Codable, Equatable {
    var unitId: String
    var name: String
    var faction: Faction
    var role: String
    var strength: Int
    var organization: Int?
    var supplyState: String
    var locationLabel: String
}
struct SupplySummary: Codable, Equatable {
    var status: String
    var riskLevel: Int
    var threatenedNodes: [String]
    var notes: [String]
}
数值风险统一使用 0...100。
七、上下文构建器
实现或规划：
struct LayeredAgentContextBuilder {
    func makeContext(for agent: GameAgent, state: GameState) throws -> AgentContextEnvelope
}
内部按 role 分发：
ruler -> NationalContext
armamentsMinister -> MinisterContext(department: .armaments)
armyMinister -> MinisterContext(department: .army)
foreignMinister -> MinisterContext(department: .foreignAffairs)
intelligenceMinister -> MinisterContext(department: .intelligence)
fieldMarshal -> TheaterContext
general -> CommanderContext
如果 agent role 与 scope 不匹配，应返回明确错误：
enum AgentContextBuildError: Error, Equatable {
    case unsupportedRole
    case invalidScope
    case missingFactionState
    case missingAssignedUnits
    case permissionDenied
}
八、Token 控制
本地 LLM 后续会读取这些 context，因此必须限制摘要长度。
请实现或规划：
struct AgentContextBudget {
    var maxSummaryCharacters: Int
    var maxRecentEvents: Int
    var maxFronts: Int
    var maxUnits: Int
}
推荐默认值：
国家级：最多 6 条 front summary、10 条 recent events；
部长级：最多 4 条 relevant fronts、8 条 recent events；
战区级：最多 3 条 army group、12 个 unit summary；
将领级：最多 8 个 friendly unit、8 个 visible enemy unit。
如果数据超过限制：
按重要性排序截断；
在 summary 或 notes 中标记“已截断”；
不要无限拼接日志。
九、权限和信息隔离
必须保证：
部长不能获得单位级完整命令权限；
情报部长不能看到不可见敌军真实位置；
高层 agent 得到的是摘要，不是完整 GameState；
AgentContextEnvelope 不包含可直接修改状态的引用；
context builder 是只读逻辑，不改变 GameState。
十、测试要求
基础测试至少覆盖：
统治者 agent 能生成 NationalContext；
四类部长能生成对应 MinisterContext；
元帅能生成 TheaterContext；
将领能生成 CommanderContext；
不同角色拿到不同粒度信息；
情报部长和将领不会拿到不可见敌军；
超过预算的日志和单位列表会被截断；
placeholder 经济/前线字段明确标记 isPlaceholder = true；
context builder 不修改 GameState；
无效 scope 会返回错误。
十一、明确不要做
本 agent 不负责：
新增 agent 角色模型；
新增内阁 JSON；
StrategicDirective 仲裁器；
部长 MockAI；
统治者决策；
聊天命令解析；
UI；
真实 LLM 调用；
真实经济、科技、外交规则；
直接执行单位命令。
十二、交付标准
完成后请输出：
新增或修改的 context 类型清单；
LayeredAgentContextBuilder 的行为说明；
每种角色能读取哪些信息；
Token/长度预算规则；
placeholder 字段说明；
测试清单和测试结果；
后续 Agent C/D/E/F 应该依赖哪些类型。
验收标准：
所有 v0.5 角色都能生成稳定、可 JSON 编码的上下文；
上下文按角色裁剪，不暴露完整 GameState；
没有完整经济/前线系统时仍可运行；
输出结构可被后续 StrategicDirective、部长 MockAI 和 UI 直接使用；
不破坏 v0 的 AgentContext / DecisionProvider 管线。

没写完，下一条继续 **Agent 3：战略意图与仲裁系统**。

星期三18:10






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写

下面是 Agent 3 = Agent C：战略意图与仲裁系统 的提示词。
你是 Agent 3，负责 v0.5「国家与部长 Agent」中的战略意图与仲裁系统。

当前项目目标：
这是一个 iOS 二战大战略/战棋游戏。v0.5 要建立“国家级 agent + 部长 agent + 指挥链仲裁”的骨架。多个 agent 会提出战略建议，例如统治者要求进攻、陆军部长要求补员、军工部长要求优先生产坦克、情报部长警告某条前线风险升高。你的任务是建立一个统一的 `StrategicDirective` 和 `DirectiveBoard` 系统，负责接收、记录、仲裁、批准、拒绝和下发这些建议。

你的任务范围只包含：
1. 战略指令数据模型；
2. 指令状态流转；
3. 多 agent 建议仲裁；
4. 冲突检测与记录；
5. 将批准后的高层战略意图下发给战区/将领层；
6. 单元测试。

请先阅读这些文档：
- `md/phase0_v0_minimum_scope.md`
- `md/phase3_v0_engineering_architecture.md`
- `md/phase5_long_term_roadmap.md`
- `md/阶段性4:第一版可玩测试板任务拆解/README.md`

并依赖 Agent 1 和 Agent 2 的交付：
- `GameAgent`
- `AgentRole`
- `AgentAuthorityLevel`
- `AgentPermissions`
- `AgentScope`
- `AgentContextEnvelope`
- `NationalContext`
- `MinisterContext`
- `TheaterContext`
- `CommanderContext`

如果项目代码已经存在，先检查现有：
- `Command`
- `CommandIssuer`
- `GameState`
- `GameEvent`
- `AgentDecisionEnvelope`
- `DecisionProvider`
- `AgentContext`
- 日志系统

沿用现有命名和 Codable/Equatable 风格。不要重写无关模块。

核心设计要求：

一、StrategicDirective 模型

定义统一战略指令：

```swift
struct StrategicDirective: Codable, Identifiable, Equatable {
    let id: String
    var schemaVersion: Int
    var title: String
    var summary: String
    var issuedByAgentId: String
    var issuedByRole: AgentRole
    var faction: Faction
    var domain: DirectiveDomain
    var target: DirectiveTarget
    var priority: DirectivePriority
    var status: DirectiveStatus
    var createdTurn: Int
    var expiresOnTurn: Int?
    var rationale: String
    var expectedEffect: String?
    var riskAssessment: String?
    var sourceDecisionId: String?
    var arbitration: DirectiveArbitrationRecord?
}
定义领域：
enum DirectiveDomain: String, Codable, CaseIterable {
    case nationalStrategy
    case theaterOperations
    case production
    case manpower
    case diplomacy
    case intelligence
    case logistics
}
定义目标：
struct DirectiveTarget: Codable, Equatable {
    var scope: AgentScope
    var objectiveId: String?
    var provinceOrRegionId: String?
    var theaterId: String?
    var armyGroupId: String?
    var tags: [String]
}
v0.5 不要求真实省份图或完整前线系统存在。若相关系统未完成，provinceOrRegionId、theaterId 可以使用占位 id，但必须可序列化、可展示、可测试。
二、优先级与状态
定义优先级：
enum DirectivePriority: String, Codable, CaseIterable {
    case low
    case normal
    case high
    case critical
}
定义状态：
enum DirectiveStatus: String, Codable, CaseIterable {
    case proposed
    case approved
    case rejected
    case active
    case expired
    case completed
    case superseded
}
状态流转规则：
proposed -> approved
proposed -> rejected
approved -> active
active -> completed
active -> expired
active -> superseded
approved -> superseded
禁止状态流转：
rejected 不能重新变成 active；
completed 不能重新变成 active；
expired 不能重新变成 active；
superseded 不能重新变成 active。
实现或规划：
struct DirectiveStateMachine {
    func canTransition(from: DirectiveStatus, to: DirectiveStatus) -> Bool
}
三、DirectiveBoard / AgentCouncil
实现统一指令板：
struct DirectiveBoard: Codable, Equatable {
    var directives: [StrategicDirective]
    var conflicts: [DirectiveConflict]
    var arbitrationLog: [DirectiveArbitrationRecord]
}
建议提供管理器：
final class DirectiveBoardManager {
    func submit(_ directive: StrategicDirective, from agent: GameAgent, state: GameState) throws -> StrategicDirective
    func arbitratePendingDirectives(board: DirectiveBoard, agents: [GameAgent], state: GameState) -> DirectiveBoard
    func approve(_ directiveId: String, reason: String) throws
    func reject(_ directiveId: String, reason: String) throws
    func activateApprovedDirectives(currentTurn: Int) throws
    func expireOutdatedDirectives(currentTurn: Int) throws
}
如果项目更偏函数式，也可以使用纯函数：
struct DirectiveArbitrator {
    func arbitrate(board: DirectiveBoard, agents: [GameAgent], state: GameState) -> DirectiveArbitrationResult
}
优先保证：
可测试；
不依赖 UI；
不直接修改单位、地图、资源；
不绕过 Command / RuleEngine。
四、仲裁规则
定义仲裁记录：
struct DirectiveArbitrationRecord: Codable, Identifiable, Equatable {
    let id: String
    var directiveId: String
    var turn: Int
    var decision: ArbitrationDecision
    var decidedByAgentId: String?
    var reason: String
    var conflictingDirectiveIds: [String]
}
enum ArbitrationDecision: String, Codable, CaseIterable {
    case approved
    case rejected
    case deferred
    case superseded
}
基础仲裁规则：
统治者的国家级指令优先级最高。
领域内部长建议只在对应领域有较高权重：军工部长：production；
陆军部长：manpower、logistics、theaterOperations 的补员/训练侧；
外交部长：diplomacy；
情报部长：intelligence；
元帅：theaterOperations；
将领：局部 theaterOperations，但低于元帅。

critical 优先级高于 high、normal、low，但不能突破权限边界。
没有权限的 agent 提出的 directive 必须 rejected，并记录原因。
同一目标、同一领域、相反意图的 directive 需要冲突检测。
如果统治者明确批准某条 directive，其他冲突 directive 变成 superseded 或 rejected。
实现权限检查：
struct DirectivePermissionValidator {
    func validate(agent: GameAgent, directive: StrategicDirective) -> DirectivePermissionResult
}
权限示例：
ruler 可以提交并批准 nationalStrategy；
fieldMarshal 可以提交 theaterOperations；
armamentsMinister 可以提交 production；
armyMinister 可以提交 manpower、logistics；
foreignMinister 可以提交 diplomacy；
intelligenceMinister 可以提交 intelligence；
general 可以提交局部 theaterOperations，但不能批准国家级战略；
部长不能提交会直接移动单位的 directive。
五、冲突检测
定义冲突模型：
struct DirectiveConflict: Codable, Identifiable, Equatable {
    let id: String
    var directiveIds: [String]
    var domain: DirectiveDomain
    var target: DirectiveTarget
    var severity: ConflictSeverity
    var reason: String
    var detectedTurn: Int
    var resolved: Bool
}
enum ConflictSeverity: String, Codable, CaseIterable {
    case minor
    case moderate
    case severe
}
v0.5 至少支持以下冲突：
同一战区同时存在“立即进攻”和“撤退/休整”；
同一生产方向同时存在“优先坦克”和“优先步兵装备”；
陆军部长要求补员休整，而统治者要求立即进攻；
情报部长标记某方向高风险，而元帅要求该方向进攻。
可以通过 tags 做简化判断，例如：
offensive
defensive
rest
reinforce
tank_priority
infantry_priority
high_risk
不要为了 v0.5 做复杂自然语言语义分析。结构化字段优先。
六、下发给战区/将领层
批准后的高层指令不能直接变成单位 Command。它只能转换为下级 agent 可读的 directive。
定义：
struct TheaterDirective: Codable, Identifiable, Equatable {
    let id: String
    var sourceStrategicDirectiveId: String
    var theaterId: String
    var faction: Faction
    var summary: String
    var priority: DirectivePriority
    var objectiveTags: [String]
    var expiresOnTurn: Int?
}
struct CommanderDirective: Codable, Identifiable, Equatable {
    let id: String
    var sourceStrategicDirectiveId: String
    var commanderAgentId: String
    var faction: Faction
    var summary: String
    var priority: DirectivePriority
    var objectiveTags: [String]
    var expiresOnTurn: Int?
}
实现或规划：
struct DirectiveDispatcher {
    func makeTheaterDirectives(from directive: StrategicDirective, agents: [GameAgent]) -> [TheaterDirective]
    func makeCommanderDirectives(from directive: StrategicDirective, agents: [GameAgent]) -> [CommanderDirective]
}
重要约束：
TheaterDirective / CommanderDirective 不是单位命令；
它们只能进入后续元帅/将领的 context；
实际移动、攻击、撤退仍由将领 agent 输出 Command，并由 RuleEngine 校验执行。
七、日志与事件
如果已有 GameEvent，扩展事件类型：
case directiveSubmitted
case directiveApproved
case directiveRejected
case directiveConflictDetected
case directiveDispatched
case directiveExpired
如果不适合修改 GameEvent，先定义独立日志：
struct DirectiveLogEntry: Codable, Identifiable, Equatable {
    let id: String
    var turn: Int
    var directiveId: String?
    var message: String
    var level: DirectiveLogLevel
}
enum DirectiveLogLevel: String, Codable {
    case info
    case warning
    case error
}
要求：
每条拒绝必须有 reason；
每条冲突必须有 reason；
每条批准必须记录是谁批准或由什么规则批准；
日志可被 UI 展示。
八、测试要求
基础测试至少覆盖：
合法部长建议可以进入 DirectiveBoard；
无权限 agent 提交 directive 会被拒绝；
统治者可以批准国家级 directive；
部长不能批准国家级 directive；
同一战区“进攻”和“休整”会被识别为冲突；
同一生产领域“优先坦克”和“优先步兵装备”会被识别为冲突；
仲裁器能稳定批准高权限、高优先级 directive；
被批准 directive 能生成 TheaterDirective 或 CommanderDirective；
下发结果不会直接生成单位 Command；
状态机禁止非法状态流转；
过期 directive 会变为 expired；
冲突、批准、拒绝都有可展示日志。
九、明确不要做
本 agent 不负责：
新增 agent 数据模型；
构建 NationalContext / MinisterContext；
部长 MockAI；
统治者聊天入口；
UI；
真实 LLM 调用；
真实经济、科技、外交规则；
前线自动排兵；
单位移动、攻击、撤退执行。
十、交付标准
完成后请输出：
新增或修改的 directive 类型清单；
仲裁规则说明；
权限校验规则说明；
冲突检测规则说明；
指令下发规则说明；
日志事件说明；
测试清单和测试结果；
后续 Agent D/E/F 应该依赖哪些类型。
验收标准：
多 agent 建议能统一表示为 StrategicDirective；
DirectiveBoard 能接收、批准、拒绝、过期和记录冲突；
仲裁器能处理至少 2 条冲突建议并记录原因；
被批准指令能下发给战区/将领层；
不直接移动单位，不绕过 Command / RuleEngine；
没有真实 LLM 时仍可完整测试。

没写完，下一条继续 **Agent 4：部长 Agent 原型**。

星期三18:12






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三18:12






下面是 Agent 4 = Agent D：部长 Agent 原型 的提示词。
你是 Agent 4，负责 v0.5「国家与部长 Agent」中的部长 Agent 原型。

当前项目目标：
这是一个 iOS 二战大战略/战棋游戏。v0.5 要建立国家级 agent、部长 agent、元帅 agent、将领 agent 的骨架。你的任务是实现第一批部长 agent 的 Mock 决策能力，让军工部长、陆军部长、外交部长、情报部长可以读取自己的 `MinisterContext`，输出结构化建议，并提交给 `DirectiveBoard`。v0.5 默认使用 MockAI，不强制调用真实本地 LLM。

你的任务范围只包含：
1. 4 个部长 agent 的 Mock 决策；
2. 部长建议 JSON schema；
3. 部长 prompt 模板占位；
4. 将部长建议转换为 `StrategicDirective`；
5. 部长会议建议列表；
6. 单元测试。

请先阅读这些文档：
- `md/phase0_v0_minimum_scope.md`
- `md/phase3_v0_engineering_architecture.md`
- `md/phase5_long_term_roadmap.md`
- `md/阶段性4:第一版可玩测试板任务拆解/README.md`

并依赖 Agent 1、2、3 的交付：
- `GameAgent`
- `AgentRole`
- `CabinetDepartment`
- `MinisterContext`
- `AgentContextEnvelope`
- `StrategicDirective`
- `DirectiveBoard`
- `DirectiveDomain`
- `DirectiveTarget`
- `DirectivePriority`
- `DirectiveStatus`
- `DirectiveBoardManager` 或 `DirectiveArbitrator`

如果项目代码已经存在，先检查现有：
- `DecisionProvider`
- `MockAIClient`
- `AgentDecisionEnvelope`
- `AgentOrder`
- `AgentContextBuilder`
- `GameEvent`
- 日志系统

沿用现有 provider 风格，不要重写无关 AI 管线。

核心设计要求：

一、部长决策协议

定义部长决策 provider，建议沿用现有 `DecisionProvider` 思路：

```swift
protocol MinisterDecisionProvider {
    func decide(agent: GameAgent, context: MinisterContext) async throws -> MinisterDecisionEnvelope
}
定义统一部长输出：
struct MinisterDecisionEnvelope: Codable, Equatable {
    let schemaVersion: Int
    let decisionId: String
    let agentId: String
    let role: AgentRole
    let department: CabinetDepartment
    let turn: Int
    let summary: String
    let recommendations: [MinisterRecommendation]
    let rawJSON: String?
}
struct MinisterRecommendation: Codable, Identifiable, Equatable {
    let id: String
    var title: String
    var summary: String
    var domain: DirectiveDomain
    var target: DirectiveTarget
    var priority: DirectivePriority
    var rationale: String
    var expectedEffect: String?
    var riskAssessment: String?
    var tags: [String]
}
要求：
每个部长至少输出 0-3 条建议；
建议必须能转换为 StrategicDirective；
空建议是合法结果，例如局势稳定时；
不能输出单位级 Command。
二、部长 Mock Provider
实现一个总入口：
final class MockMinisterDecisionProvider: MinisterDecisionProvider {
    func decide(agent: GameAgent, context: MinisterContext) async throws -> MinisterDecisionEnvelope
}
内部按 CabinetDepartment 分发：
armaments -> 军工部长逻辑；
army -> 陆军部长逻辑；
foreignAffairs -> 外交部长逻辑；
intelligence -> 情报部长逻辑。
也可以拆成：
MockArmamentsMinisterProvider
MockArmyMinisterProvider
MockForeignMinisterProvider
MockIntelligenceMinisterProvider
优先保持测试简单、输出稳定。
三、军工部长逻辑
输入重点：
EconomySnapshot
ProductionSnapshot
WarSummary
recentEvents
输出方向：
工厂不足时建议扩充军工；
装甲损耗高或进攻计划多时建议优先坦克；
步兵损耗高或补员压力大时建议优先步兵装备；
补给风险高时建议优先卡车/后勤；
v0.5 没有真实经济时，必须标记建议基于 placeholder。
示例建议 tags：
production
tank_priority
infantry_priority
logistics_priority
placeholder_economy
示例输出：
{
  "title": "Prioritize armored replacements",
  "domain": "production",
  "priority": "high",
  "tags": ["production", "tank_priority"],
  "summary": "Armored formations are central to the current offensive plan."
}
四、陆军部长逻辑
输入重点：
ManpowerSnapshot
WarSummary
FrontSummary
SupplySummary
组织度、损失、补给风险摘要
输出方向：
组织度低时建议休整；
前线缺口风险高时建议补充兵力；
包围风险高时建议后撤或重整；
损失高时建议暂停进攻、优先补员；
战线稳定时建议训练或预备队。
示例 tags：
manpower
reinforce
rest
training
front_gap
supply_risk
注意：
陆军部长不能直接命令某个师移动；
只能提出战略/战区建议。
五、外交部长逻辑
v0.5 不做真实外交规则，只做态势建议。
输入重点：
WarSummary
外交 placeholder snapshot，如无则使用 context 的 recentEvents
战局优势/劣势
输出方向：
战局不利时建议寻求外援或避免多线战争；
战局有利时建议施压敌方或争取中立方；
高风险时建议谨慎扩大冲突；
没有外交系统时建议必须带 placeholder_diplomacy tag。
示例 tags：
diplomacy
avoid_escalation
seek_support
pressure_enemy
placeholder_diplomacy
六、情报部长逻辑
输入重点：
IntelligenceSnapshot
FrontSummary
visibleEnemySummary
未知区域、敌军压力、风险等级
输出方向：
前线敌强我弱时发出高风险警告；
未知区域多时建议侦察；
补给节点受威胁时建议加强防护；
敌军集中时建议提高该战区风险。
示例 tags：
intelligence
high_risk
recon
enemy_concentration
supply_node_threat
重要约束：
情报部长不能读取不可见敌军真实位置；
只能基于 MinisterContext 中提供的情报摘要输出。
七、转换为 StrategicDirective
实现或规划转换器：
struct MinisterRecommendationConverter {
    func makeDirective(
        from recommendation: MinisterRecommendation,
        decision: MinisterDecisionEnvelope,
        agent: GameAgent
    ) -> StrategicDirective
}
转换规则：
issuedByAgentId = decision.agentId
issuedByRole = decision.role
domain = recommendation.domain
target = recommendation.target
priority = recommendation.priority
status = .proposed
sourceDecisionId = decision.decisionId
rationale = recommendation.rationale
expectedEffect = recommendation.expectedEffect
riskAssessment = recommendation.riskAssessment
转换器不负责批准或执行，只提交给 DirectiveBoard。
八、部长会议
实现或规划部长会议结果：
struct CabinetMeetingResult: Codable, Equatable {
    let turn: Int
    let faction: Faction
    let decisions: [MinisterDecisionEnvelope]
    let submittedDirectiveIds: [String]
    let logs: [String]
}
实现服务：
final class CabinetMeetingService {
    func runMinisterMeeting(
        ministers: [GameAgent],
        contexts: [String: MinisterContext],
        provider: MinisterDecisionProvider,
        directiveBoard: DirectiveBoard
    ) async throws -> CabinetMeetingResult
}
如果项目更适合纯函数，可改为 service + result，不直接持有全局状态。
要求：
只处理部长 role；
非部长 agent 应跳过并记录；
单个部长失败不应导致整个会议崩溃；
失败要记录日志；
成功建议提交为 proposed directive。
九、Prompt 模板占位
虽然 v0.5 默认 MockAI，但需要为后续 LLM 留 prompt 模板。
定义：
struct MinisterPromptTemplate: Codable, Equatable {
    var department: CabinetDepartment
    var systemInstruction: String
    var outputSchemaDescription: String
}
提供 4 个部门的模板文本，要求：
只输出 JSON；
不输出单位级命令；
不绕过规则系统；
所有建议必须能转换为 StrategicDirective；
如信息不足，应输出低优先级或空建议。
不需要真实调用 LLM。
十、日志与原始 JSON
要求：
Mock provider 也生成可展示的 JSON 字符串；
JSON decode 后应等价于 MinisterDecisionEnvelope；
每个部长决策记录到 agent 日志或 directive 日志；
失败时记录错误，不修改游戏核心状态。
十一、测试要求
基础测试至少覆盖：
军工部长能根据 placeholder 经济输出生产建议；
陆军部长能根据低组织度/高补给风险输出休整或补员建议；
外交部长在无真实外交系统时输出带 placeholder_diplomacy 的建议；
情报部长能根据高风险摘要输出侦察或警告建议；
每个部长输出 JSON 可解析为 MinisterDecisionEnvelope；
每条 MinisterRecommendation 可转换为 StrategicDirective；
转换后的 directive 状态为 proposed；
部长不会输出单位 Command；
单个部长 provider 失败不会导致整个部长会议失败；
非部长 agent 不参与部长会议；
同一固定 context 下 Mock 输出稳定；
关闭真实 LLM 时完整跑通。
十二、明确不要做
本 agent 不负责：
新增 agent 角色模型；
构建 MinisterContext；
仲裁和批准 directive；
统治者 agent；
玩家聊天入口；
UI；
真实 LLM 调用；
真实经济、科技、外交规则；
直接修改资源、单位、地图或科技；
前线自动执行。
十三、交付标准
完成后请输出：
新增或修改的部长 provider 类型清单；
每个部长的 Mock 决策规则；
JSON schema 说明；
MinisterRecommendation -> StrategicDirective 转换说明；
部长会议服务说明；
Prompt 模板说明；
测试清单和测试结果；
后续 Agent E/F 应该依赖哪些类型。
验收标准：
4 个部长都能基于 MinisterContext 输出稳定结构化建议；
建议能进入 DirectiveBoard 的 proposed 状态；
MockAI 默认可用，无真实 LLM 也能跑通；
部长不能直接控制单位或修改游戏状态；
输出和日志足够 UI 展示。

没写完，下一条继续 **Agent 5：统治者与玩家角色入口**。

星期三18:14






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写

下面是 Agent 5 = Agent E：统治者与玩家角色入口 的提示词。
你是 Agent 5，负责 v0.5「国家与部长 Agent」中的统治者与玩家角色入口。

当前项目目标：
这是一个 iOS 二战大战略/战棋游戏。v0.5 要建立国家级 agent、部长 agent、元帅 agent、将领 agent 的骨架。你的任务是实现“统治者 agent + 玩家国家级命令入口”的数据与逻辑层，让玩家可以以统治者身份发布国家级命令，统治者 agent 可以综合部长建议，形成最终战略目标，并通过 `DirectiveBoard` 下发给战区/将领层。

v0.5 不实现完整角色扮演、政变、真实外交、真实经济，也不实现玩家扮演将领的完整生涯模式。只预留接口。

你的任务范围只包含：
1. `RulerAgent` 行为模型；
2. 玩家国家级命令数据模型；
3. 自然语言命令到 `StrategicDirective` 的简化转换；
4. 统治者综合部长建议的 Mock 决策；
5. 统治者决策 JSON schema；
6. 与 `DirectiveBoard` 的接入；
7. 单元测试。

请先阅读这些文档：
- `md/phase0_v0_minimum_scope.md`
- `md/phase3_v0_engineering_architecture.md`
- `md/phase5_long_term_roadmap.md`
- `md/阶段性4:第一版可玩测试板任务拆解/README.md`

并依赖 Agent 1、2、3、4 的交付：
- `GameAgent`
- `AgentRole`
- `AgentPersonality`
- `AgentRelationship`
- `AgentMemory`
- `AgentPermissions`
- `NationalContext`
- `AgentContextEnvelope`
- `MinisterDecisionEnvelope`
- `MinisterRecommendation`
- `StrategicDirective`
- `DirectiveBoard`
- `DirectiveBoardManager`
- `DirectivePriority`
- `DirectiveStatus`
- `DirectiveDomain`
- `DirectiveTarget`

如果项目代码已经存在，先检查现有：
- `DecisionProvider`
- `AgentDecisionEnvelope`
- `AgentContextBuilder`
- `GameEvent`
- `CommandIssuer`
- 聊天命令相关结构
- 日志系统

沿用现有风格，不要重写无关模块。

核心设计要求：

一、统治者 Agent

v0.5 中统治者仍然使用 `GameAgent(role: .ruler)` 表示。不要另建完全平行且割裂的模型。

可以新增统治者偏好：

```swift
struct RulerProfile: Codable, Equatable {
    var agentId: String
    var nationalGoals: [String]
    var politicalStyle: PoliticalStyle
    var warPriority: WarPriority
    var trustPolicy: TrustPolicy
}
enum PoliticalStyle: String, Codable, CaseIterable {
    case cautious
    case pragmatic
    case aggressive
    case ideological
}
enum WarPriority: String, Codable, CaseIterable {
    case preserveForces
    case balanced
    case rapidOffensive
    case totalWar
}
enum TrustPolicy: String, Codable, CaseIterable {
    case delegateToCommanders
    case balancedControl
    case directIntervention
}
要求：
RulerProfile.agentId 必须对应一个 GameAgent(role: .ruler)；
统治者可以批准或拒绝国家级战略指令；
统治者不直接移动单位；
统治者输出的是 StrategicDirective 或批准/拒绝决定。
二、玩家国家级命令
定义玩家输入模型：
struct PlayerNationalOrder: Codable, Identifiable, Equatable {
    let id: String
    var turn: Int
    var faction: Faction
    var issuedByPlayerId: String?
    var rawText: String
    var targetScope: AgentScope
    var priorityHint: DirectivePriority?
    var createdAt: Date?
}
如果项目中避免 Date 影响测试稳定性，可以用字符串或 turn 代替：
var createdTurn: Int
定义解析结果：
struct PlayerOrderParseResult: Codable, Equatable {
    var orderId: String
    var directives: [StrategicDirective]
    var warnings: [String]
    var confidence: Int
}
v0.5 只做规则/关键词式解析，不做真实 LLM 解析。
三、玩家命令解析规则
实现或规划：
struct PlayerNationalOrderParser {
    func parse(order: PlayerNationalOrder, ruler: GameAgent, context: NationalContext) -> PlayerOrderParseResult
}
关键词到 directive 的简化映射：
包含“进攻”、“突破”、“夺取”、“占领” -> domain = .theaterOperations，tags 包含 offensive
包含“防守”、“固守”、“守住” -> domain = .theaterOperations，tags 包含 defensive
包含“休整”、“补员”、“恢复” -> domain = .manpower 或 .logistics，tags 包含 rest / reinforce
包含“生产”、“工厂”、“坦克”、“飞机”、“装备” -> domain = .production
包含“侦察”、“情报”、“敌军动向” -> domain = .intelligence
包含“外交”、“停战”、“盟友” -> domain = .diplomacy
优先级解析：
包含“立即”、“必须”、“不惜代价” -> critical
包含“优先”、“尽快” -> high
未识别 -> normal
目标解析：
v0.5 可以先从文本中识别简单关键词，例如 阿登、巴斯托涅、东线、西线；
如果无法识别具体目标，使用 targetScope = order.targetScope；
未识别目标时添加 warning，但仍可生成 directive。
要求：
解析器输出的 directive 状态必须是 .proposed；
issuedByAgentId 使用统治者 agent id；
issuedByRole = .ruler；
rationale 说明来自玩家国家级命令；
不生成单位 Command。
四、统治者综合部长建议
定义 provider：
protocol RulerDecisionProvider {
    func decide(
        ruler: GameAgent,
        profile: RulerProfile,
        context: NationalContext,
        ministerDecisions: [MinisterDecisionEnvelope],
        activeDirectives: [StrategicDirective]
    ) async throws -> RulerDecisionEnvelope
}
定义输出：
struct RulerDecisionEnvelope: Codable, Equatable {
    let schemaVersion: Int
    let decisionId: String
    let rulerAgentId: String
    let faction: Faction
    let turn: Int
    let summary: String
    let proposedDirectives: [StrategicDirective]
    let approvalDecisions: [DirectiveApprovalDecision]
    let rawJSON: String?
}
struct DirectiveApprovalDecision: Codable, Identifiable, Equatable {
    let id: String
    var directiveId: String
    var decision: ArbitrationDecision
    var reason: String
}
实现 Mock provider：
final class MockRulerDecisionProvider: RulerDecisionProvider
Mock 决策规则：
如果 WarPriority.rapidOffensive，更容易批准 offensive、tank_priority；
如果 WarPriority.preserveForces，更容易批准 rest、reinforce、defensive；
如果 PoliticalStyle.cautious，高风险情报建议权重更高；
如果 PoliticalStyle.aggressive，进攻建议权重更高；
如果部长建议互相冲突，统治者只输出审批建议，不直接改 board；
真正批准/拒绝仍交给 DirectiveBoardManager。
五、统治者决策 JSON schema
Mock provider 也要生成可展示 JSON。
JSON 顶层字段：
{
  "schemaVersion": 1,
  "decisionId": "ruler-decision-turn-3",
  "rulerAgentId": "hitler_or_churchill_placeholder",
  "faction": "germany",
  "turn": 3,
  "summary": "...",
  "proposedDirectives": [],
  "approvalDecisions": [],
  "rawJSON": null
}
要求：
JSON 能 decode 为 RulerDecisionEnvelope；
proposedDirectives 状态必须是 proposed；
approvalDecisions 只表达建议，由 DirectiveBoardManager 落地；
不包含单位命令。
六、接入 DirectiveBoard
实现或规划服务：
final class RulerCommandService {
    func submitPlayerOrder(
        _ order: PlayerNationalOrder,
        ruler: GameAgent,
        context: NationalContext,
        board: DirectiveBoard
    ) throws -> PlayerOrderParseResult

    func runRulerDecision(
        ruler: GameAgent,
        profile: RulerProfile,
        context: NationalContext,
        ministerDecisions: [MinisterDecisionEnvelope],
        board: DirectiveBoard,
        provider: RulerDecisionProvider
    ) async throws -> RulerDecisionEnvelope
}
要求：
玩家命令生成的 directive 进入 DirectiveBoard 的 proposed 状态；
统治者 provider 生成的 proposed directive 也进入 board；
统治者 approval decision 通过 board manager 记录批准/拒绝原因；
服务不直接改单位、地图、资源；
所有结果写入日志。
七、玩家扮演将领预留接口
v0.5 不实现完整将领生涯模式，但要预留角色模式：
enum PlayerRoleMode: String, Codable, CaseIterable {
    case ruler
    case fieldMarshal
    case general
}
struct PlayerRoleState: Codable, Equatable {
    var mode: PlayerRoleMode
    var controlledAgentId: String?
    var faction: Faction
}
要求：
v0.5 默认 mode = .ruler；
不实现任免、政变、抗命完整规则；
只保证后续能扩展玩家扮演元帅/将领。
八、日志
如果已有 GameEvent，扩展或复用：
playerNationalOrderSubmitted
playerNationalOrderParsed
rulerDecisionGenerated
rulerDirectiveProposed
rulerApprovalSuggested
如果不适合修改 GameEvent，定义独立日志：
struct RulerDecisionLogEntry: Codable, Identifiable, Equatable {
    let id: String
    var turn: Int
    var agentId: String
    var message: String
    var rawJSON: String?
}
要求：
玩家原始文本可展示；
解析 warning 可展示；
统治者决策 JSON 可展示；
所有批准/拒绝建议有 reason。
九、测试要求
基础测试至少覆盖：
RulerProfile.agentId 能匹配 GameAgent(role: .ruler)；
玩家输入“立即进攻阿登”能生成 theaterOperations + offensive + critical/high directive；
玩家输入“优先补员休整”能生成 manpower/logistics + rest/reinforce directive；
玩家输入“增加坦克生产”能生成 production + tank_priority directive；
无法识别目标时仍生成 directive，并返回 warning；
玩家命令不会生成单位 Command；
Mock 统治者在 rapidOffensive 下更倾向批准进攻建议；
Mock 统治者在 preserveForces 下更倾向批准休整/补员建议；
统治者决策 JSON 可 decode；
proposed directive 能进入 DirectiveBoard；
approval decision 能被 board manager 记录；
PlayerRoleState 默认 ruler 模式可创建；
无真实 LLM 时完整跑通。
十、明确不要做
本 agent 不负责：
新增基础 agent 角色模型；
构建 NationalContext；
实现部长 MockAI；
实现仲裁器底层规则；
UI 聊天框；
真实 LLM 调用；
真实政变系统；
真实外交、经济、科技规则；
玩家扮演将领完整生涯；
任免、清洗、抗命的完整机制；
直接执行单位命令。
十一、交付标准
完成后请输出：
新增或修改的统治者相关类型清单；
玩家国家级命令解析规则；
Mock 统治者决策规则；
RulerDecisionEnvelope JSON schema；
DirectiveBoard 接入说明；
玩家角色模式预留说明；
日志说明；
测试清单和测试结果；
后续 Agent F 应该依赖哪些类型。
验收标准：
玩家能以统治者身份提交国家级自然语言命令；
命令能转换为 proposed StrategicDirective；
统治者 agent 能综合部长建议输出审批建议；
所有结果进入 directive/log 管线；
不直接移动单位，不绕过规则系统；
v0.5 默认 MockAI，无真实 LLM 也能跑通。

没写完，下一条继续 **Agent 6：内阁/Agent UI 与日志**。

星期三18:17






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三18:17






下面是 Agent 6 = Agent F：内阁/Agent UI 与日志 的提示词。
你是 Agent 6，负责 v0.5「国家与部长 Agent」中的内阁 / Agent UI 与日志展示。

当前项目目标：
这是一个 iOS 二战大战略/战棋游戏。v0.5 已规划国家级 agent、部长 agent、元帅 agent、将领 agent、分层上下文、战略指令、仲裁、部长 Mock 决策、统治者入口。你的任务是让玩家能看懂这些 agent 在做什么：谁提出了建议、谁批准或拒绝了指令、为什么冲突、战略指令如何下发给战区/将领。

你的任务范围只包含：
1. 内阁成员展示；
2. agent 决策和原始 JSON 展示；
3. `DirectiveBoard` 展示；
4. 仲裁、冲突、批准、拒绝日志展示；
5. 玩家国家级命令入口的 UI 壳；
6. UI smoke test 或手动验收步骤。

请先阅读这些文档：
- `md/phase0_v0_minimum_scope.md`
- `md/phase3_v0_engineering_architecture.md`
- `md/phase5_long_term_roadmap.md`
- `md/阶段性4:第一版可玩测试板任务拆解/README.md`

并依赖 Agent 1-5 的交付：
- `GameAgent`
- `AgentRole`
- `AgentAuthorityLevel`
- `AgentPersonality`
- `AgentRelationship`
- `AgentMemory`
- `AgentContextEnvelope`
- `MinisterDecisionEnvelope`
- `RulerDecisionEnvelope`
- `StrategicDirective`
- `DirectiveBoard`
- `DirectiveConflict`
- `DirectiveArbitrationRecord`
- `TheaterDirective`
- `CommanderDirective`
- `PlayerNationalOrder`
- `PlayerOrderParseResult`
- `PlayerRoleState`
- directive/ruler/minister 日志类型

如果项目代码已经存在，先检查现有：
- `RootGameView`
- `HUDView`
- `AgentPanelView`
- `EventLogView`
- `CommandPanelView`
- `GameEvent`
- `ObservableObject` / state store
- SwiftUI 主题和布局风格

沿用现有 UI 风格，不要重写整个 App。不要让 UI 直接修改 `GameState`，只调用已有 service 或提交 intent。

核心设计要求：

一、UI 总体结构

新增或扩展以下 SwiftUI 视图：

```swift
struct CabinetView: View
struct AgentCouncilView: View
struct DirectiveBoardView: View
struct AgentDecisionLogView: View
struct PlayerNationalOrderView: View
struct DirectiveDetailView: View
如果现有 AgentPanelView 已存在，优先扩展它，而不是新增重复入口。
建议布局：
iPad / 宽屏：地图左侧或中央，右侧为 agent/cabinet 面板；
iPhone：使用 bottom sheet、tab 或 segmented control 切换；
v0.5 只要求原型可用，不追求最终美术。
二、CabinetView
展示国家内阁成员。
每个 agent 显示：
姓名；
阵营；
角色；
权限层级；
忠诚度；
政治可靠性；
当前作用范围；
最近一条建议或决策摘要。
建议结构：
struct CabinetViewModel: Equatable {
    var agents: [GameAgent]
    var selectedAgentId: String?
    var recentDecisionSummaries: [String: String]
}
交互：
点击 agent，打开详情；
详情显示人格、关系、记忆、权限；
不允许在 UI 里直接编辑权限或状态，除非已有 debug 模式。
三、AgentCouncilView
展示部长会议和统治者决策。
显示：
本回合部长建议列表；
统治者综合结论；
每条建议的来源 agent；
建议领域、优先级、目标、tags；
是否已提交到 DirectiveBoard；
原始 JSON 查看按钮或展开区。
要求：
MockAI 和未来 LLM 输出都能显示；
JSON 使用等宽字体、可复制或可滚动；
JSON 解析失败时显示错误原因。
四、DirectiveBoardView
展示战略指令板。
每条 directive 显示：
标题；
状态；
优先级；
领域；
来源 agent；
目标 scope；
创建回合；
过期回合；
简短理由；
是否存在冲突。
支持筛选：
proposed；
approved；
active；
rejected；
expired；
by domain；
by priority。
不要求复杂搜索，但筛选要能处理基本验收。
五、DirectiveDetailView
点击某条 directive 后展示：
完整 summary；
rationale；
expectedEffect；
riskAssessment；
sourceDecisionId；
arbitration 记录；
conflictingDirectiveIds；
下发生成的 TheaterDirective / CommanderDirective；
相关日志。
关键说明：
明确显示“战略指令不是单位命令”；
如果它尚未下发，显示 pending；
如果 v0.3 前线系统未实现，显示“计划已记录，尚未驱动前线自动执行”。
六、AgentDecisionLogView
扩展现有日志视图，支持多 agent 类型。
日志类别：
agent context generated；
minister decision generated；
player national order submitted；
player order parsed；
ruler decision generated；
directive submitted；
directive approved；
directive rejected；
directive conflict detected；
directive dispatched；
directive expired；
JSON parse error；
permission denied。
每条日志显示：
turn；
agent；
类型；
message；
severity；
可选 rawJSON；
可选 directiveId。
建议定义 UI VM：
struct AgentDecisionLogRowViewModel: Identifiable, Equatable {
    let id: String
    var turn: Int
    var title: String
    var subtitle: String
    var severity: LogSeverity
    var rawJSON: String?
    var relatedDirectiveId: String?
}
七、PlayerNationalOrderView
提供玩家作为统治者的国家级命令入口 UI 壳。
显示：
当前玩家角色模式；
当前控制 agent；
文本输入框；
priority hint 选择；
target scope 简化选择；
submit 按钮；
解析结果 warnings；
生成的 proposed directive 列表。
约束：
UI 不直接生成或修改 directive；
点击 submit 调用 RulerCommandService 或上层 intent；
如果服务未接好，提供 disabled 状态或 mock preview 数据；
不实现真实聊天模型调用。
八、状态管理
请不要让 SwiftUI 视图直接持有复杂游戏状态并到处修改。
推荐新增 view model：
final class AgentCouncilViewModel: ObservableObject {
    @Published var cabinet: [GameAgent]
    @Published var directiveBoard: DirectiveBoard
    @Published var ministerDecisions: [MinisterDecisionEnvelope]
    @Published var rulerDecisions: [RulerDecisionEnvelope]
    @Published var logs: [AgentDecisionLogRowViewModel]
}
如果项目已有全局 store，例如 GameStore、AppState、GameViewModel，应接入现有 store。
要求：
UI 层只读展示模型；
提交玩家命令时走 service；
不绕过 DirectiveBoardManager；
不直接移动单位或改地图。
九、视觉和交互要求
遵守项目现有风格。若没有现有风格，使用简洁工具型界面：
tabs 或 segmented control 区分 Cabinet / Directives / Logs / Order；
状态使用小型标签或颜色点；
priority 使用清晰颜色，但不要过度装饰；
JSON 用可滚动代码块；
长文本可折叠；
iPhone 上避免横向溢出；
iPad 上利用侧栏展示详情。
不要做营销式 landing page。直接做工具界面。
十、Preview / Mock Data
如果真实后端数据尚未全部接好，提供 SwiftUI Preview/mock data。
Mock 数据至少包含：
德国统治者；
4 个部长；
1 个元帅；
古德里安将领；
3 条 proposed directive；
1 条 approved directive；
1 条 rejected directive；
1 条 conflict；
1 条 minister JSON；
1 条 ruler JSON。
Mock 数据只用于 preview 或 debug，不要污染正式规则逻辑。
十一、测试与验收
基础验收至少覆盖：
内阁面板能显示统治者、4 个部长、元帅、将领；
点击 agent 能看到角色、权限、人格、记忆摘要；
部长会议建议能显示来源、领域、优先级和 JSON；
DirectiveBoard 能显示 proposed、approved、rejected、active、expired；
冲突 directive 能显示冲突原因；
指令详情能显示仲裁记录和下发结果；
玩家国家级命令输入框能提交到 service 或展示 disabled/mock 状态；
解析 warning 能显示；
JSON 解析失败日志能显示；
iPhone 宽度下文本不溢出；
UI 不直接调用 RuleEngine 移动单位；
关闭真实 LLM 时 UI 仍能展示 MockAI 决策。
如果项目已有 UI test 框架，可写 smoke test。否则提供手动验收步骤。
十二、明确不要做
本 agent 不负责：
新增 agent 数据模型；
构建 context；
实现部长 MockAI；
实现统治者 Mock 决策；
实现 directive 仲裁逻辑；
真实 LLM 调用；
真实经济、科技、外交规则；
前线自动执行；
单位移动、攻击、撤退；
美术资源大规模制作；
内购。
十三、交付标准
完成后请输出：
新增或修改的 UI 文件清单；
每个视图展示的数据来源；
玩家命令提交路径；
Directive/log 展示规则；
Preview/mock data 说明；
UI 验收步骤或测试结果；
明确说明没有绕过 DirectiveBoard / Command / RuleEngine。
验收标准：
玩家能在 UI 中看见内阁成员、部长建议、统治者决策、战略指令、仲裁结果和日志；
玩家能提交国家级命令入口；
所有展示来自 v0.5 数据管线；
UI 不直接修改核心游戏状态；
没有真实 LLM 时仍能通过 Mock 数据和 MockAI 跑通。

已完成。