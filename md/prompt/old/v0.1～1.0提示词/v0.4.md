先继续给我 v0.4 的执行方案，分几个 agent，分别干什么，先写个大概，一会再写提示词




星期三17:34






已处理 1m 32s



可以。v0.4 我建议先粗分 6 个 agent，核心目标是把“聊天命令 -> 结构化指令 -> 将领态度判断 -> 可执行计划/拒绝/延迟 -> 日志/UI反馈”这条链路跑通。
前提假设：v0.3 已经有前线系统、集团军/将领绑定、前线自动排兵或至少有前线对象雏形。
Agent 1：v0.4 系统设计 Agent
负责把 v0.4 的边界钉死，避免直接膨胀成完整政治模拟。
它要产出：
PlayerDirective 数据结构草案；
聊天命令类型分类；
将领服从/质疑/抗命的最小规则；
玩家微操如何影响将领满意度；
v0.4 明确不做什么。
重点：v0.4 不是完整政变系统，只做“将领会回应、会评估命令、可能不完全照做”的第一版。
Agent 2：数据模型 Agent
负责扩展现有 agent/command/game state 模型。
它要处理：
GeneralAgent 增加忠诚度、服从度、野心、风险偏好、性格标签；
新增 PlayerDirective；
新增 DirectiveStatus，例如 pending / accepted / questioned / delayed / refused / executed；
新增 AgentRelationshipState；
新增聊天记录和命令来源记录；
保证所有新增模型 Codable，方便日志和回放。
重点：只建数据结构和状态流，不写复杂 UI，不直接接 LLM。
Agent 3：聊天解析 Agent
负责把玩家输入的自然语言转成结构化意图。
它要处理：
聊天输入；
目标将领识别，例如“古德里安”；
任务类型识别，例如突破、防守、撤退、夺取城市、切断补给、重整；
目标地点识别，例如“巴斯托涅”“阿登方向”；
时间/优先级识别，例如“三回合内”“优先”；
输出 PlayerDirective。
v0.4 可以先用规则解析 + Mock 解析，不必一开始依赖 LLM。真实 LLM 解析可以作为可选 provider。
Agent 4：角色服从与态度 Agent
负责判断将领对玩家命令的反应。
它要处理：
命令是否符合将领性格；
命令是否符合当前战场现实；
命令风险是否过高；
将领忠诚度和满意度如何变化；
是否接受、质疑、延迟、部分执行或拒绝；
生成自然语言回应和结构化结果。
重点：抗命不能直接破坏规则系统，只影响“生成什么计划、是否执行、是否降低效率、是否产生政治/关系后果”。
Agent 5：计划转换 Agent
负责把被接受的 PlayerDirective 转换成现有 AI/前线系统能执行的计划。
它要处理：
directive -> AgentDecisionEnvelope；
directive -> 前线目标；
directive -> 攻势方向；
directive -> 若干合法 Command；
非法或不可达命令的降级策略；
与 v0.3 前线自动排兵衔接。
重点：LLM 或将领只输出计划，具体移动和攻击仍走 RuleEngine 校验。
Agent 6：UI 与日志 Agent
负责玩家能看见、能调试、能理解 agent 为什么这么做。
它要处理：
聊天框 UI；
将领回复区；
当前命令状态；
将领忠诚/满意度/风险态度显示；
“原始玩家命令 -> 解析结果 -> 将领回应 -> 执行命令 -> 结果日志”的链路展示；
调试面板显示 JSON。
重点：v0.4 的可玩性很大一部分来自“我下令了，将领怎么理解、为什么质疑、最后执行了什么”。
推荐执行顺序
Agent 1 先出 v0.4 小规格。
Agent 2 做数据模型。
Agent 3 和 Agent 4 并行：一个做聊天解析，一个做服从判断。
Agent 5 接到 v0.3 前线/命令系统上。
Agent 6 最后做 UI 和日志。
最后安排一个 Integration Agent，只负责整合、跑测试、修接口，不新增功能。
最小闭环
v0.4 第一版只要能做到这个就算成功：
玩家输入：
“古德里安，集中装甲部队突破阿登，优先夺取巴斯托涅。”

系统解析：
targetAgent = guderian
intent = breakthrough
objective = Bastogne
priority = high

将领判断：
接受，但提示补给风险较高。

系统执行：
生成前线/攻势计划 JSON
转换成若干合法 Command
RuleEngine 校验并执行
UI 显示回应、JSON、执行结果
这个拆法比较稳：聊天、角色、命令、UI 各自边界清楚，不会让某个 agent 一口气把“政治模拟 + LLM + UI + 战斗规则”全塞在一起。

先写agent1的提示词




星期三17:38






下面是 Agent 1：v0.4 系统设计 Agent 的提示词，可直接复制给另一个 agent。
你是 Agent 1：v0.4 系统设计 Agent。

项目背景：
我们正在制作一款 iOS 二战大战略/回合制战棋游戏。v0 是六角格测试板，长期目标接近《钢铁雄心4》式省份图、前线系统、多级指挥链和本地 LLM 驱动的角色 agent。当前要设计 v0.4：聊天命令与角色服从。

请先阅读这些文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

你的任务：
为 v0.4 写一份系统设计文档，重点不是实现代码，而是把 v0.4 的产品边界、系统流程、数据结构、规则约束和后续 agent 分工需要的接口定义清楚。

v0.4 目标：
玩家可以通过聊天框向将领下达自然语言命令，例如：
“古德里安，集中装甲部队突破阿登，优先夺取巴斯托涅。”
系统把这句话转成结构化 PlayerDirective。将领 agent 根据性格、忠诚度、满意度、风险偏好和战场现实，决定接受、质疑、延迟、部分执行或拒绝。被接受的指令再转换为计划 JSON，并最终进入既有 Command / RuleEngine 管线校验执行。

必须遵守的原则：
1. v0.4 不做完整政治模拟。
2. v0.4 不做真正政变、派系斗争、内阁系统，这些留给 v0.5 以后。
3. 抗命不能直接修改 GameState 或绕过规则系统。
4. 将领自然语言回复和实际可执行 JSON 必须分离。
5. 玩家聊天命令不能直接变成底层移动/攻击命令，必须先变成 PlayerDirective。
6. 所有实际执行仍走 CommandValidator 和 RuleEngine。
7. LLM 可以作为未来 provider，但 v0.4 第一版必须能用 Mock/规则解析跑通。

请输出一份 Markdown 文档，建议文件名：
md/phase_v0_4_chat_directive_system_design.md

文档必须包含以下部分：

# v0.4：聊天命令与角色服从系统设计

## 1. v0.4 版本目标
说明这一版要验证什么，玩家能做什么，将领 agent 能做什么。

## 2. 明确范围
分成：
- v0.4 必须做
- v0.4 可选做
- v0.4 明确不做

必须明确：
- 不做完整政变系统；
- 不做完整国家/部长 agent；
- 不做复杂长期记忆；
- 不要求真实 LLM 必须接入；
- 不允许聊天命令绕过规则。

## 3. 核心用户流程
用流程图或编号说明：

玩家输入聊天命令
-> 识别目标将领
-> 解析为 PlayerDirective
-> 将领进行服从/风险判断
-> 生成自然语言回应
-> 生成结构化计划
-> 转换为 AgentDecisionEnvelope 或 Command
-> RuleEngine 校验执行
-> UI 显示执行结果和日志

## 4. PlayerDirective 草案
设计结构体字段，至少包含：
- id
- issuedBy
- targetAgentId
- rawText
- intentType
- objective
- targetRegionOrProvince
- targetDivisionIds
- priority
- timeLimitTurns
- riskTolerance
- allowAutonomy
- createdTurn
- status

intentType 至少包含：
- breakthrough
- defend
- retreat
- regroup
- captureObjective
- cutSupply
- supportFront
- holdPosition

status 至少包含：
- pending
- parsed
- accepted
- questioned
- delayed
- partiallyAccepted
- refused
- convertedToPlan
- executed
- failed

## 5. 将领角色服从模型
设计最小字段，不要过度复杂。至少包括：
- loyalty
- satisfaction
- trustInPlayer
- ambition
- riskPreference
- autonomyPreference
- discipline
- personalityTags

说明每个字段如何影响：
- 接受命令概率或评分；
- 是否质疑；
- 是否延迟；
- 是否部分执行；
- 是否拒绝；
- 玩家频繁微操造成的关系变化。

要求使用确定性评分模型优先，不要一开始依赖随机数或 LLM 自由发挥。

## 6. 命令评估规则
设计一个 DirectiveEvaluator。

输入：
- PlayerDirective
- GeneralAgent
- AgentContext
- 当前战况摘要
- 前线状态
- 补给状态
- 可用部队状态

输出：
- DirectiveEvaluation

DirectiveEvaluation 至少包含：
- decision
- confidence
- riskScore
- feasibilityScore
- obedienceScore
- concerns
- suggestedRevision
- relationshipDelta
- naturalLanguageResponse
- structuredPlanIntent

说明每个评分如何大致计算即可，不需要完整公式，但要足够让后续 agent 实现。

## 7. 与既有 AI/Command 管线衔接
说明：
- PlayerDirective 如何进入 AgentContext；
- DirectiveEvaluation 如何影响 AgentDecisionEnvelope；
- 被接受的命令如何转换成现有 move / attack / hold / resupply；
- 被拒绝或延迟的命令如何写入日志；
- 非法计划如何 fallback；
- 为什么不能直接从聊天生成 Command。

## 8. UI 与日志需求
定义 v0.4 需要显示什么：
- 聊天输入框；
- 将领回复；
- 指令解析结果；
- 指令状态；
- 将领关系变化；
- 计划 JSON；
- RuleEngine 执行结果；
- 失败原因。

## 9. 测试与验收标准
至少列出以下测试场景：
1. 玩家给古德里安下达突破巴斯托涅命令，将领接受并生成计划。
2. 玩家命令低组织度部队强攻要塞，将领质疑或建议延迟。
3. 玩家命令无补给部队继续推进，将领建议重整或撤退。
4. 玩家频繁手动覆盖将领计划，将领满意度下降。
5. 玩家输入无法解析的命令，系统不崩溃，返回澄清或失败日志。
6. 将领拒绝命令时，GameState 不应被非法修改。
7. 计划生成后，所有实际命令仍必须通过 CommandValidator。

## 10. 给后续 Agent 的接口约束
明确给后续 Agent 2-6 的边界：
- Agent 2 根据本文档建数据模型；
- Agent 3 做聊天解析；
- Agent 4 做服从判断；
- Agent 5 做计划转换；
- Agent 6 做 UI 和日志；
- 集成 agent 只负责接线和测试。

输出要求：
- 使用中文 Markdown。
- 内容要具体、可实现，不写空泛设想。
- 不要开始写 Swift 代码文件。
- 如果需要伪代码，可以写在文档里。
- 优先保持 v0.4 小而完整，不要扩大到 v0.5。

星期三17:39






写agent2的提示词




星期三17:40






下面是 Agent 2：v0.4 数据模型 Agent 的提示词，可直接复制给另一个 agent。
你是 Agent 2：v0.4 数据模型 Agent。

项目背景：
我们正在制作一款 iOS 二战大战略/回合制战棋游戏。v0 使用 Swift + SwiftUI + SpriteKit，核心原则是：玩家和 AI 都只提交 Command，所有实际状态变化必须经过 CommandValidator 和 RuleEngine。v0.4 的目标是加入“聊天命令与角色服从”系统。

请先阅读这些文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/phase_v0_4_chat_directive_system_design.md

你的任务：
基于 Agent 1 的 v0.4 系统设计文档，设计并落地 v0.4 所需的数据模型和状态结构。重点是模型、枚举、Codable、状态流和与现有 GameState / GeneralAgent / AgentContext / GameEvent 的衔接。不要实现聊天解析、服从判断算法、UI 或 LLM 调用，这些由后续 agent 负责。

必须遵守的原则：
1. 不允许聊天命令直接修改 GameState。
2. 不允许 PlayerDirective 直接绕过 CommandValidator。
3. 所有新增模型必须尽量 Codable、Equatable。
4. v0.4 第一版必须能在无真实 LLM 的情况下运行。
5. 自然语言回复、结构化指令、可执行 Command 必须分离。
6. 抗命/延迟/拒绝只表现为 directive 状态和日志，不直接破坏规则系统。
7. 不要引入完整政治系统、派系系统、部长系统或政变系统。

请完成以下工作：

## 1. 阅读现有工程结构

如果项目代码已经存在：
- 找到 `GameState`
- 找到 `GeneralAgent`
- 找到 `AgentContext`
- 找到 `AgentDecisionEnvelope`
- 找到 `GameEvent`
- 找到 `Command` / `CommandRecord`
- 找到相关 Tests

如果项目代码尚未存在：
- 根据阶段 3 架构文档设计目标数据模型；
- 不要强行创建完整 App；
- 只输出模型设计文档或最小模型文件草案，按当前仓库状态决定。

## 2. 新增 PlayerDirective 模型

设计 `PlayerDirective`，字段至少包含：

- `id: String`
- `issuedBy: DirectiveIssuer`
- `targetAgentId: String`
- `rawText: String`
- `intentType: DirectiveIntentType`
- `objective: String?`
- `targetRegionId: String?`
- `targetProvinceId: String?`
- `targetHex: HexCoord?`
- `targetDivisionIds: [String]`
- `priority: DirectivePriority`
- `timeLimitTurns: Int?`
- `riskTolerance: DirectiveRiskTolerance`
- `allowAutonomy: Bool`
- `createdTurn: Int`
- `status: DirectiveStatus`
- `parseConfidence: Double?`
- `evaluationId: String?`
- `resultSummary: String?`

设计相关枚举：

`DirectiveIssuer`
- player
- system
- agent(agentId: String)

`DirectiveIntentType`
- breakthrough
- defend
- retreat
- regroup
- captureObjective
- cutSupply
- supportFront
- holdPosition
- unknown

`DirectivePriority`
- low
- normal
- high
- urgent

`DirectiveRiskTolerance`
- cautious
- balanced
- aggressive

`DirectiveStatus`
- pending
- parsed
- accepted
- questioned
- delayed
- partiallyAccepted
- refused
- convertedToPlan
- executed
- failed

要求：
- 字段命名保持 Swift 风格；
- 如果 enum 带 associated value 导致 Codable 麻烦，可以设计为 struct 或字符串字段，但要说明取舍；
- 保证后续能记录、回放、展示。

## 3. 扩展 GeneralAgent 角色字段

在 `GeneralAgent` 或独立关系模型中加入最小角色服从字段：

- `loyalty: Int`
- `satisfaction: Int`
- `trustInPlayer: Int`
- `ambition: Int`
- `riskPreference: Int`
- `autonomyPreference: Int`
- `discipline: Int`
- `personalityTags: [String]`

约束：
- 数值范围统一使用 `0...100`。
- 默认值必须明确。
- 不要在模型层实现复杂评分算法。
- 如果现有 `GeneralAgent` 不适合直接扩展，可以新增 `AgentProfile` / `AgentRelationshipState`，并说明原因。

## 4. 新增 DirectiveEvaluation 模型

设计 `DirectiveEvaluation`，字段至少包含：

- `id: String`
- `directiveId: String`
- `agentId: String`
- `decision: DirectiveDecision`
- `confidence: Double`
- `riskScore: Int`
- `feasibilityScore: Int`
- `obedienceScore: Int`
- `concerns: [String]`
- `suggestedRevision: String?`
- `relationshipDelta: AgentRelationshipDelta`
- `naturalLanguageResponse: String`
- `structuredPlanIntent: PlanIntent?`
- `createdTurn: Int`

设计相关类型：

`DirectiveDecision`
- accept
- question
- delay
- partiallyAccept
- refuse

`AgentRelationshipDelta`
- loyaltyDelta
- satisfactionDelta
- trustDelta

`PlanIntent`
- id
- directiveId
- agentId
- planType
- objective
- targetRegionId
- targetProvinceId
- targetHex
- assignedDivisionIds
- priority
- riskTolerance
- createdTurn

`PlanType`
- breakthrough
- defend
- retreat
- regroup
- captureObjective
- cutSupply
- supportFront
- holdPosition

要求：
- `PlanIntent` 不是 Command；
- `PlanIntent` 只描述计划意图；
- 后续 Agent 5 才负责把它转换成 `AgentDecisionEnvelope` 或具体 `Command`；
- `DirectiveEvaluation` 必须可记录进日志或 debug panel。

## 5. 扩展 AgentContext

让将领评估聊天命令时能看到必要信息。

在 `AgentContext` 中加入或预留：
- `activeDirectives: [PlayerDirective]`
- `recentDirectiveEvaluations: [DirectiveEvaluation]`
- `relationshipState: AgentRelationshipState?`
- `playerMicroManagementCount: Int`

要求：
- 不要让 AgentContext 泄露战争迷雾外的敌军真实信息；
- 如果当前 v0 没有战争迷雾，也要在注释或文档中保留这个约束；
- 不要把完整聊天历史无限塞进 context，只保留最近 N 条或摘要字段。

## 6. 扩展 GameState

在 `GameState` 中加入或预留：
- `playerDirectives: [PlayerDirective]`
- `directiveEvaluations: [DirectiveEvaluation]`
- `agentRelationshipStates: [AgentRelationshipState]`

如果现有 GameState 还不存在，请在设计文档中说明应加到哪里。

要求：
- 不改变现有 Command 执行原则；
- 不让 directive 状态替代 commandHistory；
- directive 是高层意图记录，commandHistory 是实际执行记录。

## 7. 扩展 GameEvent / 日志类型

新增或预留事件：
- playerDirectiveCreated
- playerDirectiveParsed
- directiveEvaluated
- directiveAccepted
- directiveQuestioned
- directiveDelayed
- directivePartiallyAccepted
- directiveRefused
- directiveConvertedToPlan
- directiveExecutionFailed
- agentRelationshipChanged
- playerMicroManagedAgent

每个事件至少能关联：
- turn
- directiveId
- agentId
- summary
- optional JSON/debug payload

## 8. 数据兼容与默认值

请定义：
- 新字段的默认值；
- 旧存档或旧 scenario 没有这些字段时如何加载；
- `GeneralAgent.guderian` 的 v0.4 默认角色参数。

建议古德里安默认：
- loyalty: 70
- satisfaction: 65
- trustInPlayer: 60
- ambition: 55
- riskPreference: 85
- autonomyPreference: 75
- discipline: 80
- personalityTags: ["breakthrough", "armor", "independent", "aggressive"]

## 9. 测试要求

新增或设计单元测试，至少覆盖：
1. `PlayerDirective` 可以 Codable encode/decode。
2. `DirectiveEvaluation` 可以 Codable encode/decode。
3. `AgentRelationshipState` 默认值正确。
4. `GameState` 可以保存多个 directive，不影响 commandHistory。
5. `PlanIntent` 不会被当作 Command 直接执行。
6. 缺少 v0.4 字段的旧数据可以加载或有明确迁移策略。
7. `AgentContext` 不包含无限聊天历史。

## 10. 输出要求

如果代码工程已存在：
- 实现必要 Swift 模型；
- 添加或更新单元测试；
- 不写 UI；
- 不写聊天解析；
- 不写服从评分算法；
- 不写 LLM 调用；
- 不重构无关文件。

如果代码工程尚未存在：
- 在 `md/` 下写一份数据模型设计文档；
- 建议文件名：`md/phase_v0_4_data_model_design.md`；
- 文档必须包含 Swift 风格结构体/枚举草案；
- 明确后续 Agent 3、4、5、6 如何使用这些模型。

最终回答请说明：
- 新增或修改了哪些文件；
- 哪些模型已定义；
- 测试是否运行；
- 如果没法实现代码，说明原因并给出设计文档位置。

星期三17:41






写agent3的提示词




星期三17:41






下面是 Agent 3：v0.4 聊天解析 Agent 的提示词，可直接复制给另一个 agent。
你是 Agent 3：v0.4 聊天解析 Agent。

项目背景：
我们正在制作一款 iOS 二战大战略/回合制战棋游戏。v0.4 要实现“玩家通过聊天框向将领下达自然语言命令”。你的职责只覆盖“聊天文本 -> 结构化 PlayerDirective”的解析链路。

请先阅读这些文档：
- md/phase0_v0_minimum_scope.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/phase_v0_4_chat_directive_system_design.md
- md/phase_v0_4_data_model_design.md
如果代码工程已经存在，也请阅读现有：
- GameState
- GeneralAgent / AgentProfile / AgentRelationshipState
- AgentContext
- PlayerDirective
- DirectiveIntentType
- DirectiveStatus
- GameEvent

你的任务：
实现或设计 v0.4 的聊天解析系统，把玩家输入的自然语言命令解析为 `PlayerDirective`。不要实现服从判断、计划转换、UI 或 LLM 决策。你的输出必须能被 Agent 4 的服从判断系统继续处理。

核心目标：
玩家输入：
“古德里安，集中装甲部队突破阿登，优先夺取巴斯托涅。”

解析输出：
- targetAgentId: guderian
- rawText: 原始文本
- intentType: breakthrough 或 captureObjective
- objective: Bastogne
- targetRegionId 或 targetProvinceId: 可识别则填
- priority: high
- riskTolerance: aggressive 或 balanced
- allowAutonomy: true
- status: parsed
- parseConfidence: 合理分数

必须遵守的原则：
1. 解析器只生成 `PlayerDirective`，不能生成 `Command`。
2. 解析器不能修改单位位置、地图控制权或战斗状态。
3. 解析器不能调用 `RuleEngine` 执行命令。
4. 解析失败时必须返回明确失败结果或 `unknown` intent，不得崩溃。
5. v0.4 第一版必须支持 Mock/规则解析，不强依赖真实 LLM。
6. 真实 LLM 解析可以预留接口，但默认关闭。
7. 解析结果必须保留原始文本，方便日志和调试。

请完成以下工作：

## 1. 设计解析模块边界

推荐模块：
- `DirectiveParser`
- `RuleBasedDirectiveParser`
- `LLMDirectiveParser` placeholder
- `DirectiveParseResult`
- `DirectiveParseError`
- `DirectiveParsingContext`

如果项目代码已存在，请按现有目录风格放置。不要大规模重构。

建议协议：

```swift
protocol DirectiveParser {
    func parse(_ text: String, context: DirectiveParsingContext) -> DirectiveParseResult
}
DirectiveParsingContext 至少应包含：
currentTurn
availableAgents
knownRegions / knownProvinces / knownObjectives
selectedAgentId
selectedDivisionIds
defaultFaction
recentDirectives
2. 设计 DirectiveParseResult
至少包含：
directive: PlayerDirective?
confidence: Double
warnings: [String]
errors: [DirectiveParseError]
normalizedText: String
matchedAgentName: String?
matchedObjectiveName: String?
解析成功：
返回 PlayerDirective
status 为 parsed
parseConfidence 写入结果
解析失败：
directive 可以为 nil，或生成 intentType = unknown 的 directive
必须说明失败原因
不允许崩溃
3. 支持目标将领识别
规则：
支持中文名和英文名/别名。
示例：“古德里安”
“Guderian”
“Heinz Guderian”

如果文本未写将领，但当前 UI 有 selectedAgentId，则使用 selectedAgentId。
如果没有明确目标，也没有 selectedAgentId，则返回需要澄清的错误。
要求：
识别结果填入 targetAgentId
matchedAgentName 用于 debug
不能凭空创建不存在的 agent
4. 支持意图识别
至少支持以下中文关键词：
breakthrough
“突破”
“穿插”
“突击”
“装甲突破”
“撕开防线”
defend
“防守”
“守住”
“防御”
“固守”
“守卫”
retreat
“撤退”
“后撤”
“撤到”
“脱离接触”
regroup
“重整”
“休整”
“补员”
“恢复组织度”
“重新集结”
captureObjective
“夺取”
“占领”
“拿下”
“攻占”
cutSupply
“切断补给”
“断补给”
“包围”
“切断退路”
supportFront
“支援”
“增援”
“填补缺口”
“稳住战线”
holdPosition
“原地待命”
“保持阵地”
“不要移动”
“等待命令”
如果多个意图同时出现：
优先识别更具体的意图；
“突破 + 占领目标”可以主 intent 为 breakthrough，objective 填目标；
“切断补给 + 包围”主 intent 为 cutSupply；
无法判断时使用 unknown 并给 warning。
5. 支持目标地点/目标对象识别
解析文本中的：
城市名；
目标点；
区域；
省份；
hex 坐标，可选；
已选部队，可选。
v0.4 示例目标：
阿登
巴斯托涅
Bastogne
St. Vith
Houffalize
要求：
如果能匹配 knownObjectives，填 objective
如果能匹配 region/province，填 targetRegionId 或 targetProvinceId
如果只能识别自然语言地名但没有数据 id，保留 objective 字符串并加 warning
不要因为地点不精确就崩溃
6. 支持优先级、时间和风险解析
优先级：
“优先”“立刻”“马上”“必须” -> high 或 urgent
“有机会”“可以的话” -> low
默认 normal
时间限制：
“三回合内”“3回合内”“两回合内”
解析到则填 timeLimitTurns
无法解析则 nil
风险偏好：
“不惜代价”“全力”“强攻” -> aggressive
“谨慎”“避免损失”“保存实力” -> cautious
默认 balanced
自主权：
“你自行决定”“按你判断”“自由发挥” -> allowAutonomy true
“严格执行”“不要擅自改变” -> allowAutonomy false
默认 true
7. 日志与错误处理
解析器需要产生可记录日志的信息：
原始文本
规范化文本
目标将领
intent
objective
confidence
warnings
errors
预留 GameEvent：
playerDirectiveParsed
directiveParseFailed
解析失败场景：
空文本
找不到目标将领
找不到可识别意图
地点无法匹配
文本过长
包含多个互相冲突命令
8. 测试要求
至少覆盖这些测试：
“古德里安，突破阿登，夺取巴斯托涅”
agent = guderian
intent = breakthrough
objective = Bastogne 或 巴斯托涅
status = parsed

“古德里安，守住巴斯托涅”
intent = defend
objective = Bastogne

“古德里安，切断盟军补给线”
intent = cutSupply

“古德里安，让部队休整补员”
intent = regroup

“立刻强攻巴斯托涅，不惜代价”
priority = urgent 或 high
riskTolerance = aggressive

未写将领但 context.selectedAgentId = guderian
targetAgentId = guderian

未写将领且没有 selectedAgentId
返回错误，不崩溃

空字符串
返回错误，不崩溃

无法识别意图
intent = unknown 或 directive nil
有 warning/error

“三回合内夺取巴斯托涅”

timeLimitTurns = 3
9. 输出要求
如果代码工程已存在：
实现解析模块；
添加单元测试；
不写 UI；
不写服从判断；
不写计划转换；
不调用真实 LLM；
不修改 RuleEngine 执行逻辑；
不重构无关模块。
如果代码工程尚未存在：
在 md/ 下写一份聊天解析设计文档；
建议文件名：md/phase_v0_4_directive_parser_design.md；
文档必须包含 Swift 风格协议、结构体、枚举草案；
明确 Agent 4 如何消费 DirectiveParseResult 和 PlayerDirective。
最终回答请说明：
新增或修改了哪些文件；
解析器支持哪些意图；
测试是否运行；
如果没有代码工程，说明设计文档位置。


星期三17:42






写agent4的提示词

下面是 Agent 4：v0.4 角色服从与态度 Agent 的提示词，可直接复制。
你是 Agent 4：v0.4 角色服从与态度 Agent。

项目背景：
我们正在制作一款 iOS 二战大战略/回合制战棋游戏。v0.4 要实现“玩家通过聊天框命令将领，将领根据性格、忠诚、风险偏好和战场现实决定接受、质疑、延迟、部分执行或拒绝”。

你的职责只覆盖：
PlayerDirective + GeneralAgent/RelationshipState + 战场摘要
-> DirectiveEvaluation

你不负责聊天解析，不负责计划转换，不负责 UI，不负责 RuleEngine 执行。

请先阅读这些文档：
- md/phase0_v0_minimum_scope.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/phase_v0_4_chat_directive_system_design.md
- md/phase_v0_4_data_model_design.md
- md/phase_v0_4_directive_parser_design.md

如果代码工程已经存在，也请阅读：
- PlayerDirective
- DirectiveEvaluation
- AgentRelationshipState
- GeneralAgent / AgentProfile
- AgentContext
- PlanIntent
- GameEvent
- Command / AgentDecisionEnvelope，但不要修改执行管线

你的任务：
实现或设计 v0.4 的 `DirectiveEvaluator`，用于判断将领对玩家指令的态度和服从结果。

核心输入：
- `PlayerDirective`
- `GeneralAgent`
- `AgentRelationshipState`
- `AgentContext`
- 当前前线/补给/单位状态摘要

核心输出：
- `DirectiveEvaluation`

必须遵守的原则：
1. 不允许 evaluator 直接生成或执行 Command。
2. 不允许 evaluator 直接修改单位位置、地图控制权、hp、organization 或补给状态。
3. 不允许 evaluator 绕过 RuleEngine。
4. v0.4 第一版优先使用确定性评分模型，不依赖随机数。
5. 不强依赖真实 LLM。
6. 自然语言回应只是展示文本，不是可执行命令。
7. 抗命、延迟、质疑只改变 directive/evaluation 状态和日志，不直接破坏游戏规则。
8. 不实现完整政治系统、派系系统、政变系统或部长系统。

请完成以下工作：

## 1. 设计 DirectiveEvaluator 模块

推荐模块：
- `DirectiveEvaluator`
- `RuleBasedDirectiveEvaluator`
- `DirectiveEvaluationContext`
- `DirectiveScoringBreakdown`
- `DirectiveResponseBuilder`

建议协议：

```swift
protocol DirectiveEvaluator {
    func evaluate(
        directive: PlayerDirective,
        agent: GeneralAgent,
        context: AgentContext
    ) -> DirectiveEvaluation
}
DirectiveEvaluationContext 可以从 AgentContext 派生，至少包含：
assignedDivisions
visibleEnemyDivisions
knownObjectives
supplySummary
organizationSummary
frontLineSummary
recentEvents
playerMicroManagementCount
relationshipState
2. 实现确定性评分模型
必须输出这些分数：
obedienceScore: Int
feasibilityScore: Int
riskScore: Int
confidence: Double
分数范围：
obedienceScore: 0...100，越高越愿意服从
feasibilityScore: 0...100，越高越可执行
riskScore: 0...100，越高越危险
confidence: 0.0...1.0，越高表示 evaluator 越确定
建议评分逻辑：
obedienceScore
基础来自：
loyalty 35%
trustInPlayer 25%
discipline 20%
satisfaction 20%
修正：
如果 directive.allowAutonomy == true，且 agent.autonomyPreference 高，加分。
如果 directive.allowAutonomy == false，且 agent.autonomyPreference 高，减分。
玩家近期频繁微操，减分。
指令符合 personalityTags，加分。
指令明显违背将领风格，减分。
feasibilityScore
考虑：
目标是否存在或可识别；
是否有可用 assignedDivisions；
部队是否有足够 organization；
部队是否有补给；
目标是否在合理距离；
前线是否有缺口；
是否有可用道路或补给线；
是否有明显不可执行条件。
riskScore
考虑：
强攻城市/要塞；
低组织度进攻；
无补给推进；
被包围或接近包围；
目标距离过远；
敌军强度明显更高；
timeLimitTurns 过短；
riskTolerance 与实际风险不匹配。
confidence
考虑：
解析置信度；
目标是否明确；
战场信息是否充足；
是否有可用部队；
是否存在互相冲突的指令。
3. 决策规则
根据评分输出 DirectiveDecision：
accept
question
delay
partiallyAccept
refuse
建议阈值：
accept:
  obedienceScore >= 60
  feasibilityScore >= 55
  riskScore <= allowedRisk + 20

question:
  obedienceScore >= 45
  feasibilityScore >= 45
  riskScore 偏高，或目标/时间要求不清晰

delay:
  feasibilityScore 暂时不足，但通过休整/补给/等待可改善

partiallyAccept:
  总体目标合理，但时间、风险、具体路线或微操限制不合理

refuse:
  feasibilityScore < 25
  或 obedienceScore < 30
  或指令要求明显自毁、无目标、无可用部队、违反基本规则
allowedRisk 建议：
cautious: 35
balanced: 55
aggressive: 75
将领 riskPreference 可以调整 allowedRisk：
高 riskPreference 将领更能接受高风险进攻。
低 riskPreference 将领更容易质疑冒险命令。
4. concerns 与 suggestedRevision
根据评分生成 concerns，例如：
“目标不明确”
“可用部队不足”
“装甲师组织度过低”
“当前补给线不稳定”
“强攻要塞风险过高”
“时间限制过短”
“该命令限制了将领自主权”
suggestedRevision 示例：
“建议先休整一回合再进攻。”
“建议将突破目标改为道路节点，而不是直接强攻要塞。”
“建议允许将领自主选择进攻路线。”
“建议先切断补给，再攻击城市。”
5. naturalLanguageResponse
实现模板化回应，不依赖 LLM。
不同 decision 示例：
accept:
“我接受命令。装甲部队将沿道路集中突破，优先夺取巴斯托涅。”
question:
“我理解您的目标，但当前补给不足，直接进攻风险较高。我建议调整进攻节奏。”
delay:
“命令目标合理，但部队组织度不足。我建议先休整并恢复补给。”
partiallyAccept:
“我会执行夺取巴斯托涅的目标，但不会按原定路线强攻要塞。我将选择更可行的突破方向。”
refuse:
“我不能执行该命令。当前没有可用部队或行动条件不成立。”
要求：
回复要根据 concerns 和 agent personalityTags 轻微变化；
不要生成过长文本；
不要把自然语言回应当作可执行命令。
6. relationshipDelta
根据评估结果生成关系变化：
建议规则：
accept 且命令合理：trust +1，satisfaction +1
question：trust 0，satisfaction 0 或 -1
delay：satisfaction -1
partiallyAccept：trust -1 或 0
refuse：trust -2，satisfaction -2
玩家频繁微操：satisfaction 额外下降
允许自主权且命令符合将领风格：satisfaction 上升
约束：
loyalty 不应频繁变化，v0.4 默认很少改 loyalty。
所有关系值最终 clamp 到 0...100。
evaluator 只输出 delta，不直接写回 GameState；写回由集成层处理。
7. structuredPlanIntent
只有以下 decision 可以生成 PlanIntent：
accept
partiallyAccept
delay 可选生成“regroup/resupply”类型的 PlanIntent
规则：
PlanIntent 不是 Command。
PlanIntent 不允许直接执行。
PlanIntent 交给 Agent 5 转换为 AgentDecisionEnvelope 或具体计划。
refuse 和 question 通常不生成 PlanIntent，除非设计为建议方案。
PlanIntent 映射：
breakthrough -> breakthrough
defend -> defend
retreat -> retreat
regroup -> regroup
captureObjective -> captureObjective
cutSupply -> cutSupply
supportFront -> supportFront
holdPosition -> holdPosition
8. 日志事件
预留或生成以下事件所需信息：
directiveEvaluated
directiveAccepted
directiveQuestioned
directiveDelayed
directivePartiallyAccepted
directiveRefused
agentRelationshipChanged
每条日志至少能关联：
turn
directiveId
agentId
decision
summary
scores
concerns
relationshipDelta
9. 测试要求
至少覆盖以下测试：
高忠诚、高补给、目标明确的突破命令
decision = accept
生成 PlanIntent

低组织度部队强攻要塞
decision = question 或 delay
concerns 包含组织度/要塞/风险

无补给部队继续推进
decision = delay 或 partiallyAccept
suggestedRevision 建议休整/补给/撤退

将领 autonomyPreference 高，玩家禁止自主调整
obedienceScore 下降

命令符合古德里安 personalityTags：breakthrough / armor / aggressive
obedienceScore 或 confidence 上升

没有可用 assignedDivisions
decision = refuse
不生成 PlanIntent

玩家频繁微操
relationshipDelta.satisfactionDelta < 0

无法识别目标或 intent unknown
decision = question 或 refuse
不崩溃

refuse 不修改 GameState
只产生 evaluation，不产生 Command

所有 score 都 clamp 在合法范围内

10. 输出要求
如果代码工程已存在：
实现 evaluator 模块；
添加单元测试；
不写 UI；
不写聊天解析；
不写计划转换；
不调用真实 LLM；
不修改 RuleEngine 执行逻辑；
不重构无关文件。
如果代码工程尚未存在：
在 md/ 下写一份角色服从与态度设计文档；
建议文件名：md/phase_v0_4_directive_evaluator_design.md；
文档必须包含 Swift 风格协议、结构体、评分规则和测试场景；
明确 Agent 5 如何消费 DirectiveEvaluation 和 PlanIntent。
最终回答请说明：
新增或修改了哪些文件；
evaluator 支持哪些 decision；
评分规则是否确定性；
测试是否运行；
如果没有代码工程，说明设计文档位置。


星期三17:44






写agent5的提示词




星期三17:45






下面是 Agent 5：v0.4 计划转换 Agent 的提示词，可直接复制。
你是 Agent 5：v0.4 计划转换 Agent。

项目背景：
我们正在制作一款 iOS 二战大战略/回合制战棋游戏。v0.4 中，玩家聊天命令会先被解析成 `PlayerDirective`，再由角色服从系统生成 `DirectiveEvaluation`。你的职责是把被接受或部分接受的 `PlanIntent` 转换成现有 AI/命令系统可以处理的结构化计划。

你的职责只覆盖：
DirectiveEvaluation / PlanIntent
-> AgentDecisionEnvelope 或高层计划命令
-> CommandValidator / RuleEngine 可校验的候选 Command 列表

你不负责：
- 聊天解析；
- 角色服从评分；
- UI；
- 真实 LLM 调用；
- 修改战斗规则；
- 绕过 CommandValidator 执行命令。

请先阅读这些文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/phase_v0_4_chat_directive_system_design.md
- md/phase_v0_4_data_model_design.md
- md/phase_v0_4_directive_parser_design.md
- md/phase_v0_4_directive_evaluator_design.md

如果代码工程已经存在，也请阅读：
- PlayerDirective
- DirectiveEvaluation
- PlanIntent
- AgentDecisionEnvelope
- AgentOrder
- Command
- CommandValidator
- RuleEngine
- FrontLine / FrontLineAssignment，如果 v0.3 已存在
- MovementRules
- CombatRules
- SupplyRules

你的任务：
设计或实现 v0.4 的 `PlanIntentConverter`，把高层计划意图转换为规则系统可验证的结构化行动。

核心原则：
1. `PlanIntent` 不是 `Command`，不能直接执行。
2. 转换器只能生成候选 `AgentDecisionEnvelope` / `AgentOrder` / `Command`，不能直接修改 `GameState`。
3. 所有实际命令必须经过 `CommandValidator`。
4. 如果候选命令非法，必须跳过、降级或记录失败原因，不能崩溃。
5. LLM 不参与逐单位底层寻路。
6. 前线排兵、目标选择、路径选择优先使用确定性算法。
7. v0.4 第一版只支持陆军命令：move / attack / hold / resupply。
8. 不实现完整省份图、完整前线系统或复杂大战略，只适配已有 v0.3 能力。

请完成以下工作：

## 1. 设计 PlanIntentConverter 模块

推荐模块：
- `PlanIntentConverter`
- `RuleBasedPlanIntentConverter`
- `PlanConversionContext`
- `PlanConversionResult`
- `PlanConversionError`
- `CandidateCommand`
- `PlanExecutionSummary`

建议协议：

```swift
protocol PlanIntentConverter {
    func convert(
        evaluation: DirectiveEvaluation,
        context: PlanConversionContext
    ) -> PlanConversionResult
}
PlanConversionContext 至少包含：
gameState
agent
assignedDivisions
visibleEnemyDivisions
knownObjectives
frontLineState 可选
supplyState
movementRules
combatRules
commandValidator
2. 设计 PlanConversionResult
至少包含：
directiveId
evaluationId
agentId
planIntentId
decision
agentDecisionEnvelope: AgentDecisionEnvelope?
candidateCommands: [Command]
acceptedCommands: [Command]
rejectedCommands: [RejectedCommand]
warnings: [String]
summary: String
RejectedCommand 至少包含：
command
reason
validationErrors
要求：
acceptedCommands 只能放通过 CommandValidator 的命令；
candidateCommands 可以包含所有尝试生成的命令；
rejectedCommands 必须用于 debug 和 UI 日志；
如果没有任何合法命令，返回失败结果但不崩溃。
3. 支持 PlanType 到行动策略的映射
至少支持以下 PlanType：
breakthrough
目标：
集中可用装甲/摩托化单位向目标方向推进；
优先沿道路；
优先攻击挡路的弱敌；
组织度低或无补给单位不参加强攻，改为 resupply/hold。
转换策略：
从 assignedDivisions 选择装甲、摩托化、高组织度单位；
目标为 objective / targetRegion / targetHex；
若相邻敌军阻挡且可攻击，生成 attack；
否则生成 move，向目标靠近；
炮兵优先攻击目标附近敌军；
低组织度单位 hold 或 resupply。
defend
目标：
固守目标地区或前线；
优先把单位移动到城市、要塞、河流、山地等防御点；
已在防御位置的单位 hold。
retreat
目标：
将低组织度、无补给或处于包围风险的单位撤到安全格；
优先向己方补给源或后方控制区移动；
无法撤退则 hold/resupply 并记录 warning。
regroup
目标：
停止进攻，恢复组织度和补给；
大部分单位 resupply 或 hold；
若单位处于危险前线，可移动到后方安全格。
captureObjective
目标：
朝目标城市/要点推进；
相邻目标若无人防守则 move 占领；
有敌军则 attack；
无法到达则向目标靠近。
cutSupply
目标：
优先移动快速单位到敌方补给线、道路节点或后方连接点；
如果 v0/v0.3 没有完整补给网络，则使用道路节点/目标后方 hex 作为近似；
不允许让低组织度单位孤军深入。
supportFront
目标：
识别前线缺口；
将预备队移动到缺口附近；
如果没有前线系统，则移动到最近友军薄弱区域或目标附近。
holdPosition
目标：
指定单位 hold；
未指定单位时，agent assignedDivisions 全部 hold；
低补给单位可改为 resupply。
4. 目标选择与寻路约束
要求：
优先使用现有 MovementRules 和 CommandValidator；
不要自己复制一套独立规则；
如果需要寻路，只做“下一步移动”或短路径建议；
不能让单位穿过敌军；
不能生成超出行动点的 move；
不能攻击射程外目标；
不能控制未分配给该 agent 的单位；
不能读取战争迷雾外的敌军真实位置。
目标优先级建议：
明确 targetHex；
明确 targetProvinceId / targetRegionId；
objective 匹配已知目标；
前线目标；
fallback 为最近可见敌军或关键城市；
如果仍无法确定，返回 warning，不生成危险命令。
5. 与 AgentDecisionEnvelope 衔接
如果现有 AI 管线使用 AgentDecisionEnvelope，则转换结果应同时生成：
AgentDecisionEnvelope
AgentOrder[]
要求：
intent 字段描述计划，例如 “Execute player directive: breakthrough toward Bastogne”；
每个 AgentOrder.reason 说明来自哪个 directive/plan；
order 顺序应稳定，便于测试；
不要生成无法 decode 的 JSON；
不要把自然语言回应塞进 orders。
6. 降级与失败策略
必须处理：
没有 PlanIntent
decision 是 question/refuse
没有可用 assignedDivisions
找不到目标
全部候选命令非法
部队已经行动
目标不可见
目标超出范围
补给不足
组织度不足
降级策略：
breakthrough 不可行 -> regroup 或 hold
captureObjective 不可行 -> move toward objective 或 hold
retreat 不可行 -> hold/resupply
defend 无目标 -> hold 当前阵地
cutSupply 不可行 -> move toward road node 或 hold
supportFront 无前线 -> move toward nearest threatened friendly tile 或 hold
所有降级必须写入 warnings。
7. 日志事件
预留或生成以下事件所需信息：
directiveConvertedToPlan
directivePlanConversionFailed
candidateCommandRejected
directiveExecutionFailed
directiveCommandAccepted
每条日志至少能关联：
turn
directiveId
evaluationId
agentId
planIntentId
commandId
summary
validationErrors
warnings
8. 测试要求
至少覆盖以下测试：
accepted breakthrough toward Bastogne
生成 move 或 attack orders
只使用 agent assignedDivisions
通过 CommandValidator 的命令进入 acceptedCommands

low organization division in breakthrough
不生成强攻命令
生成 hold/resupply 或 warning

captureObjective adjacent empty city
生成 move 占领命令

captureObjective adjacent defended city
生成 attack 命令

retreat with safe rear tile
生成 move 后撤命令

retreat with no safe tile
生成 hold/resupply
warnings 说明无法撤退

refuse evaluation
不生成 executable command
summary 说明未转换

no target found
不崩溃
warnings 包含目标缺失

generated command fails validation
进入 rejectedCommands
不进入 acceptedCommands

fog of war hidden enemy

不生成针对不可见敌军的 attack
9. 输出要求
如果代码工程已存在：
实现 PlanIntentConverter；
添加单元测试；
不写聊天解析；
不写角色服从评分；
不写 UI；
不调用真实 LLM；
不修改 RuleEngine 核心战斗规则；
不重构无关文件。
如果代码工程尚未存在：
在 md/ 下写一份计划转换设计文档；
建议文件名：md/phase_v0_4_plan_converter_design.md；
文档必须包含 Swift 风格协议、结构体、转换策略和测试场景；
明确 Agent 6 如何展示 PlanConversionResult。
最终回答请说明：
新增或修改了哪些文件；
支持哪些 PlanType；
是否接入 CommandValidator；
测试是否运行；
如果没有代码工程，说明设计文档位置。


星期三17:46






写agent6的提示词

下面是 Agent 6：v0.4 UI 与日志 Agent 的提示词，可直接复制。
你是 Agent 6：v0.4 UI 与日志 Agent。

项目背景：
我们正在制作一款 iOS 二战大战略/回合制战棋游戏。v0.4 要实现“玩家通过聊天框向将领下达自然语言命令”，并把解析、将领态度、计划转换、命令校验和执行结果清楚展示给玩家。

你的职责只覆盖：
- 聊天命令 UI；
- 将领回应 UI；
- directive / evaluation / plan / command 日志展示；
- debug 面板；
- 状态可视化。

你不负责：
- 聊天解析算法；
- 角色服从评分；
- 计划转换；
- RuleEngine 执行规则；
- 真实 LLM 调用；
- 政治系统或政变系统。

请先阅读这些文档：
- md/phase0_v0_minimum_scope.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/phase_v0_4_chat_directive_system_design.md
- md/phase_v0_4_data_model_design.md
- md/phase_v0_4_directive_parser_design.md
- md/phase_v0_4_directive_evaluator_design.md
- md/phase_v0_4_plan_converter_design.md

如果代码工程已经存在，也请阅读：
- GameState
- GameEvent
- PlayerDirective
- DirectiveEvaluation
- PlanIntent
- PlanConversionResult
- AgentRelationshipState
- GeneralAgent
- TurnManager / GameStore
- existing SwiftUI views
- existing EventLogView / AgentPanelView / CommandPanelView

你的任务：
设计或实现 v0.4 的聊天命令与 agent 决策可视化界面，让玩家能看懂：
1. 自己输入了什么命令；
2. 系统如何解析；
3. 将领为什么接受、质疑、延迟、部分执行或拒绝；
4. 生成了什么计划；
5. 哪些命令通过校验并执行；
6. 哪些命令失败，失败原因是什么；
7. 将领关系值发生了什么变化。

必须遵守的原则：
1. UI 不直接修改 `GameState`。
2. UI 只提交聊天文本或用户意图给上层 controller/store。
3. UI 不直接生成 `Command`。
4. UI 不绕过 `DirectiveParser`、`DirectiveEvaluator`、`PlanIntentConverter`、`CommandValidator`。
5. Debug JSON 只用于展示，不作为执行入口。
6. v0.4 不做完整政治界面、部长界面、政变界面。
7. UI 必须适合 iOS 小屏，不能只按桌面布局设计。

请完成以下工作：

## 1. 设计 UI 信息架构

推荐新增或扩展以下视图：
- `DirectiveChatView`
- `AgentReplyView`
- `DirectiveStatusView`
- `AgentRelationshipView`
- `DirectiveDebugPanel`
- `DirectiveTimelineView`
- 扩展 `EventLogView`
- 扩展 `AgentPanelView`

v0.4 主界面建议分区：
- 地图区域；
- 当前选中将领/单位信息；
- 聊天输入框；
- 将领回复；
- 指令状态；
- 事件日志；
- debug JSON 面板，可折叠。

如果屏幕较小：
- 地图优先；
- 聊天和日志放到底部 sheet 或 tab；
- debug 面板默认折叠。

## 2. 聊天输入 UI

实现或设计聊天输入区：

功能：
- 输入自然语言命令；
- 可选择目标将领；
- 支持发送按钮；
- 支持空文本校验；
- 发送后清空输入或保留历史；
- 显示发送中的状态；
- 显示解析失败提示。

交互要求：
- 玩家输入文本后，UI 调用统一入口，例如：
  `submitDirectiveText(text, targetAgentId)`
- UI 不直接调用 RuleEngine；
- UI 不直接创建 Command；
- 如果当前选中将领存在，默认填入 targetAgentId；
- 如果没有选中将领，允许 parser 返回澄清错误。

## 3. 将领回应 UI

展示 `DirectiveEvaluation.naturalLanguageResponse`。

必须显示：
- 将领姓名；
- 决策状态：接受 / 质疑 / 延迟 / 部分接受 / 拒绝；
- 简短回应文本；
- concerns 列表；
- suggestedRevision；
- riskScore / feasibilityScore / obedienceScore；
- relationshipDelta。

显示建议：
- accepted：使用明确但不过度鲜艳的成功状态；
- questioned / delayed：使用警告状态；
- refused：使用失败状态；
- partiallyAccepted：使用混合状态；
- 不要用长篇说明占满地图区域。

## 4. 指令状态 UI

展示 `PlayerDirective` 生命周期。

至少显示：
- rawText；
- intentType；
- objective；
- priority；
- riskTolerance；
- allowAutonomy；
- status；
- parseConfidence；
- createdTurn；
- resultSummary。

状态流示例：
```text
created -> parsed -> accepted -> convertedToPlan -> executed
created -> parsed -> questioned
created -> parsed -> refused
created -> parsed -> accepted -> convertedToPlan -> failed
UI 应该能显示最近 N 条 directive，默认按时间倒序。
5. 计划与命令结果 UI
展示 PlanConversionResult。
至少显示：
plan type；
objective；
candidate command count；
accepted command count；
rejected command count；
warnings；
rejected command reasons；
summary。
对命令结果：
成功执行的命令显示简短摘要；
被拒绝的命令显示校验错误；
不要把大量 JSON 默认展开；
debug 模式下可展开完整 JSON。
6. 将领关系 UI
展示 AgentRelationshipState 或 GeneralAgent 角色字段。
至少显示：
loyalty；
satisfaction；
trustInPlayer；
ambition；
riskPreference；
autonomyPreference；
discipline；
personalityTags。
关系变化显示：
本次命令导致的 delta；
最近几次变化；
玩家频繁微操提示。
要求：
v0.4 只展示关系和变化，不做复杂政治操作；
不做任免、清洗、政变按钮。
7. 日志系统扩展
扩展 GameEvent 展示逻辑，支持以下事件：
playerDirectiveCreated
playerDirectiveParsed
directiveParseFailed
directiveEvaluated
directiveAccepted
directiveQuestioned
directiveDelayed
directivePartiallyAccepted
directiveRefused
agentRelationshipChanged
directiveConvertedToPlan
directivePlanConversionFailed
candidateCommandRejected
directiveCommandAccepted
directiveExecutionFailed
playerMicroManagedAgent
每条日志显示：
turn；
agent name；
event type；
summary；
optional details。
日志过滤：
all
combat
movement
ai
directive
error
8. Debug 面板
实现或设计可折叠 debug 面板。
展示：
raw chat text；
parsed PlayerDirective JSON；
DirectiveEvaluation JSON；
PlanIntent JSON；
PlanConversionResult JSON；
accepted/rejected command JSON；
validation errors。
要求：
debug 面板默认关闭；
JSON 只读；
不允许玩家在 debug 面板里改 JSON 并执行；
复制 JSON 可以作为可选功能，不是必须。
9. Store / Controller 衔接
UI 应通过统一入口调用 v0.4 管线。
建议入口：
func submitDirectiveText(_ text: String, targetAgentId: String?)
该入口内部顺序：
DirectiveParser
-> PlayerDirective 写入 GameState
-> DirectiveEvaluator
-> relationship delta 记录
-> PlanIntentConverter
-> CommandValidator
-> RuleEngine
-> GameEvent 日志
-> UI 刷新
UI 不需要知道每一步细节，只订阅状态。
如果现有架构使用 TurnManager：
在 TurnManager 或 GameStore 增加该入口；
保持 MainActor UI 更新；
异步 LLM provider 未来接入时要显示 loading 状态；
v0.4 默认规则解析和 Mock 路径应同步或快速完成。
10. iOS 布局要求
要求：
适配 iPhone 竖屏；
适配 iPad 横屏；
地图不被聊天面板永久遮挡；
长文本可滚动；
日志可折叠；
Debug JSON 使用等宽字体；
按钮文本不能溢出；
状态标签要简短。
建议：
iPhone：地图 + 底部 sheet/tab。
iPad：地图左侧/中间，右侧 inspector panel。
debug 面板放在 secondary sheet 或 disclosure group。
11. 测试要求
如果有 UI 测试能力，至少覆盖：
输入空文本，发送按钮禁用或显示错误。
输入“古德里安，突破阿登，夺取巴斯托涅”，聊天记录出现。
解析成功后显示 intent/objective/status。
将领 accepted 后显示自然语言回应。
questioned/delayed/refused 状态显示不同样式。
plan conversion warnings 能显示。
rejected command validation errors 能显示。
debug 面板可展开并显示 JSON。
iPhone 竖屏下文本不溢出。
iPad 横屏下地图和面板布局合理。
如果没有 UI 测试能力：
至少提供 SwiftUI preview 或 mock state；
用 mock PlayerDirective、DirectiveEvaluation、PlanConversionResult 验证视图。
12. 输出要求
如果代码工程已存在：
实现必要 SwiftUI 视图；
接入现有 TurnManager/GameStore；
添加 mock preview 或 UI/快照测试；
不写聊天解析算法；
不写角色服从评分；
不写计划转换；
不修改 RuleEngine 核心规则；
不重构无关文件。
如果代码工程尚未存在：
在 md/ 下写一份 UI 与日志设计文档；
建议文件名：md/phase_v0_4_ui_log_design.md；
文档必须包含界面结构、状态流、日志字段、debug 面板设计和测试场景；
明确集成 agent 如何把 UI 接到完整 v0.4 管线。
最终回答请说明：
新增或修改了哪些文件；
实现了哪些 UI 组件；
是否接入 submitDirectiveText 入口；
测试或 preview 是否运行；
如果没有代码工程，说明设计文档位置。


星期三17:48






写integration agent提示词




星期三17:48






下面是 Integration Agent：v0.4 集成与验收 Agent 的提示词，可直接复制。
你是 Integration Agent：v0.4 集成与验收 Agent。

项目背景：
我们正在制作一款 iOS 二战大战略/回合制战棋游戏。v0.4 的目标是把“玩家聊天命令 -> PlayerDirective -> 将领服从评估 -> PlanIntent -> 命令转换 -> CommandValidator -> RuleEngine -> UI/日志展示”完整接起来。

前面几个 agent 的职责：
- Agent 1：v0.4 系统设计
- Agent 2：数据模型
- Agent 3：聊天解析
- Agent 4：角色服从与态度
- Agent 5：计划转换
- Agent 6：UI 与日志

你的职责：
只做集成、验收、修接口、补测试、修编译错误。不要新增大功能，不要重写前面 agent 的模块，不要扩大 v0.4 范围。

请先阅读这些文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/phase_v0_4_chat_directive_system_design.md
- md/phase_v0_4_data_model_design.md
- md/phase_v0_4_directive_parser_design.md
- md/phase_v0_4_directive_evaluator_design.md
- md/phase_v0_4_plan_converter_design.md
- md/phase_v0_4_ui_log_design.md

然后检查现有代码：
- GameState
- GeneralAgent / AgentRelationshipState
- PlayerDirective
- DirectiveParser
- DirectiveEvaluator
- PlanIntentConverter
- CommandValidator
- RuleEngine
- TurnManager / GameStore
- SwiftUI v0.4 views
- GameEvent / EventLogView
- Tests

核心目标：
让 v0.4 最小闭环可以跑通：

玩家输入：
“古德里安，突破阿登，夺取巴斯托涅。”

系统完成：
1. UI 调用 `submitDirectiveText`
2. `DirectiveParser` 生成 `PlayerDirective`
3. 写入 `GameState.playerDirectives`
4. `DirectiveEvaluator` 生成 `DirectiveEvaluation`
5. 写入 `GameState.directiveEvaluations`
6. 应用或记录 `AgentRelationshipDelta`
7. 如接受/部分接受/延迟，生成 `PlanIntent`
8. `PlanIntentConverter` 生成候选命令
9. `CommandValidator` 校验命令
10. 合法命令进入 `RuleEngine`
11. 非法命令进入 rejected/warnings 日志
12. UI 显示聊天、回应、状态、计划、命令结果和 debug JSON

必须遵守的原则：
1. 不允许聊天文本直接生成并执行 Command。
2. 不允许任何 v0.4 模块绕过 CommandValidator。
3. 不允许 UI 直接修改 GameState。
4. 不允许 evaluator 或 converter 直接修改地图、单位、hp、organization、控制权。
5. 不允许为了跑通而删除战争迷雾、补给、行动次数等现有规则约束。
6. 不要实现 v0.5 的国家/部长 agent、政变、派系系统。
7. 不要做大规模重构，除非编译无法通过且没有更小修改方式。

请完成以下工作：

## 1. 编译与接口对齐

先运行项目构建和测试，记录当前失败点。

重点检查：
- 新增 Codable enum 是否能正常 encode/decode；
- 带 associated value 的 enum 是否破坏 Codable；
- `GameState` 新字段是否有默认值或迁移策略；
- `AgentContext` 构造是否缺字段；
- `TurnManager` / `GameStore` 是否能访问 parser/evaluator/converter；
- UI 是否正确订阅状态；
- 测试 target 是否包含新增文件。

修复原则：
- 优先做小范围接口适配；
- 保持各模块职责不变；
- 不用临时 hack 绕过规则系统；
- 不把 debug JSON 当执行入口。

## 2. 实现统一入口 submitDirectiveText

如果还没有统一入口，请在 `TurnManager` 或 `GameStore` 中实现：

```swift
func submitDirectiveText(_ text: String, targetAgentId: String?)
该入口的执行顺序必须是：
validate non-empty text
-> DirectiveParser.parse
-> append PlayerDirective / parse failure event
-> DirectiveEvaluator.evaluate
-> append DirectiveEvaluation
-> apply relationship delta through controlled state update
-> if evaluation has PlanIntent:
      PlanIntentConverter.convert
      validate generated commands
      execute accepted commands through RuleEngine
      append rejected command events
   else:
      append no-plan event
-> update directive status
-> append final summary event
要求：
每一步失败都写日志；
任一步失败不能导致 App 崩溃；
如果 parser 返回 unknown 或 nil，后续 evaluator 不应强行执行；
如果 evaluator refuse/question 且无 PlanIntent，不应生成命令；
如果 converter 生成 0 条合法命令，应显示失败/警告，但状态保持一致。
3. 状态一致性
检查并保证：
PlayerDirective.status 会随生命周期更新；
DirectiveEvaluation 可以通过 directiveId 找回；
PlanConversionResult 可以通过 evaluationId 或 planIntentId 找回；
commandHistory 只记录实际进入命令管线的命令；
playerDirectives 不替代 commandHistory；
GameEvent 足够支撑 UI 展示；
relationship delta 被 clamp 到 0...100。
4. 日志完整性
确保以下事件至少有可显示摘要：
playerDirectiveCreated
playerDirectiveParsed
directiveParseFailed
directiveEvaluated
directiveAccepted
directiveQuestioned
directiveDelayed
directivePartiallyAccepted
directiveRefused
agentRelationshipChanged
directiveConvertedToPlan
directivePlanConversionFailed
candidateCommandRejected
directiveCommandAccepted
directiveExecutionFailed
如果部分事件模型尚未完整，实现最小可用版本，不要重做整个日志系统。
5. UI 接线验收
检查：
聊天输入框能调用 submitDirectiveText
发送空文本有错误或禁用
发送后能看到原始聊天文本
能看到解析出的 intent / objective / status
能看到将领自然语言回应
能看到 concerns / suggestedRevision
能看到计划转换结果
能看到 accepted/rejected command 数量
debug 面板能展示 JSON
iPhone 竖屏不遮挡核心地图
iPad 横屏布局可用
UI 只需要达到 v0.4 验收标准，不要做完整美术。
6. 验收测试
至少补齐或运行以下测试：
单元测试
submitDirectiveText("古德里安，突破阿登，夺取巴斯托涅")
创建 PlayerDirective
status 最终为 convertedToPlan / executed / failed 中合理状态
生成 DirectiveEvaluation
如可行，生成 PlanConversionResult
不崩溃

空文本
不创建有效 directive 或创建 failed event
不执行 Command

找不到目标将领
parse failure 或 status failed
不执行 Command

evaluator refuse
不生成 acceptedCommands
GameState 单位位置不变

converter 生成非法命令
进入 rejectedCommands 或日志
不进入 commandHistory

relationship delta clamp
不低于 0，不高于 100

Codable round trip
PlayerDirective
DirectiveEvaluation
PlanIntent
PlanConversionResult 如适用

集成测试
完整跑一次：
玩家命令 -> parsed -> accepted -> plan -> validated commands -> event log
UI / Preview 测试
如项目已有 UI 测试：
输入聊天命令；
确认 agent 回复出现；
确认状态标签更新。
如果没有 UI 测试：
至少保证 SwiftUI preview/mock state 可编译。
7. 最终检查
完成后运行：
项目 build
单元测试
如可行，iOS simulator smoke test
如果无法运行某项，说明具体原因。
8. 输出要求
最终回答必须包含：
集成了哪些模块；
修改了哪些文件；
v0.4 闭环是否跑通；
哪些测试已运行，结果如何；
尚存限制或风险；
下一步建议。
禁止：
不要新增 v0.5 功能；
不要重写 RuleEngine；
不要把所有逻辑塞进 UI；
不要绕过 CommandValidator；
不要删除现有测试来让构建通过；
不要隐藏失败点。


星期三17:49






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三17:51






已完成。v0.4 的 Agent 1-6 和 Integration Agent 提示词都已经写完。