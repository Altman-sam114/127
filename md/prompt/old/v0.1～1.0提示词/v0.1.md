## v0.1计划
当前 goal 是“先继续给我 v0.1 的执行方案，分几个 agent，分别干什么，先写个大概，一会再写提示词”。如果你要继续，我建议下一步直接拆 v0.1：
Agent A：组织度/兵力规则模型
Agent B：撤退与补员规则
Agent C：包围/补给惩罚接入
Agent D：战斗日志与 UI 展示
Agent E：测试与集成验收

## 子agent：

### 1
你是 v0.1 Tests/Integration worker。工作目录：/Users/a114514/Desktop/codexapp/test。项目：WWIIHexV0。当前 v0 有 Tests 目录。v0.1 目标：strength / organization、撤退、补员、组织度恢复、包围导致补给和组织度惩罚、简化战斗日志。

你的任务负责测试和最终集成风险扫描。请先读取相关 Core/Rules/Commands/Tests 文件。你可以新增或修改测试文件，但不要改生产代码，除非是明显编译错误且最小修复。

写入范围：
- WWIIHexV0/Tests/RuleEngineCoreTests.swift
- WWIIHexV0/Tests/GameStateTests.swift
- WWIIHexV0/Tests/AgentPipelineTests.swift（只在命令 schema 变化影响 AI 时）
- 可新增 WWIIHexV0/Tests/V01OrganizationRetreatTests.swift（如 Xcode project 不自动包含新文件，则优先改已有测试文件）

具体测试目标：
1. 攻击同时降低 strength/hp 与 organization。
2. organization 归零后单位不能继续普通行动或进入 routed/retreating 状态。
3. resupply/rest 在 supplied 状态恢复 organization，并可恢复少量 strength/hp。
4. lowSupply/encircled 降低恢复或造成惩罚。
5. encircled 回合结算扣 organization 和少量 strength/hp。
6. 撤退选择安全相邻格；无安全格时记录失败或施加惩罚。
7. 旧 v0 移动、攻击、补给基础测试仍通过。

约束：
- 你不是唯一 worker；不要回滚别人改动，不要全项目格式化。
- 如果 worker 们还没完成生产代码，你先写能表达目标的测试；若编译暂时失败，在最终回复中列出失败点。
- 尽量运行 xcodebuild test 或 swift 测试命令；如果环境不支持，说明具体错误。

最终回复请列出：改了哪些测试、覆盖了哪些 v0.1 行为、当前测试运行结果和失败原因。

### 2
你是 v0.1 UI/Logs worker。工作目录：/Users/a114514/Desktop/codexapp/test。项目：WWIIHexV0。当前 v0 已有 SwiftUI 面板、SpriteKit 单位显示、事件日志和 Agent 面板。v0.1 目标包含 strength / organization、撤退、补员、组织度恢复、包围惩罚、简化战斗日志。

你的任务只负责 UI 展示和日志可读性，不负责规则公式。

写入范围：
- WWIIHexV0/UI/UnitInspectorView.swift
- WWIIHexV0/UI/EventLogView.swift
- WWIIHexV0/UI/HUDView.swift（仅如必要）
- WWIIHexV0/SpriteKit/UnitNode.swift（仅显示 strength/organization 需要时）
- WWIIHexV0/SpriteKit/BoardScene.swift（仅刷新显示需要时）
- WWIIHexV0/Core/GameLogEntry.swift（仅如需要支持 log category；避免大改）

请先读取这些文件以及 Core/Division.swift 当前实现，再实施。

具体目标：
1. 单位详情面板显示 strength/maxStrength 或兼容 hp/maxHP，同时显示 organization/maxOrganization。
2. 明确显示 supplyState、hasActed、retreating/routed 状态（如果 Core 尚未有字段，可用 organization <= 0 推导显示 Routed）。
3. 地图单位 marker 能显示组织度的简要状态。优先低风险：在现有 hp 文本旁增加 org 缩写，避免重写渲染。
4. 事件日志中 v0.1 新日志要可读：攻击伤害、组织度损失、撤退、补员、包围损耗。若规则层只是写 message 字符串，EventLogView 只需保证展示完整。
5. 不要改规则、不改命令执行、不改 Agents。

约束：
- 你不是唯一 worker；不要回滚别人改动，不要格式化全项目。
- UI 应兼容旧字段。若 Core worker 还没完成，请用 hp/maxHP + organization 的当前形态做兼容。
- 保持界面简单，不引入新设计系统。

最终回复请列出：改了哪些文件、UI 显示了哪些 v0.1 状态、是否依赖 Core worker 后续字段。

### 3
你是 v0.1 Retreat/Supply worker。工作目录：/Users/a114514/Desktop/codexapp/test。项目：WWIIHexV0。当前 v0 已有 SupplyRules、TurnManager、RuleEngine，基础补给和包围 attrition。v0.1 目标：撤退、补员、组织度恢复、包围导致补给和组织度惩罚。

你的任务负责撤退/补员/结算规则，不负责 UI，不负责 LLM。

写入范围：
- WWIIHexV0/Rules/SupplyRules.swift
- WWIIHexV0/Rules/RuleEngine.swift
- WWIIHexV0/Rules/CommandExecutor.swift（只改 retreat/resupply/rest 执行相关）
- WWIIHexV0/Rules/CommandValidator.swift（只改 retreat/resupply/rest 校验相关）
- WWIIHexV0/Turn/TurnManager.swift（只改回合结算调用顺序，如需要）

请先读取这些文件以及 Core/Division.swift、Commands/Command.swift 当前实现，再实施。如果 Core worker 尚未完成，你可以用现有 organization 和 hp 兼容实现。

具体目标：
1. 实现组织度恢复：回合结算或 resupply/rest 时，supplied 恢复最多，lowSupply 恢复较少，encircled 几乎不恢复或不恢复。
2. 实现补员/补强：resupply/rest 命令在 supplied 状态下恢复少量 strength/hp 和 organization；lowSupply 只恢复 organization 或少量；encircled 不补 strength。
3. 实现撤退：routed 或手动 retreat 的单位移动到安全撤退格。优先选择离己方补给源更近、非敌 ZOC、可通行、无单位的相邻格。找不到撤退格时记录失败，并加重组织度/strength 惩罚。
4. 包围惩罚升级：encircled 每回合扣组织度，并少量扣 strength/hp；不要直接秒杀，最低规则由 Core/Combat 决定。
5. 回合结算顺序清晰：补给状态更新 -> 撤退处理/组织恢复/包围惩罚 -> 胜利检查 -> 重置行动。若现有顺序不同，做最小调整。
6. 写入 GameLogEntry，说明恢复、撤退、包围损耗。

约束：
- 你不是唯一 worker；不要回滚别人改动，不要全项目格式化。
- 保持确定性，避免随机。
- 不要实现复杂投降、装备库存或生产系统。

最终回复请列出：改了哪些文件、撤退选择规则、恢复/惩罚数值、需要其他 worker 接入的点。

### 4
你是 v0.1 Combat Rules worker。工作目录：/Users/a114514/Desktop/codexapp/test。项目：WWIIHexV0，iOS Swift + SwiftUI + SpriteKit。当前 v0 已实现基础战斗。v0.1 目标：strength / organization、撤退、补员、组织度恢复、包围导致补给和组织度惩罚、简化战斗日志。

你的任务只负责战斗规则和命令执行接入，不负责 UI，不负责数据文件。

写入范围：
- WWIIHexV0/Rules/CombatRules.swift
- WWIIHexV0/Rules/CommandExecutor.swift
- WWIIHexV0/Rules/CommandValidator.swift（只改战斗/撤退/休整校验相关）
- WWIIHexV0/Rules/RuleEngine.swift（只改执行链需要的最小部分）

请先读取这些文件以及 Core/Division.swift、Commands/Command.swift 当前实现，再实施。如果 Core worker 尚未完成，你可以基于现有 hp/organization 做兼容代码，避免依赖未落地 API。

具体目标：
1. 战斗从单一 hp 伤害升级为 strengthDamage + organizationDamage。建议：现有 damage 继续表示 strengthDamage，新增 organizationDamage 通常更高，例如 strengthDamage * 8-14 的范围，按地形/侧翼/补给可调。
2. 攻击优先显著消耗组织度；strength 仍会下降，但不应每次都快速全灭。
3. organization <= 0 时单位 routed/retreating：不能继续普通行动，并由撤退系统处理。如果 Core 还没有状态字段，先用 organization <= 0 作为 routed 判断。
4. 反击同样造成组织度伤害，但比主动攻击低。
5. 攻击后写入更清楚的 GameLogEntry：包含 strength damage、organization damage、是否溃退/被摧毁。
6. 被摧毁条件：strength <= 0；如果被包围且组织度崩溃，可允许更严重 strength 损失，但不要实现复杂投降系统。
7. 不要改 SpriteKit/UI/Agents/Data，除非编译需要。

约束：
- 你不是唯一 worker；不要回滚别人改动，不要全项目格式化。
- 保持旧测试尽量兼容。若旧测试预期 hp 降低，需要结合兼容字段处理。
- 代码保持简单确定性，便于单元测试。

最终回复请列出：改了哪些文件、伤害公式、需要哪些测试覆盖、是否依赖 Core worker 的字段/API。

### 5
你是 v0.1 Core Model worker。工作目录：/Users/a114514/Desktop/codexapp/test。项目：WWIIHexV0，iOS Swift + SwiftUI + SpriteKit。当前 v0 已实现六角格地图、移动、攻击、占领、基础补给、MockAI。用户要求完成 v0.1：strength / organization、撤退、补员、组织度恢复、包围导致补给和组织度惩罚、简化战斗日志。

你的任务只负责 Core/Commands 数据模型层，避免和其他 worker 冲突。

写入范围：
- WWIIHexV0/Core/Division.swift
- WWIIHexV0/Core/SupplyState.swift（仅如必要）
- WWIIHexV0/Commands/Command.swift
- WWIIHexV0/Commands/CommandValidation.swift（仅如必要）
- WWIIHexV0/Commands/CommandResult.swift（仅如必要）

请先读取这些文件和相关引用，再实施。

具体目标：
1. 把现有 Division 的 hp/maxHP 语义升级为 strength/maxStrength，同时尽量保持兼容：如果大量代码仍引用 hp/maxHP，保留 hp/maxHP 作为 compatibility API 或明确迁移，但不要破坏编译。
2. 明确 organization/maxOrganization，当前已有 organization 但缺 maxOrganization，需要加入上限和 clamp。
3. 增加 v0.1 状态字段，最小够用：isRetreating、retreatTarget 或 routed/retreatTurnsRemaining，如你认为更合适。保持简单。
4. 增加 Division helper：isDestroyed、isRouted、canAct、restoreOrganization、receiveStrengthDamage、receiveOrganizationDamage 等简单 mutating 方法。不要过度抽象。
5. Command 增加 retreat / reinforce 或 rest 类命令。注意现有 resupply 可复用为休整/补员时，优先少加命令；若加命令，Command.displayName 和 actingDivisionId 必须更新。
6. 不要改 UI、Rules、Agents、Tests，除非编译需要的最小适配。

约束：
- 你不是代码库里唯一 worker，不要重写别人模块，不要格式化全项目，不要回滚任何别人改动。
- 保持 v0 行为兼容，v0.1 只是增强。
- 所有新增字段必须 Codable/Equatable 兼容。
- 完成后运行你能运行的相关检查；如果无法运行，说明原因。

最终回复请列出：改了哪些文件、核心字段/API、是否需要其他 worker 接入。

## 完成：
v0.1 已完成并标记 goal complete。
我启动并协调了 5 个实现 agent：Core/Commands、Combat、Retreat/Supply、UI/Logs、Tests/Integration。最终集成结果已验证通过：
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test \
  -scheme WWIIHexV0 \
  -destination 'id=A1EA6908-3306-44E0-9F99-F0F77DAE2404' \
  -derivedDataPath /private/tmp/WWIIHexV0DerivedData \
  CODE_SIGNING_ALLOWED=NO
结果：41 tests, 0 failures，TEST SUCCEEDED。
主要完成内容：
Division 支持 strength/maxStrength，保留 hp/maxHP 兼容。
加入 maxOrganization、撤退状态、routed/canAct 等 helper。
战斗改为 strength + organization 双轨伤害。
组织度低于阈值会溃退，撤退失败会受惩罚。
resupply/rest 恢复组织度和少量 strength。
包围每回合扣组织度和少量 strength。
UI 显示 Strength、Organization、Routed/Retreating 状态。
日志显示 combat/org/retreat/reinforce/encircle/supply 分类。
测试覆盖 v0.1 核心行为，并保持 v0 基础行为通过。
涉及的核心文件包括 [Division.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/Core/Division.swift)、[CombatRules.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/Rules/CombatRules.swift)、[SupplyRules.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/Rules/SupplyRules.swift)、[CommandExecutor.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/Rules/CommandExecutor.swift)、[CommandValidator.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/Rules/CommandValidator.swift)、[UnitInspectorView.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/UI/UnitInspectorView.swift)、[UnitNode.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/SpriteKit/UnitNode.swift)、[EventLogView.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/UI/EventLogView.swift)、[RuleEngineCoreTests.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/Tests/RuleEngineCoreTests.swift)。

## 组织力移除

已完成。核心规则现在改成“只看兵力”，组织度相关字段、恢复、日志、测试都移除了。
主要改动：
[Division.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/Core/Division.swift): 新增 RetreatMode.retreatable / hold，默认 retreatable，取消 organization。
[CombatRules.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/Rules/CombatRules.swift): 战斗只造成兵力伤害；HOLD 防御 +20%。
[CommandExecutor.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/Rules/CommandExecutor.swift): RETREATABLE 单次损失比例 >= 35% 自动撤退；HOLD 不撤退并追加 20% 兵力消耗。
[Command.swift](/Users/a114514/Desktop/codexapp/test/WWIIHexV0/Commands/Command.swift): 新增 allowRetreat 命令。
UI 已加“死守 / 允许撤退”按钮，单位详情和棋盘标记会显示当前姿态。
场景 JSON 已移除 organization，初始单位加了默认 "retreatMode": "retreatable"。
验证结果：
xcodebuild test -scheme WWIIHexV0 ... 已通过，42 tests, 0 failures。
另外我扫了一遍 WWIIHexV0，没有找到 organization / maxOrganization / isRouted 等旧机制残留。当前目录不是 Git 仓库，所以没法输出 git diff --stat。

已编辑 15 个文件+147-334查看更改撤销审核

WWIIHexV0/Core/Division.swift+12-76
WWIIHexV0/Rules/CombatRules.swift+12-72
WWIIHexV0/Rules/CommandExecutor.swift+37-19
WWIIHexV0/Commands/Command.swift+5-0
WWIIHexV0/Rules/CommandValidator.swift+3-1
WWIIHexV0/Rules/SupplyRules.swift+19-70
WWIIHexV0/App/AppContainer.swift+9-0
WWIIHexV0/UI/CommandPanelView.swift+17-3
WWIIHexV0/UI/RootGameView.swift+2-0
WWIIHexV0/UI/UnitInspectorView.swift+15-57
WWIIHexV0/SpriteKit/UnitNode.swift+9-17
WWIIHexV0/UI/EventLogView.swift+0-7
WWIIHexV0/Commands/CommandResult.swift+1-4
WWIIHexV0/Data/ScenarioDefinition.swift+3-4
WWIIHexV0/Tests/RuleEngineCoreTests.swift