先继续给我 v1.0 的执行方案，分几个 agent，分别干什么，先写个大概，一会再写提示词




星期三17:34






已处理 1m 23s



v1.0 建议按“可玩的大战略原型”拆，不要按功能清单硬拆。核心目标是：一个小型欧洲战区或西线战区，能跑通国家经济、生产、科技、前线、陆空海简化战争、多 agent 决策和玩家聊天指挥。
v1.0 Agent 分工大概
Agent 1：总架构 / 集成负责人
负责把 v0-v0.5 的系统升级成 v1.0 可扩展架构。
主要任务：
定义 v1.0 的最终模块边界。
统一 GameState、ProvinceNode、Command、AgentIntent、TurnPipeline。
处理各 agent 产物之间的接口冲突。
决定哪些逻辑放规则层，哪些放 AI 层，哪些放 UI 层。
最后负责集成、编译、跑通完整 demo。
这个 agent 最关键，不能省。其他 agent 都容易只做自己的功能，最后接不上。
Agent 2：地图 / 省份图 / 前线系统
负责把地图从 v0 六角格测试板升级成省份图底层。
主要任务：
设计 ProvinceNode / RegionNode。
设计省份相邻关系、控制权、核心区、占领区。
设计城市、机场、港口、资源、工厂挂载方式。
实现前线识别。
实现集团军分配前线。
实现自动填线和缺口检测。
实现攻势箭头 / 突破方向的第一版逻辑。
关键要求：
省份图是规则底层。
六角格只作为旧测试板或显示层，不再承担大战略逻辑。
前线算法必须是确定性规则，不依赖 LLM 逐格判断。
Agent 3：陆战 / 补给 / 组织度系统
负责大战略陆军规则。
主要任务：
完善师级单位模型。
实现 strength、organization、补员、撤退。
实现包围、补给线、补给节点、基建影响。
实现战斗宽度或简化参战容量。
实现地形、河流、要塞、城市修正。
实现自动撤退和失败后重组。
实现单位经验、将领加成的接口位置。
关键要求：
不要让战斗变成简单 hp 对砍。
组织度决定能不能继续战斗。
strength 决定长期损失和补员压力。
Agent 4：经济 / 生产 / 人力系统
负责大战略底层资源循环。
主要任务：
资源：钢、油、铝、橡胶等可以先简化。
工厂：民用工厂、军用工厂、船坞可先简化。
税收或财政：建议 v1.0 做轻量数值，不做复杂预算。
人力：核心区、占领区、征兵法案接口。
装备生产：步兵装备、坦克、火炮、飞机、舰船。
补员消耗：strength 恢复需要人力和装备。
生产队列和产能分配。
关键要求：
经济要服务战争，不要先做复杂内政模拟。
v1.0 只需要“资源 -> 工厂 -> 装备 -> 补员/新单位”闭环。
Agent 5：科技 / 国家差异 / 装备系统
负责让各国有成长和差异。
主要任务：
科技树第一版。
陆军科技：步兵、装甲、火炮、补给。
空军科技：战斗机、轰炸机。
海军科技：舰队基础。
国家装备模板：德国坦克、盟军坦克、苏联坦克等。
科技影响生产、战斗、移动、补给。
doctrine / 学说预留接口。
关键要求：
科技不要太大，v1.0 只做能影响战斗和生产的核心项。
装备差异先用数据表，不要写死在代码里。
Agent 6：空军 / 天气系统
负责第一版实体化空军和天气。
主要任务：
机场挂载到省份。
飞机作为空军联队，不在地图上像陆军一样逐格移动。
空域 / 战区空军任务。
任务类型：制空、近距支援、战略轰炸、侦察。
天气影响：移动、视野、空军效率、战斗修正。
空军结果进入战斗结算和日志。
关键要求：
空军实体化，但不要做复杂空战模拟。
天气要简单、可解释、可被 UI 显示。
Agent 7：简化海军 / 港口 / 海域系统
负责 v1.0 的海军雏形。
主要任务：
港口挂载到省份。
海域节点图。
舰队作为海域单位。
任务类型：护航、拦截、登陆支援、海域控制。
海军影响补给、登陆、贸易路线。
船坞生产舰船的接口。
关键要求：
海军必须简化。
v1.0 不做复杂舰船部件、射程、索敌细节。
主要服务登陆、补给和海域控制。
Agent 8：多 Agent AI / LLM 决策系统
负责核心创新点。
主要任务：
统治者 agent。
元帅 / 战区司令 agent。
集团军将领 agent。
部长 agent。
AgentContext 分层摘要。
AgentIntent / WarPlan / ProductionAdvice / FrontDirective 等结构化输出。
多 agent 仲裁层。
本地 LLM provider 和 Mock provider。
决策日志、失败回退、非法命令修复。
关键要求：
LLM 不直接移动每个单位。
LLM 输出计划，规则系统负责拆解和执行。
每个 agent 只能读取自己权限内的信息摘要。
必须有 MockAI，否则无法稳定测试。
Agent 9：聊天指挥 / 角色扮演系统
负责玩家与 agent 的交互体验。
主要任务：
聊天框。
玩家命令解析为 PlayerDirective。
将领回复。
忠诚、满意度、服从、抗命概率。
玩家越级微操的政治/关系影响。
玩家身份：统治者、元帅、将领的接口预留。
任免、调动、战功、派系、政变的轻量雏形。
关键要求：
v1.0 不要把政变系统做深。
重点是让“命令将领”和“将领有性格反馈”跑通。
聊天文本和可执行命令必须分离。
Agent 10：UI / UX / 可观察性
负责把复杂系统做成可玩界面。
主要任务：
大地图界面。
省份详情面板。
单位详情面板。
前线编辑界面。
生产界面。
科技界面。
空军/海军任务界面。
agent 决策面板。
聊天框。
战报和日志。
调试面板。
关键要求：
UI 不直接改游戏状态，只提交命令。
v1.0 UI 要优先服务调试和理解，不追求最终美术。
所有 AI 决策必须可查看，否则 LLM 系统无法调试。
Agent 11：数据 / 场景 / 平衡
负责内容落地。
主要任务：
v1.0 测试战区数据。
国家数据。
省份数据。
单位模板。
科技数据。
装备数据。
将领、元帅、部长、统治者配置。
初始经济、资源、工厂、人力。
胜利条件。
平衡参数。
关键要求：
全部尽量数据驱动。
第一版建议只做 2-4 个国家，不要直接全世界。
场景建议先做“西线 1940/1944”之一，范围比全球征服小。
Agent 12：测试 / 性能 / 工具链
负责项目能不能长期维护。
主要任务：
单元测试。
回放测试。
固定随机种子模拟。
AI 输出 schema 测试。
性能 profiling。
大回合压力测试。
数据校验工具。
日志导出。
崩溃保护和 fallback。
关键要求：
本地 LLM 慢，所以必须测试“无 LLM / MockAI / LLM 超时”三种路径。
每个回合的状态变化要能重放。
数据错误不能直接崩溃，必须给出可读错误。
推荐并行方式
第一批先开 4 个 agent：
总架构 / 接口。
省份图 / 前线。
陆战 / 补给 / 组织度。
经济 / 生产 / 人力。
第二批再开：
多 Agent AI。
UI / UX。
数据 / 场景。
测试 / 工具链。
第三批最后开：
科技 / 装备。
空军 / 天气。
海军。
聊天指挥 / 角色扮演。
原因很简单：前四个决定游戏骨架。骨架没稳定前，AI、UI、科技、空海军都会返工。v1.0 的核心风险不是功能少，而是系统之间接口混乱。

星期三17:36






先写agent1的提示词

下面是给 Agent 1：总架构 / 集成负责人 的提示词，可直接复制给另一个 agent。
你是 Agent 1：v1.0 总架构 / 集成负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v0 已规划为六角格测试板，长期目标是演进到类似《钢铁雄心4》的省份图、前线、经济、生产、科技、陆空海战争、多 agent 指挥链和聊天指挥系统。

你的任务不是一次性实现全部 v1.0 功能，而是先建立 v1.0 的总架构、接口边界和集成标准，让后续各功能 agent 可以并行开发且最终能接起来。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

你的核心职责：

1. 定义 v1.0 总架构
- 明确 v1.0 的模块划分。
- 明确哪些系统属于规则层、数据层、AI 层、UI 层、渲染层、工具层。
- 设计各模块之间的依赖方向。
- 保证 UI、AI、LLM、渲染层都不能直接修改游戏状态。
- 所有状态变化必须通过 Command -> Validation -> RuleEngine -> GameEvent 这条管线。

2. 统一核心领域模型
请设计或修订以下核心类型的 v1.0 版本：
- GameState
- WorldState
- CountryState
- ProvinceNode / RegionNode
- Division / AirWing / Fleet
- Commander / AgentProfile
- Command
- AgentIntent
- FrontLine
- BattlePlan
- ProductionQueue
- TechnologyState
- GameEvent
- TurnPipeline

要求：
- v0 的 HexCoord 可以保留为局部战术或测试板实现，但 v1.0 高层规则不能继续绑定纯六角格。
- v1.0 地图规则底层应以 ProvinceNode / RegionNode 为核心。
- 所有模型优先 Codable，方便 JSON 数据驱动、存档、日志和回放。

3. 定义 Command 与 AgentIntent 分层
必须明确区分：
- 玩家输入
- 聊天自然语言
- Agent 高层意图
- 可校验的结构化命令
- 规则系统最终执行结果

推荐链路：
PlayerInput / ChatMessage
-> PlayerDirective
-> AgentIntent / BattlePlan / ProductionAdvice
-> Command
-> CommandValidation
-> RuleEngine execution
-> GameEvent

要求：
- LLM 不能直接生成最终状态变化。
- LLM 可以输出计划 JSON，但必须经过解析、校验、仲裁和规则执行。
- 多 agent 输出冲突时，需要有统一仲裁层。

4. 设计 TurnPipeline
请定义一回合的标准执行顺序，至少包含：
- 玩家命令阶段
- Agent 决策阶段
- 命令仲裁阶段
- 行动执行阶段
- 战斗结算阶段
- 补给结算阶段
- 生产与补员结算阶段
- 科技推进阶段
- 天气/空军/海军影响阶段
- 胜利条件检查
- 日志与回放快照

要求：
- 顺序必须固定、可测试、可重放。
- 每个阶段输入输出明确。
- 失败或非法命令不能破坏 GameState。

5. 制定各功能 agent 的接口契约
后续会有多个 agent 分别负责：
- 省份图 / 前线系统
- 陆战 / 补给 / 组织度
- 经济 / 生产 / 人力
- 科技 / 装备
- 空军 / 天气
- 海军 / 港口 / 海域
- 多 Agent AI / LLM
- 聊天指挥 / 角色扮演
- UI / UX
- 数据 / 场景 / 平衡
- 测试 / 性能 / 工具链

你需要为这些 agent 定义统一接口边界，避免他们互相写穿。

6. 输出内容
请输出一份 v1.0 总架构设计文档，建议命名：
md/v1_agent1_architecture_integration_plan.md

文档必须包含：
- v1.0 架构目标
- 模块划分
- 核心数据模型
- Command / AgentIntent / RuleEngine 管线
- TurnPipeline
- 多 agent 集成边界
- 数据驱动规范
- 日志与回放规范
- 测试策略
- 后续 agent 开发约束
- 明确哪些内容不由 Agent 1 实现

7. 重要约束
- 不要实现完整 v1.0 游戏。
- 不要做 UI 美术。
- 不要深入实现经济、科技、空军、海军等具体规则。
- 你的任务是“架构和接口”，不是“所有功能”。
- 如果必须写代码，只写最小骨架、协议、类型定义和示例，不写深业务逻辑。
- 不要让任何系统绕过 RuleEngine。
- 不要让 LLM 直接改 GameState。
- 不要把 v1.0 继续锁死在纯六角格上。

8. 完成标准
你的产出完成后，后续 agent 应该能够根据你的文档并行开工，并且知道：
- 自己能改哪些模块；
- 不能碰哪些模块；
- 输入输出是什么；
- Command 怎么提交；
- GameState 怎么读写；
- AgentIntent 怎么转成 Command；
- TurnPipeline 在哪里接入；
- 如何写测试；
- 如何避免破坏其他系统。
这个 Agent 1 的重点是“定接口、定边界、定状态流转”。不要让它去实现经济或空海军，否则它会变成全功能 agent，后面分工会失控。

星期三17:39






写agent2的提示词




星期三17:40






下面是 Agent 2：地图 / 省份图 / 前线系统 的提示词。
你是 Agent 2：v1.0 地图 / 省份图 / 前线系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v0 使用六角格测试板验证移动、攻击、占领、补给和 AI 命令管线；v1.0 需要升级到更接近《钢铁雄心4》的省份图 / 区块图底层，并支持前线、自动填线、攻势方向和大战略地图扩展。

你的任务不是做完整游戏，而是设计并实现 v1.0 的地图规则底层和前线系统，使后续陆战、补给、经济、空军、海军、AI 和 UI 都能挂接到统一地图结构上。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md

如果 Agent 1 的架构文档与旧文档冲突，以 Agent 1 的接口和边界为准，并在你的输出中记录冲突点。

你的核心职责：

1. 设计 v1.0 地图规则底层
- 将 v0 的纯 HexTile 思路升级为 ProvinceNode / RegionNode。
- ProvinceNode 是大战略规则底层，不是单纯显示格子。
- 每个 ProvinceNode 至少支持：
  - id
  - name
  - owner
  - controller
  - terrain
  - neighbors
  - city / port / airbase / fortress
  - infrastructure
  - supplyValue
  - resources
  - factories
  - coreOf
  - occupationState
  - weatherZone / airZone / seaZone 引用位置
- 设计 ProvinceEdge / RegionEdge，用于表示相邻关系、河流、海峡、山口、边境、通行限制。
- 保留 v0 六角格兼容路径，但 v1.0 高层规则不能继续绑定纯 HexCoord。

2. 设计地图数据格式
请设计 JSON 数据结构，用于加载：
- 省份列表
- 相邻关系
- 地形
- 城市、机场、港口、要塞
- 工厂、资源、补给值
- 初始 owner / controller
- 核心区 / 占领区
- 战区、空域、海域引用
- 可选显示信息，例如简化多边形、中心点、标签位置

要求：
- 数据必须可校验。
- 相邻关系必须能检测非法引用。
- 省份 id 必须稳定，不能依赖数组下标。
- 规则层不能依赖 SpriteKit 节点。

3. 设计 v0 到 v1.0 的地图迁移策略
v0 可能仍使用六角格测试板，因此需要兼容方案：

推荐策略：
- v0：一个 HexTile 临时视为一个 RegionNode。
- v1.0：一个 ProvinceNode 可以对应一个不规则区块。
- 后续可选：一个 ProvinceNode 对应多个显示 hex 或 polygon。

请明确：
- 哪些 v0 类型可以保留；
- 哪些类型应废弃或改名；
- HexCoord 在 v1.0 中只能出现在哪些层；
- UI / 渲染层如何从 ProvinceNode 获得显示信息。

4. 实现或设计前线系统
前线定义：
己方控制省份与敌方控制省份之间的边界集合。

请设计：
- FrontLine
- FrontSegment
- FrontAssignment
- ArmyGroupFront
- OffensiveAxis / BattlePlanArrow
- FrontGap
- FrontRisk
- FrontPriority

前线系统必须支持：
- 自动识别当前前线。
- 按阵营生成所有前线。
- 判断某条前线是否连续。
- 检测缺口。
- 检测战线过长。
- 识别关键防守点：城市、河流、要塞、山地、补给节点。
- 支持集团军分配到某条前线。
- 支持自动填线建议。
- 支持攻势箭头 / 突破方向。
- 为 AI agent 提供前线摘要。

5. 设计自动填线算法
自动填线优先使用确定性算法，不依赖 LLM。

规则建议：
- 每个 FrontSegment 计算 priority。
- priority 受地形、城市、补给、敌军威胁、缺口、突破风险影响。
- 防守型部队优先填城市、要塞、山地、河流。
- 装甲 / 摩托化部队不平均铺满前线，优先集中在突破点或预备队。
- 组织度低、补给差的部队不应被派到高风险突破点。
- 前线太长且部队不足时，输出 warning，不要强行制造虚假覆盖。

请输出算法伪代码，并说明输入输出。

6. 设计攻势方向系统
需要支持类似 HOI4 的“进攻箭头”雏形。

请设计：
- 从一条或多条己方前线省份出发；
- 指向一个或多个目标省份；
- 系统计算推荐进攻路径；
- AI 或玩家可以指定突破重点；
- 装甲部队优先参与突破；
- 步兵负责跟进和填线；
- 补给不足时降低攻势评分或阻止执行。

要求：
- LLM 可以提出“阿登方向突破”这种高层意图。
- 具体路径、合法性和单位调度由规则系统 / 自动规划器决定。
- 攻势方向不能绕过 Command 管线。

7. 与其他系统的接口边界
你必须为后续 agent 定义清晰接口，尤其是：

给陆战 / 补给系统：
- 查询省份相邻关系。
- 查询地形、河流、通行限制。
- 查询控制权和补给节点。
- 查询前线和包围相关邻接信息。

给经济 / 生产系统：
- 查询省份资源、工厂、核心区、占领区。
- 更新省份控制权后的经济归属变化。

给空军 / 天气系统：
- 查询省份所属 airZone / weatherZone。
- 查询机场位置和容量。

给海军系统：
- 查询港口、省份-海域连接、海峡通行限制。

给 AI / LLM 系统：
- 输出 FrontSummary。
- 输出 ProvinceSummary。
- 输出 ThreatSummary。
- 输出 SupplyRiskSummary。
- 不暴露不可见敌军的真实位置。

给 UI / 渲染系统：
- 提供只读地图快照。
- 提供省份中心点、标签、边界、前线段、攻势箭头数据。
- UI 不能直接修改地图控制权。

8. 输出内容
请输出一份 v1.0 地图与前线系统设计文档，建议命名：
md/v1_agent2_map_frontline_plan.md

文档必须包含：
- 设计目标
- 地图规则底层模型
- ProvinceNode / RegionNode 数据结构
- ProvinceEdge / RegionEdge 数据结构
- 地图 JSON 数据格式
- v0 六角格兼容与迁移策略
- 前线识别规则
- 自动填线算法
- 攻势箭头 / 突破方向系统
- 与其他系统的接口契约
- AI 可读取的地图摘要格式
- 测试策略
- 明确哪些内容不由 Agent 2 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON 示例

不要实现 UI 美术、完整战斗规则、经济系统、空军、海军或 LLM 逻辑。

9. 重要约束
- 不要继续把 v1.0 锁死在纯六角格上。
- 不要让 SpriteKit 节点成为规则数据来源。
- 不要让 LLM 直接决定每个省份的最终状态。
- 不要让前线系统直接修改 GameState。
- 前线系统只能输出建议、计划或 Command 候选。
- 所有控制权变化必须通过 RuleEngine 执行。
- 所有可执行行动必须进入 Command -> Validation -> RuleEngine -> GameEvent 管线。

10. 完成标准
你的产出完成后，其他 agent 应该能够明确知道：
- 如何读取地图；
- 如何判断省份相邻；
- 如何判断前线；
- 如何把集团军分配到前线；
- 如何生成自动填线建议；
- 如何表达进攻箭头；
- 如何把地图数据写成 JSON；
- 如何从 v0 六角格过渡到 v1.0 省份图；
- 哪些接口可以调用，哪些状态不能直接修改。
Agent 2 的关键是把“地图显示”和“地图规则”彻底分开。只要这个边界立住，后面的前线、补给、经济、空海军、AI 都能接上。

星期三17:43






写agent3的提示词




星期三17:43






下面是 Agent 3：陆战 / 补给 / 组织度系统 的提示词。
你是 Agent 3：v1.0 陆战 / 补给 / 组织度系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v0 已规划六角格测试板，v1.0 要升级为基于省份图 / 区块图的大战略原型。你的任务是设计并实现 v1.0 的陆军战斗、组织度、撤退、补给、包围、补员规则，让陆战不再是简单 hp 对砍，而是接近《钢铁雄心4》的“组织度崩溃、撤退、补给压力、包围歼灭”模型。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md

如果旧文档与 Agent 1/Agent 2 的 v1.0 架构冲突，以 Agent 1/Agent 2 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计 v1.0 陆军单位模型

请设计或修订以下类型：
- Division
- DivisionTemplate
- Battalion / DivisionComponent
- EquipmentStockpileRef
- CombatStats
- EffectiveCombatStats
- OrganizationState
- StrengthState
- SupplyState
- RetreatState
- CombatModifier
- Battle
- BattleParticipant
- BattleResult

每个师至少包含：
- id
- name
- countryId
- currentProvinceId
- assignedCommanderId
- assignedArmyId
- templateId
- strength
- maxStrength
- organization
- maxOrganization
- manpower
- equipment
- experience
- supplyState
- entrenchment
- movementState
- retreatState
- currentOrder
- statusEffects

要求：
- v1.0 高层位置使用 `provinceId / regionId`，不要绑定 `HexCoord`。
- `strength` 表示人力、装备、单位完整度。
- `organization` 表示战斗秩序、士气、疲劳、指挥效率。
- 组织度低时应先撤退或停止进攻，而不是直接死亡。
- strength 恢复必须依赖人力、装备和补给接口。

2. 设计陆战规则

请设计以下规则：
- 进攻合法性
- 防守合法性
- 多单位参战
- 地形修正
- 河流修正
- 城市 / 要塞修正
- 侧翼 / 多方向攻击修正
- 将领修正接口
- 科技 / 装备修正接口
- 空军支援修正接口
- 天气修正接口
- 战斗宽度或简化参战容量
- 组织度伤害
- strength 伤害
- 撤退判定
- 追击 / 包围歼灭判定

推荐原则：
- 大部分战斗先消耗 organization。
- strength 损失较慢，但决定长期战争消耗。
- 组织度归零时触发撤退。
- 被包围且无合法撤退路径时，才可能投降或被歼灭。
- 装甲和摩托化单位在平原、道路、突破战中有优势。
- 山地、森林、城市、要塞应明显限制装甲优势。

3. 设计补给系统

请基于省份图设计补给规则。

必须支持：
- supplySource
- supplyHub
- infrastructure
- railway / roadLevel 或简化补给通路
- portSupply
- supplyRange
- provinceSupplyValue
- unitSupplyNeed
- lowSupply
- outOfSupply
- encircled

补给状态建议：
- supplied
- lowSupply
- outOfSupply
- encircled

补给影响：
- 移动力下降
- 攻击力下降
- 防御力下降
- 组织度恢复下降
- 补员效率下降
- 长期缺补给导致 attrition

要求：
- 补给路径通过 ProvinceNode / RegionNode 相邻关系计算。
- 不要依赖 SpriteKit 或 UI。
- 补给系统不能直接绕过 RuleEngine 修改单位状态。
- 补给结算结果必须形成 GameEvent。

4. 设计包围与撤退系统

请设计：
- encirclement detection
- safe retreat province
- retreat target selection
- forced retreat
- shattered / routed 状态
- surrender / overrun 条件
- retreat movement lock
- retreat recovery

推荐规则：
- 单位组织度归零后尝试撤退到安全友方省份。
- 安全撤退省份必须可通行、无敌军控制、能连接补给或后方。
- 没有安全撤退路径时，单位进入 trapped 状态。
- trapped 状态再次组织度归零或 strength 过低时，触发投降 / 被歼灭。
- 高机动敌军可对撤退中低组织度单位造成 overrun。

5. 设计补员与恢复系统

请设计：
- organization recovery
- strength reinforcement
- manpower replacement
- equipment replacement
- field hospital / maintenance 接口预留
- supply 对恢复速度的影响
- frontLine / reserve 状态对恢复速度的影响

要求：
- organization 可以较快恢复。
- strength 恢复较慢，需要经济 / 生产系统提供人力和装备。
- 正在战斗、撤退、缺补给时恢复大幅降低。
- 预备队和后方省份恢复更快。

6. 定义 Command 接口

请设计陆战相关命令，但不要绕过 Agent 1 的 Command 管线。

至少包含：
- MoveDivisionCommand
- AttackProvinceCommand
- SupportAttackCommand
- DefendProvinceCommand
- RetreatCommand
- HoldCommand
- EntrenchCommand
- ReorganizeCommand
- ReinforceCommand

每个命令必须有：
- issuedBy
- countryId / factionId
- unitId 或 armyId
- sourceProvinceId
- targetProvinceId
- reason
- validation result
- execution result
- generated GameEvent

要求：
- 玩家和 AI 使用同一套命令。
- LLM 只能输出意图或候选命令。
- RuleEngine 负责最终合法性和执行。
- 非法命令只记录失败，不破坏 GameState。

7. 与其他系统的接口边界

给地图 / 前线系统：
- 读取 province 相邻关系、地形、河流、要塞、城市。
- 读取 front assignment 和 offensive axis。
- 输出单位能否进入某省份、能否攻击某省份。

给经济 / 生产系统：
- 请求人力、装备、补员。
- 上报损失、消耗、缴获接口。

给科技 / 装备系统：
- 读取单位模板、装备属性、科技修正。
- 不直接修改科技状态。

给空军 / 天气系统：
- 读取空军支援、制空权、天气修正。
- 不直接调度空军。

给 AI / LLM 系统：
- 输出 BattleSummary、UnitCombatSummary、SupplyRiskSummary。
- 不暴露不可见敌军真实信息。
- 提供“为什么不能攻击 / 不能移动 / 不能补员”的可解释原因。

给 UI 系统：
- 提供只读战斗预览。
- 提供单位详情、组织度、strength、补给、撤退状态。
- UI 不能直接修改单位状态。

8. 输出内容

请输出一份 v1.0 陆战 / 补给 / 组织度系统设计文档，建议命名：
md/v1_agent3_land_combat_supply_plan.md

文档必须包含：
- 设计目标
- 陆军单位模型
- strength / organization 规则
- 战斗流程
- 战斗伤害公式或伪代码
- 地形、河流、城市、要塞、侧翼修正
- 补给规则
- 包围规则
- 撤退规则
- 补员与恢复规则
- 陆战 Command 接口
- 与其他系统的接口契约
- AI 可读取的陆战摘要格式
- 测试策略
- 明确哪些内容不由 Agent 3 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON 示例

不要实现：
- UI 美术
- 完整经济系统
- 完整科技树
- 空军调度
- 海军系统
- LLM prompt
- 多 agent 仲裁
- 省份图底层本身

9. 重要约束

- 不要让陆战系统直接修改 GameState，必须走 RuleEngine。
- 不要让 LLM 直接决定战斗结果。
- 不要把单位位置绑定到 HexCoord。
- 不要把补给写成简单布尔值，至少要区分 supplied / lowSupply / outOfSupply / encircled。
- 不要让 organization 和 strength 混成一个血条。
- 不要做过度复杂的 HOI4 复刻，v1.0 需要可实现、可测试、可调参。
- 所有结算必须生成 GameEvent，方便日志和回放。

10. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 一个师的数据结构是什么；
- organization 和 strength 怎么变化；
- 战斗怎么开始、结算、结束；
- 单位什么时候撤退；
- 单位什么时候投降或被歼灭；
- 补给如何判定；
- 包围如何判定；
- 补员和恢复需要哪些外部资源；
- AI 和 UI 如何读取陆战摘要；
- 哪些接口可以调用，哪些状态不能直接修改。
Agent 3 的关键是把陆战核心做成“组织度驱动 + 补给驱动”，否则后面的前线、AI 指挥、经济生产都会失去意义。

星期三17:44






写agent4的提示词

下面是 Agent 4：经济 / 生产 / 人力系统 的提示词。
你是 Agent 4：v1.0 经济 / 生产 / 人力系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v1.0 的目标不是完整复刻《钢铁雄心4》，而是实现一个可玩的大战略原型：国家拥有资源、工厂、人力、生产队列和装备库存；陆军、空军、海军的补员和扩军都依赖经济系统。

你的任务是设计 v1.0 的经济、生产、人力和补员资源循环，让战争消耗能反向影响国家能力。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md

如果旧文档与 Agent 1/2/3 的 v1.0 架构冲突，以 Agent 1/2/3 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计 v1.0 国家经济模型

请设计或修订以下类型：
- CountryEconomyState
- ResourceStockpile
- ResourceIncome
- FactoryState
- ProductionLine
- ProductionQueue
- EquipmentStockpile
- ManpowerPool
- RecruitmentPolicy
- ReinforcementRequest
- ConstructionQueue
- EconomyEvent

国家经济至少包含：
- countryId
- controlledProvinces
- coreProvinces
- occupiedProvinces
- resources
- civilianFactories
- militaryFactories
- dockyards
- manpower
- equipmentStockpiles
- productionLines
- constructionQueue
- consumerGoods / tax / stability 的接口预留

要求：
- v1.0 经济服务战争，不做复杂财政模拟。
- 资源、工厂、人力、装备必须能影响补员和新单位生产。
- 数据结构优先 Codable，支持 JSON、存档、日志和回放。
- 经济系统不能直接依赖 UI 或 SpriteKit。

2. 设计资源系统

v1.0 资源建议先包含：
- steel
- oil
- aluminum
- rubber
- chromium，可选
- fuel，可选但建议预留

资源来源：
- 控制省份资源。
- 核心区资源产出效率更高。
- 占领区资源产出效率较低。
- 港口 / 海域 / 贸易路线接口预留。

资源用途：
- 步兵装备：steel
- 火炮：steel
- 坦克：steel + oil / chromium
- 卡车：steel + oil / rubber
- 飞机：aluminum + oil / rubber
- 舰船：steel + oil / chromium

要求：
- v1.0 可以允许资源不足时继续生产，但降低效率。
- 不要做复杂全球贸易系统，只预留贸易接口。

3. 设计工厂与生产系统

工厂类型：
- civilianFactory
- militaryFactory
- dockyard

生产线至少支持：
- infantryEquipment
- artillery
- tank
- truck
- fighter
- bomber
- convoy / ship，可简化

每条生产线包含：
- id
- countryId
- equipmentType
- assignedFactoryCount
- efficiency
- dailyOutput / turnOutput
- requiredResources
- producedAmount
- priority

生产规则：
- 军用工厂生产陆空装备。
- 船坞生产舰船或运输船。
- 民用工厂用于建造、修复、基础设施或消费品接口。
- 工厂越多，产量越高。
- 资源不足降低生产效率。
- 科技可以修改生产效率和装备型号。
- 生产结果进入 EquipmentStockpile。

要求：
- v1.0 先做回合制产出，不做逐日复杂模拟。
- 生产效率可以简化为 0.0-1.0。
- 不要写死具体国家装备，装备数据应来自数据表或科技系统接口。

4. 设计人力系统

人力来源：
- 核心省份人口。
- 占领区少量人力，效率较低。
- 征兵政策接口。
- 事件 / 国家精神接口预留。

人力用途：
- 新建师。
- 补充 strength 损失。
- 空军联队人员。
- 海军舰队人员。

人力规则：
- 可用人力不足时，补员和新建单位受限。
- 伤亡应从 ManpowerPool 中扣除或进入 casualties 统计。
- 被包围投降的单位造成较大人力和装备损失。
- 补员优先级可由玩家或 AI 设置。

请设计：
- totalPopulation
- recruitablePopulation
- availableManpower
- deployedManpower
- casualties
- reinforcementManpowerNeed
- recruitmentPolicyModifier

5. 设计补员与装备流转接口

需要与 Agent 3 的陆战系统对接。

请设计：
- ReinforcementRequest
- EquipmentRequest
- ManpowerRequest
- ReinforcementPriority
- ReplacementResult
- LossReport

流程建议：
BattleResult / Attrition
-> LossReport
-> EconomySystem 更新人力和装备消耗
-> Unit 发出 ReinforcementRequest
-> EconomySystem 根据库存和优先级分配
-> RuleEngine 应用 ReplacementResult
-> GameEvent 记录

要求：
- strength 恢复需要人力和装备。
- organization 恢复主要由陆战系统处理，但补给和后方状态可影响恢复。
- 经济系统只分配资源，不直接修改单位最终状态。
- 所有补员结果必须通过 Command / RuleEngine / GameEvent 管线。

6. 设计建造系统

v1.0 建造系统可以简化，但需要接口。

支持建造：
- civilianFactory
- militaryFactory
- dockyard
- infrastructure
- supplyHub
- airbase
- port
- fortress

建造规则：
- 民用工厂提供建设能力。
- 建造绑定到 ProvinceNode。
- 占领区建设效率较低。
- 被轰炸或战斗破坏后可修复。
- 建造完成后通过 RuleEngine 更新 ProvinceNode / CountryEconomyState。

不要实现复杂建筑槽系统，除非 Agent 2 的地图设计已经提供 province building slots。

7. 定义经济相关 Command

请设计经济系统相关命令，但必须遵守 Agent 1 的 Command 管线。

至少包含：
- CreateProductionLineCommand
- AssignFactoriesCommand
- ChangeProductionPriorityCommand
- CancelProductionLineCommand
- StartConstructionCommand
- CancelConstructionCommand
- SetReinforcementPriorityCommand
- TrainDivisionCommand
- DisbandUnitCommand
- ChangeRecruitmentPolicyCommand

每个命令必须包含：
- issuedBy
- countryId
- targetId / provinceId / productionLineId
- payload
- reason
- validation result
- execution result
- generated GameEvent

要求：
- 玩家、AI、部长 agent 使用同一套命令。
- 非法命令只记录失败，不破坏 GameState。
- 经济结算必须可重放。

8. 与其他系统的接口边界

给地图 / 前线系统：
- 读取省份 owner / controller / coreOf / occupationState。
- 读取资源、工厂、基础设施、港口、机场。
- 不直接修改省份控制权。

给陆战系统：
- 接收 LossReport。
- 处理 ReinforcementRequest。
- 返回 ReplacementResult。
- 提供装备和人力是否充足的解释。

给科技 / 装备系统：
- 读取装备型号、生产成本、资源需求、科技修正。
- 不直接修改科技状态。

给空军 / 海军系统：
- 提供飞机、舰船、燃料、人员、港口/机场建设接口。
- 不直接调度空军和海军任务。

给 AI / LLM 系统：
- 输出 EconomySummary。
- 输出 ProductionSummary。
- 输出 ManpowerSummary。
- 输出 ReinforcementShortageSummary。
- 给部长 agent 提供可读的经济建议输入。

给 UI 系统：
- 提供只读经济快照。
- 提供生产队列、库存、人力、资源、建造进度。
- UI 不能直接修改经济状态，只能提交 Command。

9. 输出内容

请输出一份 v1.0 经济 / 生产 / 人力系统设计文档，建议命名：
md/v1_agent4_economy_production_manpower_plan.md

文档必须包含：
- 设计目标
- 国家经济模型
- 资源系统
- 工厂与生产系统
- 装备库存系统
- 人力系统
- 补员与装备流转接口
- 建造系统
- 经济 Command 接口
- 与其他系统的接口契约
- AI 可读取的经济摘要格式
- 测试策略
- 明确哪些内容不由 Agent 4 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON 示例

不要实现：
- UI 美术
- 省份图底层
- 陆战战斗结算
- 完整科技树
- 空军任务
- 海军任务
- LLM prompt
- 多 agent 仲裁

10. 重要约束

- 不要做过度复杂的经济模拟。
- 不要把财政、税收、通胀、贸易做成 v1.0 核心复杂系统。
- 经济系统必须优先服务战争循环。
- 不要让经济系统直接绕过 RuleEngine 修改单位、省份或国家状态。
- 不要把装备生产写死到单个国家。
- 不要让部长 agent 直接改生产队列，必须通过 Command。
- 所有经济变化必须生成 GameEvent，方便日志和回放。

11. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 国家资源如何计算；
- 工厂如何生产装备；
- 装备如何进入库存；
- 人力如何产生和消耗；
- 陆军如何申请补员；
- 经济系统如何响应战斗损失；
- 建造如何绑定省份；
- AI 如何读取经济摘要；
- 玩家和 AI 如何通过 Command 修改生产、建造和补员优先级。
Agent 4 的核心是建立“战争消耗 -> 经济压力 -> 生产分配 -> 补员恢复”的闭环。不要让它做复杂国家经营，先把战争机器跑起来。

星期三17:46






写agent5的提示词




星期三17:47






下面是 Agent 5：科技 / 国家差异 / 装备系统 的提示词。
你是 Agent 5：v1.0 科技 / 国家差异 / 装备系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v1.0 需要实现一个轻量但可扩展的科技、装备和国家差异系统，使德国、盟军、苏联等国家在陆军、空军、海军、工业和指挥风格上产生可感知差异。

你的任务不是制作完整《钢铁雄心4》级别科技树，而是设计 v1.0 可落地的科技与装备系统，为陆战、经济、生产、空军、海军和 AI 决策提供统一接口。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md

如果旧文档与 Agent 1/2/3/4 的 v1.0 架构冲突，以 Agent 1/2/3/4 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计 v1.0 科技系统

请设计或修订以下类型：
- TechnologyState
- TechnologyDefinition
- TechnologyTree
- ResearchSlot
- ResearchProject
- ResearchProgress
- ResearchCategory
- DoctrineTree
- DoctrineNode
- TechnologyModifier
- ResearchEvent

科技分类建议：
- industry
- infantry
- artillery
- armor
- logistics
- air
- naval
- doctrine

v1.0 必须支持：
- 国家拥有若干研究槽。
- 科技需要回合数完成。
- 科技有前置条件。
- 科技完成后解锁装备、修正规则或提升生产效率。
- 科技数据来自 JSON 或数据表。
- 科技效果必须通过 modifier 影响其他系统，不能散落硬编码。

2. 设计装备系统

请设计或修订以下类型：
- EquipmentType
- EquipmentModel
- EquipmentVariant
- EquipmentStats
- EquipmentCost
- EquipmentRequirement
- EquipmentUnlock
- EquipmentStockpileRef
- ObsoleteEquipmentPolicy

装备分类建议：
- infantryEquipment
- artillery
- antiTank
- truck
- lightTank
- mediumTank
- heavyTank
- fighter
- bomber
- transportPlane
- destroyer
- submarine
- convoy

每个装备型号至少包含：
- id
- countryId 或 generic
- equipmentType
- displayName
- techRequired
- year / tier
- combatStats
- productionCost
- requiredResources
- reliability
- speed / armor / piercing 等可选战斗属性
- obsoleteBy

要求：
- 装备型号必须数据驱动。
- 不同国家可以有不同装备名称和数值，例如德国坦克、盟军坦克、苏联坦克。
- v1.0 不做复杂装备设计器，但要预留 variant 接口。
- 老装备不能直接消失，应保留库存，可用于补员或二线部队。

3. 设计国家差异系统

请设计或修订以下类型：
- CountryProfile
- CountryTechnologyProfile
- CountryEquipmentCatalog
- NationalTrait
- NationalModifier
- StartingTechnologySet
- DoctrinePreference

国家差异来源：
- 初始科技不同。
- 初始装备型号不同。
- 工业效率不同。
- 人力修正不同。
- 陆军 / 空军 / 海军 doctrine 倾向不同。
- 将领和部长 agent 的偏好不同。
- 特定国家装备名称和基础属性不同。

v1.0 建议先支持：
- germany
- allies / usa / uk 任选简化
- sovietUnion 可选
- neutral / generic

要求：
- 国家差异不能写死在规则代码里。
- 通过数据表、modifier 和 profile 表达。
- AI agent 可以读取国家优势和短板摘要。

4. 设计科技效果与 modifier 系统

科技完成后可能影响：
- 陆军攻击、防御、组织度、恢复速度。
- 装甲速度、突破、装甲值、穿甲。
- 炮兵火力。
- 补给效率。
- 工厂产出。
- 资源使用效率。
- 生产效率增长。
- 空军战斗力、航程、制空效率。
- 海军战斗力、护航效率。
- 解锁新装备型号。
- 解锁 doctrine 节点。

请设计统一 modifier 表达方式，例如：
- targetSystem
- targetType
- stat
- operation
- value
- condition

要求：
- modifier 必须可组合、可测试。
- 不要让各系统各自发明一套修正格式。
- modifier 应能被 RuleEngine、EconomySystem、CombatSystem 查询。

5. 设计 doctrine / 学说系统

v1.0 可以做轻量 doctrine。

建议分类：
- mobileWarfare
- superiorFirepower
- grandBattleplan
- massAssault
- airSuperiority
- navalInterdiction

每个 doctrine 节点提供：
- 组织度修正
- 突破修正
- 补给修正
- 火炮修正
- 空军支援修正
- 指挥效率修正

要求：
- doctrine 是科技系统的一部分或相邻系统。
- v1.0 不需要完整大树，先做每类 3-5 个节点。
- doctrine 应影响 AI 偏好，例如德国更偏向装甲突破。

6. 定义科技相关 Command

请设计科技系统相关命令，但必须遵守 Agent 1 的 Command 管线。

至少包含：
- StartResearchCommand
- CancelResearchCommand
- ChangeResearchPriorityCommand
- SelectDoctrineCommand
- UnlockEquipmentVariantCommand
- MarkEquipmentObsoleteCommand
- SetProductionEquipmentModelCommand

每个命令必须包含：
- issuedBy
- countryId
- technologyId / equipmentModelId / doctrineId
- payload
- reason
- validation result
- execution result
- generated GameEvent

要求：
- 玩家、AI、部长 agent 使用同一套命令。
- 非法命令只记录失败，不破坏 GameState。
- 科技完成和装备解锁必须生成 GameEvent。

7. 与其他系统的接口边界

给经济 / 生产系统：
- 提供装备生产成本、资源需求、生产效率修正。
- 提供可生产装备列表。
- 提供过时装备策略。
- 不直接修改生产队列。

给陆战系统：
- 提供单位 stats modifier。
- 提供装备属性，例如 armor、piercing、softAttack、hardAttack、speed、reliability。
- 提供 doctrine 对 organization、breakthrough、defense 的修正。
- 不直接结算战斗。

给空军系统：
- 提供飞机型号、航程、制空、轰炸、支援属性。
- 提供空军科技修正。

给海军系统：
- 提供舰船型号、海战、护航、潜艇属性。
- 提供海军科技修正。

给 AI / LLM 系统：
- 输出 TechnologySummary。
- 输出 EquipmentSummary。
- 输出 ResearchRecommendationContext。
- 输出国家优势、短板和可研究方向。
- 不暴露玩家不可知的敌方完整科技，除非情报系统允许。

给 UI 系统：
- 提供只读科技树快照。
- 提供研究进度、可研究项、已解锁装备、过时装备。
- UI 不能直接修改科技状态，只能提交 Command。

8. 输出内容

请输出一份 v1.0 科技 / 国家差异 / 装备系统设计文档，建议命名：
md/v1_agent5_technology_equipment_country_plan.md

文档必须包含：
- 设计目标
- 科技系统模型
- 装备系统模型
- 国家差异系统
- modifier 系统
- doctrine 系统
- 科技和装备 JSON 数据格式
- 科技 Command 接口
- 与其他系统的接口契约
- AI 可读取的科技 / 装备摘要格式
- 测试策略
- 明确哪些内容不由 Agent 5 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON 示例

不要实现：
- UI 美术
- 完整生产系统
- 陆战结算
- 空军任务
- 海军任务
- LLM prompt
- 多 agent 仲裁
- 完整国家国策树

9. 重要约束

- 不要做完整 HOI4 科技树。
- 不要把国家差异写死进战斗公式。
- 不要让科技系统直接修改单位、生产线或战斗结果。
- 科技只能通过解锁、modifier 和 Command 管线影响其他系统。
- 装备必须数据驱动。
- 老装备必须能继续存在于库存。
- 所有科技变化必须生成 GameEvent，方便日志和回放。

10. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 科技如何研究；
- 科技如何解锁装备；
- 装备型号如何定义；
- 国家差异如何表达；
- doctrine 如何影响战斗和 AI 偏好；
- 经济系统如何读取装备成本；
- 陆战系统如何读取装备和科技修正；
- AI 如何读取科技和装备摘要；
- 玩家和 AI 如何通过 Command 修改研究方向。
Agent 5 的重点是建立“科技/装备/国家差异”三者的统一数据层。不要让它深入做战斗或生产，否则会和 Agent 3、4 的边界重叠。

星期三17:48






写agent6的提示词




星期三17:48






下面是 Agent 6：空军 / 天气系统 的提示词。
你是 Agent 6：v1.0 空军 / 天气系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v1.0 需要实现第一版实体化空军和天气系统。空军不需要像陆军一样逐省份移动，而是以机场、空军联队、空域任务的方式影响陆战、海战、侦察、轰炸和补给。天气系统需要影响移动、视野、空军效率和战斗修正。

你的任务不是做完整空战模拟，而是设计 v1.0 可落地、可测试、可扩展的空军与天气系统。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md
- md/v1_agent5_technology_equipment_country_plan.md

如果旧文档与 Agent 1/2/3/4/5 的 v1.0 架构冲突，以 Agent 1/2/3/4/5 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计 v1.0 空军模型

请设计或修订以下类型：
- AirWing
- AirWingTemplate
- AircraftModelRef
- AirBase
- AirZone
- AirMission
- AirMissionAssignment
- AirSuperiorityState
- AirCombatResult
- AirSupportModifier
- AirReconResult
- AirEvent

每个 AirWing 至少包含：
- id
- countryId
- name
- airbaseProvinceId
- aircraftModelId
- aircraftCount
- maxAircraftCount
- manpower
- organization / readiness
- experience
- mission
- targetAirZoneId
- range
- status
- assignedCommanderId，可选

要求：
- 空军联队挂载到机场所在省份。
- 空军任务作用于 AirZone，不是逐格移动。
- AirZone 由 Agent 2 的地图系统提供或引用。
- 飞机型号、航程、攻击、防御、可靠性来自 Agent 5 的装备系统。
- 飞机数量和补员依赖 Agent 4 的生产与库存系统。

2. 设计机场与空域系统

机场：
- 挂载在 ProvinceNode。
- 有等级 / 容量。
- 影响可驻扎飞机数量。
- 可能被战略轰炸破坏。
- 可由建造系统升级或修复。

空域：
- 由多个 ProvinceNode 组成。
- 空军任务以 AirZone 为目标。
- 陆战省份通过 airZoneId 查询空军影响。
- 海域可以通过 seaZoneId / airZoneId 接收空军影响。

请设计：
- AirBaseCapacity
- AirZoneCoverage
- ProvinceAirInfluence
- AirRangeCheck
- AirMissionEfficiency

要求：
- 航程不足时不能有效覆盖目标空域。
- 机场超容量会降低任务效率。
- 空域控制状态必须可被陆战、海军、AI 和 UI 查询。

3. 设计空军任务

v1.0 至少支持：
- airSuperiority：制空
- closeAirSupport：近距支援
- strategicBombing：战略轰炸
- interdiction：阻滞 / 打击移动与补给
- reconnaissance：侦察
- navalStrike：对海攻击，可作为海军系统接口

每种任务需要定义：
- 输入
- 合法性
- 效率计算
- 结算结果
- 生成的 GameEvent
- 对其他系统的影响

建议规则：
- 制空影响敌方空军任务效率。
- 近距支援为陆战提供攻击修正或组织度伤害。
- 战略轰炸破坏工厂、机场、基础设施或补给节点。
- 阻滞降低敌军移动、补给效率或撤退效率。
- 侦察提高可见性和 AI 战场摘要质量。
- 对海攻击影响海域控制、舰队损失或登陆风险。

4. 设计空战与任务结算

请设计简化空战流程。

推荐结算顺序：
- 统计某 AirZone 内双方执行任务的 AirWing。
- 计算任务效率：机场容量、航程、天气、科技、经验、制空状态。
- 先结算制空。
- 再结算轰炸、近距支援、阻滞、侦察、对海攻击。
- 生成 AirCombatResult 和 AirMissionResult。
- 通过 GameEvent 写入日志。
- 损失飞机后向经济系统发出补充请求。

要求：
- v1.0 不做复杂逐架飞机模拟。
- 结果应确定性或使用固定随机种子，便于回放。
- 空军系统不能直接绕过 RuleEngine 修改工厂、省份、单位或舰队。
- 对陆战和海战的影响应输出 modifier 或 result，由对应系统应用。

5. 设计天气系统

请设计或修订以下类型：
- WeatherZone
- WeatherState
- WeatherForecast
- WeatherEffect
- WeatherEvent
- SeasonalPattern

天气状态建议：
- clear
- rain
- storm
- snow
- blizzard
- fog
- mud

天气影响：
- 陆军移动速度
- 陆军攻击 / 防御修正
- 组织度恢复
- 补给效率
- 视野 / 侦察
- 空军任务效率
- 空战损失
- 海军任务效率，可预留

要求：
- 天气按 WeatherZone 结算。
- ProvinceNode 通过 weatherZoneId 获取天气。
- v1.0 可以用简化随机或脚本天气。
- 天气变化必须可重放，使用固定 seed 或写入 GameEvent。
- 天气不能直接修改单位状态，只提供修正或结算事件。

6. 定义空军与天气相关 Command

请设计相关命令，但必须遵守 Agent 1 的 Command 管线。

至少包含：
- CreateAirWingCommand
- DisbandAirWingCommand
- TransferAirWingCommand
- AssignAirMissionCommand
- CancelAirMissionCommand
- SetAirWingReinforcementPriorityCommand
- SetAirZonePriorityCommand
- RepairAirBaseCommand，可与建造系统衔接

每个命令必须包含：
- issuedBy
- countryId
- airWingId / airBaseId / airZoneId
- payload
- reason
- validation result
- execution result
- generated GameEvent

天气通常不由玩家直接命令，但需要：
- WeatherTickEvent
- WeatherChangedEvent
- WeatherEffectAppliedEvent

要求：
- 玩家、AI、空军部长 agent 使用同一套命令。
- 非法命令只记录失败，不破坏 GameState。
- 空军任务结果和天气变化必须可回放。

7. 与其他系统的接口边界

给地图 / 前线系统：
- 读取 ProvinceNode 的 airbase、airZoneId、weatherZoneId。
- 查询前线附近空域，用于近距支援和侦察。
- 不直接修改省份控制权。

给陆战系统：
- 提供 AirSupportModifier。
- 提供敌方制空、近距支援、阻滞、侦察结果。
- 不直接结算陆战。

给经济 / 生产系统：
- 请求飞机补充。
- 上报飞机损失。
- 读取机场修复、建设、飞机库存。
- 不直接修改生产线。

给科技 / 装备系统：
- 读取飞机型号、航程、空战、轰炸、可靠性、任务效率修正。
- 读取天气相关科技或 doctrine 修正。

给海军系统：
- 提供 navalStrike 结果。
- 提供空中侦察和空域覆盖。
- 不直接结算海战细节。

给 AI / LLM 系统：
- 输出 AirSituationSummary。
- 输出 AirZoneSummary。
- 输出 WeatherSummary。
- 输出 BombingDamageSummary。
- 输出 AirSupportAvailabilitySummary。
- 不暴露不可见敌军完整空军部署，除非情报允许。

给 UI 系统：
- 提供只读空军快照。
- 提供空域状态、机场容量、联队任务、天气预报、制空状态。
- UI 不能直接修改空军和天气状态，只能提交 Command。

8. 输出内容

请输出一份 v1.0 空军 / 天气系统设计文档，建议命名：
md/v1_agent6_airforce_weather_plan.md

文档必须包含：
- 设计目标
- 空军单位模型
- 机场与空域模型
- 空军任务系统
- 空战与任务结算流程
- 天气系统模型
- 天气效果表
- 空军 Command 接口
- 与其他系统的接口契约
- AI 可读取的空军 / 天气摘要格式
- 测试策略
- 明确哪些内容不由 Agent 6 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON 示例

不要实现：
- UI 美术
- 省份图底层
- 陆战完整结算
- 经济生产系统
- 科技树
- 海军完整系统
- LLM prompt
- 多 agent 仲裁

9. 重要约束

- 不要把飞机做成像陆军一样逐省份微操的单位。
- 不要做复杂逐架飞机模拟。
- 不要让空军系统直接修改陆军战斗结果、工厂、省份或舰队状态。
- 空军影响必须通过 modifier、result、Command 或 RuleEngine 应用。
- 天气必须可重放。
- 天气和空军效果必须可解释，方便 UI 和 AI 读取。
- 所有空军和天气结算必须生成 GameEvent。

10. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 空军联队如何定义；
- 机场和空域如何工作；
- 空军任务如何分配；
- 制空、近距支援、轰炸、侦察如何影响其他系统；
- 天气如何生成和结算；
- 天气如何影响陆战、空军、补给和视野；
- AI 如何读取空军与天气摘要；
- 玩家和 AI 如何通过 Command 调整空军部署。
Agent 6 的重点是“空域任务 + 天气修正”，不要把空军做成另一个复杂战棋层。

星期三17:49






写agent7的提示词

下面是 Agent 7：简化海军 / 港口 / 海域系统 的提示词。
你是 Agent 7：v1.0 简化海军 / 港口 / 海域系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v1.0 需要实现第一版简化海军系统。海军不做复杂舰船部件和逐舰细节，而是服务大战略核心目标：海域控制、港口补给、护航、拦截、登陆支援、贸易 / 补给线风险。

你的任务不是完整复刻《钢铁雄心4》的海军，而是设计 v1.0 可落地、可测试、可扩展的海军、港口和海域系统。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md
- md/v1_agent5_technology_equipment_country_plan.md
- md/v1_agent6_airforce_weather_plan.md

如果旧文档与 Agent 1-6 的 v1.0 架构冲突，以 Agent 1-6 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计 v1.0 海军模型

请设计或修订以下类型：
- Fleet
- TaskForce
- ShipGroup
- ShipModelRef
- NavalBase
- Port
- SeaZone
- SeaRoute
- NavalMission
- NavalMissionAssignment
- NavalControlState
- NavalCombatResult
- NavalSupplyResult
- NavalEvent

每个 Fleet 至少包含：
- id
- countryId
- name
- homePortProvinceId
- currentSeaZoneId
- taskForces
- mission
- targetSeaZoneIds
- organization / readiness
- fuelNeed，可选
- manpower
- experience
- commanderId，可选
- status

要求：
- 舰队以 SeaZone 为行动区域，不逐格移动。
- 港口挂载到 ProvinceNode。
- 海域由 Agent 2 的地图系统提供或引用。
- 舰船型号、舰船属性、生产成本来自 Agent 5。
- 舰船生产和补充依赖 Agent 4。
- 空军对海攻击和侦察由 Agent 6 提供接口。

2. 设计港口与海域系统

港口：
- 挂载在沿海 ProvinceNode。
- 有等级 / 容量。
- 决定舰队驻扎、维修、登陆、海上补给能力。
- 可被战略轰炸或海军攻击破坏。
- 可由建造系统升级或修复。

海域：
- 由多个 coastal province 或 sea node 组成。
- 海军任务以 SeaZone 为目标。
- 海域之间存在相邻关系。
- 海域可以连接港口、贸易路线、登陆目标。
- 海域可以受到天气和空军覆盖影响。

请设计：
- PortCapacity
- NavalBaseRepairRate
- SeaZoneAdjacency
- SeaRoutePath
- CoastalAccess
- AmphibiousLandingPoint
- NavalRangeCheck

要求：
- 海军规则不能依赖 SpriteKit 或 UI。
- SeaZone id 必须稳定，不能依赖数组下标。
- 港口控制权随 ProvinceNode controller 变化。
- 港口容量和控制权影响补给、维修、登陆。

3. 设计海军任务

v1.0 至少支持：
- convoyEscort：护航
- convoyRaiding：袭击运输线
- patrol：巡逻 / 索敌
- navalInterdiction：海上拦截
- shoreBombardment：岸轰支援
- amphibiousSupport：登陆支援
- navalInvasionTransport：登陆运输
- portStrikeDefense，可选

每种任务需要定义：
- 输入
- 合法性
- 效率计算
- 结算结果
- 生成的 GameEvent
- 对其他系统的影响

建议规则：
- 护航降低运输线损失和海上补给风险。
- 袭击运输线造成装备、人力、资源或补给损失。
- 巡逻提高发现敌舰概率。
- 拦截尝试阻止敌方舰队、登陆或补给。
- 岸轰为沿海陆战提供攻击 / 组织度伤害修正。
- 登陆支援提高登陆成功率。
- 登陆运输允许陆军跨海攻击指定沿海省份。

4. 设计简化海战结算

请设计简化海战流程。

推荐结算顺序：
- 统计同一 SeaZone 内敌对舰队。
- 根据任务、巡逻、侦察、天气、空军覆盖计算发现概率。
- 如果双方接触，生成 NavalEngagement。
- 计算舰队战斗力：舰种、数量、组织度、科技、经验、指挥官、天气、空中支援。
- 结算舰船损失、组织度损失、撤退或继续交战。
- 生成 NavalCombatResult。
- 通过 GameEvent 写入日志。
- 向经济系统上报舰船损失和维修需求。

要求：
- v1.0 不做逐舰炮击、装甲穿透、鱼雷细节。
- 可以按 ShipGroup 结算，例如 destroyerGroup、submarineGroup、capitalShipGroup、convoyGroup。
- 结果应确定性或使用固定随机种子，便于回放。
- 海军系统不能绕过 RuleEngine 直接修改国家、省份、单位或经济状态。

5. 设计海上补给、运输线和登陆

请设计：
- NavalSupplyRoute
- ConvoyNeed
- ConvoyAvailability
- RouteRisk
- BlockadeState
- AmphibiousInvasionPlan
- LandingCombatModifier

海上补给：
- 如果陆军补给线跨海，需要港口、运输船和安全海域。
- 敌方袭击运输线会降低补给效率。
- 制海权不足时，登陆和海上补给风险提高。
- 港口等级影响可承载补给量。

登陆：
- 登陆必须从己方港口出发。
- 目标必须是沿海敌方省份。
- 需要运输船 / convoy。
- 需要准备时间或命令延迟。
- 登陆受海域控制、天气、岸防、空军、岸轰影响。
- 登陆失败会造成组织度和 strength 损失。

要求：
- 登陆不要在 v1.0 做得过细，但必须有合法性、准备、执行、失败结果。
- 海上补给结果应提供给 Agent 3 的补给系统。
- 贸易系统可以先预留，不做完整全球贸易。

6. 定义海军相关 Command

请设计海军相关命令，但必须遵守 Agent 1 的 Command 管线。

至少包含：
- CreateFleetCommand
- DisbandFleetCommand
- TransferFleetCommand
- AssignNavalMissionCommand
- CancelNavalMissionCommand
- SetHomePortCommand
- RepairFleetCommand
- StartNavalInvasionCommand
- CancelNavalInvasionCommand
- AssignConvoyEscortCommand
- SetSeaZonePriorityCommand

每个命令必须包含：
- issuedBy
- countryId
- fleetId / portId / seaZoneId / provinceId
- payload
- reason
- validation result
- execution result
- generated GameEvent

要求：
- 玩家、AI、海军部长 agent 使用同一套命令。
- 非法命令只记录失败，不破坏 GameState。
- 海军任务、海战、登陆和补给结果必须可回放。

7. 与其他系统的接口边界

给地图 / 前线系统：
- 读取 ProvinceNode 的 port、coastal、seaZoneId。
- 读取省份控制权、沿海登陆点、海峡通行限制。
- 不直接修改省份控制权。

给陆战系统：
- 提供 shoreBombardment modifier。
- 提供 amphibiousLanding modifier。
- 提供跨海补给结果。
- 不直接结算陆战。

给经济 / 生产系统：
- 请求舰船、运输船、维修资源。
- 上报舰船损失、运输线损失。
- 读取船坞产出和 convoy 库存。
- 不直接修改生产线。

给科技 / 装备系统：
- 读取舰船型号、海战属性、护航效率、潜艇效率、登陆科技修正。
- 读取舰队组织度、索敌、维修相关 modifier。

给空军 / 天气系统：
- 读取 navalStrike 结果。
- 读取空中侦察、制空、天气对海战和登陆的影响。
- 不直接调度空军任务。

给 AI / LLM 系统：
- 输出 NavalSituationSummary。
- 输出 SeaZoneControlSummary。
- 输出 ConvoyRiskSummary。
- 输出 AmphibiousInvasionSummary。
- 输出 PortCapacitySummary。
- 不暴露不可见敌军完整舰队部署，除非情报允许。

给 UI 系统：
- 提供只读海军快照。
- 提供海域控制、舰队任务、港口容量、登陆计划、运输线风险。
- UI 不能直接修改海军状态，只能提交 Command。

8. 输出内容

请输出一份 v1.0 简化海军 / 港口 / 海域系统设计文档，建议命名：
md/v1_agent7_navy_ports_seazones_plan.md

文档必须包含：
- 设计目标
- 海军单位模型
- 港口与海域模型
- 海军任务系统
- 简化海战结算流程
- 海上补给与运输线规则
- 登陆系统规则
- 海军 Command 接口
- 与其他系统的接口契约
- AI 可读取的海军摘要格式
- 测试策略
- 明确哪些内容不由 Agent 7 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON 示例

不要实现：
- UI 美术
- 省份图底层
- 陆战完整结算
- 经济生产系统
- 科技树
- 空军完整系统
- LLM prompt
- 多 agent 仲裁
- 复杂逐舰海战模拟

9. 重要约束

- 不要做完整 HOI4 海军复刻。
- 不要做逐舰复杂炮击和装甲细节。
- 不要让舰队像陆军一样逐省份移动。
- 不要让海军系统直接修改陆军、省份、经济或科技状态。
- 海军影响必须通过 modifier、result、Command 或 RuleEngine 应用。
- 海上补给、登陆、海域控制必须可解释，方便 UI 和 AI 读取。
- 所有海军结算必须生成 GameEvent，方便日志和回放。

10. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 舰队如何定义；
- 港口和海域如何工作；
- 海军任务如何分配；
- 护航、袭击、巡逻、拦截、岸轰、登陆如何结算；
- 海上补给如何影响陆军；
- 登陆如何准备、执行和失败；
- 空军和天气如何影响海军；
- AI 如何读取海军摘要；
- 玩家和 AI 如何通过 Command 调整海军部署。
Agent 7 的边界要压住：海军只做大战略必要层，不做复杂细节。它的 v1.0 价值主要是让港口、跨海补给、护航、登陆和海域控制成立。

星期三17:51






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三17:53






下面是 Agent 8：多 Agent AI / LLM 决策系统 的提示词。
你是 Agent 8：v1.0 多 Agent AI / LLM 决策系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。核心创新是：将领、元帅、国家统治者、部长等角色都可以是 agent。agent 根据权限内的战场摘要、国家状态、角色性格、职位和历史背景，输出结构化计划或建议，再由规则系统校验、仲裁、拆解和执行。

你的任务不是实现完整聊天角色扮演，也不是让 LLM 直接控制游戏状态。你的任务是设计 v1.0 的多 agent 决策架构、上下文摘要、结构化输出、仲裁机制、MockAI fallback、本地 LLM provider 和决策日志系统。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md
- md/v1_agent5_technology_equipment_country_plan.md
- md/v1_agent6_airforce_weather_plan.md
- md/v1_agent7_navy_ports_seazones_plan.md

如果旧文档与 Agent 1-7 的 v1.0 架构冲突，以 Agent 1-7 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计多 Agent 层级

请设计或修订以下类型：
- AgentProfile
- AgentRole
- AgentAuthority
- AgentPersonality
- AgentMemory
- AgentContext
- AgentDecision
- AgentIntent
- AgentPlan
- AgentDirective
- AgentDecisionRecord
- AgentArbitrationResult
- AgentEvent

v1.0 至少支持这些 agent：
- rulerAgent：国家统治者，总体战略、战争目标、政治方向。
- fieldMarshalAgent：元帅 / 战区司令，负责战区、前线、攻势计划。
- generalAgent：集团军 / 军团将领，负责局部战斗、推进、撤退、补给。
- industryMinisterAgent：军工 / 经济部长，负责生产、建造、补员建议。
- armyMinisterAgent：陆军部长，负责编制、训练、将领任免建议。
- airMinisterAgent：空军部长，负责空军部署、机场、制空和轰炸建议。
- navyMinisterAgent：海军部长，负责舰队、港口、护航和登陆建议。

要求：
- v1.0 可以只实现部分 agent，但架构必须支持上述层级。
- 低层 agent 不应覆盖高层战略目标。
- 高层 agent 不应逐个移动每个单位。
- 每个 agent 只能读取自己权限内的摘要，不读取完整真实 GameState。

2. 设计 Agent 权限与信息边界

请设计：
- AgentAuthorityScope
- InformationAccessLevel
- FogOfWarPolicy
- StrategicSummaryAccess
- TheatreSummaryAccess
- LocalBattleSummaryAccess
- EconomySummaryAccess
- IntelligenceSummaryAccess

信息原则：
- 统治者 agent 可读取国家级摘要和各战区概要。
- 元帅 agent 可读取所属战区、前线、补给、空军支援概要。
- 将领 agent 可读取所属集团军、局部前线、可见敌军、补给状态。
- 部长 agent 可读取对应系统摘要，例如经济、科技、空军、海军。
- 不允许 agent 直接读取不可见敌军真实位置，除非情报系统明确允许。
- LLM prompt 中不能塞完整地图和完整单位列表，必须摘要化。

3. 设计 AgentContext 摘要系统

请定义不同 agent 的上下文格式：

- RulerContext
- FieldMarshalContext
- GeneralContext
- IndustryMinisterContext
- ArmyMinisterContext
- AirMinisterContext
- NavyMinisterContext

每个 context 至少包含：
- agentId
- role
- countryId
- turn
- authorityScope
- activeObjectives
- relevantGameSummary
- recentEvents
- availableCommands
- constraints
- personalityPrompt
- memorySummary

上下文摘要来源：
- Agent 2：ProvinceSummary、FrontSummary、ThreatSummary。
- Agent 3：BattleSummary、UnitCombatSummary、SupplyRiskSummary。
- Agent 4：EconomySummary、ProductionSummary、ManpowerSummary。
- Agent 5：TechnologySummary、EquipmentSummary。
- Agent 6：AirSituationSummary、WeatherSummary。
- Agent 7：NavalSituationSummary、ConvoyRiskSummary。

要求：
- context 必须可序列化为 JSON。
- context 必须可裁剪，适配本地 LLM token 限制。
- context 需要包含“当前不能做什么”的约束，减少非法输出。
- context 需要包含可解释的局势摘要，方便日志和调试。

4. 设计结构化输出 Schema

LLM / MockAI 不能输出散文作为最终命令，必须输出结构化 JSON。

请设计以下输出类型：
- StrategicIntent
- TheatrePlan
- FrontDirective
- BattlePlan
- ProductionAdvice
- ResearchAdvice
- AirDirective
- NavalDirective
- CommanderOrder
- AgentMessage，可选，用于自然语言回复
- AgentDecisionEnvelope

AgentDecisionEnvelope 至少包含：
- schemaVersion
- agentId
- role
- countryId
- turn
- decisionType
- summary
- confidence
- assumptions
- risks
- intents
- proposedCommands
- requiresApproval
- rationale
- fallbackPlan

要求：
- proposedCommands 只是候选命令，不能直接执行。
- 每个候选命令必须经过 CommandValidator。
- intent 和 command 要分层，高层 agent 可以只输出 intent。
- 输出 schema 必须支持 JSON decode 失败、字段缺失、非法命令的处理。

5. 设计决策链路

推荐链路：
PlayerDirective / GameNeed
-> AgentContextBuilder
-> DecisionProvider
-> AgentDecisionEnvelope
-> AgentDecisionParser
-> AgentIntentNormalizer
-> AgentArbitrator
-> CommandCandidate
-> CommandValidator
-> RuleEngine
-> GameEvent
-> AgentDecisionRecord

请定义每一步的输入、输出和失败处理。

要求：
- LLM 超时必须 fallback 到 MockAI 或 hold plan。
- JSON 解析失败不能破坏 GameState。
- 非法命令必须记录原因。
- agent 决策结果必须可回放。
- 每次决策必须保存原始 prompt 摘要、原始响应、解析结果、校验结果和最终执行结果。

6. 设计多 Agent 仲裁机制

多 agent 可能给出冲突建议，例如：
- 统治者要求进攻，元帅认为补给不足。
- 军工部长建议生产坦克，陆军部长要求补充步兵装备。
- 将领请求撤退，但统治者要求坚守。
- 空军部长要战略轰炸，陆军将领需要近距支援。

请设计：
- AgentArbitrator
- DecisionPriority
- AuthorityOverride
- ConflictReason
- ArbitrationPolicy
- FinalCommandSet

仲裁原则：
- 规则合法性优先于角色意愿。
- 高层战略可以约束低层计划，但不能强迫非法命令执行。
- 低层 agent 可以提出异议或风险提示。
- 玩家直接命令优先级高，但可能产生忠诚、满意度或服从后果，该部分由 Agent 9 详细设计。
- 仲裁结果必须可解释并写入 GameEvent。

7. 设计本地 LLM Provider

请设计：
- DecisionProvider protocol
- MockDecisionProvider
- LocalLLMDecisionProvider
- LLMClient
- LLMConfiguration
- PromptBuilder
- ResponseParser
- RetryPolicy
- TimeoutPolicy
- FallbackPolicy

本地 LLM 可支持：
- Ollama
- LM Studio
- llama.cpp server
- OpenAI-compatible local endpoint

要求：
- v1.0 默认必须能用 MockAI 跑通。
- 本地 LLM 连接失败不能阻塞游戏。
- provider 可配置启用或关闭。
- prompt 必须要求 JSON 输出。
- provider 不应知道 UI 细节。
- 不要把 API key 或本地 endpoint 写死。

8. 设计 Agent 记忆系统

请设计轻量记忆：
- shortTermMemory：最近几回合事件、命令、结果。
- longTermMemory：角色偏好、战绩、关系、历史承诺。
- strategicMemory：当前战争目标、长期计划。
- failureMemory：上次失败原因，避免重复非法命令。

要求：
- v1.0 不做复杂向量数据库。
- 记忆应可序列化。
- 记忆摘要可进入 prompt。
- 记忆更新必须通过事件或决策记录驱动。
- 记忆不能成为绕过战争迷雾的作弊通道。

9. 定义 AI / LLM 相关 Command

AI 系统本身不应直接执行游戏动作，但需要一些管理命令。

至少包含：
- SetAgentObjectiveCommand
- AssignAgentToTheatreCommand
- AssignCommanderToArmyCommand
- RequestAgentPlanCommand
- ApproveAgentPlanCommand
- RejectAgentPlanCommand
- SetAgentAutonomyCommand
- SetDecisionProviderCommand
- ClearAgentMemoryCommand，可作为调试命令

每个命令必须包含：
- issuedBy
- countryId
- agentId
- payload
- reason
- validation result
- execution result
- generated GameEvent

要求：
- agent 产生的军事、经济、科技、空军、海军行动仍然使用对应系统的 Command。
- AI 管理命令只管理 agent 状态、目标和权限。
- 所有变更必须可回放。

10. 与其他系统的接口边界

给地图 / 前线系统：
- 读取 FrontSummary、ProvinceSummary、ThreatSummary。
- 输出 FrontDirective、OffensiveIntent。
- 不直接修改省份控制权或前线状态。

给陆战系统：
- 读取 UnitCombatSummary、BattleSummary、SupplyRiskSummary。
- 输出 AttackIntent、RetreatIntent、ReorganizeIntent。
- 不直接结算战斗。

给经济 / 生产系统：
- 读取 EconomySummary、ProductionSummary、ManpowerSummary。
- 输出 ProductionAdvice、ReinforcementPriorityIntent。
- 不直接修改生产线。

给科技 / 装备系统：
- 读取 TechnologySummary、EquipmentSummary。
- 输出 ResearchAdvice、EquipmentPreferenceIntent。
- 不直接完成研究或解锁装备。

给空军 / 天气系统：
- 读取 AirSituationSummary、WeatherSummary。
- 输出 AirDirective。
- 不直接调度空军任务。

给海军系统：
- 读取 NavalSituationSummary、ConvoyRiskSummary。
- 输出 NavalDirective。
- 不直接调度舰队。

给聊天 / 角色扮演系统：
- 接收 PlayerDirective。
- 输出 agent 自然语言回复和结构化计划。
- 忠诚、满意度、抗命等深层角色后果由 Agent 9 负责。

给 UI 系统：
- 提供 AgentDecisionRecord、AgentContext 摘要、原始 JSON、校验结果。
- UI 不能直接修改 agent 决策结果，只能提交 Command。

11. 输出内容

请输出一份 v1.0 多 Agent AI / LLM 决策系统设计文档，建议命名：
md/v1_agent8_multi_agent_ai_llm_plan.md

文档必须包含：
- 设计目标
- 多 agent 层级
- agent 权限与信息边界
- AgentContext 摘要系统
- 结构化输出 JSON schema
- 决策链路
- 仲裁机制
- 本地 LLM provider 设计
- MockAI fallback 策略
- Agent 记忆系统
- AI 管理 Command 接口
- 与其他系统的接口契约
- 日志、回放和调试策略
- 测试策略
- 明确哪些内容不由 Agent 8 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON schema 示例
- prompt 模板示例

不要实现：
- UI 美术
- 省份图底层
- 陆战结算
- 经济生产
- 科技树
- 空军任务结算
- 海军任务结算
- 深层聊天角色扮演
- 政变、派系、忠诚完整系统

12. 重要约束

- 不要让 LLM 直接修改 GameState。
- 不要让 LLM 逐个控制所有单位的底层移动。
- 不要把完整地图真相塞给每个 agent。
- 不要让多个 agent 同时覆盖同一批单位而无仲裁。
- 不要依赖真实 LLM 才能运行游戏。
- 不要把自然语言回复当作可执行命令。
- 所有 agent 决策必须可记录、可解释、可回放。
- 所有可执行行为必须进入 Command -> Validation -> RuleEngine -> GameEvent 管线。

13. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 有哪些 agent 角色；
- 每个 agent 能看什么；
- 每个 agent 能输出什么；
- LLM 输出 JSON schema 是什么；
- MockAI 和本地 LLM 如何替换；
- 多 agent 冲突如何仲裁；
- agent 决策如何转成 Command 候选；
- 非法输出如何处理；
- 决策日志如何保存；
- AI 系统如何与前线、陆战、经济、科技、空军、海军、聊天和 UI 对接。
还没写完。下一个是 Agent 9：聊天指挥 / 角色扮演系统。

星期三17:54






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三17:54






下面是 Agent 9：聊天指挥 / 角色扮演系统 的提示词。
你是 Agent 9：v1.0 聊天指挥 / 角色扮演系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。核心特色之一是玩家不只点击按钮微操，还可以通过聊天框向统治者、元帅、将领、部长下达自然语言命令；同时角色有职位、性格、忠诚、满意度、服从意愿和抗命风险。玩家未来可以扮演统治者、元帅、将领甚至部长，从不同权力层级参与战争。

你的任务不是实现完整政治模拟，也不是实现 UI 聊天界面美术。你的任务是设计 v1.0 的聊天指挥、玩家身份、角色服从、抗命、关系和角色扮演规则，让自然语言命令能稳定转成结构化 PlayerDirective，并进入 Agent 8 的多 agent 决策系统和 Agent 1 的 Command 管线。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md
- md/v1_agent5_technology_equipment_country_plan.md
- md/v1_agent6_airforce_weather_plan.md
- md/v1_agent7_navy_ports_seazones_plan.md
- md/v1_agent8_multi_agent_ai_llm_plan.md

如果旧文档与 Agent 1-8 的 v1.0 架构冲突，以 Agent 1-8 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计玩家身份系统

请设计或修订以下类型：
- PlayerRole
- PlayerAuthority
- PlayerPosition
- PlayerCommandScope
- PlayerDirective
- PlayerDirectiveTarget
- PlayerOverride
- PlayerReputation
- PlayerRoleEvent

v1.0 至少支持这些玩家身份：
- ruler：国家统治者，可以制定大战略、任免高层、调整经济和战争目标。
- fieldMarshal：元帅 / 战区司令，可以指挥战区、前线和多个集团军。
- general：将领，可以指挥所属集团军或部队，需服从上级命令。
- minister：部长，可以建议或管理经济、科技、空军、海军等系统。

要求：
- v1.0 可以默认玩家是 ruler，但架构必须支持其他身份。
- 玩家身份决定能命令谁、能看到什么、能直接微操什么。
- 玩家越权命令可以被允许，但要产生关系、服从、政治或声望后果。
- 玩家角色系统不能绕过 RuleEngine。

2. 设计聊天输入到结构化指令的转换

自然语言示例：
“古德里安，三周内突破阿登，优先夺取巴斯托涅，必要时绕过坚固城市。”
“陆军部长，停止扩编，优先补充前线步兵装备。”
“隆美尔，守住北非港口，如果补给低于 40% 就撤退。”

请设计：
- ChatMessage
- ChatThread
- ParsedDirective
- DirectiveIntent
- DirectiveConstraint
- DirectivePriority
- DirectiveTimeframe
- DirectiveConfidence
- DirectiveParseResult

转换链路建议：
ChatMessage
-> DirectiveParser
-> PlayerDirective
-> TargetResolver
-> AuthorityValidator
-> Agent 8 AgentContext / AgentDecision
-> CommandCandidate
-> CommandValidator
-> RuleEngine

要求：
- 聊天文本不能直接作为可执行命令。
- 解析失败时要返回澄清请求或低风险 fallback。
- 指令必须包含目标、意图、约束、优先级、时间范围。
- 指令可以是战略级、战区级、集团军级、经济级、科技级、空军级、海军级。
- 指令应能进入 Agent 8 的多 agent 决策系统。

3. 设计角色关系与服从系统

请设计或修订以下类型：
- CharacterRelationship
- LoyaltyState
- SatisfactionState
- TrustState
- ObedienceScore
- AutonomyPreference
- InsubordinationRisk
- CommandPressure
- RelationshipEvent

影响服从的因素：
- 玩家身份和正式权限。
- 命令是否符合角色职责。
- 命令是否符合角色性格。
- 命令风险是否过高。
- 命令是否与上级战略冲突。
- 角色忠诚度。
- 角色满意度。
- 玩家是否频繁越级微操。
- 最近战绩、补给状况、伤亡、失败责任。
- 角色历史偏好，例如古德里安偏好装甲突破。

服从结果建议：
- accept：接受命令。
- acceptWithConcern：接受但提出风险。
- requestClarification：要求澄清。
- delay：拖延或降低执行优先级。
- modifyPlan：按自己判断修改执行方案。
- refuse：拒绝执行。
- appealToSuperior：请求上级裁决。
- covertResistance：表面接受但执行效率降低，v1.0 可只预留。

要求：
- 抗命不能直接破坏规则系统。
- 抗命表现为不生成命令、降低优先级、请求确认、修改计划或产生事件。
- v1.0 不做完整政变模拟，但要预留 politicalRisk / coupRisk 接口。

4. 设计角色性格与命令反应

请设计：
- CharacterProfile
- PersonalityTrait
- CommandPreference
- RiskTolerance
- DoctrinePreference
- CommunicationStyle
- HistoricalBias

示例：
古德里安：
- 偏好装甲突破。
- 高进攻性。
- 高风险承受。
- 不喜欢把装甲分散填线。
- 面对保守命令可能提出异议。
- 面对不合理微操可能满意度下降。

要求：
- 性格影响回复语气、计划倾向、服从概率和异议类型。
- 性格不能直接让角色获得非法能力。
- 性格数据应可序列化，方便 JSON 配置。
- Agent 8 的 prompt 可以读取性格摘要，但最终决策仍要结构化。

5. 设计玩家微操与越级指挥规则

玩家可能直接操作前线部队，也可能绕过将领下命令。

请设计：
- DirectControlAction
- MicromanagementLevel
- CommandInterference
- ChainOfCommandViolation
- CommanderReaction
- AuthorityCost

规则建议：
- 玩家作为 ruler 直接微操师级部队属于越级指挥。
- 越级指挥可以提高短期控制，但降低相关将领满意度和自主性。
- 玩家作为 general 时，不能直接命令其他战区，除非获得授权。
- 玩家频繁推翻 agent 计划，会降低该 agent 的信任。
- 高忠诚角色更能接受玩家干预。
- 强势或自负角色更容易抗议。

要求：
- v1.0 只做轻量后果：满意度、信任、服从评分变化。
- 不要把政治系统做深。
- 所有后果必须通过 GameEvent 记录。

6. 设计任免、调动和职责系统

请设计：
- Appointment
- CommandAssignment
- Dismissal
- Promotion
- Demotion
- TransferOrder
- PositionVacancy
- AppointmentEvent

支持：
- 任命将领到集团军。
- 任命元帅到战区。
- 任命部长到内阁位置。
- 解除职务。
- 调动将领。
- 晋升和降级接口预留。

要求：
- 任免会影响忠诚、满意度、声望和指挥链。
- 不同职位有不同权限。
- 被撤职或降级可能产生不满。
- v1.0 不做复杂派系政治，但要预留 faction / clique 字段。

7. 定义聊天与角色相关 Command

请设计相关命令，但必须遵守 Agent 1 的 Command 管线。

至少包含：
- SendChatMessageCommand
- IssuePlayerDirectiveCommand
- ClarifyDirectiveCommand
- CancelDirectiveCommand
- ApproveAgentPlanCommand
- RejectAgentPlanCommand
- OverrideAgentPlanCommand
- SetPlayerRoleCommand
- AppointCharacterCommand
- DismissCharacterCommand
- TransferCommanderCommand
- SetCommanderAutonomyCommand
- DirectControlUnitCommand
- RevokeDirectControlCommand

每个命令必须包含：
- issuedBy
- countryId
- playerRole
- targetCharacterId / targetAgentId / targetScope
- payload
- reason
- validation result
- execution result
- generated GameEvent

要求：
- 玩家聊天、玩家直接指令和 agent 计划审批都必须进入 Command 管线。
- 非法命令只记录失败，不破坏 GameState。
- 角色关系变化必须生成 GameEvent。
- 自然语言回复不是 Command，必须与结构化指令分离。

8. 与其他系统的接口边界

给 Agent 8 多 Agent AI 系统：
- 提供 PlayerDirective。
- 提供角色关系、服从、满意度、性格摘要。
- 接收 AgentDecisionEnvelope、AgentMessage、计划风险。
- 不直接执行 Agent 8 的计划。

给地图 / 前线系统：
- 将前线、战区、省份目标作为 directive target。
- 不直接修改前线或省份控制权。

给陆战系统：
- 将进攻、防守、撤退、休整、补员优先级作为 directive。
- 不直接结算战斗。

给经济 / 生产系统：
- 将生产、补员、建造优先级作为 directive。
- 不直接修改生产线。

给科技 / 装备系统：
- 将研究方向、装备偏好作为 directive。
- 不直接完成研究。

给空军 / 海军系统：
- 将空军任务、海军任务、登陆计划作为 directive。
- 不直接调度空军或舰队。

给 UI 系统：
- 提供聊天记录、角色回复、指令解析结果、权限警告、服从风险、关系变化。
- UI 只提交 Command，不直接改角色状态。

9. 输出内容

请输出一份 v1.0 聊天指挥 / 角色扮演系统设计文档，建议命名：
md/v1_agent9_chat_roleplay_command_plan.md

文档必须包含：
- 设计目标
- 玩家身份系统
- 聊天到 PlayerDirective 的转换流程
- 结构化 PlayerDirective schema
- 角色关系与服从系统
- 性格与命令反应规则
- 微操与越级指挥规则
- 任免、调动和职责系统
- 聊天与角色 Command 接口
- 与其他系统的接口契约
- AI 可读取的角色 / 指令摘要格式
- 日志、回放和调试策略
- 测试策略
- 明确哪些内容不由 Agent 9 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON schema 示例
- 聊天解析示例

不要实现：
- UI 美术
- 完整聊天界面
- LLM provider 底层
- 多 agent 仲裁核心
- 陆战结算
- 经济生产
- 科技树
- 空军 / 海军结算
- 完整政变、派系和内战系统

10. 重要约束

- 不要让自然语言直接执行游戏动作。
- 不要让聊天系统绕过 Agent 8 和 RuleEngine。
- 不要把角色扮演系统做成复杂政治模拟。
- 不要让抗命直接改 GameState 或破坏规则。
- 不要让 UI 直接修改角色关系。
- 玩家越权可以存在，但必须有权限校验和后果记录。
- 所有聊天、指令、关系变化必须可记录、可解释、可回放。

11. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 玩家可以扮演哪些身份；
- 聊天文本如何转成结构化指令；
- 指令如何进入 Agent 8 决策系统；
- 角色为什么接受、质疑、拖延或拒绝命令；
- 玩家微操如何影响将领关系；
- 任免和调动如何影响指挥链；
- UI 应显示哪些聊天和角色状态；
- 哪些行为必须通过 Command 管线。
还没写完。下一个是 Agent 10：UI / UX / 可观察性。

星期三17:56






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写

下面是 Agent 7：简化海军 / 港口 / 海域系统 的提示词。
你是 Agent 7：v1.0 简化海军 / 港口 / 海域系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v1.0 需要实现第一版简化海军系统。海军不做复杂舰船部件和逐舰细节，而是服务大战略核心目标：海域控制、港口补给、护航、拦截、登陆支援、贸易 / 补给线风险。

你的任务不是完整复刻《钢铁雄心4》的海军，而是设计 v1.0 可落地、可测试、可扩展的海军、港口和海域系统。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md
- md/v1_agent5_technology_equipment_country_plan.md
- md/v1_agent6_airforce_weather_plan.md

如果旧文档与 Agent 1-6 的 v1.0 架构冲突，以 Agent 1-6 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计 v1.0 海军模型

请设计或修订以下类型：
- Fleet
- TaskForce
- ShipGroup
- ShipModelRef
- NavalBase
- Port
- SeaZone
- SeaRoute
- NavalMission
- NavalMissionAssignment
- NavalControlState
- NavalCombatResult
- NavalSupplyResult
- NavalEvent

每个 Fleet 至少包含：
- id
- countryId
- name
- homePortProvinceId
- currentSeaZoneId
- taskForces
- mission
- targetSeaZoneIds
- organization / readiness
- fuelNeed，可选
- manpower
- experience
- commanderId，可选
- status

要求：
- 舰队以 SeaZone 为行动区域，不逐格移动。
- 港口挂载到 ProvinceNode。
- 海域由 Agent 2 的地图系统提供或引用。
- 舰船型号、舰船属性、生产成本来自 Agent 5。
- 舰船生产和补充依赖 Agent 4。
- 空军对海攻击和侦察由 Agent 6 提供接口。

2. 设计港口与海域系统

港口：
- 挂载在沿海 ProvinceNode。
- 有等级 / 容量。
- 决定舰队驻扎、维修、登陆、海上补给能力。
- 可被战略轰炸或海军攻击破坏。
- 可由建造系统升级或修复。

海域：
- 由多个 coastal province 或 sea node 组成。
- 海军任务以 SeaZone 为目标。
- 海域之间存在相邻关系。
- 海域可以连接港口、贸易路线、登陆目标。
- 海域可以受到天气和空军覆盖影响。

请设计：
- PortCapacity
- NavalBaseRepairRate
- SeaZoneAdjacency
- SeaRoutePath
- CoastalAccess
- AmphibiousLandingPoint
- NavalRangeCheck

要求：
- 海军规则不能依赖 SpriteKit 或 UI。
- SeaZone id 必须稳定，不能依赖数组下标。
- 港口控制权随 ProvinceNode controller 变化。
- 港口容量和控制权影响补给、维修、登陆。

3. 设计海军任务

v1.0 至少支持：
- convoyEscort：护航
- convoyRaiding：袭击运输线
- patrol：巡逻 / 索敌
- navalInterdiction：海上拦截
- shoreBombardment：岸轰支援
- amphibiousSupport：登陆支援
- navalInvasionTransport：登陆运输
- portStrikeDefense，可选

每种任务需要定义：
- 输入
- 合法性
- 效率计算
- 结算结果
- 生成的 GameEvent
- 对其他系统的影响

建议规则：
- 护航降低运输线损失和海上补给风险。
- 袭击运输线造成装备、人力、资源或补给损失。
- 巡逻提高发现敌舰概率。
- 拦截尝试阻止敌方舰队、登陆或补给。
- 岸轰为沿海陆战提供攻击 / 组织度伤害修正。
- 登陆支援提高登陆成功率。
- 登陆运输允许陆军跨海攻击指定沿海省份。

4. 设计简化海战结算

请设计简化海战流程。

推荐结算顺序：
- 统计同一 SeaZone 内敌对舰队。
- 根据任务、巡逻、侦察、天气、空军覆盖计算发现概率。
- 如果双方接触，生成 NavalEngagement。
- 计算舰队战斗力：舰种、数量、组织度、科技、经验、指挥官、天气、空中支援。
- 结算舰船损失、组织度损失、撤退或继续交战。
- 生成 NavalCombatResult。
- 通过 GameEvent 写入日志。
- 向经济系统上报舰船损失和维修需求。

要求：
- v1.0 不做逐舰炮击、装甲穿透、鱼雷细节。
- 可以按 ShipGroup 结算，例如 destroyerGroup、submarineGroup、capitalShipGroup、convoyGroup。
- 结果应确定性或使用固定随机种子，便于回放。
- 海军系统不能绕过 RuleEngine 直接修改国家、省份、单位或经济状态。

5. 设计海上补给、运输线和登陆

请设计：
- NavalSupplyRoute
- ConvoyNeed
- ConvoyAvailability
- RouteRisk
- BlockadeState
- AmphibiousInvasionPlan
- LandingCombatModifier

海上补给：
- 如果陆军补给线跨海，需要港口、运输船和安全海域。
- 敌方袭击运输线会降低补给效率。
- 制海权不足时，登陆和海上补给风险提高。
- 港口等级影响可承载补给量。

登陆：
- 登陆必须从己方港口出发。
- 目标必须是沿海敌方省份。
- 需要运输船 / convoy。
- 需要准备时间或命令延迟。
- 登陆受海域控制、天气、岸防、空军、岸轰影响。
- 登陆失败会造成组织度和 strength 损失。

要求：
- 登陆不要在 v1.0 做得过细，但必须有合法性、准备、执行、失败结果。
- 海上补给结果应提供给 Agent 3 的补给系统。
- 贸易系统可以先预留，不做完整全球贸易。

6. 定义海军相关 Command

请设计海军相关命令，但必须遵守 Agent 1 的 Command 管线。

至少包含：
- CreateFleetCommand
- DisbandFleetCommand
- TransferFleetCommand
- AssignNavalMissionCommand
- CancelNavalMissionCommand
- SetHomePortCommand
- RepairFleetCommand
- StartNavalInvasionCommand
- CancelNavalInvasionCommand
- AssignConvoyEscortCommand
- SetSeaZonePriorityCommand

每个命令必须包含：
- issuedBy
- countryId
- fleetId / portId / seaZoneId / provinceId
- payload
- reason
- validation result
- execution result
- generated GameEvent

要求：
- 玩家、AI、海军部长 agent 使用同一套命令。
- 非法命令只记录失败，不破坏 GameState。
- 海军任务、海战、登陆和补给结果必须可回放。

7. 与其他系统的接口边界

给地图 / 前线系统：
- 读取 ProvinceNode 的 port、coastal、seaZoneId。
- 读取省份控制权、沿海登陆点、海峡通行限制。
- 不直接修改省份控制权。

给陆战系统：
- 提供 shoreBombardment modifier。
- 提供 amphibiousLanding modifier。
- 提供跨海补给结果。
- 不直接结算陆战。

给经济 / 生产系统：
- 请求舰船、运输船、维修资源。
- 上报舰船损失、运输线损失。
- 读取船坞产出和 convoy 库存。
- 不直接修改生产线。

给科技 / 装备系统：
- 读取舰船型号、海战属性、护航效率、潜艇效率、登陆科技修正。
- 读取舰队组织度、索敌、维修相关 modifier。

给空军 / 天气系统：
- 读取 navalStrike 结果。
- 读取空中侦察、制空、天气对海战和登陆的影响。
- 不直接调度空军任务。

给 AI / LLM 系统：
- 输出 NavalSituationSummary。
- 输出 SeaZoneControlSummary。
- 输出 ConvoyRiskSummary。
- 输出 AmphibiousInvasionSummary。
- 输出 PortCapacitySummary。
- 不暴露不可见敌军完整舰队部署，除非情报允许。

给 UI 系统：
- 提供只读海军快照。
- 提供海域控制、舰队任务、港口容量、登陆计划、运输线风险。
- UI 不能直接修改海军状态，只能提交 Command。

8. 输出内容

请输出一份 v1.0 简化海军 / 港口 / 海域系统设计文档，建议命名：
md/v1_agent7_navy_ports_seazones_plan.md

文档必须包含：
- 设计目标
- 海军单位模型
- 港口与海域模型
- 海军任务系统
- 简化海战结算流程
- 海上补给与运输线规则
- 登陆系统规则
- 海军 Command 接口
- 与其他系统的接口契约
- AI 可读取的海军摘要格式
- 测试策略
- 明确哪些内容不由 Agent 7 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON 示例

不要实现：
- UI 美术
- 省份图底层
- 陆战完整结算
- 经济生产系统
- 科技树
- 空军完整系统
- LLM prompt
- 多 agent 仲裁
- 复杂逐舰海战模拟

9. 重要约束

- 不要做完整 HOI4 海军复刻。
- 不要做逐舰复杂炮击和装甲细节。
- 不要让舰队像陆军一样逐省份移动。
- 不要让海军系统直接修改陆军、省份、经济或科技状态。
- 海军影响必须通过 modifier、result、Command 或 RuleEngine 应用。
- 海上补给、登陆、海域控制必须可解释，方便 UI 和 AI 读取。
- 所有海军结算必须生成 GameEvent，方便日志和回放。

10. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 舰队如何定义；
- 港口和海域如何工作；
- 海军任务如何分配；
- 护航、袭击、巡逻、拦截、岸轰、登陆如何结算；
- 海上补给如何影响陆军；
- 登陆如何准备、执行和失败；
- 空军和天气如何影响海军；
- AI 如何读取海军摘要；
- 玩家和 AI 如何通过 Command 调整海军部署。
Agent 7 的边界要压住：海军只做大战略必要层，不做复杂细节。它的 v1.0 价值主要是让港口、跨海补给、护航、登陆和海域控制成立。

星期三17:51






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三17:53






下面是 Agent 8：多 Agent AI / LLM 决策系统 的提示词。
你是 Agent 8：v1.0 多 Agent AI / LLM 决策系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。核心创新是：将领、元帅、国家统治者、部长等角色都可以是 agent。agent 根据权限内的战场摘要、国家状态、角色性格、职位和历史背景，输出结构化计划或建议，再由规则系统校验、仲裁、拆解和执行。

你的任务不是实现完整聊天角色扮演，也不是让 LLM 直接控制游戏状态。你的任务是设计 v1.0 的多 agent 决策架构、上下文摘要、结构化输出、仲裁机制、MockAI fallback、本地 LLM provider 和决策日志系统。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md
- md/v1_agent5_technology_equipment_country_plan.md
- md/v1_agent6_airforce_weather_plan.md
- md/v1_agent7_navy_ports_seazones_plan.md

如果旧文档与 Agent 1-7 的 v1.0 架构冲突，以 Agent 1-7 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计多 Agent 层级

请设计或修订以下类型：
- AgentProfile
- AgentRole
- AgentAuthority
- AgentPersonality
- AgentMemory
- AgentContext
- AgentDecision
- AgentIntent
- AgentPlan
- AgentDirective
- AgentDecisionRecord
- AgentArbitrationResult
- AgentEvent

v1.0 至少支持这些 agent：
- rulerAgent：国家统治者，总体战略、战争目标、政治方向。
- fieldMarshalAgent：元帅 / 战区司令，负责战区、前线、攻势计划。
- generalAgent：集团军 / 军团将领，负责局部战斗、推进、撤退、补给。
- industryMinisterAgent：军工 / 经济部长，负责生产、建造、补员建议。
- armyMinisterAgent：陆军部长，负责编制、训练、将领任免建议。
- airMinisterAgent：空军部长，负责空军部署、机场、制空和轰炸建议。
- navyMinisterAgent：海军部长，负责舰队、港口、护航和登陆建议。

要求：
- v1.0 可以只实现部分 agent，但架构必须支持上述层级。
- 低层 agent 不应覆盖高层战略目标。
- 高层 agent 不应逐个移动每个单位。
- 每个 agent 只能读取自己权限内的摘要，不读取完整真实 GameState。

2. 设计 Agent 权限与信息边界

请设计：
- AgentAuthorityScope
- InformationAccessLevel
- FogOfWarPolicy
- StrategicSummaryAccess
- TheatreSummaryAccess
- LocalBattleSummaryAccess
- EconomySummaryAccess
- IntelligenceSummaryAccess

信息原则：
- 统治者 agent 可读取国家级摘要和各战区概要。
- 元帅 agent 可读取所属战区、前线、补给、空军支援概要。
- 将领 agent 可读取所属集团军、局部前线、可见敌军、补给状态。
- 部长 agent 可读取对应系统摘要，例如经济、科技、空军、海军。
- 不允许 agent 直接读取不可见敌军真实位置，除非情报系统明确允许。
- LLM prompt 中不能塞完整地图和完整单位列表，必须摘要化。

3. 设计 AgentContext 摘要系统

请定义不同 agent 的上下文格式：

- RulerContext
- FieldMarshalContext
- GeneralContext
- IndustryMinisterContext
- ArmyMinisterContext
- AirMinisterContext
- NavyMinisterContext

每个 context 至少包含：
- agentId
- role
- countryId
- turn
- authorityScope
- activeObjectives
- relevantGameSummary
- recentEvents
- availableCommands
- constraints
- personalityPrompt
- memorySummary

上下文摘要来源：
- Agent 2：ProvinceSummary、FrontSummary、ThreatSummary。
- Agent 3：BattleSummary、UnitCombatSummary、SupplyRiskSummary。
- Agent 4：EconomySummary、ProductionSummary、ManpowerSummary。
- Agent 5：TechnologySummary、EquipmentSummary。
- Agent 6：AirSituationSummary、WeatherSummary。
- Agent 7：NavalSituationSummary、ConvoyRiskSummary。

要求：
- context 必须可序列化为 JSON。
- context 必须可裁剪，适配本地 LLM token 限制。
- context 需要包含“当前不能做什么”的约束，减少非法输出。
- context 需要包含可解释的局势摘要，方便日志和调试。

4. 设计结构化输出 Schema

LLM / MockAI 不能输出散文作为最终命令，必须输出结构化 JSON。

请设计以下输出类型：
- StrategicIntent
- TheatrePlan
- FrontDirective
- BattlePlan
- ProductionAdvice
- ResearchAdvice
- AirDirective
- NavalDirective
- CommanderOrder
- AgentMessage，可选，用于自然语言回复
- AgentDecisionEnvelope

AgentDecisionEnvelope 至少包含：
- schemaVersion
- agentId
- role
- countryId
- turn
- decisionType
- summary
- confidence
- assumptions
- risks
- intents
- proposedCommands
- requiresApproval
- rationale
- fallbackPlan

要求：
- proposedCommands 只是候选命令，不能直接执行。
- 每个候选命令必须经过 CommandValidator。
- intent 和 command 要分层，高层 agent 可以只输出 intent。
- 输出 schema 必须支持 JSON decode 失败、字段缺失、非法命令的处理。

5. 设计决策链路

推荐链路：
PlayerDirective / GameNeed
-> AgentContextBuilder
-> DecisionProvider
-> AgentDecisionEnvelope
-> AgentDecisionParser
-> AgentIntentNormalizer
-> AgentArbitrator
-> CommandCandidate
-> CommandValidator
-> RuleEngine
-> GameEvent
-> AgentDecisionRecord

请定义每一步的输入、输出和失败处理。

要求：
- LLM 超时必须 fallback 到 MockAI 或 hold plan。
- JSON 解析失败不能破坏 GameState。
- 非法命令必须记录原因。
- agent 决策结果必须可回放。
- 每次决策必须保存原始 prompt 摘要、原始响应、解析结果、校验结果和最终执行结果。

6. 设计多 Agent 仲裁机制

多 agent 可能给出冲突建议，例如：
- 统治者要求进攻，元帅认为补给不足。
- 军工部长建议生产坦克，陆军部长要求补充步兵装备。
- 将领请求撤退，但统治者要求坚守。
- 空军部长要战略轰炸，陆军将领需要近距支援。

请设计：
- AgentArbitrator
- DecisionPriority
- AuthorityOverride
- ConflictReason
- ArbitrationPolicy
- FinalCommandSet

仲裁原则：
- 规则合法性优先于角色意愿。
- 高层战略可以约束低层计划，但不能强迫非法命令执行。
- 低层 agent 可以提出异议或风险提示。
- 玩家直接命令优先级高，但可能产生忠诚、满意度或服从后果，该部分由 Agent 9 详细设计。
- 仲裁结果必须可解释并写入 GameEvent。

7. 设计本地 LLM Provider

请设计：
- DecisionProvider protocol
- MockDecisionProvider
- LocalLLMDecisionProvider
- LLMClient
- LLMConfiguration
- PromptBuilder
- ResponseParser
- RetryPolicy
- TimeoutPolicy
- FallbackPolicy

本地 LLM 可支持：
- Ollama
- LM Studio
- llama.cpp server
- OpenAI-compatible local endpoint

要求：
- v1.0 默认必须能用 MockAI 跑通。
- 本地 LLM 连接失败不能阻塞游戏。
- provider 可配置启用或关闭。
- prompt 必须要求 JSON 输出。
- provider 不应知道 UI 细节。
- 不要把 API key 或本地 endpoint 写死。

8. 设计 Agent 记忆系统

请设计轻量记忆：
- shortTermMemory：最近几回合事件、命令、结果。
- longTermMemory：角色偏好、战绩、关系、历史承诺。
- strategicMemory：当前战争目标、长期计划。
- failureMemory：上次失败原因，避免重复非法命令。

要求：
- v1.0 不做复杂向量数据库。
- 记忆应可序列化。
- 记忆摘要可进入 prompt。
- 记忆更新必须通过事件或决策记录驱动。
- 记忆不能成为绕过战争迷雾的作弊通道。

9. 定义 AI / LLM 相关 Command

AI 系统本身不应直接执行游戏动作，但需要一些管理命令。

至少包含：
- SetAgentObjectiveCommand
- AssignAgentToTheatreCommand
- AssignCommanderToArmyCommand
- RequestAgentPlanCommand
- ApproveAgentPlanCommand
- RejectAgentPlanCommand
- SetAgentAutonomyCommand
- SetDecisionProviderCommand
- ClearAgentMemoryCommand，可作为调试命令

每个命令必须包含：
- issuedBy
- countryId
- agentId
- payload
- reason
- validation result
- execution result
- generated GameEvent

要求：
- agent 产生的军事、经济、科技、空军、海军行动仍然使用对应系统的 Command。
- AI 管理命令只管理 agent 状态、目标和权限。
- 所有变更必须可回放。

10. 与其他系统的接口边界

给地图 / 前线系统：
- 读取 FrontSummary、ProvinceSummary、ThreatSummary。
- 输出 FrontDirective、OffensiveIntent。
- 不直接修改省份控制权或前线状态。

给陆战系统：
- 读取 UnitCombatSummary、BattleSummary、SupplyRiskSummary。
- 输出 AttackIntent、RetreatIntent、ReorganizeIntent。
- 不直接结算战斗。

给经济 / 生产系统：
- 读取 EconomySummary、ProductionSummary、ManpowerSummary。
- 输出 ProductionAdvice、ReinforcementPriorityIntent。
- 不直接修改生产线。

给科技 / 装备系统：
- 读取 TechnologySummary、EquipmentSummary。
- 输出 ResearchAdvice、EquipmentPreferenceIntent。
- 不直接完成研究或解锁装备。

给空军 / 天气系统：
- 读取 AirSituationSummary、WeatherSummary。
- 输出 AirDirective。
- 不直接调度空军任务。

给海军系统：
- 读取 NavalSituationSummary、ConvoyRiskSummary。
- 输出 NavalDirective。
- 不直接调度舰队。

给聊天 / 角色扮演系统：
- 接收 PlayerDirective。
- 输出 agent 自然语言回复和结构化计划。
- 忠诚、满意度、抗命等深层角色后果由 Agent 9 负责。

给 UI 系统：
- 提供 AgentDecisionRecord、AgentContext 摘要、原始 JSON、校验结果。
- UI 不能直接修改 agent 决策结果，只能提交 Command。

11. 输出内容

请输出一份 v1.0 多 Agent AI / LLM 决策系统设计文档，建议命名：
md/v1_agent8_multi_agent_ai_llm_plan.md

文档必须包含：
- 设计目标
- 多 agent 层级
- agent 权限与信息边界
- AgentContext 摘要系统
- 结构化输出 JSON schema
- 决策链路
- 仲裁机制
- 本地 LLM provider 设计
- MockAI fallback 策略
- Agent 记忆系统
- AI 管理 Command 接口
- 与其他系统的接口契约
- 日志、回放和调试策略
- 测试策略
- 明确哪些内容不由 Agent 8 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON schema 示例
- prompt 模板示例

不要实现：
- UI 美术
- 省份图底层
- 陆战结算
- 经济生产
- 科技树
- 空军任务结算
- 海军任务结算
- 深层聊天角色扮演
- 政变、派系、忠诚完整系统

12. 重要约束

- 不要让 LLM 直接修改 GameState。
- 不要让 LLM 逐个控制所有单位的底层移动。
- 不要把完整地图真相塞给每个 agent。
- 不要让多个 agent 同时覆盖同一批单位而无仲裁。
- 不要依赖真实 LLM 才能运行游戏。
- 不要把自然语言回复当作可执行命令。
- 所有 agent 决策必须可记录、可解释、可回放。
- 所有可执行行为必须进入 Command -> Validation -> RuleEngine -> GameEvent 管线。

13. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 有哪些 agent 角色；
- 每个 agent 能看什么；
- 每个 agent 能输出什么；
- LLM 输出 JSON schema 是什么；
- MockAI 和本地 LLM 如何替换；
- 多 agent 冲突如何仲裁；
- agent 决策如何转成 Command 候选；
- 非法输出如何处理；
- 决策日志如何保存；
- AI 系统如何与前线、陆战、经济、科技、空军、海军、聊天和 UI 对接。
还没写完。下一个是 Agent 9：聊天指挥 / 角色扮演系统。

星期三17:54






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三17:54






下面是 Agent 9：聊天指挥 / 角色扮演系统 的提示词。
你是 Agent 9：v1.0 聊天指挥 / 角色扮演系统负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。核心特色之一是玩家不只点击按钮微操，还可以通过聊天框向统治者、元帅、将领、部长下达自然语言命令；同时角色有职位、性格、忠诚、满意度、服从意愿和抗命风险。玩家未来可以扮演统治者、元帅、将领甚至部长，从不同权力层级参与战争。

你的任务不是实现完整政治模拟，也不是实现 UI 聊天界面美术。你的任务是设计 v1.0 的聊天指挥、玩家身份、角色服从、抗命、关系和角色扮演规则，让自然语言命令能稳定转成结构化 PlayerDirective，并进入 Agent 8 的多 agent 决策系统和 Agent 1 的 Command 管线。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md
- md/v1_agent5_technology_equipment_country_plan.md
- md/v1_agent6_airforce_weather_plan.md
- md/v1_agent7_navy_ports_seazones_plan.md
- md/v1_agent8_multi_agent_ai_llm_plan.md

如果旧文档与 Agent 1-8 的 v1.0 架构冲突，以 Agent 1-8 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计玩家身份系统

请设计或修订以下类型：
- PlayerRole
- PlayerAuthority
- PlayerPosition
- PlayerCommandScope
- PlayerDirective
- PlayerDirectiveTarget
- PlayerOverride
- PlayerReputation
- PlayerRoleEvent

v1.0 至少支持这些玩家身份：
- ruler：国家统治者，可以制定大战略、任免高层、调整经济和战争目标。
- fieldMarshal：元帅 / 战区司令，可以指挥战区、前线和多个集团军。
- general：将领，可以指挥所属集团军或部队，需服从上级命令。
- minister：部长，可以建议或管理经济、科技、空军、海军等系统。

要求：
- v1.0 可以默认玩家是 ruler，但架构必须支持其他身份。
- 玩家身份决定能命令谁、能看到什么、能直接微操什么。
- 玩家越权命令可以被允许，但要产生关系、服从、政治或声望后果。
- 玩家角色系统不能绕过 RuleEngine。

2. 设计聊天输入到结构化指令的转换

自然语言示例：
“古德里安，三周内突破阿登，优先夺取巴斯托涅，必要时绕过坚固城市。”
“陆军部长，停止扩编，优先补充前线步兵装备。”
“隆美尔，守住北非港口，如果补给低于 40% 就撤退。”

请设计：
- ChatMessage
- ChatThread
- ParsedDirective
- DirectiveIntent
- DirectiveConstraint
- DirectivePriority
- DirectiveTimeframe
- DirectiveConfidence
- DirectiveParseResult

转换链路建议：
ChatMessage
-> DirectiveParser
-> PlayerDirective
-> TargetResolver
-> AuthorityValidator
-> Agent 8 AgentContext / AgentDecision
-> CommandCandidate
-> CommandValidator
-> RuleEngine

要求：
- 聊天文本不能直接作为可执行命令。
- 解析失败时要返回澄清请求或低风险 fallback。
- 指令必须包含目标、意图、约束、优先级、时间范围。
- 指令可以是战略级、战区级、集团军级、经济级、科技级、空军级、海军级。
- 指令应能进入 Agent 8 的多 agent 决策系统。

3. 设计角色关系与服从系统

请设计或修订以下类型：
- CharacterRelationship
- LoyaltyState
- SatisfactionState
- TrustState
- ObedienceScore
- AutonomyPreference
- InsubordinationRisk
- CommandPressure
- RelationshipEvent

影响服从的因素：
- 玩家身份和正式权限。
- 命令是否符合角色职责。
- 命令是否符合角色性格。
- 命令风险是否过高。
- 命令是否与上级战略冲突。
- 角色忠诚度。
- 角色满意度。
- 玩家是否频繁越级微操。
- 最近战绩、补给状况、伤亡、失败责任。
- 角色历史偏好，例如古德里安偏好装甲突破。

服从结果建议：
- accept：接受命令。
- acceptWithConcern：接受但提出风险。
- requestClarification：要求澄清。
- delay：拖延或降低执行优先级。
- modifyPlan：按自己判断修改执行方案。
- refuse：拒绝执行。
- appealToSuperior：请求上级裁决。
- covertResistance：表面接受但执行效率降低，v1.0 可只预留。

要求：
- 抗命不能直接破坏规则系统。
- 抗命表现为不生成命令、降低优先级、请求确认、修改计划或产生事件。
- v1.0 不做完整政变模拟，但要预留 politicalRisk / coupRisk 接口。

4. 设计角色性格与命令反应

请设计：
- CharacterProfile
- PersonalityTrait
- CommandPreference
- RiskTolerance
- DoctrinePreference
- CommunicationStyle
- HistoricalBias

示例：
古德里安：
- 偏好装甲突破。
- 高进攻性。
- 高风险承受。
- 不喜欢把装甲分散填线。
- 面对保守命令可能提出异议。
- 面对不合理微操可能满意度下降。

要求：
- 性格影响回复语气、计划倾向、服从概率和异议类型。
- 性格不能直接让角色获得非法能力。
- 性格数据应可序列化，方便 JSON 配置。
- Agent 8 的 prompt 可以读取性格摘要，但最终决策仍要结构化。

5. 设计玩家微操与越级指挥规则

玩家可能直接操作前线部队，也可能绕过将领下命令。

请设计：
- DirectControlAction
- MicromanagementLevel
- CommandInterference
- ChainOfCommandViolation
- CommanderReaction
- AuthorityCost

规则建议：
- 玩家作为 ruler 直接微操师级部队属于越级指挥。
- 越级指挥可以提高短期控制，但降低相关将领满意度和自主性。
- 玩家作为 general 时，不能直接命令其他战区，除非获得授权。
- 玩家频繁推翻 agent 计划，会降低该 agent 的信任。
- 高忠诚角色更能接受玩家干预。
- 强势或自负角色更容易抗议。

要求：
- v1.0 只做轻量后果：满意度、信任、服从评分变化。
- 不要把政治系统做深。
- 所有后果必须通过 GameEvent 记录。

6. 设计任免、调动和职责系统

请设计：
- Appointment
- CommandAssignment
- Dismissal
- Promotion
- Demotion
- TransferOrder
- PositionVacancy
- AppointmentEvent

支持：
- 任命将领到集团军。
- 任命元帅到战区。
- 任命部长到内阁位置。
- 解除职务。
- 调动将领。
- 晋升和降级接口预留。

要求：
- 任免会影响忠诚、满意度、声望和指挥链。
- 不同职位有不同权限。
- 被撤职或降级可能产生不满。
- v1.0 不做复杂派系政治，但要预留 faction / clique 字段。

7. 定义聊天与角色相关 Command

请设计相关命令，但必须遵守 Agent 1 的 Command 管线。

至少包含：
- SendChatMessageCommand
- IssuePlayerDirectiveCommand
- ClarifyDirectiveCommand
- CancelDirectiveCommand
- ApproveAgentPlanCommand
- RejectAgentPlanCommand
- OverrideAgentPlanCommand
- SetPlayerRoleCommand
- AppointCharacterCommand
- DismissCharacterCommand
- TransferCommanderCommand
- SetCommanderAutonomyCommand
- DirectControlUnitCommand
- RevokeDirectControlCommand

每个命令必须包含：
- issuedBy
- countryId
- playerRole
- targetCharacterId / targetAgentId / targetScope
- payload
- reason
- validation result
- execution result
- generated GameEvent

要求：
- 玩家聊天、玩家直接指令和 agent 计划审批都必须进入 Command 管线。
- 非法命令只记录失败，不破坏 GameState。
- 角色关系变化必须生成 GameEvent。
- 自然语言回复不是 Command，必须与结构化指令分离。

8. 与其他系统的接口边界

给 Agent 8 多 Agent AI 系统：
- 提供 PlayerDirective。
- 提供角色关系、服从、满意度、性格摘要。
- 接收 AgentDecisionEnvelope、AgentMessage、计划风险。
- 不直接执行 Agent 8 的计划。

给地图 / 前线系统：
- 将前线、战区、省份目标作为 directive target。
- 不直接修改前线或省份控制权。

给陆战系统：
- 将进攻、防守、撤退、休整、补员优先级作为 directive。
- 不直接结算战斗。

给经济 / 生产系统：
- 将生产、补员、建造优先级作为 directive。
- 不直接修改生产线。

给科技 / 装备系统：
- 将研究方向、装备偏好作为 directive。
- 不直接完成研究。

给空军 / 海军系统：
- 将空军任务、海军任务、登陆计划作为 directive。
- 不直接调度空军或舰队。

给 UI 系统：
- 提供聊天记录、角色回复、指令解析结果、权限警告、服从风险、关系变化。
- UI 只提交 Command，不直接改角色状态。

9. 输出内容

请输出一份 v1.0 聊天指挥 / 角色扮演系统设计文档，建议命名：
md/v1_agent9_chat_roleplay_command_plan.md

文档必须包含：
- 设计目标
- 玩家身份系统
- 聊天到 PlayerDirective 的转换流程
- 结构化 PlayerDirective schema
- 角色关系与服从系统
- 性格与命令反应规则
- 微操与越级指挥规则
- 任免、调动和职责系统
- 聊天与角色 Command 接口
- 与其他系统的接口契约
- AI 可读取的角色 / 指令摘要格式
- 日志、回放和调试策略
- 测试策略
- 明确哪些内容不由 Agent 9 实现

如果需要写少量代码，只允许写：
- 纯数据模型
- 协议
- 伪实现
- 单元测试样例
- JSON schema 示例
- 聊天解析示例

不要实现：
- UI 美术
- 完整聊天界面
- LLM provider 底层
- 多 agent 仲裁核心
- 陆战结算
- 经济生产
- 科技树
- 空军 / 海军结算
- 完整政变、派系和内战系统

10. 重要约束

- 不要让自然语言直接执行游戏动作。
- 不要让聊天系统绕过 Agent 8 和 RuleEngine。
- 不要把角色扮演系统做成复杂政治模拟。
- 不要让抗命直接改 GameState 或破坏规则。
- 不要让 UI 直接修改角色关系。
- 玩家越权可以存在，但必须有权限校验和后果记录。
- 所有聊天、指令、关系变化必须可记录、可解释、可回放。

11. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 玩家可以扮演哪些身份；
- 聊天文本如何转成结构化指令；
- 指令如何进入 Agent 8 决策系统；
- 角色为什么接受、质疑、拖延或拒绝命令；
- 玩家微操如何影响将领关系；
- 任免和调动如何影响指挥链；
- UI 应显示哪些聊天和角色状态；
- 哪些行为必须通过 Command 管线。
还没写完。下一个是 Agent 10：UI / UX / 可观察性。

星期三17:56






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写

下面是 Agent 10：UI / UX / 可观察性 的提示词。
你是 Agent 10：v1.0 UI / UX / 可观察性负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v1.0 会包含省份图、前线、陆战、补给、经济、生产、科技、空军、海军、多 agent AI、聊天指挥和角色扮演。系统复杂度高，因此 UI 的首要目标不是最终商业美术，而是让玩家和开发者能理解局势、发出命令、观察 AI 决策、调试规则结果。

你的任务不是实现完整美术资源，也不是直接改游戏规则。你的任务是设计并实现 v1.0 的 UI/UX 信息架构、主要界面、命令入口、可观察性面板、日志和调试视图，保证所有用户操作都进入 Command 管线。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md
- md/v1_agent5_technology_equipment_country_plan.md
- md/v1_agent6_airforce_weather_plan.md
- md/v1_agent7_navy_ports_seazones_plan.md
- md/v1_agent8_multi_agent_ai_llm_plan.md
- md/v1_agent9_chat_roleplay_command_plan.md

如果旧文档与 Agent 1-9 的 v1.0 架构冲突，以 Agent 1-9 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计 v1.0 UI 信息架构

请设计主要界面：
- MainMapView：大地图主界面。
- ProvinceInspectorView：省份详情。
- UnitInspectorView：陆军 / 空军 / 海军单位详情。
- FrontLineView：前线和攻势箭头编辑。
- EconomyView：资源、工厂、生产、库存、人力。
- TechnologyView：研究、装备、学说。
- AirForceView：机场、空域、联队任务、制空。
- NavyView：港口、海域、舰队任务、登陆计划。
- AgentCommandView：agent 列表、目标、权限、决策记录。
- ChatCommandView：聊天指挥。
- EventLogView：战报、命令、结算、AI 决策日志。
- DebugOverlayView：开发调试信息。

要求：
- UI 层不能直接修改 GameState。
- 所有按钮、聊天、拖拽、选择、计划编辑最终都必须提交 Command。
- UI 必须能显示 CommandValidation 失败原因。
- v1.0 UI 优先信息清晰，不追求最终商业美术。

2. 设计主地图交互

主地图必须支持：
- 省份选择。
- 省份控制权显示。
- 地形、城市、港口、机场、要塞、工厂、补给节点显示。
- 前线高亮。
- 选中集团军或前线后显示分配关系。
- 攻势箭头显示。
- 补给风险覆盖层。
- 空域覆盖层。
- 海域覆盖层。
- 天气覆盖层。
- 战争迷雾 / 情报可见性显示。
- 战斗进行中省份标记。
- AI 计划和玩家计划显示。

要求：
- 渲染层只读取快照，不做规则计算。
- 地图点击只产生选择或 CommandIntent。
- 地图缩放、拖拽、图层切换要适合 iPad/iPhone。
- 小屏幕优先用底部抽屉和分层面板，避免信息堆满。

3. 设计前线与作战计划 UI

需要支持：
- 查看自动识别前线。
- 创建或选择前线。
- 将集团军分配到前线。
- 查看缺口警告。
- 查看战线过长警告。
- 绘制或选择攻势箭头。
- 设置突破方向和目标省份。
- 查看 AI 建议的作战计划。
- 批准、拒绝或修改 agent 计划。

要求：
- 前线编辑最终提交 Command，例如 AssignArmyToFrontCommand、CreateOffensiveAxisCommand。
- UI 不直接移动单位。
- 显示自动填线建议和不可执行原因。

4. 设计经济、科技、空军、海军界面

经济界面：
- 资源收入和消耗。
- 工厂数量和分配。
- 生产线。
- 装备库存。
- 人力。
- 补员需求和短缺。

科技界面：
- 研究槽。
- 可研究科技。
- 研究进度。
- 已解锁装备。
- 学说节点。

空军界面：
- 机场容量。
- 空军联队。
- 空域任务。
- 制空状态。
- 轰炸和近距支援结果。
- 天气影响。

海军界面：
- 港口容量。
- 舰队任务。
- 海域控制。
- 运输线风险。
- 登陆计划。
- 护航 / 袭击结果。

要求：
- 所有修改都提交 Command。
- 所有界面都显示“为什么不能执行”的验证反馈。
- 数值复杂时优先显示摘要，再允许展开详情。

5. 设计 Agent 决策可观察性

必须支持查看：
- agent 角色、职位、权限。
- agent 当前目标。
- agent 可见信息摘要。
- agent prompt 摘要，不需要完整敏感内容也可。
- LLM / MockAI 原始 JSON。
- 解析后的 AgentIntent。
- 仲裁结果。
- 转换后的 Command 候选。
- CommandValidation 成功 / 失败原因。
- 最终执行的 GameEvent。
- agent 记忆摘要。
- 决策耗时、fallback、超时、错误。

要求：
- 这是 v1.0 核心调试面板，不可省略。
- 决策日志必须按回合、agent、系统过滤。
- 玩家可以从 AI 决策面板理解“为什么 AI 这么做”。

6. 设计聊天指挥界面

必须支持：
- 输入自然语言命令。
- 选择目标角色 / agent。
- 显示指令解析结果。
- 显示权限和越级警告。
- 显示角色回应。
- 显示服从风险、满意度变化、信任变化。
- 显示由聊天产生的结构化 PlayerDirective。
- 显示等待批准的计划。
- 支持批准、拒绝、要求修改。

要求：
- 聊天 UI 不直接执行自然语言。
- 发送消息后必须经过 Agent 9 的解析和 Agent 8 的决策链路。
- 需要清楚区分“角色说的话”和“真正会执行的命令”。

7. 设计日志、回放和调试视图

请设计：
- CommandLog
- GameEventLog
- BattleLog
- EconomyLog
- AgentDecisionLog
- ValidationLog
- ReplayTimeline
- DebugSnapshotView

必须支持：
- 按回合过滤。
- 按系统过滤。
- 按 agent 过滤。
- 查看某个命令从发出到执行的完整链路。
- 查看某个 GameEvent 来源。
- 查看 replay snapshot。
- 导出日志接口预留。

要求：
- 日志不是纯文本堆积，需要结构化展示。
- 调试视图可以只在 debug build 显示。
- 回放视图优先展示状态变化，不要求电影式动画。

8. 定义 UI 事件与 Command 入口

请设计 UI 到规则系统的输入边界：
- UIAction
- ViewIntent
- CommandDraft
- CommandSubmission
- ValidationFeedback
- SelectionState
- MapOverlayState
- PanelNavigationState

要求：
- UIAction 可以是点击、拖拽、输入、选择。
- UIAction 转换成 CommandDraft。
- CommandDraft 经过本地基础检查后提交 CommandValidator。
- RuleEngine 执行后 UI 通过新的 GameState snapshot 刷新。
- UI 不持久化权威状态，只保留展示状态和选择状态。

9. 与其他系统的接口边界

给地图 / 前线系统：
- 读取地图快照、省份显示信息、前线段、攻势箭头。
- 提交前线相关 Command。
- 不直接修改前线数据。

给陆战系统：
- 读取单位状态、战斗预览、补给状态、组织度。
- 提交移动、攻击、防守、撤退、休整 Command。
- 不直接修改单位状态。

给经济 / 科技 / 空军 / 海军系统：
- 读取只读快照。
- 提交生产、研究、空军任务、海军任务 Command。
- 不直接修改系统状态。

给 Agent 8 / Agent 9：
- 显示 agent 决策、聊天、角色关系、PlayerDirective。
- 提交聊天和计划审批 Command。
- 不直接改 agent 记忆或关系。

给测试 / 工具链：
- 提供 UI snapshot 测试入口。
- 提供 debug overlay 开关。
- 提供日志导出入口。

10. 输出内容

请输出一份 v1.0 UI / UX / 可观察性设计文档，建议命名：
md/v1_agent10_ui_ux_observability_plan.md

文档必须包含：
- 设计目标
- UI 信息架构
- 主地图交互
- 前线和作战计划 UI
- 经济 / 科技 / 空军 / 海军界面
- Agent 决策可观察性
- 聊天指挥界面
- 日志、回放和调试视图
- UI 到 Command 的输入边界
- 与其他系统的接口契约
- iPhone / iPad 响应式布局原则
- 测试策略
- 明确哪些内容不由 Agent 10 实现

如果需要写少量代码，只允许写：
- SwiftUI 视图骨架
- ViewModel 协议
- UI state 类型
- CommandDraft 示例
- mock snapshot 示例
- UI 测试样例

不要实现：
- 规则系统
- 省份图底层
- 陆战结算
- 经济生产逻辑
- 科技树逻辑
- 空军 / 海军结算
- LLM provider
- 多 agent 仲裁
- 最终商业美术

11. 重要约束

- UI 不能直接修改 GameState。
- UI 不能直接执行自然语言命令。
- UI 不能把 agent 的自然语言回复当作命令。
- UI 不能隐藏验证失败原因。
- UI 不能依赖真实 LLM 才能显示。
- UI 必须支持 Mock 数据和调试数据。
- 所有玩家操作必须能追踪到 Command 或 ViewIntent。
- 所有重要系统状态必须有可观察入口。

12. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- v1.0 有哪些主要界面；
- 玩家如何查看地图、前线、单位、经济、科技、空军、海军；
- 玩家如何下达命令；
- 聊天指挥如何展示；
- AI 决策如何被观察和调试；
- 日志和回放如何组织；
- UI 如何提交 Command；
- UI 哪些状态可以本地持有，哪些状态必须来自 GameState snapshot。
还没写完。下一个是 Agent 11：数据 / 场景 / 平衡。

星期三17:57






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三17:58






下面是 Agent 11：数据 / 场景 / 平衡 的提示词。
你是 Agent 11：v1.0 数据 / 场景 / 平衡负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v1.0 的目标不是一次性做完整全球地图，而是做一个可玩的大战略原型：有限战区、有限国家、可运行经济、陆空海简化系统、多 agent 指挥和可观察日志。你的任务是把各系统需要的数据组织成可维护、可校验、可调参的内容体系，并设计 v1.0 测试场景和平衡方案。

你的任务不是写完整规则系统，也不是做最终美术。你的任务是定义数据标准、场景结构、初始数值、平衡流程和数据校验工具，让后续实现可以用数据驱动游戏，而不是把国家、单位、地图、装备、科技和角色写死在代码里。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md
- md/v1_agent5_technology_equipment_country_plan.md
- md/v1_agent6_airforce_weather_plan.md
- md/v1_agent7_navy_ports_seazones_plan.md
- md/v1_agent8_multi_agent_ai_llm_plan.md
- md/v1_agent9_chat_roleplay_command_plan.md
- md/v1_agent10_ui_ux_observability_plan.md

如果旧文档与 Agent 1-10 的 v1.0 架构冲突，以 Agent 1-10 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计 v1.0 数据目录和文件规范

请设计数据目录，例如：

- Data/Scenarios/
- Data/Maps/
- Data/Countries/
- Data/Provinces/
- Data/Units/
- Data/Equipment/
- Data/Technologies/
- Data/Doctrines/
- Data/Economy/
- Data/Agents/
- Data/Characters/
- Data/Air/
- Data/Navy/
- Data/Weather/
- Data/Localization/
- Data/Balance/

要求：
- 数据优先 JSON。
- id 必须稳定，不依赖数组下标。
- 文件之间引用必须可校验。
- 数据必须能支持 Codable / Decodable。
- 需要版本字段 schemaVersion。
- 需要数据迁移和兼容策略预留。
- 需要 debug 场景和正式场景分离。

2. 设计 v1.0 场景范围

v1.0 不建议直接做全球征服。请设计一个有限但能验证系统的大战略场景。

候选方向：
- 西线 1944：德国 vs 盟军，适合前线、空军、补给、港口、登陆、工业压力。
- 西线 1940：德国进攻法国，适合装甲突破和前线系统。
- 东线小剧场：德国 vs 苏联，适合补给、纵深、天气和组织度。
- 地中海 / 北非：适合海军、港口、补给和空军，但陆战规模较窄。

请为 v1.0 推荐一个主场景，并定义：
- scenarioId
- 时间范围
- 参战国家
- 地图范围
- 初始前线
- 初始城市、港口、机场、工厂、资源
- 初始陆军、空军、海军
- 初始科技和装备
- 初始经济
- 初始 agent 和角色
- 胜利条件
- 失败条件
- 测试目标

要求：
- 场景规模必须足够小，能在 iOS 上稳定运行。
- 必须覆盖陆战、补给、经济、空军、海军或海上补给至少一部分。
- 必须能验证多 agent 决策和玩家聊天命令。

3. 设计地图与省份数据

基于 Agent 2 的 ProvinceNode / RegionNode 设计，定义地图数据。

请设计：
- province id 命名规则。
- province name。
- owner / controller。
- terrain。
- neighbors。
- province edges。
- city / port / airbase / fortress。
- infrastructure。
- supplyValue。
- resources。
- factories。
- coreOf。
- occupationState。
- airZoneId。
- weatherZoneId。
- seaZoneId。
- displayCenter / labelPosition / polygon 简化显示字段。

要求：
- 相邻关系必须双向校验。
- 港口必须连接合法 seaZone。
- 机场必须有容量。
- 资源和工厂必须能被经济系统读取。
- 地图数据不能依赖 SpriteKit 节点。

4. 设计国家数据

请设计 CountryDefinition / CountryInitialState 数据。

至少包含：
- countryId
- displayName
- ideology / governmentType，可选
- capitalProvinceId
- coreProvinceIds
- controlledProvinceIds
- startingResources
- startingFactories
- startingManpower
- startingEquipmentStockpiles
- startingTechnologies
- startingProductionLines
- startingResearchProjects
- startingAgents
- startingCommanders
- nationalModifiers
- AI personality defaults

要求：
- 国家差异通过数据和 modifier 表达。
- 不要把国家差异写死进规则代码。
- v1.0 建议先支持 2-4 个国家，不要强行做全世界。

5. 设计单位、装备和科技数据

请定义数据格式：
- divisionTemplates
- startingDivisions
- airWingTemplates
- startingAirWings
- fleetTemplates
- startingFleets
- equipmentModels
- technologyDefinitions
- doctrineNodes

每个 starting unit 至少要能指定：
- id
- name
- countryId
- provinceId / airbaseProvinceId / seaZoneId
- templateId
- equipmentModelIds
- strength
- organization
- experience
- commanderId
- assignedArmy / front / mission

要求：
- 单位模板与初始单位分离。
- 装备型号与科技解锁分离。
- 初始库存必须能支撑初始单位。
- 科技数据必须能被 Agent 5 的 modifier 系统读取。

6. 设计经济、生产和建造数据

请定义：
- startingResourceStockpiles
- resourceIncomeFromProvinces
- factoryAssignments
- productionLines
- constructionQueue
- manpowerPools
- reinforcementPriorities
- occupationEfficiency
- consumerGoods / tax 接口预留

要求：
- 初始生产线必须能生产当前装备或已解锁装备。
- 生产线不能引用不存在装备。
- 建造项目必须绑定合法 provinceId。
- 初始经济必须能在 5-10 回合内产生可观察变化。

7. 设计 Agent、角色和聊天数据

请定义：
- agentProfiles
- characterProfiles
- commanderDefinitions
- ministerDefinitions
- rulerDefinitions
- personalityTraits
- loyalty / satisfaction 初始值
- commandStyle
- autonomyPreference
- historicalBias
- startingObjectives
- promptFragments
- mockAI behavior profile

要求：
- 角色数据必须能支持 Agent 8 和 Agent 9。
- 人格和历史偏好只影响决策倾向，不直接修改规则结果。
- v1.0 至少包含统治者、1-2 个元帅、若干将领、若干部长。
- 需要提供 MockAI 数据，让无本地 LLM 时也能跑。

8. 设计胜利条件和场景脚本

请设计：
- VictoryConditionDefinition
- ObjectiveDefinition
- ScenarioEvent
- ScriptedTrigger
- ScriptedEffect
- TurnLimit
- ScoreBreakdown

v1.0 胜利条件建议包含：
- 控制关键城市。
- 保持补给线。
- 消灭或迫退敌军一定比例。
- 保持港口 / 机场 / 工业能力。
- 限定回合内完成突破或防守。

要求：
- 胜利条件必须数据驱动。
- 脚本事件不能绕过 RuleEngine。
- 场景脚本只生成 Command、Event 或 Modifier 候选。
- 不要做复杂历史事件树。

9. 设计平衡参数体系

请设计 BalanceConfig：

- combatBalance
- movementBalance
- supplyBalance
- organizationBalance
- reinforcementBalance
- economyBalance
- productionBalance
- technologyBalance
- airBalance
- navalBalance
- agentBalance
- difficultyBalance

平衡目标：
- 玩家能在 5-10 回合内看到前线变化。
- 组织度比 strength 更频繁变化。
- 缺补给必须明显影响进攻。
- 空军支援有感知，但不能单独决定战局。
- 海军影响补给和登陆，但不拖慢主玩法。
- 经济短缺能影响补员和生产。
- Agent 决策不能过于随机，必须可解释。

要求：
- 参数集中管理。
- 不要把魔法数字散落在规则代码里。
- 需要提供 easy / normal / hard 难度参数接口。

10. 设计数据校验工具

请设计 DataValidator 规则。

必须校验：
- 所有 id 唯一。
- 所有引用存在。
- 省份邻接关系合法。
- 港口连接合法海域。
- 机场容量合法。
- 单位所在省份合法。
- 国家核心区和控制区合法。
- 初始装备库存非负。
- 生产线装备已解锁。
- 科技前置关系无环。
- agent 绑定角色存在。
- commander 绑定单位存在。
- 胜利条件引用目标存在。
- localization key 缺失检测。

要求：
- 校验错误要可读。
- 严重错误阻止加载。
- 警告允许加载但写入日志。
- 数据校验应能在单元测试或构建前运行。

11. 与其他系统的接口边界

给总架构 / 集成：
- 提供完整数据加载顺序。
- 提供 DataManifest。
- 提供 schemaVersion 和 migration 预留。

给地图 / 前线系统：
- 提供 province、edge、front seed、display 数据。
- 不实现地图规则本身。

给陆战系统：
- 提供单位模板、初始单位、战斗平衡参数。
- 不实现战斗结算。

给经济 / 科技 / 空军 / 海军系统：
- 提供对应初始数据和配置。
- 不实现系统逻辑。

给 AI / 聊天系统：
- 提供 agent、角色、人格、目标、mock behavior 数据。
- 不实现 LLM provider。

给 UI 系统：
- 提供 displayName、localization、图标引用、颜色、标签位置。
- 不实现 UI。

给测试 / 工具链：
- 提供测试场景、数据校验规则、平衡测试参数。
- 提供 deterministic seed。

12. 输出内容

请输出一份 v1.0 数据 / 场景 / 平衡设计文档，建议命名：
md/v1_agent11_data_scenario_balance_plan.md

文档必须包含：
- 设计目标
- 数据目录结构
- JSON schema 规范
- v1.0 推荐场景
- 地图与省份数据
- 国家数据
- 单位 / 装备 / 科技数据
- 经济 / 生产 / 建造数据
- agent / 角色 / 聊天数据
- 胜利条件和场景脚本
- 平衡参数体系
- 数据校验工具
- 数据加载顺序
- 与其他系统的接口契约
- 测试策略
- 明确哪些内容不由 Agent 11 实现

如果需要写少量代码，只允许写：
- JSON 示例
- schema 示例
- 数据校验伪代码
- 示例场景片段
- balance config 示例
- 单元测试样例

不要实现：
- 规则系统
- UI 美术
- 省份图算法
- 陆战结算
- 经济生产逻辑
- 科技树逻辑
- 空军 / 海军结算
- LLM provider
- 多 agent 仲裁
- 完整全球地图

13. 重要约束

- 不要直接做全球地图。
- 不要把数据写死进代码。
- 不要让场景脚本绕过 RuleEngine。
- 不要让平衡参数散落在各系统里。
- 不要做过大国家列表。
- 不要要求真实 LLM 才能跑场景。
- 数据必须可校验、可重放、可调参。
- v1.0 场景必须服务测试和可玩性，不追求完整历史覆盖。

14. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- v1.0 数据放在哪里；
- 每类数据怎么命名和引用；
- 推荐场景是什么；
- 初始国家、地图、单位、经济、科技、agent 如何配置；
- 胜利条件如何数据化；
- 平衡参数如何集中管理；
- 数据加载顺序是什么；
- 数据错误如何校验；
- 如何用小场景验证完整大战略闭环。
还没写完。下一个是 Agent 12：测试 / 性能 / 工具链。

星期三17:59






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三18:01






下面是 Agent 12：测试 / 性能 / 工具链 的提示词。
你是 Agent 12：v1.0 测试 / 性能 / 工具链负责人。

项目背景：
这是一个 iOS 二战大战略 + 回合制战棋 + 本地 LLM 多 agent 决策游戏。v1.0 会包含省份图、前线、陆战、补给、经济、生产、科技、空军、海军、多 agent AI、聊天指挥、角色关系、日志和回放。系统复杂，必须从 v1.0 起建立可测试、可回放、可诊断、可性能分析的工程基础。

你的任务不是实现玩法功能，也不是做 UI 美术。你的任务是设计并实现测试策略、性能预算、回放验证、数据校验、日志导出、模拟器压力测试和开发工具链，保证后续功能 agent 的产物能稳定集成。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md
- md/phase5_long_term_roadmap.md
- wwii_ai_strategy_game_plan.md

如果存在以下文件，也必须优先阅读并遵守：
- md/v1_agent1_architecture_integration_plan.md
- md/v1_agent2_map_frontline_plan.md
- md/v1_agent3_land_combat_supply_plan.md
- md/v1_agent4_economy_production_manpower_plan.md
- md/v1_agent5_technology_equipment_country_plan.md
- md/v1_agent6_airforce_weather_plan.md
- md/v1_agent7_navy_ports_seazones_plan.md
- md/v1_agent8_multi_agent_ai_llm_plan.md
- md/v1_agent9_chat_roleplay_command_plan.md
- md/v1_agent10_ui_ux_observability_plan.md
- md/v1_agent11_data_scenario_balance_plan.md

如果旧文档与 Agent 1-11 的 v1.0 架构冲突，以 Agent 1-11 的接口为准，并在输出中记录冲突点。

你的核心职责：

1. 设计 v1.0 测试体系

请设计测试分层：
- Unit Tests：纯规则、数据结构、计算函数。
- Integration Tests：多个系统组合，例如经济补员影响陆战。
- Scenario Tests：加载完整场景，跑固定回合。
- Replay Tests：命令日志重放后状态一致。
- AI Schema Tests：agent 输出 JSON 可解析、可校验。
- Data Validation Tests：所有 JSON 数据合法。
- UI Snapshot / Interaction Tests：关键界面可显示、命令入口可用。
- Performance Tests：回合结算、AI 调用、地图渲染、日志规模。

要求：
- 优先测试纯逻辑，不依赖 UI。
- 所有随机必须可固定 seed。
- 测试应能在无真实 LLM 时通过。
- MockAI 是必需测试路径。
- 本地 LLM 超时、失败、非法 JSON 必须有测试。

2. 设计确定性与回放系统

请设计或修订以下类型：
- DeterministicRandom
- GameSeed
- CommandRecord
- GameEventRecord
- StateSnapshot
- ReplaySession
- ReplayVerifier
- ReplayMismatch
- SnapshotHash

要求：
- 每一局有固定 seed。
- 所有随机结果来自统一 RNG。
- 每个 Command 记录输入、验证结果、执行结果。
- 每个 GameEvent 可追溯来源。
- 支持从初始场景 + CommandHistory 重放到指定回合。
- 重放后的 GameState hash 应与原始 snapshot hash 一致。
- LLM 原始输出不应在回放时重新调用，必须使用记录的 AgentDecisionRecord。

3. 设计日志与诊断工具

请设计：
- StructuredLogger
- LogCategory
- LogSeverity
- DiagnosticEvent
- CommandTrace
- AgentDecisionTrace
- BattleTrace
- EconomyTrace
- DataLoadTrace
- PerformanceTrace
- LogExportBundle

日志类别至少包含：
- command
- validation
- ruleEngine
- battle
- supply
- economy
- production
- technology
- air
- naval
- map
- frontline
- agent
- chat
- data
- replay
- performance
- ui

要求：
- 日志必须结构化，不只是字符串。
- 日志能按 turn、system、agent、commandId、provinceId、unitId 过滤。
- 错误必须包含可读原因和相关 id。
- 支持导出调试包，包含场景 id、seed、command history、event log、agent records、data validation report。

4. 设计数据校验与构建前检查

基于 Agent 11 的数据规范，设计 DataValidator 工具链。

必须校验：
- schemaVersion。
- id 唯一性。
- 引用完整性。
- 省份邻接双向性。
- 科技前置无环。
- 单位所在位置合法。
- 生产线引用已解锁装备。
- agent / character / commander 引用合法。
- 胜利条件引用合法目标。
- balance config 数值范围。
- localization key 是否缺失。

要求：
- 严重错误阻止加载。
- 警告写入日志。
- 可以在测试中运行。
- 可以输出人类可读 report。
- 不要依赖真实 UI。

5. 设计性能预算

请为 v1.0 定义性能预算。

需要覆盖：
- iPhone 和 iPad。
- 场景加载时间。
- 回合结算时间。
- 地图渲染帧率。
- Command validation 延迟。
- AI 决策耗时。
- 本地 LLM 超时限制。
- 日志内存增长。
- 数据规模上限。
- 存档 / 快照体积。

建议方向：
- v1.0 场景省份数控制在小规模，例如 50-150 个。
- 陆军单位控制在几十个，不要上百上千。
- AI 决策按 agent 分层调用，不对每个单位调用 LLM。
- 本地 LLM 超时后 fallback。
- 日志可分页或压缩。
- 大地图覆盖层按需计算缓存。

请输出具体预算表和超限处理策略。

6. 设计压力测试和模拟器测试

请设计：
- AutoPlaySimulation
- MockAIBattleSimulation
- EconomyBurnRateTest
- FrontlineStabilityTest
- SupplyStressTest
- AirMissionStressTest
- NavalRouteStressTest
- AgentDecisionStressTest
- LogGrowthTest

测试目标：
- 连续运行 20-50 回合不崩溃。
- CommandHistory 可重放。
- AI 不会无限循环。
- 前线自动填线不会反复震荡。
- 经济不会快速出现非法负数。
- 补给和战斗不会出现不可恢复状态。
- 日志不会无限占用内存。
- LLM 失败路径不会卡死游戏。

7. 设计 CI / 本地开发命令

请设计本地开发工具命令或脚本规范。

可能包含：
- run unit tests
- run integration tests
- validate data
- run scenario smoke test
- run replay verification
- run performance smoke test
- export debug bundle
- generate schema docs
- format / lint

要求：
- 优先使用 Xcode / Swift Package / XCTest 生态。
- 如果项目还未确定工具链，先输出命令设计和职责。
- 不要要求联网。
- 不要要求真实 LLM。
- 本地 LLM 测试必须可选。

8. 设计测试数据与 Mock 系统

请设计：
- MinimalScenarioFixture
- ArdennesV0Fixture
- WestFrontV1Fixture
- MockDecisionProvider
- MockLLMClient
- MockEconomyProvider
- MockWeatherProvider
- FixedCombatResolver
- FixedRandomProvider

要求：
- 每个系统都应该有小型 fixture。
- Mock 必须稳定、可预测。
- AI 测试默认使用 MockDecisionProvider。
- 可以注入固定非法 JSON、超时、冲突命令，用于验证失败处理。

9. 与其他系统的接口边界

给总架构 / 集成：
- 提供测试目录结构、测试命名规范、回放验证接口。
- 定义所有系统必须生成可追踪 GameEvent。

给地图 / 前线系统：
- 提供省份图校验、前线识别测试、自动填线稳定性测试。

给陆战系统：
- 提供战斗公式测试、撤退测试、补给测试、组织度测试、包围测试。

给经济 / 科技系统：
- 提供生产、库存、人力、研究进度、modifier 测试。

给空军 / 海军系统：
- 提供任务结算、天气、海域控制、运输线风险测试。

给 AI / 聊天系统：
- 提供 JSON schema 测试、fallback 测试、仲裁冲突测试、聊天解析测试。

给 UI 系统：
- 提供 snapshot fixture、ViewModel mock、命令入口测试。

给数据系统：
- 提供 DataValidator、schema docs、data smoke test。

10. 输出内容

请输出一份 v1.0 测试 / 性能 / 工具链设计文档，建议命名：
md/v1_agent12_testing_performance_tooling_plan.md

文档必须包含：
- 设计目标
- 测试分层
- 确定性与回放系统
- 日志与诊断工具
- 数据校验工具链
- 性能预算
- 压力测试和模拟器测试
- 本地开发命令 / CI 规范
- 测试 fixture 与 Mock 系统
- 与其他系统的接口契约
- 验收标准
- 明确哪些内容不由 Agent 12 实现

如果需要写少量代码，只允许写：
- XCTest 示例
- 回放验证伪代码
- DataValidator 伪代码
- Mock provider 示例
- 性能测试样例
- 日志结构示例

不要实现：
- UI 美术
- 玩法规则主体
- 省份图算法主体
- 陆战结算主体
- 经济生产主体
- 科技树主体
- 空军 / 海军主体
- LLM provider 主体
- 多 agent 仲裁主体
- 完整场景内容

11. 重要约束

- 不要依赖真实 LLM 才能测试。
- 不要让随机不可重放。
- 不要只做 UI 测试而忽略规则测试。
- 不要让日志只有纯文本。
- 不要让数据错误在运行中才暴露。
- 不要让性能预算停留在口号，必须给具体指标。
- 不要让回放重新调用 LLM。
- 所有重要状态变化必须能追溯到 Command 或 GameEvent。

12. 完成标准

你的产出完成后，其他 agent 应该能够明确知道：
- 每个系统需要写什么测试；
- 如何固定随机和重放；
- 如何验证数据；
- 如何导出调试日志；
- 如何测试 LLM 失败路径；
- 如何做场景 smoke test；
- v1.0 的性能预算是多少；
- 什么情况下构建或加载应该失败；
- 如何判断一次集成是否稳定。
已完成。

星期三18:03
