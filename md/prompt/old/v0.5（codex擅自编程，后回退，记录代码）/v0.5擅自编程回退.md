# v0.5 / AI Agent 误实现回滚报告

## 1. 说明

本次回滚针对一次误执行：用户原意是先编写 v0.5 的 agent 分工与提示词，但我错误地开始实现 v0.5 国家与部长 Agent 代码。

随后又发现工作区中存在一批 AI/agent 管线文件和 Xcode target 引用，导致工程处于“半实现/半回滚”的不一致状态。本报告记录第二次核验后的实际清理结果。

本次清理目标：回到“只保留规划文档，不保留 v0.5/AI Swift 实现”的状态。

## 2. 本次删除的 v0.5 / AI 实现文件

以下文件已删除：

- `WWIIHexV0/Agents/GameAgent.swift`
- `WWIIHexV0/Agents/StrategicDirective.swift`
- `WWIIHexV0/Agents/AgentContexts.swift`
- `WWIIHexV0/Agents/MinisterDecisionProvider.swift`
- `WWIIHexV0/Agents/RulerDirectiveFactory.swift`
- `WWIIHexV0/Agents/AgentDecision.swift`
- `WWIIHexV0/Agents/AgentCommandMapper.swift`
- `WWIIHexV0/Agents/AgentConfiguration.swift`
- `WWIIHexV0/Agents/AgentDecisionParser.swift`
- `WWIIHexV0/Agents/AgentDecisionRecord.swift`
- `WWIIHexV0/Agents/AgentPromptBuilder.swift`
- `WWIIHexV0/Agents/LLMClient.swift`
- `WWIIHexV0/Agents/LocalLLMDecisionProvider.swift`
- `WWIIHexV0/Agents/MockAIClient.swift`
- `WWIIHexV0/Data/CabinetDefinition.swift`
- `WWIIHexV0/Data/national_cabinet.json`
- `WWIIHexV0/Turn/TurnManager.swift`
- `WWIIHexV0/Tests/AgentPipelineTests.swift`

说明：`WWIIHexV0/UI/CabinetPanelModel.swift` 在第二次核验时已不存在，因此本轮无需再次删除。

## 3. 本次恢复的文件

以下文件已去除 v0.5/AI 实现引用：

- `WWIIHexV0/Core/GameState.swift`
- `WWIIHexV0/Data/ScenarioDefinition.swift`
- `WWIIHexV0/Data/DataLoader.swift`
- `WWIIHexV0/App/AppContainer.swift`
- `WWIIHexV0/Agents/DecisionProvider.swift`
- `WWIIHexV0/UI/RootGameView.swift`
- `WWIIHexV0/UI/AgentPanelView.swift`
- `WWIIHexV0.xcodeproj/project.pbxproj`

## 4. 已撤销的内容

已撤销的误实现包括：

- `CabinetState` / `DirectiveBoard` 挂入 `GameState`。
- `GameState.initial(cabinetState:)` 参数。
- `ScenarioDataSet.cabinetState` 字段。
- `DataLoader.loadCabinetState()`。
- `DataLoader.loadInitialGameState()` 自动加载 `national_cabinet.json`。
- `DataLoader.validate(_:)` 中的 cabinet / game agent 校验。
- `AppContainer` 中的 `TurnManager`、`GameAgent`、`MockAIClient`、`AgentDecisionRecord` 依赖。
- `AppContainer.runAIIfNeeded()`。
- `DecisionProvider` 对 `AgentContext` / `AgentDecisionEnvelope` 的依赖，已恢复为 `commands(for state:) async throws -> [Command]`。
- `RootGameView` 的自动 AI task。
- `RootGameView` 向 `AgentPanelView` 传入 `record`。
- `AgentPanelView` 对 `AgentDecisionRecord` / `CommandResultSummary` 的依赖。
- `project.pbxproj` 中所有已删除 AI/v0.5 Swift 文件、`Turn` group、`national_cabinet.json`、`AgentPipelineTests.swift` 的引用。

保留说明：`GameState.initial()` 当前仍使用 `ger_*` / `all_*` 单位 ID，与当前 `ardennes_v0_scenario.json` 和 `general_agents.json` 保持一致。这个一致性修正不依赖 v0.5 类型，因此未回滚。

## 5. 未触碰内容

本次回滚没有删除或修改以下内容：

- `md/phase5_long_term_roadmap.md`
- 阶段 0-4 规划文档
- 现有 v0 规则代码
- 现有 v0 UI 代码
- 现有 v0 测试文件
- `WWIIHexV0/Data/ardennes_v0_scenario.json`
- `WWIIHexV0/Data/general_agents.json`
- `WWIIHexV0/Data/terrain_rules.json`
- `WWIIHexV0/Data/unit_templates.json`

## 6. 第二次核验结果

已执行静态检查：

- `rg --files` 确认 `WWIIHexV0/Agents` 下只剩 `DecisionProvider.swift`。
- `rg --files` 确认 `WWIIHexV0/Turn` 下无文件。
- `rg --files` 确认 `WWIIHexV0/Data/national_cabinet.json` 不存在。
- `rg --files` 确认 `WWIIHexV0/Tests/AgentPipelineTests.swift` 不存在。
- `plutil -lint WWIIHexV0.xcodeproj/project.pbxproj` 返回 OK。
- `rg` 搜索确认 `WWIIHexV0` 源码和 `project.pbxproj` 中不再引用：
  - `CabinetState`
  - `DirectiveBoard`
  - `StrategicDirective`
  - `TurnManager`
  - `GameAgent`
  - `AgentDecisionRecord`
  - `AgentContextBuilder`
  - `MockAIClient`
  - `LLMClient`
  - `LocalLLMDecisionProvider`
  - `AgentPromptBuilder`
  - `AgentDecisionParser`
  - `AgentCommandMapper`
  - `AgentConfiguration`
- 使用完整 Xcode 执行 `build-for-testing -quiet`，DerivedData 写入工作区 `DerivedData/`，退出码为 `0`。
- 使用已生成的 `.xctestrun` 执行 `test-without-building`，目标设备为 booted `iPhone 17 Pro` simulator，iOS `26.5 (23F77)`，arm64，退出码为 `0`。
- 从 `TestResults.xcresult/database.sqlite3` 读取结果确认：`41` 个 test case 全部为 `Success`，`TestIssues` 数量为 `0`。

构建与测试命令：

```bash
/usr/bin/env DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build-for-testing -project WWIIHexV0.xcodeproj -scheme WWIIHexV0 -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath ./DerivedData -quiet

/usr/bin/env DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test-without-building -xctestrun DerivedData/Build/Products/WWIIHexV0_iphonesimulator26.5-arm64-x86_64.xctestrun -destination 'platform=iOS Simulator,id=A1EA6908-3306-44E0-9F99-F0F77DAE2404' -derivedDataPath ./DerivedData -resultBundlePath ./TestResults.xcresult -quiet
```

说明：sandbox 内无法访问真实 CoreSimulator 设备列表，`test-without-building` 在 sandbox 内会因找不到 simulator 退出 `70`；最终测试是在获准访问 CoreSimulator 后执行并通过的。

注意：规划文档中仍会出现这些名称，这是预期结果，不代表源码残留。

## 7. 后续建议

后续 v0.5 工作先只写 agent 分工提示词和任务说明，不实现 Swift 代码。

建议下一步输出：

- Agent A-F 的中文任务提示词。
- 每个 agent 的输入文档、禁止事项、交付物。
- v0.5 的验收清单。
- 明确提醒执行 agent：只读规划，不改代码，除非用户再次明确要求实现。
