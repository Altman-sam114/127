先继续给我 v0.3 的执行方案，分几个 agent，分别干什么，先写个大概，一会再写提示词




星期三17:34






可以。v0.3 的核心目标是：做出 HOI4 式“前线 + 集团军自动布防 + 攻势方向”的第一版，不追求复杂，但要把系统骨架搭对。
建议分 6 个 agent。
Agent 1：前线规则与数据模型
负责定义 v0.3 的核心数据结构：
FrontLine
FrontSegment
ArmyGroup
FrontAssignment
OffensivePlan
BreakthroughObjective
FrontGapWarning
主要任务：
自动识别己方与敌方接壤边界。
把连续边界合并成一条前线。
判断前线长度、关键区块、危险缺口。
给后续 UI、AI、自动调兵提供统一数据。
重点：不要把前线写死成六角格逻辑，应该尽量基于 RegionNode / ProvinceNode 抽象。v0.3 如果还在六角格阶段，就做一个 HexRegionAdapter。
Agent 2：自动布防与调兵算法
负责“部队自动排成战线”。
主要任务：
根据前线长度和部队数量分配防区。
优先填补缺口。
优先守城市、要塞、河流、山地、补给节点。
低组织度、低补给单位自动后撤。
装甲单位不要平均铺开，保留给突破点。
输出一组合法 move / hold / resupply / retreat 命令。
核心原则：前线排兵用算法，不用 LLM 逐个排。
LLM 后续只说：
集中装甲于阿登北段，准备突破巴斯托涅方向。
算法负责拆成具体单位行动。
Agent 3：攻势箭头与突破计划
负责“从防守前线变成进攻计划”。
主要任务：
定义进攻目标区块。
定义突破方向。
计算进攻路径。
识别适合突破的前线段。
将装甲/摩步集中到突破点。
生成 OffensivePlan。
把计划拆成一组战术命令。
v0.3 不需要复杂大包围，只做三种基础计划：
holdLine：守住当前前线。
limitedAttack：局部推进 1-2 个区块。
breakthrough：集中机动部队突破目标方向。
Agent 4：元帅/将领 Agent 接口
负责把 v0.3 的前线系统接入 AI agent。
主要任务：
新增元帅 agent 概念。
元帅读取战区摘要，输出前线级计划。
将领读取集团军摘要，执行局部计划。
定义前线计划 JSON schema。
保留 MockAI，先不用真实 LLM。
记录 agent 的计划、理由、执行结果。
建议层级：
MarshalAgent
-> 生成 FrontPlan
-> Rule/Planner 校验并拆解
-> GeneralAgent 执行局部命令
-> RuleEngine 执行
不要让 agent 直接改单位位置。
Agent 5：前线 UI 与玩家操作
负责玩家能看到和操作前线。
主要任务：
地图上显示前线边界。
显示缺口、高风险段、补给差区域。
玩家可以选择一个集团军。
玩家可以把集团军分配到一条前线。
玩家可以设置进攻方向或目标区块。
UI 显示自动布防结果和 AI 计划摘要。
v0.3 UI 不要做复杂编辑器，先做基础交互：
选择集团军 -> 点击前线 -> 分配
选择前线 -> 点击目标 -> 设置攻势方向
Agent 6：测试、场景和平衡
负责让 v0.3 能稳定跑起来。
主要任务：
扩展阿登测试场景。
增加足够多的单位来验证前线。
写前线识别测试。
写自动布防测试。
写突破计划测试。
写 AI JSON schema 测试。
做性能检查，避免每回合重复重算过多内容。
测试重点：
前线能正确识别。
敌我边界变化后，前线能刷新。
有缺口时，算法优先补缺口。
低组织度单位会后撤。
装甲单位会集中到突破方向，而不是平均摊开。
AI 输出非法计划时不会破坏游戏状态。
推荐执行顺序
1. Agent 1：前线数据模型与识别
2. Agent 2：自动布防算法
3. Agent 6：基础测试和场景扩展
4. Agent 5：前线显示和玩家分配 UI
5. Agent 3：攻势箭头和突破计划
6. Agent 4：元帅/将领 agent 接入
7. Agent 6：最终集成测试和平衡
关键技术原则
v0.3 不应该追求“完整 HOI4 前线”。第一版只要做到：
能识别前线；
能显示前线；
能把集团军分配到前线；
能自动填补明显缺口；
能设置一个进攻方向；
AI 能输出前线级计划；
规则系统能把计划拆成合法命令。
这就是 v0.3 的合理边界。不要在这一版同时做复杂政治、多 agent 冲突、真实大地图、完整补给网和复杂战斗宽度。

先写agent1的提示词




星期三17:35






下面是 v0.3 Agent 1：前线规则与数据模型 的可复制提示词。
你是 v0.3 前线系统的实现 agent。你的任务是实现“前线规则与数据模型”的第一版，为后续自动布防、攻势箭头、元帅 agent 和 UI 显示提供稳定基础。

项目背景：
这是一款 iOS 二战回合制大战略/战棋混合游戏，技术栈为 Swift + SwiftUI + SpriteKit。v0 使用六角格测试板，但长期目标是接近《钢铁雄心4》的省份图/区块图底层。因此你实现前线系统时，不能把所有逻辑写死为纯六角格。当前可以通过 adapter 支持 hex，但核心概念应尽量抽象为 Region/Province。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

你的目标：
实现 v0.3 的前线识别与数据模型，不实现自动布防算法、不实现 UI、不接 LLM。你的产出必须让后续 agent 可以直接基于你的模型继续开发。

核心功能：
1. 定义前线相关模型：
   - FrontLine
   - FrontSegment
   - FrontEdge
   - ArmyGroup
   - FrontAssignment
   - FrontRiskLevel
   - FrontGapWarning
   - RegionRef / ProvinceRef 或兼容当前 hex 的抽象引用

2. 实现前线识别规则：
   - 前线 = 己方控制区块与敌方控制区块相邻的边界。
   - 支持按阵营计算所有前线。
   - 支持把相邻边界合并成连续 FrontLine。
   - 支持识别前线长度。
   - 支持识别关键区块，例如城市、要塞、补给源、道路节点。
   - 支持识别无己方部队覆盖的缺口。
   - 支持为前线段计算基础风险等级。

3. 提供适配层：
   - 如果当前工程仍是 hex 地图，实现 HexRegionAdapter。
   - adapter 负责把 HexCoord/HexTile 转换为 RegionRef/RegionNode。
   - 前线核心算法不要直接依赖 SpriteKit 或 UI。
   - 前线核心算法不要直接读取渲染节点。

4. 输出稳定 API：
   - FrontLineDetector
   - FrontAnalyzer
   - RegionGraphProviding 协议
   - UnitPositionProviding 协议
   - 可以从 GameState 生成前线摘要的方法

建议接口方向：
```swift
protocol RegionGraphProviding {
    func allRegions() -> [RegionNode]
    func neighbors(of region: RegionRef) -> [RegionRef]
    func controller(of region: RegionRef) -> Faction
    func terrainTags(of region: RegionRef) -> Set<RegionTerrainTag>
}

struct RegionRef: Codable, Hashable, Equatable {
    let id: String
}

struct FrontEdge: Codable, Hashable, Equatable {
    let friendlyRegion: RegionRef
    let enemyRegion: RegionRef
}

struct FrontSegment: Codable, Equatable {
    let id: String
    let edges: [FrontEdge]
    let keyRegions: [RegionRef]
    let riskLevel: FrontRiskLevel
    let gapWarnings: [FrontGapWarning]
}

struct FrontLine: Codable, Equatable {
    let id: String
    let faction: Faction
    let enemyFaction: Faction
    let segments: [FrontSegment]
    let totalLength: Int
}
风险等级第一版保持简单：
low：前线段附近有足够己方单位，且包含少量高价值区块。
medium：单位覆盖不足，或包含城市/补给点。
high：存在明显缺口，或敌方单位贴近关键城市/补给源。
critical：关键区块暴露且无己方单位覆盖。
缺口判定第一版保持简单：
某条 FrontEdge 的 friendlyRegion 及其 1 格邻域内没有己方可行动单位，视为缺口。
如果该区域是城市、要塞、补给源，缺口等级提高。
不要求实现复杂战斗宽度和真实兵力密度。
不要做的事：
不实现自动调兵。
不生成 move/attack 命令。
不实现 UI 显示。
不接本地 LLM。
不实现元帅/将领 agent。
不重构无关系统。
不把前线算法写进 SpriteKit 场景。
不让前线系统直接修改 GameState。
测试要求：
至少添加单元测试覆盖：
两个阵营相邻时能识别 FrontEdge。
多条连续边界能合并为 FrontLine。
不相连的边界会形成不同 FrontLine 或不同 segment。
城市/要塞/补给源能被识别为 keyRegions。
无己方单位覆盖时产生 FrontGapWarning。
有己方单位覆盖时不产生缺口警告。
风险等级能根据缺口和关键区块提高。
HexRegionAdapter 能从当前 hex 地图生成 RegionNode。
完成标准：
工程可编译。
新增模型 Codable/Equatable/Hashable 尽量完整。
前线识别逻辑有单元测试。
不破坏 v0 现有移动、攻击、补给、AI 命令管线。
后续 Agent 2 可以直接使用你的 FrontLine/FrontSegment 数据做自动布防。
后续 Agent 5 可以直接使用你的 FrontLine 数据做 UI 显示。
请先检查当前代码结构，再按现有目录风格实现。如果当前项目还没有完整代码，只创建最小必要的 Core/Rules 模型和测试骨架，并保持命名与文档一致。
```

星期三17:36






先写agent2的提示词




星期三17:38






下面是 v0.3 Agent 2：自动布防与调兵算法 的可复制提示词。
你是 v0.3 前线系统的实现 agent。你的任务是实现“自动布防与调兵算法”的第一版，让集团军可以被分配到前线后，自动填补缺口、选择防区、安排休整和集中机动部队。

项目背景：
这是一款 iOS 二战回合制大战略/战棋混合游戏，技术栈为 Swift + SwiftUI + SpriteKit。v0 使用六角格测试板，但长期目标是接近《钢铁雄心4》的省份图/区块图底层。v0.3 的目标不是完整复刻 HOI4，而是实现第一版可测试、可解释、可扩展的前线自动布防。

请先阅读以下文档：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

并优先检查 Agent 1 是否已经实现以下内容：
- FrontLine
- FrontSegment
- FrontEdge
- ArmyGroup
- FrontAssignment
- FrontRiskLevel
- FrontGapWarning
- RegionRef / RegionNode
- RegionGraphProviding
- UnitPositionProviding
- FrontLineDetector
- FrontAnalyzer
- HexRegionAdapter

如果 Agent 1 的实现不存在或不完整，请只补齐你必须依赖的最小接口，不要大规模重写 Agent 1 的职责。

你的目标：
实现 v0.3 的自动布防与调兵规划器。它读取前线、集团军、单位状态和地图摘要，输出一组“建议命令”或“调兵计划”。这些命令仍必须交给现有 RuleEngine/CommandValidator 校验后才能执行。

核心功能：
1. 定义自动布防相关模型：
   - FrontDefensePlan
   - FrontPosture
   - DefenseSlot
   - UnitAssignment
   - ReserveGroup
   - RedeploymentPlan
   - PlannerWarning
   - PlannerScore 或 AssignmentScore

2. 实现自动布防算法：
   - 根据 FrontLine / FrontSegment 生成需要防守的 DefenseSlot。
   - 根据地形、关键区块、风险等级，为每个 slot 计算优先级。
   - 将步兵、守备、炮兵优先分配到城市、要塞、河流、山地等防御区块。
   - 将装甲、摩托化单位优先保留为预备队或突破预备，不要平均铺满整条战线。
   - 前线存在缺口时，优先把最近的可用单位派去填补。
   - 单位组织度低、补给差、被包围时，不应被分配到高风险前线，优先安排 retreat/resupply/hold。
   - 前线太长、兵力不足时，输出 warning，而不是生成不合理计划。

3. 输出计划而不是直接改状态：
   - 规划器不能直接修改 GameState。
   - 规划器输出 FrontDefensePlan。
   - FrontDefensePlan 可以转换为 Command 列表。
   - Command 仍由 CommandValidator 校验，由 RuleEngine 执行。
   - 如果某个计划无法转换成合法命令，要记录 PlannerWarning。

4. 提供稳定 API：
```swift
struct FrontDefensePlan: Codable, Equatable {
    let id: String
    let faction: Faction
    let frontLineId: String
    let armyGroupId: String
    let posture: FrontPosture
    let slotAssignments: [UnitAssignment]
    let reserveGroups: [ReserveGroup]
    let proposedCommands: [Command]
    let warnings: [PlannerWarning]
}

enum FrontPosture: String, Codable {
    case hold
    case cautious
    case balanced
    case aggressive
}

struct DefenseSlot: Codable, Hashable, Equatable {
    let id: String
    let region: RegionRef
    let priority: Int
    let preferredUnitRoles: [UnitRole]
    let riskLevel: FrontRiskLevel
}

struct UnitAssignment: Codable, Equatable {
    let unitId: String
    let slotId: String?
    let role: AssignedRole
    let reason: String
}

enum AssignedRole: String, Codable {
    case lineDefense
    case strongpointDefense
    case artillerySupport
    case mobileReserve
    case breakthroughReserve
    case restAndRefit
    case retreat
}

struct ReserveGroup: Codable, Equatable {
    let id: String
    let unitIds: [String]
    let purpose: ReservePurpose
    let anchorRegion: RegionRef?
}

enum ReservePurpose: String, Codable {
    case mobileResponse
    case breakthroughPreparation
    case emergencyPlug
}
建议提供的核心服务：
protocol FrontDefensePlanning {
    func makeDefensePlan(
        frontLine: FrontLine,
        assignment: FrontAssignment,
        armyGroup: ArmyGroup,
        state: GameState
    ) -> FrontDefensePlan
}

final class FrontDefensePlanner: FrontDefensePlanning {
    ...
}
单位分类规则：
infantry / garrison：优先 lineDefense、strongpointDefense。
artillery：优先 artillerySupport，靠近但不一定站在最危险区块。
tank / panzer：优先 mobileReserve、breakthroughReserve。
motorized / mechanized：优先 mobileReserve、emergencyPlug。
organization 低于阈值：restAndRefit。
unsupplied / encircled：优先 resupply 或 retreat。
已行动单位：可分配角色，但不要生成移动命令。

第一版评分建议：
每个 DefenseSlot 的优先级可以由以下因素组成：
base risk：low 1 / medium 3 / high 6 / critical 10
city +3
fortress +4
supply source +5
road junction +2
existing gap +5
enemy adjacent unit +2
poor supply -2，表示不适合堆太多部队

单位匹配评分：
防御地形 + 步兵/守备加分。
高风险缺口 + 机动单位加分。
要塞/城市 + 炮兵支援加分。
装甲长期站线减分。
低组织度进高风险区减分。
距离越近越优先。
补给越好越优先。
命令生成规则：如果单位已在目标 slot 附近，可以生成 hold。
如果单位未行动且有合法路径，可以生成 move。
如果单位低组织度或低补给，可以生成 resupply。
如果单位被包围且存在安全后撤格，可以生成 retreat 或等价的 move。
如果无法确认合法性，只生成计划 assignment，不生成命令，并记录 warning。
不生成 attack 命令；攻击计划由 Agent 3 负责。

不要做的事：
不实现前线识别，这是 Agent 1 的职责。
不实现攻势箭头和突破计划，这是 Agent 3 的职责。
不实现 UI，这是 Agent 5 的职责。
不接 LLM，这是 Agent 4 的职责。
不直接修改 GameState。
不绕过 CommandValidator。
不把算法写进 SpriteKit。
不为了填线平均分配所有装甲单位。
不在本阶段实现完整 HOI4 战斗宽度、计划准备度、复杂补给网。
测试要求：
至少添加单元测试覆盖：
前线有缺口时，最近可用单位优先被分配到缺口。
城市/要塞/补给源 slot 优先级高于普通平原。
步兵优先进入 lineDefense，装甲优先进入 reserve。
低组织度单位不会被派往 high/critical slot。
unsupplied 或 encircled 单位优先 resupply/retreat。
兵力不足时输出 PlannerWarning。
已行动单位不会生成 move 命令。
生成的 proposedCommands 都会经过 CommandValidator；非法命令不会直接执行。
前线变化后重新规划不会依赖旧缓存产生错误结果。
Hex 地图场景下 planner 能基于 Agent 1 的 adapter 正常工作。
完成标准：
工程可编译。
FrontDefensePlanner 有清晰、可测试的输入输出。
不破坏 v0 现有移动、攻击、补给、AI 命令管线。
后续 Agent 3 可以在 reserveGroups 和 DefenseSlot 基础上实现突破计划。
后续 Agent 4 可以让元帅 agent 选择 FrontPosture，然后调用你的 planner。
后续 Agent 5 可以显示 FrontDefensePlan、slotAssignments、reserveGroups 和 warnings。
实现策略：
先做确定性算法，不要引入随机数。所有评分理由尽量写入 reason 或 warning，方便 UI 和日志显示。优先让系统可解释、可测试，而不是追求复杂。
```

星期三17:40






写agent2的提示词




星期三17:40






你是 v0.3 前线系统的 Agent 2，负责实现“自动布防与调兵算法”。

项目背景：
这是一款 iOS 二战回合制大战略/战棋混合游戏，技术栈为 Swift + SwiftUI + SpriteKit。v0 使用六角格测试板，长期目标是演进到类似《钢铁雄心4》的省份图/区块图底层。v0.3 的重点是实现第一版“前线 + 集团军自动布防”，不是完整复刻 HOI4。

请先阅读：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

并检查 Agent 1 是否已经实现：
- FrontLine
- FrontSegment
- FrontEdge
- ArmyGroup
- FrontAssignment
- FrontRiskLevel
- FrontGapWarning
- RegionRef / RegionNode
- RegionGraphProviding
- UnitPositionProviding
- FrontLineDetector
- FrontAnalyzer
- HexRegionAdapter

如果 Agent 1 的实现不存在或不完整，只补齐你必须依赖的最小接口，不要重写 Agent 1 的职责。

你的目标：
实现一个确定性 `FrontDefensePlanner`，读取前线、集团军、单位状态和地图摘要，输出 `FrontDefensePlan`。规划器不能直接修改 `GameState`。所有行动必须转换成 `Command` 后交给现有 `CommandValidator` / `RuleEngine` 校验和执行。

必须实现的模型：
```swift
struct FrontDefensePlan: Codable, Equatable {
    let id: String
    let faction: Faction
    let frontLineId: String
    let armyGroupId: String
    let posture: FrontPosture
    let slots: [DefenseSlot]
    let slotAssignments: [UnitAssignment]
    let reserveGroups: [ReserveGroup]
    let proposedCommands: [Command]
    let warnings: [PlannerWarning]
}

enum FrontPosture: String, Codable {
    case hold
    case cautious
    case balanced
    case aggressive
}

struct DefenseSlot: Codable, Hashable, Equatable {
    let id: String
    let region: RegionRef
    let priority: Int
    let preferredUnitRoles: [UnitRole]
    let riskLevel: FrontRiskLevel
    let reason: String
}

struct UnitAssignment: Codable, Equatable {
    let unitId: String
    let slotId: String?
    let role: AssignedRole
    let score: Int
    let reason: String
}

enum AssignedRole: String, Codable {
    case lineDefense
    case strongpointDefense
    case artillerySupport
    case mobileReserve
    case breakthroughReserve
    case restAndRefit
    case retreat
}

struct ReserveGroup: Codable, Equatable {
    let id: String
    let unitIds: [String]
    let purpose: ReservePurpose
    let anchorRegion: RegionRef?
}

enum ReservePurpose: String, Codable {
    case mobileResponse
    case breakthroughPreparation
    case emergencyPlug
}

struct PlannerWarning: Codable, Equatable {
    let code: PlannerWarningCode
    let message: String
    let affectedRegion: RegionRef?
    let affectedUnitId: String?
}

enum PlannerWarningCode: String, Codable {
    case insufficientUnits
    case noLegalPath
    case unitAlreadyActed
    case poorSupply
    case lowOrganization
    case exposedCriticalRegion
    case commandValidationFailed
}
如果项目中已有同名或相近模型，优先扩展现有模型，不要重复定义冲突类型。
必须实现的服务接口：
protocol FrontDefensePlanning {
    func makeDefensePlan(
        frontLine: FrontLine,
        assignment: FrontAssignment,
        armyGroup: ArmyGroup,
        state: GameState
    ) -> FrontDefensePlan
}

final class FrontDefensePlanner: FrontDefensePlanning {
    ...
}
核心算法要求：
生成 DefenseSlot根据 FrontLine / FrontSegment 生成需要防守的 slot。
一个重要前线区块至少对应一个 slot。
城市、要塞、补给源、道路节点、高风险缺口优先级更高。
不要为每条边机械生成过多 slot，避免后续大地图性能问题。

slot 优先级评分
建议第一版使用确定性整数评分：risk low +1
risk medium +3
risk high +6
risk critical +10
city +3
fortress +4
supply source +5
road junction +2
existing gap +5
enemy adjacent unit +2
poor own supply -2

单位分类infantry / garrison：优先 lineDefense、strongpointDefense
artillery：优先 artillerySupport
tank / panzer：优先 mobileReserve、breakthroughReserve
motorized / mechanized：优先 mobileReserve、emergencyPlug
organization 低于阈值：优先 restAndRefit
unsupplied / encircled：优先 resupply 或 retreat
已行动单位：可以被记录为 assignment，但不要生成 move 命令

单位匹配评分
评分应考虑：距离越近越优先
防御地形匹配步兵/守备加分
高风险缺口匹配机动单位加分
城市/要塞匹配炮兵支援加分
装甲长期站线减分
低组织度进入高风险区减分
补给状态差减分

预备队规则不要把所有装甲和摩托化单位平均分配到前线。
至少保留一部分机动单位作为 mobileReserve 或 breakthroughReserve。
如果存在 critical 缺口，可以用机动单位作为 emergencyPlug。
预备队需要有 anchorRegion，优先选择道路节点、补给源附近或前线后方安全区块。

命令生成规则规划器可以生成 move、hold、resupply，必要时用等价 move 表达 retreat。
不生成 attack，攻击与突破计划由 Agent 3 负责。
如果单位已在目标 slot 或相邻区块，可生成 hold。
如果单位未行动且存在合法路径，可生成 move。
如果单位低组织度或低补给，可生成 resupply。
如果无法确认合法性，只保留 assignment，不生成命令，并记录 warning。
所有 proposedCommands 必须经过 CommandValidator 校验；校验失败的命令不得执行，只记录 warning。

规划器不得直接修改状态不直接修改 GameState。
不直接移动单位。
不直接改变补给、组织度、控制权。
不绕过 RuleEngine。
不把算法写进 SpriteKit 或 SwiftUI。

建议实现步骤：
查清现有 Core、Rules、Commands、FrontLine 相关代码。
补齐自动布防模型。
实现 FrontDefensePlanner.makeDefensePlan。
实现 slot 生成与优先级评分。
实现单位角色分类与匹配评分。
实现预备队分组。
实现 proposedCommands 生成与校验接入。
添加单元测试。
确认现有 v0 移动、攻击、补给、AI 命令管线没有被破坏。
测试要求：
至少覆盖以下场景：
前线有缺口时，最近可用单位优先分配到缺口。
城市/要塞/补给源 slot 优先级高于普通平原。
步兵优先进入 lineDefense，装甲优先进入 reserve。
低组织度单位不会被派往 high/critical slot。
unsupplied 或 encircled 单位优先 resupply/retreat。
兵力不足时输出 insufficientUnits warning。
已行动单位不会生成 move 命令。
proposedCommands 经过 CommandValidator，非法命令不会进入执行。
前线变化后重新规划，不依赖旧缓存产生错误结果。
Hex 地图场景下 planner 能基于 Agent 1 的 adapter 正常工作。
完成标准：
工程可编译。
FrontDefensePlanner 输入输出清晰、确定性、可测试。
不破坏 v0 现有移动、攻击、补给、AI 管线。
后续 Agent 3 可以基于 reserveGroups 和 DefenseSlot 实现攻势箭头与突破计划。
后续 Agent 4 可以让元帅 agent 选择 FrontPosture 并调用 planner。
后续 Agent 5 可以显示 FrontDefensePlan、slotAssignments、reserveGroups 和 warnings。
重要边界：
本阶段只做“布防与调兵规划”。不要实现完整前线 UI，不要接 LLM，不要做攻势箭头，不要做多 agent 仲裁，不要做复杂 HOI4 战斗宽度。优先让算法简单、确定、可解释。
```

星期三17:42

写agent3的提示词




星期三17:42






你是 v0.3 前线系统的 Agent 3，负责实现“攻势箭头与突破计划”。

项目背景：
这是一款 iOS 二战回合制大战略/战棋混合游戏，技术栈为 Swift + SwiftUI + SpriteKit。v0 使用六角格测试板，长期目标是演进到类似《钢铁雄心4》的省份图/区块图底层。v0.3 已开始引入前线系统。你的任务不是做完整 HOI4 作战计划，而是实现第一版可测试、可解释、可扩展的攻势计划系统。

请先阅读：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

并检查 Agent 1 和 Agent 2 是否已经实现：
- FrontLine
- FrontSegment
- FrontEdge
- ArmyGroup
- FrontAssignment
- FrontRiskLevel
- FrontGapWarning
- RegionRef / RegionNode
- RegionGraphProviding
- FrontLineDetector
- FrontAnalyzer
- FrontDefensePlan
- DefenseSlot
- UnitAssignment
- ReserveGroup
- FrontPosture
- FrontDefensePlanner

如果这些实现不存在或不完整，只补齐你必须依赖的最小接口，不要重写 Agent 1/2 的职责。

你的目标：
实现 v0.3 的第一版攻势计划系统。系统读取前线、防御计划、集团军、目标区块和当前 GameState，生成 `OffensivePlan`。`OffensivePlan` 可以表达防守、有限进攻、突破三类计划，并能转换为一组建议命令。所有命令必须经过现有 `CommandValidator` / `RuleEngine` 校验后才能执行。

必须实现的模型：
```swift
struct OffensivePlan: Codable, Equatable {
    let id: String
    let faction: Faction
    let armyGroupId: String
    let frontLineId: String
    let planType: OffensivePlanType
    let startRegions: [RegionRef]
    let objectiveRegions: [RegionRef]
    let mainAxis: OffensiveAxis?
    let supportingAxes: [OffensiveAxis]
    let assignedUnitIds: [String]
    let reserveUnitIds: [String]
    let proposedCommands: [Command]
    let warnings: [OffensivePlanWarning]
    let summary: String
}

enum OffensivePlanType: String, Codable {
    case holdLine
    case limitedAttack
    case breakthrough
}

struct OffensiveAxis: Codable, Equatable {
    let id: String
    let from: RegionRef
    let to: RegionRef
    let path: [RegionRef]
    let priority: Int
    let purpose: OffensiveAxisPurpose
}

enum OffensiveAxisPurpose: String, Codable {
    case pinningAttack
    case mainBreakthrough
    case exploitation
    case supplyCutoff
}

struct BreakthroughObjective: Codable, Equatable {
    let targetRegion: RegionRef
    let reason: String
    let priority: Int
}

struct OffensivePlanWarning: Codable, Equatable {
    let code: OffensivePlanWarningCode
    let message: String
    let affectedRegion: RegionRef?
    let affectedUnitId: String?
}

enum OffensivePlanWarningCode: String, Codable {
    case noValidObjective
    case noValidAxis
    case insufficientMobileUnits
    case insufficientOrganization
    case poorSupply
    case blockedPath
    case commandValidationFailed
    case objectiveTooFar
}
如果项目已有相近模型，优先扩展现有模型，不要重复定义冲突类型。
必须实现的服务接口：
protocol OffensivePlanning {
    func makeOffensivePlan(
        frontLine: FrontLine,
        defensePlan: FrontDefensePlan?,
        assignment: FrontAssignment,
        armyGroup: ArmyGroup,
        objectiveRegions: [RegionRef],
        planType: OffensivePlanType,
        state: GameState
    ) -> OffensivePlan
}

final class OffensivePlanner: OffensivePlanning {
    ...
}
核心功能要求：
支持三种计划类型holdLine：不主动进攻，只生成 hold/resupply/reposition 类建议命令。
limitedAttack：选择 1-2 个邻近目标区块，进行局部推进。
breakthrough：选择一个主突破轴线，集中装甲/摩托化单位向目标方向推进。

选择突破起点
突破起点应优先来自：前线中的 high/critical 段；
敌方防守较弱的相邻区块；
我方附近有道路、补给源或机动预备队的区块；
防御计划中 breakthroughReserve 或 mobileReserve 的 anchor 附近；
不要选择己方低补给、被包围、组织度普遍过低的区域。

选择进攻目标
目标区块优先级建议：城市 +5
补给源 +6
道路节点 +3
敌方薄弱区块 +4
可以切断敌方补给的位置 +5
距离过远 -3
需要跨河/山地强攻 -2
我方补给差 -4

v0.3 不要求完整战略包围，只需要能识别“推进后可能威胁补给线/道路节点”的简单目标。
计算攻势轴线基于 RegionGraphProviding 计算从突破起点到目标区块的路径。
第一版可以使用 BFS 或 Dijkstra。
路径成本应考虑地形、道路、河流、敌方控制、己方补给。
limitedAttack 路径长度建议 1-2。
breakthrough 路径长度建议 2-4。
超出距离时记录 objectiveTooFar warning。

分配单位装甲、摩托化、机械化优先分配给主突破轴线。
步兵/守备优先留在前线防守或执行辅助牵制。
炮兵优先支援主突破起点或城市/要塞目标。
低组织度、低补给、已被包围单位不应参与突破。
已行动单位可以记录在计划中，但不要生成 move 命令。

生成 proposedCommands
可生成：move
hold
resupply
可选：如果现有命令系统支持 attack，可以为相邻目标生成 attack；否则只生成 move 到进攻起点/集结点。

限制：
不生成越权命令。
不直接修改 GameState。
不直接改变控制权。
不强制执行非法攻击。
所有 proposedCommands 必须经过 CommandValidator 校验。
校验失败时不执行，只记录 commandValidationFailed warning。
与 Agent 2 的衔接
如果存在 FrontDefensePlan：优先使用 reserveGroups 中的 breakthroughPreparation 和 mobileResponse 单位。
不要随意抽走所有 lineDefense 单位。
critical 缺口未处理时，breakthrough 计划应降级为 holdLine 或输出 warning。
如果防御计划兵力严重不足，不应强行发起突破。

输出解释
OffensivePlan.summary 必须包含简短可读说明，例如：
主攻轴线从 Bastogne East 指向 Bastogne，使用第1装甲师和第2摩步师，炮兵支援；因北侧缺口严重，保留一个摩步单位为机动预备。
不要做的事：
不实现前线识别，这是 Agent 1 的职责。
不实现自动布防，这是 Agent 2 的职责。
不实现 UI，这是 Agent 5 的职责。
不接 LLM，这是 Agent 4 的职责。
不做复杂多回合计划准备度。
不做完整 HOI4 战斗宽度。
不做复杂大包围 AI。
不把算法写进 SpriteKit 或 SwiftUI。
不绕过 CommandValidator / RuleEngine。
建议实现步骤：
查清现有前线、布防、命令、地图、单位模型。
补齐 OffensivePlan 相关模型。
实现 OffensivePlanner.makeOffensivePlan。
实现目标评分。
实现突破起点选择。
实现 BFS/Dijkstra 轴线路径计算。
实现单位筛选与分配。
实现 proposedCommands 生成和校验。
添加单元测试。
确认 v0 现有移动、攻击、补给、AI 命令管线不被破坏。
测试要求：
至少覆盖以下场景：
holdLine 不生成攻击命令，只生成 hold/resupply/reposition。
limitedAttack 选择邻近 1-2 格目标。
breakthrough 优先使用装甲/摩托化单位。
步兵不会被大量抽离关键防线。
低组织度或低补给单位不会参与突破。
没有可用机动单位时输出 insufficientMobileUnits warning。
目标过远时输出 objectiveTooFar warning。
路径被阻挡时输出 blockedPath warning。
critical 缺口未解决时，突破计划降级或输出 warning。
所有 proposedCommands 经过 CommandValidator，非法命令不会执行。
攻势轴线路径在 hex adapter / region graph 下都能计算。
重新规划不依赖旧缓存产生错误结果。
完成标准：
工程可编译。
OffensivePlanner 输入输出清晰、确定性、可测试。
OffensivePlan 能被后续 UI 显示为攻势箭头。
不破坏 v0 现有移动、攻击、补给、AI 管线。
后续 Agent 4 可以让元帅 agent 选择 OffensivePlanType 和目标区块。
后续 Agent 5 可以显示 mainAxis、supportingAxes、objectiveRegions、warnings 和 summary。
重要边界：
本阶段只做“攻势计划与突破轴线”。不要实现复杂政治、多 agent 仲裁、完整大战略大地图、真实 LLM 或前线 UI。第一版优先稳定、确定、可解释。
```

星期三17:43






写agent4的提示词




星期三17:44






你是 v0.3 前线系统的 Agent 4，负责实现“元帅/将领 Agent 接口”。

项目背景：
这是一款 iOS 二战回合制大战略/战棋混合游戏，技术栈为 Swift + SwiftUI + SpriteKit。v0 使用六角格测试板，长期目标是演进到类似《钢铁雄心4》的省份图/区块图底层。v0.3 已引入前线识别、自动布防、攻势计划。你的任务是把这些系统接入 AI agent 管线，让“元帅 agent”能基于战区摘要选择前线姿态、攻势目标和作战计划，再由确定性规划器拆解为可校验命令。

请先阅读：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

并检查 Agent 1、Agent 2、Agent 3 是否已经实现：
- FrontLine
- FrontSegment
- FrontEdge
- ArmyGroup
- FrontAssignment
- FrontRiskLevel
- FrontGapWarning
- RegionRef / RegionNode
- RegionGraphProviding
- FrontLineDetector
- FrontAnalyzer
- FrontDefensePlan
- FrontDefensePlanner
- OffensivePlan
- OffensivePlanner
- OffensivePlanType
- OffensiveAxis

如果这些实现不存在或不完整，只补齐你必须依赖的最小接口，不要重写 Agent 1/2/3 的职责。

你的目标：
实现 v0.3 的元帅/将领 agent 接口。agent 不直接移动单位，不直接攻击，不直接修改 GameState。agent 只输出“前线级作战意图 JSON”。系统解析、校验后，调用 FrontDefensePlanner / OffensivePlanner 生成计划和 proposedCommands，最后仍由 CommandValidator / RuleEngine 校验执行。

核心职责：
1. 定义元帅 agent 数据模型。
2. 定义战区级 AgentContext。
3. 定义前线计划 JSON schema。
4. 实现 MockMarshalDecisionProvider。
5. 实现 Agent 输出解析与校验。
6. 把 agent 输出接到 FrontDefensePlanner / OffensivePlanner。
7. 记录 agent 决策日志，供后续 UI 显示。
8. 保留本地 LLM 接口位置，但 v0.3 默认使用 MockAI。

必须实现或扩展的模型：
```swift
struct MarshalAgent: Codable, Identifiable, Equatable {
    let id: String
    var name: String
    var faction: Faction
    var assignedArmyGroupIds: [String]
    var personalityPrompt: String
    var commandStyle: MarshalCommandStyle
    var lastDecisionJSON: String?
    var lastDecisionSummary: String?
}

enum MarshalCommandStyle: String, Codable {
    case cautiousDefense
    case balanced
    case aggressiveBreakthrough
    case elasticDefense
}

struct TheaterAgentContext: Codable, Equatable {
    let agentId: String
    let faction: Faction
    let turn: Int
    let personalitySummary: String
    let armyGroups: [ArmyGroupSummary]
    let frontLines: [FrontLineSummary]
    let defensePlans: [FrontDefensePlanSummary]
    let offensivePlans: [OffensivePlanSummary]
    let visibleEnemySummary: EnemyForceSummary
    let supplySummary: SupplySummary
    let recentEvents: [GameEvent]
    let playerDirective: String?
}

struct MarshalDecisionEnvelope: Codable, Equatable {
    let schemaVersion: Int
    let agentId: String
    let turn: Int
    let intent: String
    let directives: [FrontDirective]
    let rationale: String
}

struct FrontDirective: Codable, Equatable {
    let frontLineId: String
    let armyGroupId: String
    let posture: FrontPosture
    let offensivePlanType: OffensivePlanType
    let objectiveRegions: [RegionRef]
    let priority: Int
    let reason: String
}

struct AgentDecisionRecord: Codable, Equatable {
    let id: String
    let turn: Int
    let agentId: String
    let rawJSON: String
    let parsedIntent: String?
    let directives: [FrontDirective]
    let generatedDefensePlanIds: [String]
    let generatedOffensivePlanIds: [String]
    let validationWarnings: [String]
}
如果项目已有 GeneralAgent、AgentContext、DecisionProvider、AgentDecisionEnvelope 等类型，优先扩展现有 agent 架构，不要创建互相冲突的平行系统。
必须实现的服务接口：
protocol MarshalDecisionProviding {
    func decide(context: TheaterAgentContext) async throws -> MarshalDecisionEnvelope
}

final class MockMarshalDecisionProvider: MarshalDecisionProviding {
    ...
}

final class MarshalAgentController {
    func runMarshalTurn(
        agent: MarshalAgent,
        state: GameState
    ) async -> MarshalAgentTurnResult
}
建议结果模型：
struct MarshalAgentTurnResult: Codable, Equatable {
    let agentId: String
    let decisionRecord: AgentDecisionRecord
    let defensePlans: [FrontDefensePlan]
    let offensivePlans: [OffensivePlan]
    let proposedCommands: [Command]
    let warnings: [String]
}
核心流程：
GameState
-> FrontLineDetector / FrontAnalyzer
-> TheaterAgentContextBuilder
-> MarshalDecisionProvider
-> MarshalDecisionEnvelope JSON
-> MarshalDirectiveValidator
-> FrontDefensePlanner
-> OffensivePlanner
-> proposedCommands
-> CommandValidator
-> GameEvent / AgentDecisionRecord
JSON 输出格式要求：
agent 必须输出严格 JSON，不输出散文。
示例：
{
  "schemaVersion": 1,
  "agentId": "model",
  "turn": 5,
  "intent": "stabilize_center_prepare_breakthrough",
  "directives": [
    {
      "frontLineId": "front_germany_allies_center",
      "armyGroupId": "army_group_a",
      "posture": "balanced",
      "offensivePlanType": "breakthrough",
      "objectiveRegions": [
        { "id": "bastogne" }
      ],
      "priority": 8,
      "reason": "Bastogne is a key road and supply objective. Mobile reserves are available near the center."
    }
  ],
  "rationale": "Hold the northern flank while concentrating mobile units for a center breakthrough."
}
决策校验规则：
schemaVersion 必须匹配当前版本。
agentId 必须存在。
frontLineId 必须存在。
armyGroupId 必须归属该 agent 或该阵营。
posture 必须是合法 FrontPosture。
offensivePlanType 必须是合法 OffensivePlanType。
objectiveRegions 必须存在于当前 RegionGraph。
不允许引用不可见或不应被 agent 知道的敌方具体单位。
不允许一条前线被多个互相冲突的 directive 重复控制。
非法 directive 只跳过并记录 warning，不得导致崩溃。
MockMarshalDecisionProvider 行为：
如果存在 critical 缺口：选择 holdLine 或 cautious，优先修补缺口。
如果前线稳定且有装甲/摩托化预备队：选择 breakthrough。
如果兵力不足：选择 holdLine 并输出 warning。
如果目标城市相邻或距离较近：选择 limitedAttack。
德军古德里安/进攻型元帅倾向 aggressiveBreakthrough，但不能无视低补给和低组织度。
Mock 决策必须确定性，不使用随机数。
与 Agent 2/3 的衔接：
每个合法 FrontDirective 先调用 FrontDefensePlanner 生成 FrontDefensePlan。
如果 directive 的 offensivePlanType == holdLine，不调用或只生成空 OffensivePlan。
如果是 limitedAttack 或 breakthrough，基于防御计划调用 OffensivePlanner。
如果防御计划包含 critical warning，进攻计划应降级或记录 warning。
最终 proposedCommands 汇总后仍必须经过 CommandValidator。
PromptBuilder 要求：
如果已有 LLMClient 或 LocalLLMDecisionProvider，可以新增：
struct MarshalPromptBuilder {
    func makePrompt(context: TheaterAgentContext) -> String
}
prompt 必须强调：
只能输出 JSON。
不允许直接写自然语言命令。
不允许引用不可见敌军。
不允许越过规则系统。
决策粒度是前线/集团军，不是每个单位逐步移动。
不要做的事：
不实现前线识别，这是 Agent 1 的职责。
不实现自动布防算法，这是 Agent 2 的职责。
不实现攻势路径算法，这是 Agent 3 的职责。
不实现 UI，这是 Agent 5 的职责。
不强制启用真实本地 LLM。
不直接修改 GameState。
不直接执行 Command。
不绕过 CommandValidator / RuleEngine。
不做多 agent 政治冲突、忠诚、抗命、政变。
不让 agent 每个单位逐步下达移动命令。
不把 agent 逻辑写进 SpriteKit 或 SwiftUI。
建议实现步骤：
检查现有 agent、DecisionProvider、Command、GameEvent 架构。
定义或扩展 MarshalAgent、TheaterAgentContext、MarshalDecisionEnvelope。
实现 TheaterAgentContextBuilder。
实现 MarshalDirectiveValidator。
实现 MockMarshalDecisionProvider。
实现 MarshalAgentController，把 directive 接到 FrontDefensePlanner / OffensivePlanner。
实现 AgentDecisionRecord 和日志事件。
可选实现 MarshalPromptBuilder，但 v0.3 默认不启用真实 LLM。
添加单元测试。
确认 v0 现有 MockAI、移动、攻击、补给、命令管线不被破坏。
测试要求：
至少覆盖以下场景：
TheaterAgentContext 不包含不可见敌军真实信息。
MockMarshalDecisionProvider 在 critical 缺口时选择 hold/cautious。
前线稳定且有机动预备队时选择 breakthrough。
兵力不足时不会强行突破。
非法 frontLineId 被跳过并记录 warning。
非法 objectiveRegion 被跳过并记录 warning。
合法 directive 能生成 FrontDefensePlan。
breakthrough directive 能继续生成 OffensivePlan。
proposedCommands 经过 CommandValidator，非法命令不会执行。
malformed JSON 不会导致崩溃，GameState 不变。
agent 决策会写入 AgentDecisionRecord / GameEvent。
没有真实 LLM 时，MockMarshalDecisionProvider 能完整跑通。
完成标准：
工程可编译。
元帅 agent 可在 v0.3 中基于前线摘要输出前线级计划。
agent 输出 JSON 可解析、校验、记录。
合法 directive 能调用 Agent 2/3 的规划器。
不破坏 v0 现有移动、攻击、补给、AI 命令管线。
后续 Agent 5 可以展示 agent 的 rawJSON、rationale、directives、生成计划和 warnings。
系统默认使用 MockMarshalDecisionProvider，真实 LLM 仅作为可选接口。
重要边界：
本阶段只做“元帅/将领 agent 接口与计划管线”。不要做 UI，不要做真实 LLM 强依赖，不要做角色忠诚/抗命，不要做多 agent 政治系统。第一版必须确定、可测试、可解释。
```

写agent5的提示词




星期三17:46






你是 v0.3 前线系统的 Agent 5，负责实现“前线 UI 与玩家操作”。

项目背景：
这是一款 iOS 二战回合制大战略/战棋混合游戏，技术栈为 Swift + SwiftUI + SpriteKit。v0 使用六角格测试板，长期目标是演进到类似《钢铁雄心4》的省份图/区块图底层。v0.3 已经规划前线识别、自动布防、攻势计划、元帅 agent 管线。你的任务是把这些系统做成玩家能看、能点、能分配、能理解的 UI。

请先阅读：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

并检查 Agent 1、Agent 2、Agent 3、Agent 4 是否已经实现：
- FrontLine
- FrontSegment
- FrontEdge
- FrontRiskLevel
- FrontGapWarning
- ArmyGroup
- FrontAssignment
- RegionRef / RegionNode
- FrontLineDetector
- FrontDefensePlan
- FrontDefensePlanner
- OffensivePlan
- OffensiveAxis
- OffensivePlanner
- MarshalAgent
- MarshalDecisionEnvelope
- AgentDecisionRecord

如果这些实现不存在或不完整，只补齐 UI 必须依赖的最小接口或 mock 数据，不要重写 Agent 1/2/3/4 的职责。

你的目标：
实现 v0.3 的前线 UI，让玩家可以：
1. 在地图上看到前线。
2. 看到前线风险、缺口和关键区块。
3. 选择集团军。
4. 把集团军分配到前线。
5. 查看自动布防计划。
6. 设置有限进攻或突破目标。
7. 查看攻势箭头。
8. 查看元帅/将领 agent 的计划、JSON、理由和警告。

核心原则：
- UI 不能直接修改 GameState。
- UI 只能提交意图或 Command。
- 前线算法、布防算法、攻势算法、agent 决策不写在 UI 里。
- SpriteKit 负责地图叠加层显示和点击命中。
- SwiftUI 负责面板、列表、按钮、日志、详情。
- 所有实际状态变更仍必须经过 TurnManager / RuleEngine / CommandValidator。

必须实现或扩展的 UI 模型：
```swift
struct FrontLineViewModel: Identifiable, Equatable {
    let id: String
    let faction: Faction
    let enemyFaction: Faction
    let riskLevel: FrontRiskLevel
    let totalLength: Int
    let segmentSummaries: [FrontSegmentViewModel]
    let warnings: [FrontGapWarning]
    let isSelected: Bool
}

struct FrontSegmentViewModel: Identifiable, Equatable {
    let id: String
    let riskLevel: FrontRiskLevel
    let keyRegionNames: [String]
    let hasGap: Bool
}

struct ArmyGroupViewModel: Identifiable, Equatable {
    let id: String
    let name: String
    let faction: Faction
    let divisionCount: Int
    let assignedFrontLineId: String?
    let posture: FrontPosture?
}

struct PlanViewModel: Identifiable, Equatable {
    let id: String
    let title: String
    let summary: String
    let warnings: [String]
}
如果项目已有 view model 风格，优先沿用现有命名和状态管理方式。
必须实现的界面：
地图前线叠加层在 SpriteKit 地图上绘制前线边界。
不同风险等级使用不同颜色或线型。
缺口位置要有明显标记。
关键区块，例如城市、要塞、补给源，可以显示小标记。
选中前线时高亮。
攻势箭头显示 OffensivePlan.mainAxis 和 supportingAxes。
地图叠加层不得直接计算前线，只读取已有 FrontLine / OffensivePlan 数据。

前线列表面板显示当前阵营可用前线。
显示前线长度、风险等级、缺口数量、关键区块。
点击列表项可以选中地图上的前线。
选中前线后显示详情。

集团军面板显示可用 ArmyGroup。
显示集团军包含多少单位。
显示当前是否已分配前线。
支持选择一个集团军。
支持把选中集团军分配到选中前线。

前线分配操作
基础流程：
选择集团军 -> 选择前线 -> 点击 Assign
Assign 不直接修改底层状态，应该通过明确的 action / intent：
enum FrontUIAction {
    case selectFrontLine(String)
    case selectArmyGroup(String)
    case assignArmyGroupToFront(armyGroupId: String, frontLineId: String)
    case setFrontPosture(frontLineId: String, posture: FrontPosture)
    case requestDefensePlan(frontLineId: String, armyGroupId: String)
    case setOffensiveObjective(frontLineId: String, target: RegionRef, planType: OffensivePlanType)
}
实际处理由 ViewModel / TurnManager / Controller 转发到对应 planner 或 command 管线。
布防计划面板显示 FrontDefensePlan 摘要。
显示每个单位的 assignment：lineDefense
strongpointDefense
artillerySupport
mobileReserve
breakthroughReserve
restAndRefit
retreat

显示 ReserveGroup。
显示 warnings。
显示 proposedCommands 数量和校验结果。
不需要在 v0.3 做复杂拖拽调兵。

攻势计划面板显示 OffensivePlan.planType。
显示目标区块。
显示主攻轴线。
显示辅助轴线。
显示参战单位和预备队。
显示 warnings 和 summary。
玩家可以点击地图上的目标区块来设置：holdLine
limitedAttack
breakthrough


第一版交互保持简单：
选择前线 -> 选择计划类型 -> 点击目标区块 -> Generate Plan
Agent 决策面板显示元帅/将领 agent 名称。
显示最近一次 raw JSON。
显示 rationale。
显示 directives。
显示生成的 defensePlans / offensivePlans。
显示 validation warnings。
可以折叠 raw JSON，避免占用太多空间。

日志集成前线分配、计划生成、agent 决策、命令校验失败都应进入事件日志。
日志要能让玩家理解 AI 为什么这样布防或进攻。
不要只显示内部 id，尽量显示可读名称。

建议 UI 结构：
UI/
  Front/
    FrontLinePanelView.swift
    ArmyGroupPanelView.swift
    FrontAssignmentView.swift
    FrontDefensePlanView.swift
    OffensivePlanView.swift
    AgentDecisionPanelView.swift
    FrontLineViewModel.swift
    FrontUIController.swift

SpriteKit/
  FrontLineOverlayNode.swift
  OffensiveArrowNode.swift
  FrontMapInteractionAdapter.swift
如果现有目录不同，按现有工程风格放置，不要强行创建冲突结构。
SpriteKit 实现要求：
前线绘制基于 region/hex 的屏幕坐标映射。
如果当前是 hex 地图，使用 HexRegionAdapter 或 HexLayout 获取位置。
前线边界可以先画在 friendlyRegion 和 enemyRegion 中点之间，或沿 hex 边绘制。
攻势箭头沿 OffensiveAxis.path 绘制。
选中前线、hover/点击反馈要清晰。
地图缩放或尺寸变化时，叠加层要能刷新。
不要让 overlay 节点保存规则状态副本，只保存可渲染快照。
SwiftUI 实现要求：
面板信息密度适中，优先适合 iPad 横屏，也要能在 iPhone 上滚动显示。
使用已有设计风格。
不做营销页、介绍页或无关装饰。
按钮必须触发明确 action。
计划类型使用 segmented control 或等价选择器。
风险等级使用颜色、标签或图标表达。
raw JSON 使用 monospaced 文本区域，支持折叠。
不要做的事：
不实现前线识别算法，这是 Agent 1 的职责。
不实现自动布防算法，这是 Agent 2 的职责。
不实现攻势路径算法，这是 Agent 3 的职责。
不实现元帅 agent 决策逻辑，这是 Agent 4 的职责。
不直接修改 GameState。
不直接执行 Command。
不绕过 RuleEngine / CommandValidator。
不做复杂拖拽编辑器。
不做完整 HOI4 式 battle plan 编辑器。
不接真实 LLM。
不做将领忠诚、抗命、政变 UI。
不把规则逻辑写进 SpriteKit 节点或 SwiftUI View。
建议实现步骤：
检查现有 UI、SpriteKit、GameState、TurnManager、front 系统代码。
定义必要的 ViewModel 和 FrontUIAction。
实现地图前线 overlay 渲染。
实现前线列表和详情面板。
实现集团军选择与前线分配 UI。
实现布防计划面板。
实现攻势计划面板和目标区块选择。
实现 agent 决策面板。
把 UI action 接到现有 controller / planner / event log。
添加 UI 相关测试或快照/状态测试。
手动运行 App，确认前线显示、选择、分配、计划生成流程可用。
测试要求：
至少覆盖以下场景：
有 FrontLine 数据时，地图能显示前线叠加层。
risk low/medium/high/critical 在 UI 中能区分。
有 FrontGapWarning 时，地图和面板都能显示缺口。
点击前线列表项可以选中对应前线。
选择集团军和前线后，可以触发 assign action。
生成 FrontDefensePlan 后，面板能显示单位分配和预备队。
生成 OffensivePlan 后，地图能显示主攻箭头。
agent 决策记录能显示 raw JSON、rationale、directives、warnings。
没有前线、没有集团军、没有计划时 UI 不崩溃，显示空状态。
UI action 不直接修改 GameState，只通过 controller / TurnManager 处理。
iPhone 小屏下主要面板可以滚动，不遮挡地图关键操作。
iPad 横屏下地图和面板布局稳定。
完成标准：
工程可编译。
前线能在地图上显示。
玩家能选择前线和集团军。
玩家能发起集团军分配。
玩家能查看布防计划、攻势计划、agent 决策。
UI 不包含规则算法。
UI 不绕过命令校验管线。
不破坏 v0 现有移动、攻击、补给、AI 日志功能。
后续版本可以在此基础上扩展更复杂的前线编辑、拖拽调兵和聊天命令。
重要边界：
本阶段只做“前线显示与玩家操作入口”。第一版交互要稳定、清晰、可调试，不追求完整大战略 UI。不要把复杂规则塞进 UI 层。
```

星期三17:49






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三17:51






下面是 v0.3 Agent 6：测试、场景和平衡 的提示词。写完这个后，v0.3 目前规划的 6 个 agent 提示词就齐了。
你是 v0.3 前线系统的 Agent 6，负责“测试、场景扩展、集成验证和平衡”。

项目背景：
这是一款 iOS 二战回合制大战略/战棋混合游戏，技术栈为 Swift + SwiftUI + SpriteKit。v0 使用六角格测试板，长期目标是演进到类似《钢铁雄心4》的省份图/区块图底层。v0.3 已规划并实现或部分实现以下模块：
- Agent 1：前线规则与数据模型
- Agent 2：自动布防与调兵算法
- Agent 3：攻势箭头与突破计划
- Agent 4：元帅/将领 agent 接口
- Agent 5：前线 UI 与玩家操作

你的任务不是新增大功能，而是确保 v0.3 这些系统能稳定集成、可测试、可调参、可演示。

请先阅读：
- md/phase0_v0_minimum_scope.md
- md/phase1_hex_core_rules.md
- md/phase3_v0_engineering_architecture.md
- md/phase5_long_term_roadmap.md
- md/阶段性4:第一版可玩测试板任务拆解/README.md

并检查 Agent 1-5 是否已经实现：
- FrontLine / FrontSegment / FrontEdge
- ArmyGroup / FrontAssignment
- FrontLineDetector / FrontAnalyzer
- FrontDefensePlanner / FrontDefensePlan
- OffensivePlanner / OffensivePlan
- MarshalAgent / MarshalDecisionEnvelope / AgentDecisionRecord
- FrontLine UI overlay / plan panels / agent decision panel

如果某些模块不存在或不完整，不要重写它们的核心职责。你的工作是：
- 补测试；
- 补测试夹具；
- 补演示场景；
- 补轻量 mock；
- 补平衡参数；
- 修复明显集成问题；
- 记录缺失项和风险。

你的目标：
让 v0.3 可以被可靠验收。验收标准是：
1. 前线能正确识别。
2. 集团军能分配到前线。
3. 自动布防能生成合理计划。
4. 攻势计划能生成主攻轴线。
5. 元帅 MockAI 能输出前线级 directive。
6. UI 能显示前线、缺口、计划和 agent 决策。
7. 所有 proposedCommands 都经过 CommandValidator。
8. 非法计划不会修改 GameState。
9. 核心流程有单元测试和集成测试覆盖。

主要任务：

1. 建立 v0.3 测试夹具
创建或整理测试用 GameState / Scenario fixture：

- `stable_front_fixture`
  - 双方形成连续前线；
  - 我方单位数量足够；
  - 没有 critical 缺口；
  - 预期 planner 生成 balanced/hold 防御计划。

- `gap_front_fixture`
  - 前线中部存在缺口；
  - 附近有一个机动单位可补缺；
  - 预期 FrontAnalyzer 生成 FrontGapWarning；
  - FrontDefensePlanner 优先派最近单位补缺。

- `critical_city_fixture`
  - 关键城市或补给源暴露；
  - 无己方单位覆盖；
  - 预期 riskLevel 为 high 或 critical。

- `breakthrough_fixture`
  - 我方有装甲/摩托化预备队；
  - 敌方某段较弱；
  - 目标区块距离 2-4；
  - 预期 OffensivePlanner 生成 breakthrough 计划。

- `low_org_supply_fixture`
  - 部分单位低组织度或低补给；
  - 预期这些单位不会参与突破；
  - 预期生成 resupply/restAndRefit 相关 assignment。

- `insufficient_units_fixture`
  - 前线过长、单位不足；
  - 预期输出 insufficientUnits warning；
  - 不应生成荒谬的全线进攻计划。

2. 扩展阿登测试场景
如果已有 `ardennes_v0_scenario.json` 或类似场景数据，请扩展出 v0.3 可演示版本。

建议新增或调整：
- 至少 2 条可识别前线，或 1 条连续前线 + 1 个缺口。
- 德军拥有：
  - 1-2 个装甲单位；
  - 1 个摩托化/机械化单位；
  - 2-3 个步兵单位；
  - 1 个炮兵单位。
- 盟军拥有：
  - 2-3 个步兵/守备单位；
  - 1 个反坦克或炮兵单位；
  - 关键城市 Bastogne；
  - 至少一个补给源。
- 地图包含：
  - 城市；
  - 要塞；
  - 道路节点；
  - 森林；
  - 河流或山地；
  - 补给源。
- 场景必须能演示：
  - 前线识别；
  - 缺口警告；
  - 集团军分配；
  - 自动布防；
  - 突破计划；
  - agent 决策日志。

3. 单元测试要求
至少覆盖：

前线识别：
- 敌我相邻生成 FrontEdge。
- 连续边界合并为 FrontLine 或 FrontSegment。
- 不连续边界分离。
- 城市/要塞/补给源识别为 key region。
- 缺口能产生 FrontGapWarning。
- 有单位覆盖时不产生缺口。

自动布防：
- 缺口优先分配最近可用单位。
- 城市/要塞/补给源 slot 优先级高。
- 步兵优先 lineDefense。
- 装甲优先 reserve。
- 低组织度单位不会被派往 high/critical slot。
- 低补给/被包围单位优先 rest/refit/retreat。
- 兵力不足产生 PlannerWarning。
- 已行动单位不生成 move 命令。

攻势计划：
- holdLine 不生成 attack。
- limitedAttack 目标距离限制在 1-2。
- breakthrough 优先使用装甲/摩托化。
- 没有机动单位时产生 insufficientMobileUnits warning。
- 目标过远产生 objectiveTooFar warning。
- 路径阻塞产生 blockedPath warning。
- critical 缺口未处理时，突破降级或产生 warning。

agent 管线：
- MockMarshalDecisionProvider 在 critical 缺口时选择防守或谨慎姿态。
- 前线稳定且有机动预备队时选择 breakthrough。
- 非法 directive 被跳过并记录 warning。
- malformed JSON 不会修改 GameState。
- AgentDecisionRecord 被写入日志。

UI/状态：
- ViewModel 能从 FrontLine / FrontDefensePlan / OffensivePlan 生成。
- 空状态不崩溃。
- UI action 不直接修改 GameState。
- assign action 会通过 controller / TurnManager / planner 管线。

4. 集成测试要求
至少实现这些端到端测试或近似集成测试：

- `testFrontPlanningFullFlow`
  - 加载 v0.3 阿登场景；
  - 识别前线；
  - 分配集团军；
  - 生成防御计划；
  - 校验 proposedCommands；
  - 确认 GameState 未被 planner 直接修改。

- `testMarshalAgentFullFlow`
  - 构建 TheaterAgentContext；
  - MockMarshalDecisionProvider 输出 directive；
  - MarshalAgentController 调用 DefensePlanner 和 OffensivePlanner；
  - 汇总 proposedCommands；
  - 写入 AgentDecisionRecord。

- `testNoInvalidCommandExecution`
  - 故意构造非法 objective 或非法 move；
  - 确认 CommandValidator 拒绝；
  - 确认 GameState 不变；
  - 确认 warning 被记录。

- `testScenarioCanRunOneTurn`
  - 加载 v0.3 场景；
  - 运行一次 AI planning；
  - 玩家/AI 回合切换不崩溃；
  - 日志包含前线、计划、命令校验结果。

5. 平衡参数整理
如果项目已有规则参数文件，优先放入数据文件；否则创建清晰的 constants 或 config。

建议整理：
```swift
struct FrontBalanceConfig: Codable, Equatable {
    var lowOrganizationThreshold: Int
    var criticalOrganizationThreshold: Int
    var maxLimitedAttackDistance: Int
    var maxBreakthroughDistance: Int
    var minimumLineCoverageRatio: Double
    var reserveMobileUnitRatio: Double
    var cityPriorityBonus: Int
    var fortressPriorityBonus: Int
    var supplySourcePriorityBonus: Int
    var gapPriorityBonus: Int
}
默认建议：
lowOrganizationThreshold = 40
criticalOrganizationThreshold = 20
maxLimitedAttackDistance = 2
maxBreakthroughDistance = 4
minimumLineCoverageRatio = 0.6
reserveMobileUnitRatio = 0.25
cityPriorityBonus = 3
fortressPriorityBonus = 4
supplySourcePriorityBonus = 5
gapPriorityBonus = 5
平衡原则：
v0.3 宁可保守，不要让 AI 无脑全线进攻。
装甲应倾向集中，不应平均铺满前线。
critical 缺口优先级高于突破。
低组织度和低补给必须显著影响规划。
前线过长时必须输出 warning。
性能检查
添加基础性能或复杂度检查，避免 v0.3 后续扩展时失控。
要求：
前线识别不要每个 UI frame 重算。
planner 不应在 SwiftUI body 或 SpriteKit draw loop 中运行。
规划结果可以按 turn / state revision 缓存，但缓存必须可失效。
单元测试中构造较大地图，例如 30x20 或等价 region graph，确认前线识别和规划在可接受时间内完成。
不要引入真实 LLM 调用作为测试依赖。
文档补充
如果发现实际实现与文档不一致，请补一个简短开发说明，例如：v0.3 当前支持哪些前线能力；
哪些是 mock；
哪些是后续版本再做；
如何运行测试；
如何加载 v0.3 阿登场景。

不要做的事：
不实现新的前线算法，除非是修复 Agent 1 的明显 bug。
不重写自动布防算法，除非是修复 Agent 2 的明显 bug。
不重写攻势计划算法，除非是修复 Agent 3 的明显 bug。
不重写 agent 管线，除非是修复 Agent 4 的明显 bug。
不重写 UI，除非是修复 Agent 5 的明显 bug。
不接真实本地 LLM。
不做复杂政治、忠诚、抗命、政变。
不做完整 HOI4 战斗宽度。
不绕过 CommandValidator / RuleEngine。
不为了测试方便直接修改生产代码的核心规则。
建议实现步骤：
检查当前工程结构和 Agent 1-5 的实现状态。
列出缺失接口和无法测试的模块。
创建 v0.3 测试 fixture。
扩展或创建阿登 v0.3 场景数据。
添加前线识别单元测试。
添加自动布防单元测试。
添加攻势计划单元测试。
添加元帅 agent 管线测试。
添加 UI ViewModel / action 测试。
添加一到两个端到端集成测试。
整理平衡参数。
运行测试并修复集成错误。
输出简短测试报告。
完成标准：
工程可编译。
核心测试通过。
v0.3 阿登场景可加载。
前线识别、自动布防、攻势计划、agent 管线至少各有测试覆盖。
非法命令不会修改 GameState。
没有真实 LLM 时系统仍可跑通。
planner 和 UI 没有直接绕过规则系统。
测试 fixture 可以被后续 agent 复用。
文档说明当前 v0.3 能力边界和已知限制。
重要边界：
你的角色是质量、集成和可演示性负责人。不要扩张新玩法。优先让 v0.3 的前线系统稳定、可验证、可调参。

已完成。

星期三17:53






继续写下一个agent的提示词，如果写完了就说已完成，没写完就继续写




星期三17:55






已完成。
v0.3 目前规划的 6 个 agent 提示词都已经写完：
Agent 1：前线规则与数据模型  
Agent 2：自动布防与调兵算法  
Agent 3：攻势箭头与突破计划  
Agent 4：元帅/将领 Agent 接口  
Agent 5：前线 UI 与玩家操作  
Agent 6：测试、场景和平衡